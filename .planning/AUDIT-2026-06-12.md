# Build audit — 2026-06-12

Five-dimension parallel audit of all completed work (phases 1-11) run before resuming the build via `/finish-build`. Dimensions: design-system compliance, voice/copy, accessibility, code quality/dead weight, governance (COI/CLAIMS + human gates). Branch at audit time: `phase-10-foundation`, 45 commits ahead of `main` (Phase 11 work; Phase 10 already merged via PR #9 on 2026-06-05).

**Headline: the completed work is in strong shape.** Voice/copy fully clean. Design tokens, CTA discipline, brand prohibitions, and section padding clean. TypeScript and ESLint clean (1 warning). A11y baseline strong with regression specs guarding the core contracts. The findings below are narrow and almost all map onto already-scheduled phases.

Status legend: `[ ]` open, `[x]` done. `/finish-build` Step 0 picks up open items scoped to the phase it runs.

---

## 1. Fix-now batch (small, mechanical; run before Phase 12) — EXECUTED 2026-06-12

- [x] **FIX-01 (a11y, medium)** `IssuesWorklist.tsx` — status label no longer hidden below the @sm container width; renders at all sizes alongside the color dot.
- [x] **FIX-02 (a11y, medium)** `axe-routes.spec.ts` + `touch-targets.spec.ts` now iterate `VISUAL_ROUTES` (23 routes) instead of `ROUTES` (11), covering `/compare`, `/platform/integrations`, all `/solutions/*`, and all `/company/*`.
- [x] **FIX-03 (governance) — RATIFIED AS-IS by Connor 2026-06-12.** The shortened footer line "DebtNext is a TSI company." stays. Andrew has given complete sign-off; the spelled-out form is not required. CLAUDE.md §6's exact-string requirement is superseded by this ratification for the footer line.
- [x] **FIX-04 (docs)** DESIGN.md §4.2 now records the sanctioned General Sans 600 wordmark exception (HERO-02 decision).
- [x] **FIX-05 (dead weight)** `public/hero/homepage-hero.mp4` (~11 MB, zero references) deleted; HANDOFF.md updated in the same commit.
- [x] **FIX-06 (dead weight)** `src/components/sections/HeroAccountsPanel.tsx` deleted (never imported).
- [x] **FIX-07 (lint)** Unused `AnimatedNumber` import removed from `IssuesWorklist.tsx`.
- [x] **FIX-08 (hygiene)** 24 untracked review PNGs deleted from repo root; `/review-screenshots/` added to `.gitignore` for future capture sessions.
- [x] **FIX-09 (docs)** Three stale deferred-items entries (TS2353 in phases 05, 05.3, 10) marked resolved; the Phase-10 `AnimatedNumber` entry closed via FIX-07.
- [ ] **FIX-10 (verify)** `container-query-layouts.spec.ts:17` passed 3/3 locally against the dev server; awaiting confirmation on the Phase 11 PR's CI run against a production build. If green there, delete the stale "known failure" notes in STATE.md/HANDOFF.

## 2. Fold into scheduled phases

### Phase 6 — Analytics wiring
- [ ] **P6-01 (code gap, high)** No GTM container loader exists anywhere. `src/lib/analytics.ts:32` pushes to `window.dataLayer` but nothing injects the GTM `<Script>` in `src/app/layout.tsx`, so zero events reach GA4 even after Jeremiah provisions the IDs. This is core ANALYTICS-01/02 scope; confirm it's in the Phase 6 plan explicitly.

### Phase 12 — Solutions per-industry visuals
- [ ] **P12-01 (scope correction)** ROADMAP calls `SolutionsIndustryCards` a "prop-less duplicate" to kill. Reality: it is live and imported by all 7 solutions pages via `LazySolutionsIndustryCards`; code comments only say its layout is subsumable by the DataStory "cards" branch. Re-scope the Phase 12 ticket as a migration (replace with per-industry archetype instances), not a dead-code deletion.
- [ ] **P12-02 (design)** Invented industry-accent hexes `#4AA8C9` / `#8472F0` / `#3D9DE0` in `SolutionsIndustryCards.tsx:22,38,46`. When Phase 12 builds the per-industry payloads, define an industry-accent token ramp in DESIGN.md or remap to existing chart tokens. No new local hexes.

### Phase 13 — Visual system consolidation
- [ ] **P13-01 (dead-code batch, gated)** The four bespoke visuals (`PlacementMatrix`, `OptimizationEngine`, `IssuesWorklist`, `ReportingDashboard`) are LIVE via the `VISUALS` registry consumed by the homepage FeatureAccordion. They become deletable only after Phase 13 repoints the registry entries to archetype/Flagship equivalents. Delete the `Lazy*` wrappers and components as a set after the repoint, and verify the `VISUALS.reporting` key migration explicitly.
- [ ] **P13-02 (design)** Off-token gradient endpoints in `src/components/sections/mockups/PlacementMockup.tsx:10-12` (`#22c55e`, `#d97706`, `#0891b2`). The mockups dir is retired in Phase 13; make sure the replacement Console payloads use chart tokens only.

### Phase 14 — Text-only page elevation (or fold into the fix-now batch if preferred)
- [ ] **P14-01 (a11y)** `DemoForm.tsx:369-378` — wire `aria-invalid` and `aria-describedby="{id}-error"` on fields with errors (form-level region is already correct).
- [ ] **P14-02 (design/a11y)** Normalize form-input focus treatment: `DemoForm.tsx:372`, `HomepageHero.tsx:236`, `AttachedForm.tsx:55` use a 3px ring or border-color change instead of the spec's 2px `#9CB4E8` outline + 2px offset. Either normalize to the token outline or document the input ring as an intentional pattern in DESIGN.md.

## 3. Backlog (non-blocking polish)

- [ ] **BL-01** `src/content/compare.ts:99` — "digital journeys" in the Symend competitor descriptor uses a banned word; optional reword ("digital outreach flows").
- [ ] **BL-02** Document the Title Case module-name convention ("Placement Management", "Optimization Engine", "Issues Management", "Reporting and Dashboards") as deliberate branded nouns so future edits don't sentence-case them.
- [ ] **BL-03** Tokenize the indigo shade family (`#3949B5`, `#4456C8`, `#2E3A93`) used in product-visual gradients (`parts.tsx:9` et al.) — visually correct, hygiene only.
- [ ] **BL-04** Optional: tokenize the spec-faithful `rgba(156,180,232,…)` literals in `button.tsx:24`.
- [ ] **BL-05** `HomepageHandoffSection.tsx:236` — inactive tabs reference `aria-controls` panels not in the DOM; render all panels hidden or set aria-controls only on the active tab.
- [ ] **BL-06** LCP enforcement belongs in field RUM/CrUX long-term; CI lab gate as regression detector (carried from M5 close-out).
- [ ] **BL-07** Content-route TBT ceiling raised 240→300ms (2026-06-12): the first two complete LHCI evaluations of the 6-URL collection failed on *different* routes (placement 260.7ms, then utilities 276.2ms while placement passed), showing the visual-heavy content routes hover in a 230-280ms band on shared runners and 240 is inside the noise. TBT is hydration-bound (JS-size cuts don't move it; a prior framer-removal attempt regressed and was reverted); the real fix is the backlogged RSC re-architecture. 300ms remains a regression detector (an eager motion/GSAP import would blow well past it), consistent with the homepage's 450ms variance ceiling.

## 4. Launch punch list — human gates (no agent can clear these)

| # | Gate | Owner | Blocks |
|---|---|---|---|
| ~~1~~ | ~~Phase 11 UAT~~ — APPROVED by Connor 2026-06-12 ("I approve, let's move forward"); CI on the PR still runs the automated checks (LHCI, Playwright, axe) | Connor | Done |
| ~~2~~ | ~~PR for the Phase 11 branch~~ — opened 2026-06-12 with audit fixes included; merge on green CI per Connor's approval | Connor | In flight |
| 3 | Blocking `[COI REVIEW]` flags on v1 narrative copy (`/company`, `/company/about`, `/why-dplat`, `/solutions`, `/platform/issues`, `IssuesWorklist.tsx:19`, `ComplianceStandards.tsx:20`) | Andrew Budish | Phase 9 DOD-02 |
| 4 | Blocking `[CLAIMS REVIEW]` flags: 60M+/$1.5B+ (`stats.ts`), "9 of 10 utilities", time-to-production ranges (`compare.ts`, `why-dplat.ts`), leadership tenures, encryption details (`platform.ts:151`, `company.ts:107`), partner count (`PartnerMap.tsx:46`); comparative claims also need legal review | Andrew Budish (+legal) | Phase 9 DOD-03 |
| 5 | Leadership titles/tenures/roles verification | Paul Goske | `/company`, `/company/leadership` |
| 6 | `ZOHO_WEBHOOK_URL` in Vercel (form fail-opens silently until set — leads dropped) | Austin Johnson | Lead capture |
| 7 | `RESEND_API_KEY` in Vercel (confirmation emails skipped until set) | Connor | Demo confirmation email |
| 8 | `NEXT_PUBLIC_GA4_ID` + `NEXT_PUBLIC_GTM_ID` (consumed only after P6-01 builds the loader) | Jeremiah Benes | Analytics |
| 9 | Restore `source-materials/` (dPlat_Solution_Overview_v04232026.pptx + DebtNext_entries.xlsx) — directory absent; claims clearance has nothing to trace against | Connor | Phase 9 claims audit trail |
| 10 | "Why dPlat" nav label decision (DOD-04) | Connor | Phase 9 |
| ~~11~~ | ~~Footer TSI line wording ratification~~ — RESOLVED 2026-06-12: Connor ratified the shortened form; Andrew has complete sign-off | Connor (+Andrew) | Done |
| 12 | Named clients/logos still require written client consent regardless of the figures pre-clearance | Andrew Budish | Any named-client copy |

Pre-cleared and non-blocking (Andrew, 2026-06): all `[COI REVIEW]`/`[CLAIMS REVIEW]` tags inside `src/content/visuals/*.ts` and the legacy visual placeholder banners — real-shaped anonymized figures + vendor/TSI framing. Tags stay for audit trail.

## 5. Corrections to planning assumptions (so phases don't plan against stale facts)

1. `SolutionsIndustryCards` is not a prop-less dead duplicate; it's live on 7 pages (see P12-01).
2. The four bespoke homepage-accordion visuals are not dead code; the `VISUALS` registry dynamic-imports them (see P13-01).
3. The `reduced-motion.spec.ts` TS2353 error logged in three deferred-items files is resolved.
4. `container-query-layouts.spec.ts:17` may no longer be failing (passed 3/3 locally; re-verify per FIX-10).
5. `homepage-hero-end.png` is already deleted; that open item in STATE.md is stale.
6. Phase 10 merged via PR #9; the branch's 45-commit lead is purely Phase 11.
7. Route map in CLAUDE.md §9 exactly matches `src/app/` — no drift.

---
*Generated 2026-06-12 by the five-agent audit (design / voice / a11y / quality / governance) preceding the `/finish-build` cycle. Full agent reports in the session transcript.*
