---
phase: 05-hero-performance
plan: 05
type: execute
wave: 3
depends_on: ["05-02-PLAN.md", "05-03-PLAN.md", "05-04-PLAN.md"]
files_modified:
  - HANDOFF.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - docs/m5-phase-5-lhci-run.md
autonomous: false
requirements: [HERO-04]
requirements_addressed: [HERO-04]
user_setup: []

must_haves:
  truths:
    - "A local `npx --no-install lhci autorun` against a fresh `npm run build && npm run start` run produces a median LCP under 2300 ms on `/`"
    - "The LHCI assertion gate passes (exit 0) — proves the gate works AND the perf delta from Plans 02/03/04 actually achieved sub-2.3s LCP"
    - "The 3-run LHCI temporary-public-storage report URL is captured and recorded in `docs/m5-phase-5-lhci-run.md`"
    - "A manual visual walkthrough on `/` at 1440/1024/768 viewports confirms: hero cinematic scrubs smoothly on all ladder variants; wordmark renders identically before/after self-host; no visible AVIF banding on the dark gradient"
    - "HANDOFF.md / .planning/PROJECT.md / .planning/STATE.md flip the hero MP4 perf-fix decision from `In flight` to `Good (Phase 5 shipped)` and record the measured LCP"
    - "REQUIREMENTS.md HERO-01..04 traceability rows flip to Plan numbers + Done status"
  artifacts:
    - path: "docs/m5-phase-5-lhci-run.md"
      provides: "Lighthouse CI run record: 3-run LCP table, median, temp-public-storage URLs, comparison vs m4-perf-baseline.md"
      min_lines: 30
  key_links:
    - from: "docs/m5-phase-5-lhci-run.md"
      to: "docs/m4-perf-baseline.md"
      via: "perf comparison table (M4 baseline LCP vs M5 post-ladder LCP)"
      pattern: "m4-perf-baseline"
    - from: "HANDOFF.md"
      to: ".planning/PROJECT.md Key Decisions table"
      via: "both documents flip 'Defer hero MP4 perf fix to M5' to 'Good (Phase 5 shipped)'"
      pattern: "Defer hero MP4 perf fix"
---

<objective>
Wire HERO-04: trip the LHCI gate locally against the post-Wave-1 build, prove `/` median LCP is under 2300 ms (the gate threshold; CLAUDE.md §12's 2500 ms public contract holds with 200 ms headroom for Phase 8 Motion). Capture the run output to `docs/m5-phase-5-lhci-run.md`. Walk the manual-verification items from `05-VALIDATION.md`. Flip HANDOFF.md / PROJECT.md / STATE.md / REQUIREMENTS.md to record Phase 5 as shipped.

Purpose: Phase 5's headline goal is "`/` LCP under 2.5s on 4G mobile so launch is unblocked." This plan is the proof-of-shipping step: the lighthouserc.json gate (Plan 01 Task 8) and perf.yml workflow (Plan 01 Task 9) are already in place, but neither has been exercised against the post-ladder, post-self-host, post-AVIF build. This plan does that exercise, records the result, and closes the phase.

This plan has a human-verify checkpoint because:
1. The LHCI output is a measured value, not a static contract — a human confirms the run was clean (no flake, no machine-load anomalies)
2. Three manual visual checks (cinematic smoothness, wordmark visual diff, AVIF banding) need a human eye per `05-VALIDATION.md` Manual-Only Verifications

Output:
- `docs/m5-phase-5-lhci-run.md` with the 3-run LCP table, median, temp-storage URLs, and a comparison table vs M4 baseline
- HANDOFF.md / .planning/PROJECT.md / .planning/STATE.md flipped to record Phase 5 ship
- REQUIREMENTS.md Traceability table HERO-01..04 rows updated (Plan column populated; Status → Done)
- Final commit closing Phase 5
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@HANDOFF.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-hero-performance/05-CONTEXT.md
@.planning/phases/05-hero-performance/05-RESEARCH.md
@.planning/phases/05-hero-performance/05-VALIDATION.md
@.planning/phases/05-hero-performance/05-01-PLAN.md
@.planning/phases/05-hero-performance/05-02-PLAN.md
@.planning/phases/05-hero-performance/05-03-PLAN.md
@.planning/phases/05-hero-performance/05-04-PLAN.md
@lighthouserc.json
@.github/workflows/perf.yml
@docs/m4-perf-baseline.md
@src/components/sections/HomepageHero.tsx
@src/content/homepage-hero.ts
@next.config.ts

<interfaces>
This plan does not introduce code interfaces. It exercises:

1. `lighthouserc.json` (Plan 01 Task 8) — the LHCI config the runner reads
2. `next start --port 3200` — production build on a fixed port (the `startServerCommand` in lighthouserc.json)
3. `/_next/image` — the optimizer that serves AVIF (HERO-03)
4. `<video><source>...</video>` — the multi-resolution ladder (HERO-01)
5. `--font-general-sans` woff2 from `/_next/static/media/` — self-hosted General Sans (HERO-02)

The contract the LHCI gate enforces:
- 3 runs of `/` under m4-perf-baseline throttling (412×823, DPR 1.75, cpuSlowdownMultiplier 4, Slow 4G)
- `aggregationMethod: 'median-run'` — picks the run with median performance score (the representative run, `isRepresentativeRun: true`)
- `maxNumericValue: 2300` on `largest-contentful-paint` — fails the assert if the representative run's LCP ≥ 2300 ms
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run lhci autorun locally against the post-Wave-1 production build</name>
  <files>(no files modified — measurement run; .lighthouseci/ output is intentionally git-ignored)</files>
  <read_first>
    - lighthouserc.json (the config; verify startServerCommand uses port 3200)
    - docs/m4-perf-baseline.md (the M4 baseline values to compare against)
    - .planning/phases/05-hero-performance/05-CONTEXT.md D-06..D-09 (measurement invariants)
  </read_first>
  <action>
    Step 1: Ensure no stale state. Kill any process holding port 3200 and clear the Next cache:
    ```
    lsof -ti tcp:3200 | xargs -r kill -9 2>/dev/null || true
    rm -rf .next .lighthouseci
    ```

    Step 2: Confirm pre-conditions. All three Wave 1 plans must have shipped before this runs. Verify:
    ```
    git log --oneline -10 | grep -c "feat(phase-5/hero-0[1234])"
    ```
    Should return at least 3 (Plans 02, 03, 04 committed). If less than 3, abort and report which Wave 1 plan has not landed.

    Step 3: Run the gate.
    ```
    npx --no-install lhci autorun
    ```

    This will:
    1. Run `npm run build` (full Next 16 production build)
    2. Start `npm run start -- --port 3200` and wait for the port to be reachable
    3. Run Lighthouse mobile-throttled audits against `http://localhost:3200/` 3 times
    4. Pick the representative run (median performance score)
    5. Assert `largest-contentful-paint < 2300 ms` against that representative run
    6. Upload all 3 run reports to Google's temporary-public-storage
    7. Exit 0 if the assertion passes, exit non-zero otherwise

    Step 4: Capture the output. Three things to record:

    a. The 3 individual LCP values per run. After autorun completes, parse them from `.lighthouseci/manifest.json`:
    ```
    cat .lighthouseci/manifest.json | python3 -c "import sys, json; m = json.load(sys.stdin); print(json.dumps([{'isRep': r['isRepresentativeRun'], 'lcp_ms': r['summary']['largest-contentful-paint']*1000 if r['summary']['largest-contentful-paint'] < 100 else r['summary']['largest-contentful-paint'], 'url': r.get('url')} for r in m], indent=2))"
    ```

    b. The 3 temp-public-storage report URLs. They appear in the autorun stdout (`Uploading...` then a URL per run). Capture them.

    c. The exit status of the assertion step.

    Step 5: Stop the dev server gracefully.
    ```
    lsof -ti tcp:3200 | xargs -r kill 2>/dev/null || true
    ```

    Possible outcomes and how to handle each:

    **Case A — green (representative-run LCP < 2300 ms):** Proceed to Task 2 with the captured numbers + URLs.

    **Case B — borderline (representative-run LCP between 2300-2500 ms):** The gate fails BUT we're still under CLAUDE.md §12's 2.5s public contract. Per RESEARCH.md, the 200 ms headroom (D-08) is intentional safety margin for Phase 8 (Motion). The gate is doing its job — flag it as a partial pass and stop. Report which lever to pull:
      - The hero PNG → AVIF re-encode might be landing higher quality than necessary. Per Assumption A1, drop AVIF quality to 40 via a custom Next image loader.
      - Or one of the ladder variants is being picked at desktop in CI when the smaller variant should be served — check the LHCI flame chart for the network panel of the temp-storage report.
    Do not proceed to Task 2; halt and report to user.

    **Case C — clearly over budget (representative-run LCP ≥ 2500 ms):** Phase 5 did not achieve the headline goal. Halt and report. Most likely cause: AVIF wasn't actually served (next.config.ts edit reverted, or Sharp not bundled), or the ladder isn't being picked (browser served 720p when 540p was available — investigate the `media` query algebra in `src/content/homepage-hero.ts`).

    **Case D — flake (one of three runs is wildly inconsistent, e.g., LCP > 5s):** Re-run once to absorb single-pass variance. If the second autorun also shows the same flake on the same run number, the build has a real intermittent issue; halt and report. If the second run is clean, use the second run's numbers and proceed.

    Do NOT proceed past Case A or Case D (after re-run). Cases B and C require investigation outside this plan's scope.
  </action>
  <verify>
    <automated>test -f .lighthouseci/manifest.json && python3 -c "import json; m=json.load(open('.lighthouseci/manifest.json')); rep=[r for r in m if r['isRepresentativeRun']][0]; lcp=rep['summary']['largest-contentful-paint']; lcp_ms=lcp*1000 if lcp<100 else lcp; assert lcp_ms<2300, f'representative-run LCP {lcp_ms}ms >= 2300ms gate'"</automated>
  </verify>
  <acceptance_criteria>
    - `npx --no-install lhci autorun` exits 0 (the assertion passed)
    - `.lighthouseci/manifest.json` exists and contains 3 run entries
    - Exactly 1 of those 3 entries has `isRepresentativeRun: true`
    - The representative run's `largest-contentful-paint` value is less than 2300 (ms; or less than 2.3 if reported in seconds)
    - 3 temp-public-storage URLs captured from stdout (for Task 3's docs/m5-phase-5-lhci-run.md)
    - Port 3200 is released after the run (`lsof -ti tcp:3200` returns nothing)
  </acceptance_criteria>
  <done>LHCI gate green locally. Median (representative-run) `/` LCP confirmed < 2300 ms; numbers + temp-storage URLs captured for Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [CHECKPOINT]: Manual visual verification on / at 3 viewports</name>
  <files>(none — human verification step; the local `next start --port 3200` process from Task 1 is the surface inspected)</files>
  <read_first>
    - .planning/phases/05-hero-performance/05-VALIDATION.md §"Manual-Only Verifications" (3 rows: cinematic smoothness, wordmark visual diff, AVIF banding)
  </read_first>
  <action>
    **CHECKPOINT TASK — pauses for human approval before Task 3 proceeds.**

    Context: Phase 5 has shipped HERO-01 (multi-resolution video ladder), HERO-02 (General Sans self-host), and HERO-03 (AVIF poster with preload + fetchPriority). The LHCI gate ran green locally (Task 1).

    Three things now need a human eye that automated specs cannot judge:
    1. **Cinematic scrub smoothness across ladder variants** — at 1440px Chrome picks 720p.webm; at 1024-1439px it picks 540p.webm; at 768-1023px it picks 360p.webm. Each variant should scrub cleanly with no stutter when GSAP tweens `video.currentTime`.
    2. **Wordmark visual diff (pre-self-host vs post-self-host)** — General Sans 600 should render letter-for-letter identical and the `.dn-node` pulse position must not have shifted.
    3. **AVIF banding on the dark gradient** — Sharp AVIF quality 50 on the cliff-frame's dark gradient should be visually indistinguishable from the source PNG.

    Open the local production build (start one if needed: `npm run start -- --port 3200` in a separate terminal).

    **Check 1 — cinematic scrub smoothness (3 viewports):**
    1. Open `http://localhost:3200/` in Chrome
    2. Open DevTools → Device toolbar → set viewport to 1440×900. Disable cache. Reload.
    3. In the Network panel, filter by "Media". Confirm the file fetched is `homepage-hero-720p.webm` (~5 MB ish). NOT `homepage-hero.mp4`. NOT `homepage-hero-720p.mp4`.
    4. Scroll the page slowly through the hero cinematic. Watch for stutter, frame-drops, or jank in the dolly-zoom. Smooth is pass; visibly choppy is fail.
    5. Change viewport to 1024×768 (laptop). Reload. Confirm Network fetches `homepage-hero-540p.webm` (~2-3 MB). Scrub. Confirm smooth.
    6. Change viewport to 820×1180 (iPad portrait). Reload. Confirm Network fetches `homepage-hero-360p.webm` (~1-1.5 MB). Scrub. Confirm smooth.

    **Check 2 — wordmark visual diff:**
    1. At 1440px, take a screenshot of the top-left corner including the DebtNext wordmark and the indigo `.dn-node` pulse.
    2. Compare to the pre-self-host wordmark (Vercel preview deployment of `main` from before Plan 03, or a screenshot you captured before Plan 03 landed).
    3. The letterforms of "DebtNext" should be pixel-identical (or near-identical — next/font/local's `adjustFontFallback: 'Arial'` metric overrides should keep CLS minimal but the rendered widths must match).
    4. The `.dn-node` pulse should sit at the exact same position relative to the final "t" of "DebtNext", and should still pulse (animation visible) — wait ~3 seconds and watch it brighten / dim.
    5. Test reduced motion: in DevTools → Rendering tab, set "Emulate CSS prefers-reduced-motion: reduce". Reload. The `.dn-node` pulse should freeze (no animation); the wordmark itself remains visible.

    **Check 3 — AVIF banding:**
    1. At 1440px viewport, in DevTools Network tab, find the `/_next/image?url=...homepage-hero-start...` request. Confirm Response Content-Type is `image/avif`.
    2. Right-click the request → "Open in new tab". The AVIF renders as an image (modern Chrome supports AVIF natively in tabs).
    3. Inspect the dark gradient region (the cliff face and the sky-to-cliff transition). Look for:
       - Banding (visible stair-step gradients instead of smooth)
       - Posterization (color regions look flat or chunky)
       - Loss of texture in the rock face
    4. Open `public/hero/homepage-hero-start.png` in another tab. Compare side-by-side.
    5. Sub-200KB AVIF that is visually indistinguishable from the source PNG = pass. Visible banding = fail (per Assumption A1, drop AVIF quality to 40 — flag and halt this plan; do not auto-fix).

    **Resume signal:** type "approved" to proceed to Task 3 (capture LHCI run record + ship docs). If any check fails, describe which check failed and what you saw; do NOT type "approved".

    Stop the local dev server when finished: `lsof -ti tcp:3200 | xargs -r kill 2>/dev/null || true`.
  </action>
  <verify>
    <automated>echo "HUMAN-VERIFY: this task pauses for explicit user approval. The executor must wait for the user to type 'approved' after running Checks 1, 2, and 3 against http://localhost:3200/ per the action body. Proceeding past this task without explicit human approval is a protocol violation."</automated>
  </verify>
  <acceptance_criteria>
    - Check 1: at 1440px viewport, Network panel shows `homepage-hero-720p.webm` fetched; at 1024×768 shows `homepage-hero-540p.webm`; at 820×1180 shows `homepage-hero-360p.webm`. All three scrub smoothly with no visible stutter or jank.
    - Check 2: wordmark letterforms render identical to the pre-self-host screenshot; .dn-node pulse position unshifted; pulse animates by default; pulse freezes when prefers-reduced-motion is emulated.
    - Check 3: `/_next/image` response Content-Type is `image/avif` at 1440px; the AVIF poster shown side-by-side with `public/hero/homepage-hero-start.png` has no visible banding, posterization, or texture loss on the dark gradient.
    - Resume signal received: user typed "approved" in chat.
  </acceptance_criteria>
  <done>User has confirmed Checks 1, 2, and 3 are all green on `/` at 1440px, 1024×768, and 820×1180. If any check fails, this task remains blocking and Plan 05 halts; user describes the failure mode and an out-of-plan investigation begins (most likely candidates: drop AVIF quality to 40 via custom Next image loader for Check 3 banding; verify `<source>` media-query algebra in `src/content/homepage-hero.ts` for wrong-variant Check 1 fetches; visually re-check Wordmark.tsx fontFamily chain for Check 2 letter-shape regression).</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/m5-phase-5-lhci-run.md (LHCI run record + M4 baseline comparison)</name>
  <files>docs/m5-phase-5-lhci-run.md</files>
  <read_first>
    - .lighthouseci/manifest.json (the 3-run output captured in Task 1)
    - docs/m4-perf-baseline.md (the LCP value to compare against — `/` was 2.86s)
    - .planning/phases/05-hero-performance/05-CONTEXT.md D-06..D-09 (measurement invariants to record)
    - Task 1's captured temp-public-storage URLs (3 URLs to embed)
  </read_first>
  <action>
    Use the Write tool to create `docs/m5-phase-5-lhci-run.md` with this structure. Substitute `<RUN_1_LCP_MS>`, `<RUN_2_LCP_MS>`, `<RUN_3_LCP_MS>`, `<REPRESENTATIVE_RUN_LCP_MS>`, `<TEMP_URL_1>`, `<TEMP_URL_2>`, `<TEMP_URL_3>`, `<DATE>`, `<HOST_INFO>`, `<DELTA>`, `<AVIF_KB>` with values from Task 1's output (and from Plan 04's spec output for AVIF_KB):

    ```markdown
    # Phase 5 LHCI run — `/` LCP under 2.3s gate

    **Date:** <DATE> (e.g., 2026-05-21)
    **Host:** <HOST_INFO> (e.g., macOS 25.4.0, Node 22.22.0, npx --no-install lhci autorun)
    **Config:** `lighthouserc.json` at repo root (Plan 01 Task 8); m4-perf-baseline throttling profile (412×823, DPR 1.75, cpuSlowdownMultiplier 4, Slow 4G); 3 runs; median-run aggregation; gate at LCP < 2300 ms.

    ## Result

    | Run | LCP (ms) | Representative? |
    |-----|----------|------------------|
    | 1   | <RUN_1_LCP_MS> | <YES/NO> |
    | 2   | <RUN_2_LCP_MS> | <YES/NO> |
    | 3   | <RUN_3_LCP_MS> | <YES/NO> |

    **Representative-run LCP:** <REPRESENTATIVE_RUN_LCP_MS> ms (gate threshold 2300 ms → **PASS**)

    Public Lighthouse reports (Google temporary-public-storage; time-bounded):

    1. <TEMP_URL_1>
    2. <TEMP_URL_2>
    3. <TEMP_URL_3>

    ## Comparison vs M4 baseline

    | Metric  | M4 baseline (`docs/m4-perf-baseline.md`) | Phase 5 result | Delta |
    |---------|-----------------------------------------|-----------------|-------|
    | `/` LCP | 2.86 s (2860 ms)                        | <REPRESENTATIVE_RUN_LCP_MS> ms | -<DELTA> ms |
    | Hero MP4 size | 11.65 MB (single 720p H.264 monolith) | 3-tier ladder; ~5 MB at desktop, ~2-3 MB at narrow-laptop, ~1-1.5 MB at iPad-portrait | >= 50% byte reduction at every tier |
    | Poster size   | 2.55 MB PNG | <AVIF_KB> KB AVIF on Accept: image/avif | >= 12x reduction |
    | Wordmark font | Fontshare CDN (1 DNS + 1 TLS + 2 fetches) | next/font/local, preloaded, content-hashed | 0 third-party CDN calls on hero path |

    ## What landed in Phase 5

    - **Plan 01 (Wave 0, 2026-05-21):** Scaffolding — encoder + audit scripts, fonts.ts + woff2, next.config.ts AVIF/WebP, lighthouserc.json, perf.yml, 3 Playwright spec stubs. REQUIREMENTS.md HERO-01 ladder shape amended to actual 720p/540p/360p (1080p was impossible: source is 1280×720).
    - **Plan 02 (Wave 1, HERO-01):** 6 ladder binaries, sources array, `<source>` mapping, source-ladder.spec.ts.
    - **Plan 03 (Wave 1, HERO-02):** Fontshare @import removed, `generalSans` wired in layout, Wordmark.tsx fontFamily defensive variable lead, wordmark-self-host.spec.ts.
    - **Plan 04 (Wave 1, HERO-03):** `<Image priority>` -> `preload` + `fetchPriority="high"`; AVIF served via Sharp/`images.formats`; poster-avif-negotiation.spec.ts.
    - **Plan 05 (Wave 2, HERO-04, this run):** LHCI gate exercised; manual visual verification on `/` at 1440/1024/768 confirmed cinematic + wordmark + AVIF are all correct.

    ## Phase 8 (Motion) headroom

    Per D-08, the gate at 2300 ms is intentionally 200 ms below CLAUDE.md §12's 2500 ms public contract. The actual representative-run LCP of <REPRESENTATIVE_RUN_LCP_MS> ms leaves (2300 - <REPRESENTATIVE_RUN_LCP_MS>) ms additional headroom inside the gate, and (2500 - <REPRESENTATIVE_RUN_LCP_MS>) ms total headroom under the public LCP contract. Phase 8's MOTION-04 verifies `/` LCP stays under 2.5s after motion ships; this report is the baseline that MOTION-04's check runs against.

    ## Operational notes

    - The 31-day `minimumCacheTTL` on `next.config.ts` `images` means the AVIF transformation is cached for ~1 month; first-visit users absorb the Sharp encode latency on the LCP element. On Vercel this is a single-cold-start cost per region per month; on `next start` locally it's a per-process cost.
    - `.lighthouseci/` is intentionally not committed (machine-specific). The temp-public-storage URLs above are the durable record.
    - The 720p anchor is the ladder ceiling; if the cinematic ever needs a true 1080p tier the source video must be re-shot or upscaled (currently 1280×720; ffprobe-verified).

    ---
    Generated by Phase 5 Plan 05 Task 3 after a green local `npx --no-install lhci autorun`.
    ```

    Substitute the placeholders with the values captured in Task 1. The temp-public-storage URLs are essential — they are the only durable record of the actual Lighthouse trace, score breakdown, and waterfall.

    For `<AVIF_KB>`: capture from the Playwright run in Plan 04 Task 2 (it prints the byte size in the test failure message format), or re-run the spec verbosely:
    ```
    npx playwright test tests/hero/poster-avif-negotiation.spec.ts --reporter=list -g "AVIF served" 2>&1 | grep -oE "[0-9]+ bytes" | head -1
    ```
    Round to the nearest KB for the table (e.g., "148 KB").

    Do NOT inline-link to any internal Lighthouse trace JSON (large, machine-specific). The three temp-public-storage URLs are sufficient.
  </action>
  <verify>
    <automated>test -f docs/m5-phase-5-lhci-run.md && grep -q "Representative-run LCP" docs/m5-phase-5-lhci-run.md && grep -q "M4 baseline" docs/m5-phase-5-lhci-run.md && grep -q "temporary-public-storage" docs/m5-phase-5-lhci-run.md && [ $(grep -c "https://" docs/m5-phase-5-lhci-run.md) -ge 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/m5-phase-5-lhci-run.md` succeeds
    - The file contains a 3-row run table with LCP values
    - The file contains 3 distinct `https://` URLs (the temp-public-storage Lighthouse reports)
    - The file contains an explicit representative-run LCP value (in ms)
    - The file contains a comparison row referencing the M4 baseline 2.86s value
    - The file contains the actual AVIF byte count (any integer-rounded KB value)
    - The file references all five plans (01..05) in the "What landed in Phase 5" section
    - The file mentions D-08 and Phase 8's MOTION-04 headroom dependency
    - No placeholder `<RUN_1_LCP_MS>`-style strings remain in the final file
  </acceptance_criteria>
  <done>docs/m5-phase-5-lhci-run.md captures the canonical Phase 5 perf record with measured values + comparison vs M4 baseline.</done>
</task>

<task type="auto">
  <name>Task 4: Flip HANDOFF.md / .planning/PROJECT.md / .planning/STATE.md / REQUIREMENTS.md to reflect Phase 5 ship</name>
  <files>HANDOFF.md, .planning/PROJECT.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - HANDOFF.md (verify section headers; "Decisions locked" section; "M5 — Launch readiness + motion pass" section)
    - .planning/PROJECT.md (Key Decisions table; "Defer hero MP4 perf fix to M5" row)
    - .planning/STATE.md (Status, Last activity, Resume from)
    - .planning/REQUIREMENTS.md (Traceability table HERO-01..04 rows)
    - docs/m5-phase-5-lhci-run.md (the LCP value to inline-reference)
  </read_first>
  <action>
    Read the representative-run LCP value from `docs/m5-phase-5-lhci-run.md` and use it inline below as `<LCP_MS>`.

    **HANDOFF.md edits:**

    1. Append to the "M5 — Launch readiness + motion pass" section:
       ```
       - Phase 5 SHIPPED (2026-05-21): LHCI gate green. Representative-run `/` LCP = <LCP_MS> ms on m4-perf-baseline throttling (412×823, DPR 1.75, cpuSlowdownMultiplier 4, Slow 4G; median-of-3). Full record in `docs/m5-phase-5-lhci-run.md`. 5 plans + 1 phase commit landed: 01 (Wave 0 scaffolding), 02 (HERO-01 ladder), 03 (HERO-02 self-host), 04 (HERO-03 AVIF poster), 05 (HERO-04 perf gate trip + docs ship). All 4 phase requirements met. Phase 8 (Motion) headroom is (2500 - <LCP_MS>) ms under the CLAUDE.md §12 public contract.
       ```

    2. In the "Decisions locked" section, append:
       ```
       - Phase 5 SHIPPED (2026-05-21): hero MP4 perf fix landed. `/` LCP <LCP_MS> ms (gate 2300 ms; public contract 2500 ms). Ladder shape 720p/540p/360p (source is 1280×720; not 1080p as initial REQUIREMENTS.md HERO-01 wording implied — corrected during Plan 01). LHCI run record at `docs/m5-phase-5-lhci-run.md`.
       ```

    **`.planning/PROJECT.md` edits:**

    1. In the Key Decisions table, change the "Defer hero MP4 perf fix to M5" row's Outcome from `— In flight (Phase 5 Wave 0 scaffolding shipped 2026-05-21)` to `✓ Good (Phase 5 shipped 2026-05-21; / LCP <LCP_MS> ms vs 2.86s M4 baseline; record in docs/m5-phase-5-lhci-run.md)`.

    **`.planning/STATE.md` edits:**

    1. Update "Status" line to:
       `Phase 5 SHIPPED 2026-05-21. 5 plans + 1 phase commit; representative-run / LCP <LCP_MS> ms (gate 2300 ms, public contract 2500 ms; M4 baseline was 2.86s = 2860 ms). All 4 HERO requirements met. Next: Phase 6 (Analytics wiring) or Phase 7 (SEO baseline) — both parallel-safe to Phase 8 which gates on this phase.`

    2. Update "Last activity" line to:
       `2026-05-21 — Phase 5 closed. HERO-04 gate green locally; docs/m5-phase-5-lhci-run.md committed; HANDOFF.md / PROJECT.md / STATE.md flipped to "shipped"; REQUIREMENTS.md Traceability updated.`

    3. Update "Resume from" line to:
       `Pick the next M5 phase: /gsd-discuss-phase 6 (Analytics) or /gsd-discuss-phase 7 (SEO). Both are independent and parallel-safe to Phase 8. Phase 8 (Motion) is now unblocked by Phase 5's LCP headroom.`

    4. Append to the trailing timestamp footer:
       `*Last updated: 2026-05-21 by Phase 5 Plan 05 (HERO-04 perf gate + ship). Phase 5 complete. M5 progress: 1/5 phases shipped.*`

    **`.planning/REQUIREMENTS.md` edits (Traceability table):**

    Find each of these 4 rows in the Traceability table:
    ```
    | HERO-01 | Phase 5 | — | Open |
    | HERO-02 | Phase 5 | — | Open |
    | HERO-03 | Phase 5 | — | Open |
    | HERO-04 | Phase 5 | — | Open |
    ```

    Replace with:
    ```
    | HERO-01 | Phase 5 | 02 | ✓ Done |
    | HERO-02 | Phase 5 | 03 | ✓ Done |
    | HERO-03 | Phase 5 | 04 | ✓ Done |
    | HERO-04 | Phase 5 | 05 | ✓ Done |
    ```

    Also flip the corresponding checkboxes at the top of REQUIREMENTS.md ("Active (M5) > Hero performance"):
    - `- [ ] **HERO-01**` -> `- [x] **HERO-01**`
    - `- [ ] **HERO-02**` -> `- [x] **HERO-02**`
    - `- [ ] **HERO-03**` -> `- [x] **HERO-03**`
    - `- [ ] **HERO-04**` -> `- [x] **HERO-04**`

    Update the footer timestamp:
    `*Last updated: 2026-05-21 after Phase 5 shipped. HERO-01..04 marked done; 21 active requirements across 5 categories, 4 shipped + 17 remaining. Coverage: 21/21.*`

    Do NOT update the ROADMAP.md progress table in this task — `/gsd-transition` handles roadmap-level updates and is the next ceremony after this plan lands.
  </action>
  <verify>
    <automated>grep -q "Phase 5 SHIPPED" HANDOFF.md && grep -q "Good (Phase 5 shipped" .planning/PROJECT.md && grep -q "Phase 5 SHIPPED 2026-05-21" .planning/STATE.md && [ $(grep -c "\[x\] \*\*HERO-0[1-4]\*\*" .planning/REQUIREMENTS.md) -eq 4 ] && grep -q "| HERO-01 | Phase 5 | 02 |" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 5 SHIPPED" HANDOFF.md` succeeds
    - `grep -q "Good (Phase 5 shipped" .planning/PROJECT.md` succeeds
    - `grep -q "Phase 5 SHIPPED 2026-05-21" .planning/STATE.md` succeeds
    - `grep -c "\[x\] \*\*HERO-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns `4`
    - `grep -c "| HERO-0[1-4] | Phase 5 | 0[2-5] | ✓ Done |" .planning/REQUIREMENTS.md` returns `4`
    - `grep -c "| HERO-0[1-4] | Phase 5 | — | Open |" .planning/REQUIREMENTS.md` returns `0`
    - The LCP value appears in HANDOFF.md, PROJECT.md, and STATE.md (cross-doc consistency)
    - No stale "In flight" text remains in PROJECT.md Key Decisions for the hero MP4 row
  </acceptance_criteria>
  <done>All 4 docs reflect Phase 5 as shipped; HERO-01..04 checkboxes flipped; Traceability table populated with Plan numbers + Done status.</done>
</task>

<task type="auto">
  <name>Task 5: Final Playwright + build sanity check + commit Phase 5 close</name>
  <files>(only the 4 docs files modified by Task 4; LHCI run doc from Task 3)</files>
  <read_first>
    - HANDOFF.md, .planning/PROJECT.md, .planning/STATE.md, .planning/REQUIREMENTS.md (all 4 edited in Task 4)
    - docs/m5-phase-5-lhci-run.md (from Task 3)
  </read_first>
  <action>
    Step 1: Final sanity check.
    ```
    npm run typecheck && npm run lint && npm run build && npx playwright test --reporter=list
    ```

    All four must exit 0. Expect the Playwright report to show 164 existing + 2 (HERO-01) + 1 (HERO-02) + 2 (HERO-03) = 169 passed; 0 skipped (all Wave 0 stubs are now filled).

    Step 2: Commit. Stage only the files Task 3 and Task 4 modified:
    ```
    git add \
      docs/m5-phase-5-lhci-run.md \
      HANDOFF.md \
      .planning/PROJECT.md \
      .planning/STATE.md \
      .planning/REQUIREMENTS.md
    ```

    Commit with GPG off + Co-Authored-By (substitute `<LCP_MS>` with the real value from docs/m5-phase-5-lhci-run.md):
    ```
    git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
    docs(phase-5): close Phase 5 — / LCP <LCP_MS>ms gate green; ship record

    Phase 5 SHIPPED 2026-05-21. The /  LCP-under-2.5s-on-4G launch
    blocker is cleared.

    Representative-run LCP under m4-perf-baseline throttling
    (412×823, DPR 1.75, cpuSlowdownMultiplier 4, Slow 4G; median-of-3):
    <LCP_MS> ms. Gate at 2300 ms passes with (2300 - <LCP_MS>) ms
    headroom under the gate and (2500 - <LCP_MS>) ms headroom under
    CLAUDE.md §12's public 2500 ms contract.

    Phase 5 plan map (all green):
    - Plan 01 (Wave 0): scaffolding — REQUIREMENTS.md HERO-01 amended
      to 720p/540p/360p ladder shape (source is 1280×720 per ffprobe);
      scripts/build-hero-ladder.sh + verify-hero-keyframes.sh; fonts.ts
      + woff2 (sha256 in commit body); next.config.ts AVIF/WebP +
      31-day TTL; lighthouserc.json + .github/workflows/perf.yml; 3
      Playwright spec stubs.
    - Plan 02 (Wave 1, HERO-01): 6 ladder binaries; sources array;
      <video> 6-source mapping; mobile-video-free preserved (D-04).
    - Plan 03 (Wave 1, HERO-02): Fontshare @import removed; generalSans
      wired in layout.tsx; Wordmark.tsx leads with --font-general-sans
      defensively; .dn-node animation + reduced-motion gate preserved.
    - Plan 04 (Wave 1, HERO-03): <Image priority> -> preload +
      fetchPriority="high" (Next 16 API); Sharp serves AVIF on
      Accept: image/avif; zero priority-deprecation warnings.
    - Plan 05 (Wave 2, HERO-04, this commit): LHCI gate exercised
      locally + manual visual walkthrough green at 1440/1024/768
      (cinematic smooth on all ladder variants; wordmark visually
      identical; no AVIF banding on dark gradient); docs/m5-phase-5-lhci-run.md
      captured + temp-public-storage URLs recorded.

    Documents flipped to "shipped":
    - HANDOFF.md: "M5 — Launch readiness" section records ship; Decisions
      locked records final ladder shape + LCP value.
    - .planning/PROJECT.md: Key Decisions table "Defer hero MP4 perf fix
      to M5" row flipped to ✓ Good (Phase 5 shipped).
    - .planning/STATE.md: Status + Last activity + Resume from updated.
    - .planning/REQUIREMENTS.md: HERO-01..04 checkboxes flipped to [x];
      Traceability table HERO-01->Plan 02, HERO-02->Plan 03, HERO-03->Plan
      04, HERO-04->Plan 05, all ✓ Done.

    M5 progress: 1/5 phases shipped. Phase 8 (Motion) is unblocked; Phases
    6 (Analytics) and 7 (SEO) are still parallel-safe to anything else.

    Refs: HERO-01, HERO-02, HERO-03, HERO-04
    Phase: 05-hero-performance / Wave 2 / Plan 05 (closing commit)

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Step 3: `git status` confirms clean working tree.

    Step 4: Print a short status block to stdout:
    ```
    PHASE 5 COMPLETE
    LCP: <LCP_MS> ms (gate 2300; public 2500)
    LHCI report URLs: <URL1> / <URL2> / <URL3>
    Files committed: 5 (docs + 4 planning docs)
    Next: /gsd-transition (then /gsd-discuss-phase 6 OR 7 OR 8)
    ```
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build && npx playwright test --reporter=list 2>&1 | tail -3 && git log -1 --pretty=%s | grep -q "docs(phase-5): close Phase 5" && git log -1 --pretty=%B | grep -q "Co-Authored-By: Claude Opus 4.7" && git diff --quiet HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` all exit 0
    - `npx playwright test` exits 0 with all 169 tests passing (164 existing + 5 new); 0 skipped
    - `git log -1 --pretty=%s` starts with `docs(phase-5): close Phase 5`
    - `git log -1 --pretty=%B` contains the measured LCP value
    - `git log -1 --pretty=%B` contains "Co-Authored-By: Claude Opus 4.7 (1M context)"
    - `git log -1 --pretty=%B` mentions all 5 plans (01..05)
    - `git diff --quiet HEAD` succeeds
    - `git show --stat HEAD` lists 5 files (the LHCI run doc + 4 planning docs)
  </acceptance_criteria>
  <done>Phase 5 closed. Final commit landed with measured LCP, plan map, and per-commit-docs-rule docs all in one commit. Clean working tree. Phase 8 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local production build → LHCI runner | `next start --port 3200` is a transient local server; LHCI hits it over loopback with no auth surface |
| LHCI runner → Google temporary-public-storage | Synthetic Lighthouse reports uploaded to Google's anonymized time-bounded public storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V10-01 | Tampering | woff2 (already-validated artifact) | accept (out of scope) | Plan 03 already validated the woff2 via next/font/local at build time. This plan only re-exercises the build; no new validation surface. |
| T-V12-01 | Repudiation (bisect cost) | public/hero/*.{mp4,webm} | accept (out of scope) | Plan 02 owns; this plan does not modify binaries. |
| T-INFO-01 | Information Disclosure | LHCI temporary-public-storage upload | accept | Phase 5's headline measurement-and-record task IS this upload. The temp-public-storage is anonymized + time-bounded by Google; marketing-site perf data is non-sensitive (no PII, no auth-bearing tokens, just synthetic LCP/CLS/INP/TBT/FCP audits). Same posture already accepted in M4 baseline runs. |
| T-TAMP-01 | Tampering | scripts/build-hero-ladder.sh | accept (out of scope) | This plan does not invoke encoder scripts. |
</threat_model>

<verification>
After Task 5 lands the commit:
- All 164 existing + 5 new Phase 5 Playwright specs pass; 0 skipped
- `npm run build` exits 0 with zero `priority` deprecation warnings
- `npx --no-install lhci autorun` exits 0 (representative-run LCP < 2300 ms)
- `docs/m5-phase-5-lhci-run.md` records the 3 LCP values + median + comparison vs M4 baseline + 3 temp-storage URLs
- HANDOFF.md / .planning/PROJECT.md / .planning/STATE.md flipped to "shipped"
- .planning/REQUIREMENTS.md HERO-01..04 checkboxes are [x] and Traceability rows have ✓ Done
- Single final commit with `docs(phase-5): close Phase 5` subject, GPG off, Co-Authored-By footer
- Clean working tree
</verification>

<validation_strategy>
This plan's tasks map to `.planning/phases/05-hero-performance/05-VALIDATION.md` Per-Task Verification Map as follows:

| 05-VALIDATION.md Row | This Plan's Task |
|---|---|
| TBD-10 (`/` median LCP < 2300 ms under matched throttling, median-of-3) | Task 1 (gate run) |
| Manual-Only Verifications row 1 (cinematic scrub smoothness) | Task 2 Check 1 |
| Manual-Only Verifications row 2 (wordmark visual diff) | Task 2 Check 2 |
| Manual-Only Verifications row 3 (AVIF banding) | Task 2 Check 3 |
| Per-commit docs rule | Task 4 (HANDOFF/PROJECT/STATE/REQUIREMENTS) + Task 5 (commit) |

Sampling cadence per VALIDATION.md:
- Pre-`/gsd-verify-work` check: Full Playwright suite + lhci autorun + zero priority-deprecation warnings — Task 5 runs all three.
- Phase gate: This is THE phase gate. Task 1's autorun + Task 2's manual walkthrough together gate the phase close.

Feedback latency: ~90s for `lhci autorun`; ~3 min for the full Playwright suite at Task 5.
</validation_strategy>

<success_criteria>
Plan 05 is complete when:
- [ ] `npx --no-install lhci autorun` exited 0 locally with representative-run LCP < 2300 ms (Task 1 captured)
- [ ] Manual visual walkthrough green at 1440/1024/768 (Task 2 checkpoint approved by human)
- [ ] `docs/m5-phase-5-lhci-run.md` captures the run record + comparison vs M4 baseline + 3 temp-public-storage URLs (Task 3)
- [ ] HANDOFF.md / .planning/PROJECT.md / .planning/STATE.md / .planning/REQUIREMENTS.md all reflect Phase 5 as shipped (Task 4)
- [ ] HERO-01..04 checkboxes in REQUIREMENTS.md are [x]; Traceability rows show ✓ Done
- [ ] PROJECT.md Key Decisions "Defer hero MP4 perf fix to M5" row flipped to ✓ Good
- [ ] Final commit landed with `docs(phase-5): close Phase 5` subject, GPG off, Co-Authored-By footer, containing the measured LCP value (Task 5)
- [ ] `git diff --quiet HEAD` succeeds
- [ ] `npx playwright test` exits 0 with all 169 tests passing (Task 5)
- [ ] `npm run build` exits 0 with zero `priority` deprecation warnings (Task 5)
</success_criteria>

<output>
After completion, create `.planning/phases/05-hero-performance/05-05-SUMMARY.md` summarizing:
- Measured representative-run LCP value (the headline number)
- The 3 temp-public-storage URLs (inline; durable Lighthouse trace record)
- The 5-plan map with brief status of each
- M4-baseline-to-M5 perf delta (2860 ms → measured ms = -X ms)
- Phase 8 (Motion) headroom remaining
- Reminder that `/gsd-transition` should be run before opening Phase 6/7/8 to advance the roadmap state machine
</output>
