---
phase: 05-hero-performance
plan: 03
type: execute
wave: 1
depends_on: ["05-01-PLAN.md"]
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/components/site/Wordmark.tsx
  - tests/hero/wordmark-self-host.spec.ts
  - HANDOFF.md
  - .planning/STATE.md
autonomous: true
requirements: [HERO-02]
requirements_addressed: [HERO-02]
user_setup: []

must_haves:
  truths:
    - "src/app/globals.css line 1 (the Fontshare @import) is deleted"
    - "src/app/layout.tsx imports generalSans from ./fonts and applies generalSans.variable to <html className>"
    - "Wordmark.tsx inline style uses `var(--font-general-sans)` ahead of Inter in the fontFamily chain"
    - "On `/`, zero network requests fire to api.fontshare.com or cdn.fontshare.com"
    - "The <head> contains a next/font preload <link> for the local woff2"
    - "The wordmark visually still renders General Sans 600 with .dn-node pulse intact (verified by Playwright + manual)"
    - "tests/hero/wordmark-self-host.spec.ts asserts no Fontshare requests, preload link, computed font-family"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "next/font/local generalSans wired into <html> className"
      contains: "generalSans.variable"
    - path: "src/app/globals.css"
      provides: "Fontshare @import removed (line 1); .dn-node keyframes block lines 14-41 intact"
    - path: "src/components/site/Wordmark.tsx"
      provides: "fontFamily uses var(--font-general-sans) for defensiveness against next/font hashing"
      contains: "var(--font-general-sans)"
    - path: "tests/hero/wordmark-self-host.spec.ts"
      provides: "active Playwright spec asserting no Fontshare requests + preload link + computed font-family"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/fonts.ts"
      via: "import { generalSans } from './fonts'"
      pattern: "from \"./fonts\""
    - from: "src/components/site/Wordmark.tsx"
      to: "--font-general-sans CSS variable"
      via: "inline style fontFamily"
      pattern: "var(--font-general-sans)"
---

<objective>
Wire HERO-02: self-host General Sans 600 via `next/font/local`. Remove the Fontshare CDN `@import` from `globals.css` line 1; import `generalSans` (from `src/app/fonts.ts` which Plan 01 created); apply `generalSans.variable` to the `<html>` className alongside Inter; defensively update Wordmark.tsx's inline fontFamily to lead with `var(--font-general-sans)`; replace the wordmark-self-host Playwright stub with an active spec.

Purpose: eliminate the only third-party CDN call on the hero render path. The Fontshare `@import` adds 1 DNS lookup + 1 TLS handshake + 2 sequential HTTP fetches (CSS, then woff2) before the wordmark can paint. next/font/local makes the woff2 a build-time static asset with preload + metric-override fallback (CLS-safe).

Output:
- `src/app/globals.css` line 1 Fontshare `@import` deleted; `.dn-node` keyframes block (lines 14-41 of the old file) intact
- `src/app/layout.tsx` imports `generalSans` and applies `generalSans.variable` to `<html>` className
- `src/components/site/Wordmark.tsx` inline style updated to use `var(--font-general-sans)` defensively
- `tests/hero/wordmark-self-host.spec.ts` filled in with 3 active assertions: no Fontshare network, preload link in head, computed font-family resolves
- HANDOFF.md / STATE.md updated per the per-commit docs rule
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@HANDOFF.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-hero-performance/05-CONTEXT.md
@.planning/phases/05-hero-performance/05-RESEARCH.md
@.planning/phases/05-hero-performance/05-VALIDATION.md
@.planning/phases/05-hero-performance/05-01-PLAN.md
@src/app/fonts.ts
@src/app/layout.tsx
@src/app/globals.css
@src/components/site/Wordmark.tsx
@tests/hero/wordmark-self-host.spec.ts

<interfaces>
<!-- BEFORE this plan -->

`src/app/globals.css` line 1:
```css
@import url("https://api.fontshare.com/v2/css?f[]=general-sans@600&display=swap");
```

`src/app/layout.tsx` lines 1-13 + line 51:
```tsx
import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";

import { SiteFooter } from "@/components/site/SiteFooter";
import { SiteHeader } from "@/components/site/SiteHeader";

import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

// ...

<html lang="en" className={`dark ${inter.variable}`}>
```

`src/components/site/Wordmark.tsx` lines 23-26:
```tsx
style={{
  fontFamily: '"General Sans", Inter, system-ui, sans-serif',
  fontWeight: 600,
}}
```

<!-- AFTER this plan -->

`src/app/globals.css` line 1: (Fontshare @import deleted; the file now starts with `@import "tailwindcss";`)

`src/app/layout.tsx`:
```tsx
import { Inter } from "next/font/google";
import { generalSans } from "./fonts";
// ...
<html lang="en" className={`dark ${inter.variable} ${generalSans.variable}`}>
```

`src/components/site/Wordmark.tsx`:
```tsx
style={{
  fontFamily: 'var(--font-general-sans), "General Sans", Inter, system-ui, sans-serif',
  fontWeight: 600,
}}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Fontshare @import from globals.css line 1</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (the @import on line 1 to remove; the .dn-node block lines 14-41 MUST be preserved)
    - .planning/phases/05-hero-performance/05-RESEARCH.md §"Pattern 3" (this is the only consumer of the Fontshare @import; removing it doesn't break .dn-node which is a pure CSS class + keyframes)
  </read_first>
  <action>
    Use the Edit tool on `src/app/globals.css`. Delete ONLY line 1.

    Find this exact string (line 1):
    `@import url("https://api.fontshare.com/v2/css?f[]=general-sans@600&display=swap");`

    Replace with: (nothing — delete the entire line including the trailing newline)

    Result: globals.css will now start with `@import "tailwindcss";` (formerly line 2; becomes line 1).

    Do NOT touch line 2+ (Tailwind import, shadcn import, custom variant, .dn-node keyframes block lines 14-41 of the old file). The .dn-node CSS class and keyframes MUST remain — Wordmark.tsx still uses it.

    Do NOT add any replacement @font-face declaration manually. next/font/local (wired by Task 2 below) injects the @font-face automatically via the Next 16 build process.

    After the edit, run `grep -q "fontshare" src/app/globals.css` and confirm it exits non-zero (no remaining Fontshare references).
  </action>
  <verify>
    <automated>! grep -qi "fontshare" src/app/globals.css && grep -q "@keyframes dn-node-pulse" src/app/globals.css && grep -q "@import \"tailwindcss\"" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "fontshare" src/app/globals.css` returns `0`
    - `grep -q "@keyframes dn-node-pulse" src/app/globals.css` succeeds (keyframes preserved)
    - `grep -q "\.dn-node {" src/app/globals.css` succeeds (class declaration preserved)
    - `grep -q "@media (prefers-reduced-motion: reduce)" src/app/globals.css` succeeds (reduced-motion suppression preserved)
    - `grep -q "@import \"tailwindcss\"" src/app/globals.css` succeeds
    - The file's first non-empty line is `@import "tailwindcss";` (verify: `head -1 src/app/globals.css`)
  </acceptance_criteria>
  <done>Fontshare @import removed; .dn-node keyframes + class + reduced-motion suppression all preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire generalSans into src/app/layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current imports + RootLayout)
    - src/app/fonts.ts (the module Plan 01 Task 6 created; exports `generalSans` with variable: '--font-general-sans')
    - .planning/phases/05-hero-performance/05-RESEARCH.md §"Pattern 3" (next/font/local layout.tsx pattern)
  </read_first>
  <action>
    Use the Edit tool on `src/app/layout.tsx`. Two edits:

    **Edit 1:** Add the `generalSans` import. Find this string (lines 1-2):
    ```tsx
    import type { Metadata, Viewport } from "next";
    import { Inter } from "next/font/google";
    ```

    Replace with:
    ```tsx
    import type { Metadata, Viewport } from "next";
    import { Inter } from "next/font/google";

    import { generalSans } from "./fonts";
    ```

    **Edit 2:** Add `${generalSans.variable}` to the html className. Find this string (line 51):
    `<html lang="en" className={`dark ${inter.variable}`}>`

    Replace with:
    `<html lang="en" className={`dark ${inter.variable} ${generalSans.variable}`}>`

    That is the complete wiring. Do not change `metadata`, `viewport`, the body className, the SiteHeader/SiteFooter/skip-link structure, or anything else.

    next/font/local automatically:
    1. Hashes the font family name to prevent collisions
    2. Injects a `<link rel="preload" as="font" type="font/woff2" href="/_next/static/media/GeneralSans-Semibold.XXXXXXXX.woff2" crossorigin>` into the `<head>` for every page (because layout.tsx is the root layout)
    3. Generates a `@font-face` rule with `size-adjust`, `ascent-override`, `descent-override`, and `line-gap-override` derived from `adjustFontFallback: 'Arial'` (declared in fonts.ts) to keep CLS low while the woff2 loads
    4. Binds the resulting family name to the CSS variable `--font-general-sans` (the variable name we configured in fonts.ts)

    Task 3 below switches Wordmark.tsx to consume the CSS variable.

    After the edit, run `npm run build` to confirm:
    - No "Failed to find font file" errors (proves Pitfall 5 is avoided — the woff2 is at src/app/fonts/ not public/fonts/)
    - The build emits `/_next/static/media/GeneralSans-Semibold.<hash>.woff2` (visible in `.next/static/media/` after build)
  </action>
  <verify>
    <automated>grep -q 'import { generalSans } from "./fonts"' src/app/layout.tsx && grep -q "generalSans.variable" src/app/layout.tsx && grep -q "inter.variable.*generalSans.variable" src/app/layout.tsx && npx tsc --noEmit && npm run build > /tmp/build.log 2>&1; tail -5 /tmp/build.log && ls .next/static/media/ 2>/dev/null | grep -i "general" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import { generalSans } from "./fonts"' src/app/layout.tsx` succeeds
    - `grep -c "generalSans.variable" src/app/layout.tsx` returns `1`
    - The html className in layout.tsx references both `inter.variable` AND `generalSans.variable` on the same line
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 with no "Failed to find font file" errors
    - After build, `ls .next/static/media/ | grep -i "general"` returns at least one `.woff2` file (proves next/font picked up and copied the asset)
  </acceptance_criteria>
  <done>generalSans wired into root layout; build emits the woff2 to /_next/static/media/ with a content hash; no font-resolution errors.</done>
</task>

<task type="auto">
  <name>Task 3: Update Wordmark.tsx inline fontFamily to lead with var(--font-general-sans)</name>
  <files>src/components/site/Wordmark.tsx</files>
  <read_first>
    - src/components/site/Wordmark.tsx (current 35 lines; inline style on lines 23-26 is the surgical edit)
    - .planning/phases/05-hero-performance/05-RESEARCH.md §"Pattern 3" (CSS variable defensiveness vs next/font family-name hashing)
  </read_first>
  <action>
    Use the Edit tool on `src/components/site/Wordmark.tsx`. Find this exact string (lines 23-26):

    ```tsx
          style={{
            fontFamily: '"General Sans", Inter, system-ui, sans-serif',
            fontWeight: 600,
          }}
    ```

    Replace with:
    ```tsx
          style={{
            // HERO-02: lead with the CSS variable next/font/local generates.
            // The variable points at the hashed family name that the @font-face
            // injection produces, so we resolve to the local woff2 even if
            // next/font hashes "General Sans" to something like "__General_Sans_abc123".
            // The literal "General Sans" + Inter + system-ui chain remains as a
            // safety net for environments where next/font hasn't hydrated yet.
            fontFamily: 'var(--font-general-sans), "General Sans", Inter, system-ui, sans-serif',
            fontWeight: 600,
          }}
    ```

    Do NOT change the className, do NOT touch the `<span>` structure, do NOT touch the `.dn-node` element, do NOT touch the WordmarkProps interface. The only edit is the fontFamily string.

    Do NOT switch the rendering to use `className={generalSans.className}` instead of the inline style — Wordmark.tsx is a Server Component (no "use client"), and importing the font module here would couple the wordmark to the fonts module's React tree position. The CSS variable approach is the recommended path per RESEARCH.md.
  </action>
  <verify>
    <automated>grep -q "var(--font-general-sans)" src/components/site/Wordmark.tsx && grep -q "fontWeight: 600" src/components/site/Wordmark.tsx && grep -q "\"General Sans\"" src/components/site/Wordmark.tsx && grep -q "Inter, system-ui, sans-serif" src/components/site/Wordmark.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--font-general-sans)" src/components/site/Wordmark.tsx` returns `1`
    - `grep -q "fontWeight: 600" src/components/site/Wordmark.tsx` succeeds
    - `grep -q "\"General Sans\"" src/components/site/Wordmark.tsx` succeeds (safety-net family name preserved)
    - The fontFamily string contains `var(--font-general-sans)` BEFORE `"General Sans"` (variable leads): verify by reading the order in the file
    - `grep -q "dn-node" src/components/site/Wordmark.tsx` still succeeds (pulse element untouched)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Wordmark.tsx leads with the CSS variable; literal family name retained as safety net; .dn-node untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Fill tests/hero/wordmark-self-host.spec.ts with live assertions</name>
  <files>tests/hero/wordmark-self-host.spec.ts</files>
  <read_first>
    - tests/hero/wordmark-self-host.spec.ts (the stub from Plan 01 Task 10; the TODO body in the comment is the implementation map)
    - src/components/site/Wordmark.tsx (the DOM the spec inspects)
    - .planning/phases/05-hero-performance/05-VALIDATION.md Per-Task Verification Map row TBD-05
  </read_first>
  <action>
    Use the Write tool to REPLACE `tests/hero/wordmark-self-host.spec.ts` entirely with this active spec:

    ```ts
    import { test, expect } from "@playwright/test";

    /**
     * HERO-02 regression: the DebtNext wordmark renders with General Sans 600
     * served from the site's own origin (next/font/local), not Fontshare's CDN.
     *
     * Three assertions in one test:
     *   1. No requests to api.fontshare.com or cdn.fontshare.com fire on /
     *   2. A next/font preload <link> for the local woff2 is present in <head>
     *   3. The wordmark's computed font-family chain includes a General Sans entry
     */
    test.describe("HERO-02: wordmark self-host", () => {
      test("no Fontshare requests + preload link + computed font-family resolves", async ({ page }) => {
        const fontshareRequests: string[] = [];
        page.on("request", (req) => {
          const url = req.url();
          if (url.includes("fontshare.com")) {
            fontshareRequests.push(url);
          }
        });

        await page.goto("/");
        await page.waitForLoadState("networkidle");

        // Assertion 1: no Fontshare network calls
        expect(
          fontshareRequests,
          `expected zero fontshare.com requests, got: ${JSON.stringify(fontshareRequests)}`
        ).toHaveLength(0);

        // Assertion 2: next/font preload link in <head>
        // next/font emits <link rel="preload" as="font" type="font/woff2"
        // href="/_next/static/media/GeneralSans-Semibold.<hash>.woff2" crossorigin>
        const preloadLink = page.locator(
          'head link[rel="preload"][as="font"][href*="GeneralSans"]'
        );
        await expect(
          preloadLink,
          "expected <head> to contain a next/font preload link for the local General Sans woff2"
        ).toHaveCount(1);

        // Assertion 3: computed font-family on the wordmark span (the
        // span inside the header that contains the .dn-node sibling)
        const wordmarkSpan = page
          .locator("header span")
          .filter({ has: page.locator(".dn-node") })
          .first();
        await expect(
          wordmarkSpan,
          "expected to find the wordmark span in the header"
        ).toBeVisible();

        const fontFamily = await wordmarkSpan.evaluate(
          (el) => getComputedStyle(el).fontFamily
        );
        // next/font hashes the family name; the computed value will contain
        // the hashed alias (e.g. __General_Sans_abc123) PLUS the safety-net
        // literal "General Sans" further down the chain. Accept either.
        expect(
          fontFamily,
          `computed font-family did not resolve to General Sans: ${fontFamily}`
        ).toMatch(/General Sans|__General_Sans/i);
      });
    });
    ```

    Note the CSS selector for the wordmark span: `header span` filtered by `:has(.dn-node)`. This matches the DOM produced by Wordmark.tsx (an outer `<span>` containing the "DebtNext" text span AND the `.dn-node` pulse span). The `.first()` is a defensive selector against any header future-proofing.

    Do NOT assert that the preload link uses `crossorigin` — next/font's exact attribute set is not load-bearing for this regression. Do NOT assert specific woff2 byte size — that drifts as the font is re-subsetted.

    After writing, run `npx playwright test tests/hero/wordmark-self-host.spec.ts --reporter=list`. Expect `1 passed`.
  </action>
  <verify>
    <automated>! grep -q "test.skip" tests/hero/wordmark-self-host.spec.ts && grep -q "fontshare.com" tests/hero/wordmark-self-host.spec.ts && grep -q "rel=\"preload\"" tests/hero/wordmark-self-host.spec.ts && grep -q "General Sans" tests/hero/wordmark-self-host.spec.ts && npx playwright test tests/hero/wordmark-self-host.spec.ts --reporter=list 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip" tests/hero/wordmark-self-host.spec.ts` returns `0`
    - `grep -c "test(" tests/hero/wordmark-self-host.spec.ts` returns `1`
    - `grep -q "fontshareRequests" tests/hero/wordmark-self-host.spec.ts` succeeds
    - `grep -q "toHaveLength(0)" tests/hero/wordmark-self-host.spec.ts` succeeds (no-fontshare assertion)
    - `grep -q 'rel="preload"' tests/hero/wordmark-self-host.spec.ts` succeeds
    - `grep -q "getComputedStyle" tests/hero/wordmark-self-host.spec.ts` succeeds
    - `grep -q "General Sans" tests/hero/wordmark-self-host.spec.ts` succeeds
    - `npx playwright test tests/hero/wordmark-self-host.spec.ts --reporter=list` exits 0 with `1 passed`
  </acceptance_criteria>
  <done>tests/hero/wordmark-self-host.spec.ts active; passes; HERO-02 contract enforced by CI.</done>
</task>

<task type="auto">
  <name>Task 5: Run full Playwright suite + update HANDOFF.md / STATE.md + commit</name>
  <files>HANDOFF.md, .planning/STATE.md</files>
  <read_first>
    - HANDOFF.md (verify section headers with `grep -n "^## " HANDOFF.md`)
    - .planning/STATE.md
    - all files modified by Tasks 1-4
  </read_first>
  <action>
    Step 1: Run the full suite.
    `npx playwright test --reporter=list && npm run build`

    Expected if Plan 02 hasn't shipped yet: 164 existing + 1 new HERO-02 = 165 passed; 2 still skipped (HERO-01 stub if Plan 02 hasn't run; HERO-03 stub).

    Expected if Plan 02 has shipped first: 164 + 2 (HERO-01) + 1 (HERO-02) = 167 passed; 1 still skipped (HERO-03).

    If any pre-existing spec fails, halt and report. The most likely surface is a CSS-cascade regression from removing the Fontshare @import (any spec asserting computed font-family on text that incorrectly fell through to "General Sans" via Fontshare's @font-face would now hit Inter instead). Investigate before committing.

    Step 2: Update HANDOFF.md "M5 — Launch readiness + motion pass" section with:
    ```
    - Phase 5 Wave 1 (2026-05-21, HERO-02 shipped): General Sans 600 self-hosted. src/app/globals.css line 1 Fontshare @import deleted; src/app/layout.tsx imports `generalSans` from ./fonts and applies generalSans.variable on <html>; Wordmark.tsx fontFamily leads with `var(--font-general-sans)` defensively. .dn-node keyframes + reduced-motion suppression intact. next/font emits the woff2 to /_next/static/media/ with content hash + automatic preload <link> in <head>. tests/hero/wordmark-self-host.spec.ts asserts zero fontshare.com requests + preload link in <head> + computed font-family resolves.
    ```

    Step 3: Update .planning/STATE.md:
    - "Last activity": `2026-05-21 — Phase 5 Wave 1 HERO-02 (Plan 03) shipped. Fontshare CDN call eliminated; next/font/local generalSans wired via --font-general-sans CSS variable; wordmark visually unchanged (.dn-node pulse + reduced-motion intact); new wordmark-self-host.spec.ts passes.`
    - "Resume from": `Wave 1 partially done. Remaining: 05-04-PLAN.md (HERO-03 AVIF poster). Plan 05 waits on Plans 02/03/04.`

    Step 4: Commit. Stage only files modified by this plan:
    ```
    git add \
      src/app/layout.tsx \
      src/app/globals.css \
      src/components/site/Wordmark.tsx \
      tests/hero/wordmark-self-host.spec.ts \
      HANDOFF.md \
      .planning/STATE.md
    ```

    Commit with GPG off + Co-Authored-By:
    ```
    git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
    feat(phase-5/hero-02): self-host General Sans 600 via next/font/local

    Ship HERO-02: eliminate the Fontshare CDN call on the hero render path
    and self-host the General Sans 600 woff2 (downloaded + sha256-recorded
    in Plan 01 Wave 0).

    - src/app/globals.css line 1 Fontshare @import deleted. .dn-node
      keyframes + class + prefers-reduced-motion suppression block all
      preserved (lines 14-41 of the pre-edit file are intact).
    - src/app/layout.tsx imports `generalSans` from ./fonts (the module
      Plan 01 Task 6 created) and applies `generalSans.variable` to the
      <html> className alongside Inter. next/font/local auto-emits the
      woff2 to /_next/static/media/ with content hash and injects the
      preload <link> in <head>.
    - src/components/site/Wordmark.tsx inline fontFamily leads with
      `var(--font-general-sans)` so we resolve to the local woff2 even
      if next/font hashes the family name. "General Sans" + Inter +
      system-ui chain retained as safety net.
    - tests/hero/wordmark-self-host.spec.ts (active now, no longer
      test.skip) asserts:
        1. Zero requests to api.fontshare.com or cdn.fontshare.com on /
        2. A next/font preload <link> exists in <head> for the local
           General Sans woff2
        3. Computed font-family on the wordmark span resolves to
           General Sans (either the hashed alias or the literal name)

    Security per T-V10-01: the woff2 was downloaded over HTTPS, file +
    sha256 verified in Plan 01 Task 5 (sha256 in the Wave 0 commit body);
    next/font/local validates the woff2 at build time. No display:optional
    (which would mask a broken/malicious font as missing).

    All 164 existing Playwright specs remain green.

    Refs: HERO-02
    Phase: 05-hero-performance / Wave 1 / Plan 03

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Step 5: `git status` — clean working tree (or only Plan 04's files if 04 has started independently).
  </action>
  <verify>
    <automated>npx playwright test --reporter=list 2>&1 | tail -3 && npm run build > /tmp/build.log 2>&1; tail -3 /tmp/build.log; git log -1 --pretty=%s | grep -q "feat(phase-5/hero-02)" && git log -1 --pretty=%B | grep -q "Co-Authored-By: Claude Opus 4.7"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test` exits 0 (new HERO-02 test passes; no existing specs regressed)
    - `npm run build` exits 0
    - `git log -1 --pretty=%s` starts with `feat(phase-5/hero-02):`
    - `git log -1 --pretty=%B` contains "Co-Authored-By: Claude Opus 4.7 (1M context)"
    - `git log -1 --pretty=%B` contains "next/font/local" or "self-host"
    - `git diff --quiet HEAD` succeeds
    - `grep -q "HERO-02 shipped" HANDOFF.md` succeeds
    - `grep -q "Phase 5 Wave 1 HERO-02" .planning/STATE.md` succeeds
  </acceptance_criteria>
  <done>HERO-02 wired and shipped. Fontshare CDN call eliminated; next/font/local pipeline live; spec green; docs + commit updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party CDN → repo (woff2 download) | Plan 01 Task 5 owned the download + checksum; this plan only consumes the binary that already passed T-V10-01 verification |
| build pipeline → static asset | next/font/local validates the woff2 at build time (Task 2 runs `npm run build`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V10-01 | Tampering | src/app/fonts/GeneralSans-Semibold.woff2 | mitigate | Plan 01 Task 5 downloaded over HTTPS with file + size + sha256 verification, recorded sha256 in Plan 01 Task 12 commit body. This plan wires the validated woff2 into the build pipeline via next/font/local, which validates the woff2 at build time. `display: "swap"` (not `"optional"`) ensures a broken or malicious font doesn't silently fall back to Inter; it would FOUT to "General Sans" first and the broken file would fail at the @font-face validation step. |
| T-V12-01 | Repudiation (bisect cost) | public/hero/*.{mp4,webm} | accept (out of scope this plan) | Plan 02 owns the 6 video binaries; this plan only modifies code + tests + docs. |
| T-INFO-01 | Information Disclosure | LHCI temporary-public-storage | accept (out of scope) | Plan 05 owns the LHCI run. |
| T-TAMP-01 | Tampering | scripts/build-hero-ladder.sh | accept (out of scope this plan) | This plan does not invoke any encoder scripts. |
</threat_model>

<verification>
After Task 5 lands the commit:
- All 164 existing Playwright specs remain green
- The new HERO-02 test in tests/hero/wordmark-self-host.spec.ts passes
- `npm run build` exits 0 with no font-resolution errors
- `.next/static/media/` contains a `GeneralSans-Semibold.<hash>.woff2` file (next/font copied the asset)
- HANDOFF.md / STATE.md reflect HERO-02 completion
- Single git commit with `feat(phase-5/hero-02):` subject, GPG off, Co-Authored-By footer
</verification>

<validation_strategy>
This plan's tasks map to `.planning/phases/05-hero-performance/05-VALIDATION.md` Per-Task Verification Map as follows:

| 05-VALIDATION.md Row | This Plan's Task |
|---|---|
| TBD-05 (Fontshare @import removed; next/font/local in layout; wordmark renders without external font request) | Tasks 1 + 2 + 3 wire; Task 4 asserts |

Sampling cadence per VALIDATION.md:
- After every task commit in this plan: `npm run typecheck && npm run lint && npx playwright test tests/hero/wordmark-self-host.spec.ts` (~10s)
- Plan-wave check: Task 5 runs the full Playwright suite (~2 min) and `npm run build`

Feedback latency: ≤30s for the per-task hero-only Playwright run.

Manual verification (per VALIDATION.md row 2): visual diff of `/` nav at 1440px before/after the swap to confirm letter shapes + .dn-node pulse position are pixel-identical. This is deferred to Plan 05's pre-ship walkthrough since the Playwright spec already covers the structural assertions.
</validation_strategy>

<success_criteria>
Plan 03 is complete when:
- [ ] `grep -ci "fontshare" src/app/globals.css` returns `0`
- [ ] `.dn-node` keyframes + class + reduced-motion suppression preserved in globals.css
- [ ] `src/app/layout.tsx` imports `generalSans` and applies `${generalSans.variable}` to `<html>` className alongside `${inter.variable}`
- [ ] `src/components/site/Wordmark.tsx` inline fontFamily leads with `var(--font-general-sans)` before the literal "General Sans"
- [ ] `tests/hero/wordmark-self-host.spec.ts` is active (no `test.skip`) with 1 test that passes
- [ ] `npm run build` exits 0 with no font-resolution errors; `.next/static/media/` contains a `GeneralSans-Semibold.<hash>.woff2`
- [ ] `npx playwright test` exits 0; new HERO-02 test passes; no existing spec regressed
- [ ] HANDOFF.md / .planning/STATE.md updated
- [ ] Single git commit with `feat(phase-5/hero-02):` subject, GPG off, Co-Authored-By footer
- [ ] `git diff --quiet HEAD` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-hero-performance/05-03-SUMMARY.md` summarizing:
- Files modified
- Confirmation that .dn-node animation + reduced-motion gate still work
- Path of the next/font-emitted woff2 in `.next/static/media/`
- Spec count delta (+1 active, -1 skipped)
- Visual-diff manual-verify reminder for Plan 05's walkthrough
</output>
