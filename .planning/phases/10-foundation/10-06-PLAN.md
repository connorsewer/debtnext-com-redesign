---
phase: 10-foundation
plan: 06
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/content/reporting.ts
  - src/content/placement.ts
  - src/content/homepage.ts
  - src/content/solutions.ts
  - src/content/optimization.ts
  - src/content/issues.ts
autonomous: true
requirements: [FND-05]
tags: [cleanup, content, assets]

must_haves:
  truths:
    - "The 6 dead dashboard-dark.png BenefitSplit media fallbacks are removed"
    - "public/product/dashboard-dark.png is NOT deleted (the hero still references it; deferred to Phase 15)"
    - "HomepageHero.tsx use of the PNG is untouched"
    - "Every page that had a removed fallback still supplies a live visual= (verified before removal)"
  artifacts:
    - path: "src/content/homepage.ts"
      provides: "BenefitSplit content without the dead media fallback"
  key_links:
    - from: "src/content/*.ts BenefitSplit objects"
      to: "live visual= prop"
      via: "every BenefitSplit supplies a visual so the media fallback is dead"
      pattern: "visual"
---

<objective>
Remove the 6 dead `dashboard-dark.png` BenefitSplit `media` fallbacks (research-verified exact
file:line locations). Every page supplies a live `visual=`, so the `media` fallback never renders.
This is the FND-05 dead-asset cleanup. The PNG file itself stays (the hero still references it;
its fate is Phase 15), and the hero's use is untouched.

Purpose: kill the vestigial references so the consolidation phase (Phase 13) and any future grep
for the asset returns only the intentional hero use.
Output: 6 content-file edits, each removing one `media: {...}` block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-foundation/10-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Exact dead-fallback locations (grep-verified 2026-06-04). -->
The 6 dead `media: { src: "/product/dashboard-dark.png", alt, width, height }` blocks on BenefitSplit
content objects:
  - src/content/reporting.ts:108
  - src/content/placement.ts:104
  - src/content/homepage.ts:134
  - src/content/solutions.ts:85
  - src/content/optimization.ts:102
  - src/content/issues.ts:121

OUT OF SCOPE (do NOT touch):
  - src/components/sections/HomepageHero.tsx:187  — <Image src="/product/dashboard-dark.png"> hero centerpiece (Phase 15)
  - public/product/dashboard-dark.png  — the PNG file itself (hero still references it; do NOT delete)

Precondition (A4): each removal is valid ONLY if that page's BenefitSplit also supplies a live
`visual=`. Verify per file BEFORE removing — if any BenefitSplit lacks a live visual, removing its
media fallback would blank that section. Research rates these dead with low-medium confidence; the
per-file check is the safety gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify each BenefitSplit supplies a live visual, then remove the 6 dead media fallbacks</name>
  <read_first>
    - src/content/reporting.ts (around line 108), placement.ts (~104), homepage.ts (~134), solutions.ts (~85), optimization.ts (~102), issues.ts (~121)
    - 10-RESEARCH.md §5 "Dead dashboard-dark.png BenefitSplit media fallbacks (6 to remove)" + Assumption A4 (per-file precondition)
  </read_first>
  <action>
    For EACH of the 6 files:
    1. Locate the BenefitSplit content object containing `media: { src: "/product/dashboard-dark.png", ... }`.
    2. PRECONDITION CHECK: confirm the SAME BenefitSplit object (or the component consuming it) supplies a
       live `visual=` / `visual:` so the `media` fallback is genuinely dead. If a file does NOT supply a live
       visual for that split, DO NOT remove its media block — instead record it in the SUMMARY as a
       false-dead exception and leave it intact (A4 safety gate).
    3. For the verified-dead ones, remove the entire `media: { ... }` property block (the src/alt/width/height
       object), leaving the rest of the BenefitSplit object valid TypeScript.
    Do NOT touch HomepageHero.tsx and do NOT delete public/product/dashboard-dark.png.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/content/ && ! grep -rn "dashboard-dark.png" src/content/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "dashboard-dark.png" src/content/` returns NO matches (all 6 dead fallbacks removed) — UNLESS the SUMMARY documents a specific file as a false-dead A4 exception, in which case that one line may remain and the count is justified.
    - `grep -rn "dashboard-dark.png" src/components/sections/HomepageHero.tsx` STILL matches (hero use untouched).
    - `ls public/product/dashboard-dark.png` still exists (PNG not deleted).
    - `npx tsc --noEmit` exits 0 (every edited content object is still valid); `npx eslint src/content/` exits 0.
    - SUMMARY records the per-file precondition result (which were confirmed dead, any A4 exceptions).
  </acceptance_criteria>
  <done>The dead dashboard-dark.png BenefitSplit media fallbacks are removed after per-file live-visual verification; the PNG file and the hero use are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content edit ← live-render | Removing a fallback that is actually load-bearing would blank a section |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Denial of Service (blank section) | removing a media fallback that is not actually dead | mitigate | Per-file A4 precondition check: confirm a live visual= exists before removal; tsc + existing render specs catch a broken content object |
| T-10-08 | Tampering (asset) | deleting the PNG the hero still uses | mitigate | Explicit do-not-delete on public/product/dashboard-dark.png; acceptance criteria assert it still exists and the hero use still matches |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx eslint src/content/` clean.
- grep confirms zero dead fallbacks (modulo documented A4 exceptions), hero use intact, PNG present.
- Runtime (CI): existing render/responsive specs on the affected routes stay green (no blanked sections).
</verification>

<success_criteria>
The 6 dead dashboard-dark.png BenefitSplit media fallbacks are removed; the hero's use and the PNG
file are preserved for Phase 15; every edited content module compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-06-SUMMARY.md`. Record the per-file
precondition results.
</output>
