---
phase: 10-foundation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/motion/tokens.ts
  - src/components/motion/Reveal.tsx
  - src/components/motion/LiveValue.tsx
  - src/components/motion/transitions.ts
  - src/components/motion/PulseDot.tsx
  - src/components/motion/Explorable.tsx
  - src/components/motion/index.ts
  - src/components/product/motion.tsx
  - DESIGN.md
  - .impeccable.md
autonomous: true
requirements: [FND-01, FND-06]
tags: [motion, framer, gsap, css, a11y, perf]

must_haves:
  truths:
    - "A developer can import the full 7-type motion vocabulary from `@/components/motion`"
    - "Every reveal/live primitive renders its final fully-visible state under prefers-reduced-motion (fails OPEN, never stuck at opacity:0)"
    - "The 17 existing consumers of `@/components/product/motion` compile unchanged (re-export shim)"
    - "No eager motion enters the `/` shared chunk; barrel modules are client-only and tree-shaken per-route; GSAP is NOT re-exported by the barrel"
    - "The engine-per-job rule (GSAP scroll-scrub only / Framer entrances+transitions / CSS hover+ambient+states) is documented in DESIGN.md §4.7"
  artifacts:
    - path: "src/components/motion/index.ts"
      provides: "7-type motion vocabulary barrel"
      exports: ["RevealStagger", "RevealItem", "inViewProps", "LiveValue", "NumberShift", "PulseDot", "AmbientField", "CursorGlow", "Hoverable", "Explorable", "crossfade"]
    - path: "src/components/product/motion.tsx"
      provides: "Zero-edit re-export shim keeping 17 consumers compiling"
      contains: "export * from"
    - path: "DESIGN.md"
      provides: "Engine-per-job rule in §4.7"
      contains: "engine-per-job"
  key_links:
    - from: "src/components/product/motion.tsx"
      to: "src/components/motion"
      via: "re-export shim"
      pattern: "export \\* from \"@/components/motion\""
    - from: "src/components/motion/LiveValue.tsx"
      to: "reduced-motion final value"
      via: "useReducedMotion early-return final state"
      pattern: "if \\(reduce\\)"
---

<objective>
Stand up the single motion barrel `src/components/motion/` exposing the full 7-type vocabulary,
migrate `AnimatedNumber`/variants/tokens out of `product/motion.tsx` via a ZERO-EDIT re-export
shim (research-verified: 17 consumers stay compiling), bake the reduced-motion fail-open contract
into the primitives, and document the engine-per-job rule in DESIGN.md §4.7 in the SAME commit
(docs-in-sync). GSAP is never re-exported by the barrel (it stays the single dynamic-import owner
in HeroCinematicController), so `/` keeps its lean eager chunk.

Purpose: this is the contract every Phase 11-15 visual imports from. Get it right once.
Output: 6 barrel modules + index, a shim, DESIGN.md §4.7 + .impeccable.md updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-foundation/10-RESEARCH.md
@.planning/phases/10-foundation/10-SKILLS-BRIEFING.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Exact source to MOVE (verbatim) into the barrel, plus the existing barrel neighbors. -->

src/components/product/motion.tsx exports TODAY (all consumed by the 17 consumers — names must remain importable):
  - tokens: EASE_ENTRANCE, EASE_STATE, STAGGER, DUR_ITEM, DUR_BAR, DUR_COUNT, TINT, TEXT_DEFAULT
  - variants: staggerContainer, fadeUpItem, popItem
  - helper: inViewProps  (initial "hidden" / whileInView "show" / viewport once amount 0.3)
  - components: AnimatedNumber, NumberShift   (full source already read; AnimatedNumber early-returns format(value) under reduce — the fail-open reference)

src/components/motion/ ALREADY contains (the barrel home — add beside these, don't overwrite):
  - CursorGlow.tsx, CursorGlow.module.css, hover.module.css

src/components/ambient/AmbientField.tsx — re-export through the barrel (already prefers-reduced-motion aware via bloomStatic).

React 19.2.4 (skills briefing): NO forwardRef — pass `ref` as a normal prop; use `use()` not `useContext()` for Explorable context.

The 7-type vocabulary → barrel module map (10-RESEARCH.md §2 table):
  1 scroll reveal (Framer)        → Reveal.tsx: RevealStagger/RevealItem + inViewProps (move staggerContainer/fadeUpItem/popItem here)
  2 live data (Framer)            → LiveValue.tsx (=AnimatedNumber), NumberShift; PulseDot.tsx (pulse → static dot under reduce)
  3 hover/cursor (CSS + rAF)      → Hoverable (wraps hover.module.css) + existing CursorGlow (re-export)
  4 ambient drift (CSS)           → re-export AmbientField from ../ambient
  5 micro-interactions (CSS)      → hover.module.css + the documented 7-state contract (no new TS needed; document)
  6 transitions (Framer)          → transitions.ts: shared crossfade variants for AnimatePresence
  7 explorable (Framer+state)     → Explorable.tsx compound-component shell (skill HIGH); keyboard-operable <button> controls, values visible by default
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create barrel modules (tokens, Reveal, LiveValue, PulseDot, transitions, Explorable shell) + index</name>
  <read_first>
    - src/components/product/motion.tsx (move source verbatim; AnimatedNumber is the fail-open reference)
    - src/components/motion/CursorGlow.tsx + hover.module.css (existing barrel neighbors to re-export)
    - src/components/ambient/AmbientField.tsx (re-export source)
    - src/components/product/visuals/parts.tsx (ValueBar/SegmentedBar fail-open pattern: `const shown = reduce || inView`, `initial={reduce ? false : ...}`)
    - 10-RESEARCH.md §2 (the 7-type table + fail-open contract) + 10-SKILLS-BRIEFING.md A (compound components, no forwardRef, use())
  </read_first>
  <behavior>
    - LiveValue (=AnimatedNumber): under reduce, renders final formatted value immediately; off-screen renders 0; in-view counts up; cleanup stops the animation.
    - PulseDot: under reduce renders a static dot (no animate-ping); otherwise renders a pulsing dot. Token-driven color (var(--status-*)).
    - RevealStagger/RevealItem: under reduce, children render at opacity 1, no transform (fail-open); otherwise stagger fade-up on in-view.
    - Explorable shell: a compound component (`<Explorable>` + `Explorable.Toggle`/`Explorable.Panel` via context using `use()`); toggle controls are real `<button>`s, keyboard-operable; values visible by default (not hover-gated). No real flagship payload yet — shell + contract only (first real explorable is Phase 11).
  </behavior>
  <action>
    1. `src/components/motion/tokens.ts` — MOVE the timing/easing tokens verbatim from product/motion.tsx: EASE_ENTRANCE, EASE_STATE, STAGGER, DUR_ITEM, DUR_BAR, DUR_COUNT, TINT, TEXT_DEFAULT. Server-safe (no "use client" needed for pure constants).
    2. `src/components/motion/Reveal.tsx` ("use client") — MOVE staggerContainer, fadeUpItem, popItem, inViewProps. Export thin wrapper components `RevealStagger` (motion.div with staggerContainer + inViewProps) and `RevealItem` (motion.div with fadeUpItem) so consumers get named primitives, not raw variants. Keep the raw variants exported too (the 17 consumers import them by name).
    3. `src/components/motion/LiveValue.tsx` ("use client") — MOVE AnimatedNumber + NumberShift verbatim. Add `export { AnimatedNumber as LiveValue }` alias (new code uses LiveValue; shim/consumers keep AnimatedNumber). Preserve the `if (reduce) { node.textContent = format(value); return; }` fail-open path.
    4. `src/components/motion/PulseDot.tsx` ("use client") — new primitive: `<PulseDot tone="indigo"|"success"|... />`. Pulsing dot via CSS/Framer; under `useReducedMotion()` render a static dot. Color via `var(--status-*)`/`var(--primary)`, never hex. role/aria per CLAUDE.md §11 (decorative → aria-hidden).
    5. `src/components/motion/transitions.ts` — shared crossfade variants for AnimatePresence (type 6); reduced-motion = instant swap (no transition).
    6. `src/components/motion/Explorable.tsx` ("use client") — compound-component shell per skills briefing A.2/A.5: context via `createContext` consumed with `use()`; `<Explorable>` root, `Explorable.Toggle` (real `<button>`, keyboard-operable, focus-visible per #9CB4E8 contract), `Explorable.Panel`. Values visible by default. NO forwardRef. Document in a header comment that the first real explorable flagship ships in Phase 11 (PLATVIS-02).
    7. `src/components/motion/index.ts` — barrel: `export *` from tokens, Reveal, LiveValue, transitions; export PulseDot, Explorable; re-export CursorGlow (existing), and re-export AmbientField from `../ambient/AmbientField`. Add `Hoverable` (a small wrapper applying hover.module.css lift/glow classes) OR document hover.module.css as the type-3 surface. Do NOT export anything from gsap.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/motion/</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/motion/index.ts` exports the 7-type vocabulary: grep confirms `RevealStagger`, `LiveValue`, `PulseDot`, `AmbientField`, `CursorGlow`, `Explorable`, and a transitions/`crossfade` export are reachable from the barrel.
    - `grep -rn "forwardRef" src/components/motion/` returns NO matches (React 19 rule).
    - `grep -rn "import.*gsap" src/components/motion/` returns NO matches (barrel never pulls GSAP).
    - LiveValue.tsx contains `if (reduce)` returning the final formatted value; PulseDot.tsx branches on `useReducedMotion()` to a static dot.
    - `grep -rn "#[0-9a-fA-F]\{6\}" src/components/motion/PulseDot.tsx` returns no hardcoded hex for status colors (uses var(--*) tokens). (TINT/TEXT_DEFAULT constants in tokens.ts carry over verbatim from the existing file and are exempt.)
    - `npx tsc --noEmit` exits 0; `npx eslint src/components/motion/` exits 0.
  </acceptance_criteria>
  <done>Barrel modules created; 7-type vocabulary exported; fail-open baked into LiveValue/PulseDot/Reveal; no forwardRef, no GSAP import; React 19 `use()` for Explorable context.</done>
</task>

<task type="auto">
  <name>Task 2: Convert product/motion.tsx to a re-export shim (zero consumer edits)</name>
  <read_first>
    - src/components/product/motion.tsx (the file being replaced by a shim)
    - src/components/motion/index.ts (the barrel it re-exports, from Task 1)
    - 10-RESEARCH.md §3 ("re-export shim, NOT a codemod"; A3 fallback to named re-exports if tsc flags a duplicate/circular)
  </read_first>
  <action>
    Replace the body of `src/components/product/motion.tsx` with a thin re-export shim so all 17
    existing consumers keep importing from `@/components/product/motion` unchanged:
    `export * from "@/components/motion";`
    Add a header comment: "Re-export shim (Phase 10 FND-06). Source moved to src/components/motion/.
    Existing consumers import unchanged; import-path cleanup is a later mechanical codemod, NOT this phase."
    If `export *` triggers a duplicate-export or circular-import tsc error (A3), fall back to explicit
    named re-exports of exactly the names the 17 consumers use (EASE_*, DUR_*, STAGGER, TINT,
    TEXT_DEFAULT, staggerContainer, fadeUpItem, popItem, inViewProps, AnimatedNumber, NumberShift).
    Verify parts.tsx (which imports DUR_BAR, EASE_ENTRANCE from product/motion) still resolves.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/product/motion.tsx` re-exports from the barrel: `grep -n "@/components/motion" src/components/product/motion.tsx` matches.
    - `npx tsc --noEmit` exits 0 (the 17 consumers compile unchanged — this is the FND-06 zero-edit migration proof).
    - `grep -rln "@/components/product/motion" src/ | wc -l` still returns 17 (no consumer import paths changed in this plan).
    - `npx eslint src/` exits 0.
  </acceptance_criteria>
  <done>product/motion.tsx is a shim; all 17 consumers compile unchanged; barrel is the single source.</done>
</task>

<task type="auto">
  <name>Task 3: Document the engine-per-job rule in DESIGN.md §4.7 + .impeccable.md (same commit)</name>
  <read_first>
    - DESIGN.md lines 484-501 (§4.7 Motion: token table + "### Motion rules" — exact insertion point)
    - .impeccable.md (design principles — add the engine-per-job principle if a principles list exists)
    - 10-RESEARCH.md §2 "Engine-per-job must be written into DESIGN.md §4.7" + 10-SKILLS-BRIEFING.md B.8
    - CLAUDE.md §5 (voice rules apply to the prose: no em dashes, no "not X it's Y", banned phrases)
  </read_first>
  <action>
    1. In DESIGN.md §4.7, AFTER the existing "### Motion rules" list (line ~501), add a new subsection
       "### Motion engine-per-job" stating the division (prose must pass CLAUDE.md §5 voice rules — no
       em dashes, sentence case headings, contractions, digits):
         - GSAP: scroll-scrub and pin cinematics only. Lazy-loaded via dynamic import, desktop-only,
           client-only. The single owner is HeroCinematicController. Never imported by the motion barrel.
         - Framer Motion: entrances, reveals, whileInView, and tab/section/route transitions.
         - CSS: hover, focus, ambient drift, and the 7 component interaction states.
         - Rule for consumers: import a named primitive from src/components/motion/. Never hand-roll
           Framer or GSAP per page.
       Also state the reduced-motion fail-open contract one line: every reveal/live primitive renders
       its final visible state under prefers-reduced-motion (content is never stuck at opacity:0).
    2. In `.impeccable.md`, add a one-line principle/reference pointing to DESIGN.md §4.7 engine-per-job
       (only if .impeccable.md has a principles or motion section; otherwise add a brief "Motion" note).
       Keep both edits in this task so they land in the same commit as the barrel (docs-in-sync rule).
    3. Run a voice self-check on the added prose: grep the additions for em dashes and the banned
       "not X, it's Y" construction; rewrite if found.
  </action>
  <verify>
    <automated>grep -n "engine-per-job" DESIGN.md && ! grep -n "—" DESIGN.md | grep -i "engine-per-job"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "engine-per-job" DESIGN.md` matches inside §4.7 (after line 501).
    - The new §4.7 prose names all three engines (GSAP scroll-scrub/pin only, Framer entrances/transitions, CSS hover/focus/ambient/states) and the "import a named primitive" consumer rule.
    - The added prose contains zero em dashes and zero "not X, it's Y" constructions (CLAUDE.md §5).
    - `.impeccable.md` references the engine-per-job rule or DESIGN.md §4.7 motion.
    - The §4.7 token table (lines 488-494) and existing "### Motion rules" list are preserved unchanged.
  </acceptance_criteria>
  <done>Engine-per-job rule documented in DESIGN.md §4.7 + cross-referenced in .impeccable.md, voice-clean, in the same commit as the barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/` eager chunk ← motion imports | Adding eager Framer/GSAP to the shared chunk re-opens the M5 LCP budget |
| caption prose ← voice/COI/CLAIMS rules | New DESIGN.md prose is governed copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of Service (perf) | motion barrel pulling GSAP or eager Framer onto `/` | mitigate | Barrel never imports gsap (grep-verified); GSAP stays the single dynamic-import owner in HeroCinematicController; route-JS-budget script (10-01) + mobile-GSAP-free spec enforce |
| T-10-04 | Information disclosure (claims) | new DESIGN.md prose making an unsourced claim | accept | §4.7 prose is internal motion doctrine, no product metrics; voice self-check run; no [CLAIMS REVIEW] surface |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (the 17-consumer zero-edit migration proof).
- `npx eslint src/` clean.
- `grep` confirms barrel exports the 7-type vocabulary, no forwardRef, no gsap import, shim in place, engine-per-job documented.
- Runtime (CI / Vercel preview): reduced-motion + reveal-fail-open + mobile-GSAP-free specs (10-01) green; route-JS-budget script confirms `/` did not grow.
</verification>

<success_criteria>
The 7-type motion vocabulary is importable from `@/components/motion`; every reveal/live primitive
fails open under reduced motion; the 17 existing consumers compile unchanged through the shim; the
barrel never pulls GSAP and `/` stays lean; the engine-per-job rule lives in DESIGN.md §4.7.
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-02-SUMMARY.md`.
</output>
