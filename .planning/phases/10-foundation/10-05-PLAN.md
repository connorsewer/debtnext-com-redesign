---
phase: 10-foundation
plan: 05
type: execute
wave: 4
depends_on: [10-02, 10-03, 10-04]
files_modified:
  - src/components/product/visuals/Console.tsx
  - src/components/product/visuals/DataStory.tsx
  - src/components/product/visuals/Schematic.tsx
  - src/components/product/visuals/archetypes.tsx
autonomous: true
requirements: [FND-02]
tags: [motion, archetypes, payloads, react19, perf, a11y]

must_haves:
  truths:
    - "Three explicit variant components (ConsoleVisual, DataStoryVisual, SchematicVisual) each render from a typed payload prop"
    - "Archetypes contain zero baked per-page numbers — every product number comes from the payload"
    - "Console is a compound component (Console + Console.* slots) using shared context via use() (React 19, no forwardRef)"
    - "Archetypes are lazy ssr:false with an inline-literal loader, so they never SSR and never enter the / eager chunk"
  artifacts:
    - path: "src/components/product/visuals/Console.tsx"
      provides: "Console archetype (compound) rendering ConsoleData"
      contains: "ConsoleData"
    - path: "src/components/product/visuals/DataStory.tsx"
      provides: "Data-story archetype rendering DataStoryData"
    - path: "src/components/product/visuals/Schematic.tsx"
      provides: "Schematic archetype rendering SchematicData (PROVISIONAL)"
    - path: "src/components/product/visuals/archetypes.tsx"
      provides: "Lazy ssr:false wrappers: ConsoleVisual / DataStoryVisual / SchematicVisual"
      contains: "ssr: false"
  key_links:
    - from: "src/components/product/visuals/Console.tsx"
      to: "src/content/visuals/types.ts"
      via: "data: ConsoleData prop"
      pattern: "data: ConsoleData"
    - from: "src/components/product/visuals/archetypes.tsx"
      to: "Console/DataStory/Schematic"
      via: "next/dynamic ssr:false inline-literal wrappers"
      pattern: "dynamic\\("
---

<objective>
Build the three parametrized archetypes as EXPLICIT VARIANT COMPONENTS (per skills briefing:
`<ConsoleVisual payload>`, `<DataStoryVisual>`, `<SchematicVisual>`, NOT one `<Visual type=...>`),
each rendering entirely from its typed payload (10-04) by composing the motion barrel (10-02) and
the primitives (10-03). Console is a compound component. This is the FND-02 keystone: it makes the
`SolutionsIndustryCards` no-prop duplicate impossible to construct (there is no component to copy,
only a payload to author).

Purpose: every Phase 11-15 visual is "import an archetype, pass a payload." Zero baked numbers, lazy
+ client-only so `/` stays lean, reduced-motion fail-open inherited from the primitives.
Output: 3 archetype components + 1 lazy-wrapper module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-foundation/10-RESEARCH.md
@.planning/phases/10-foundation/10-SKILLS-BRIEFING.md
@CLAUDE.md

<interfaces>
<!-- Everything the archetypes consume already exists after Waves 2-3. No exploration needed. -->

Payloads (10-04) — src/content/visuals/types.ts + index.ts:
  ConsoleData { header, callout?, columns?, rows[], pills?, ariaSummary }
  DataStoryData { eyebrow?, headline, chart: {kind:"area"|"spark"|"bars"|"cards", ...}, annotation?, ariaSummary }
  SchematicData { eyebrow?, title, nodes[], edges[], ariaSummary }  (PROVISIONAL/A2)
  Reference payload: placementConsole (satisfies ConsoleData) for a render smoke test.

Motion barrel (10-02) — @/components/motion:
  RevealStagger/RevealItem, LiveValue (count-up, fail-open), NumberShift, PulseDot (static under reduce),
  CursorGlow, AmbientField, Explorable (compound shell), crossfade transitions. NEVER import gsap directly.

Primitives (10-03) — @/components/product/primitives:
  WorklistRow (ConsoleRow), ChartFrame (ariaSummary-wired), FlowNode, FlowEdge (provisional)
  + existing EyebrowLabel, LiveStatus, MetricCell, StatPill, ProductCanvas, ProductCard
Bars/charts — @/components/product/visuals/parts: SegmentedBar, ValueBar, AreaLine, Sparkline, Tag.

Lazy-wrapper pattern (10-RESEARCH.md §"Lazy archetype wrapper" + existing src/components/product/visuals/index.tsx):
  next/dynamic options MUST be an INLINE OBJECT LITERAL (Turbopack static-analysis requirement):
    const LazyConsole = dynamic(() => import("./Console").then(m => m.Console), { ssr: false, loading: VisualSkeleton });
    export function ConsoleVisual({ data }: { data: ConsoleData }) { return <LazyConsole data={data} />; }
  The loading skeleton must match the resolved box (Pitfall 4, CLS) — reuse the existing Fallback shape.

Skills (10-SKILLS-BRIEFING.md): explicit variant components (A.1), compound components for explorable
(A.2), state-decouple/dependency-injection (A.3), React 19 no-forwardRef + use() for context (A.5).
A11y: meaningful visual → role="img" + aria-label fed by payload.ariaSummary; pulse via PulseDot (A1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Console compound archetype (renders ConsoleData, zero baked numbers)</name>
  <read_first>
    - src/content/visuals/types.ts + placement.ts (the ConsoleData shape + reference payload)
    - src/components/product/visuals/PlacementMatrix.tsx (the layout to generalize into the archetype — header/callout/columns/rows/pills)
    - src/components/product/primitives/WorklistRow.tsx + parts.tsx (atoms to compose)
    - src/components/motion/index.ts (LiveValue, PulseDot, RevealStagger to use)
    - 10-SKILLS-BRIEFING.md A.2 (compound component) + A.5 (use(), no forwardRef); 10-RESEARCH.md §1 A1 (pulse via PulseDot)
  </read_first>
  <action>
    Create `src/components/product/visuals/Console.tsx` ("use client") as a COMPOUND component rendering
    `{ data }: { data: ConsoleData }` with zero hardcoded product numbers (every value from `data`):
    - Root `Console` provides ConsoleData via context consumed with `use()` (React 19; no forwardRef).
    - Slots: `Console.Header` (eyebrow/title/subtitle/status via LiveStatus; when status.live render <PulseDot>
      per A1, which collapses to a static dot under reduced motion; optional kpi block via MetricCell/LiveValue),
      `Console.Callout` (icon/title/sub/action), `Console.Rows` (maps data.rows → WorklistRow, each row's bar
      → SegmentedBar/ValueBar by segment count, trailing → LiveValue when animate:"count"), `Console.Pills`
      (StatPill list). Provide a default flat render so `<Console data={...} />` works without manual slotting,
      AND expose the slots for explorable composition (skill A.2).
    - The whole console is wrapped in ProductCanvas with `role="img"` + `aria-label={data.ariaSummary}`.
    - Compose RevealStagger/RevealItem for entrance; all motion inherits fail-open from the barrel.
    - Smoke-render against `placementConsole` (import in a type-level or comment test) to confirm the schema drives it.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/product/visuals/Console.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Console.tsx` takes `data: ConsoleData` (grep `data: ConsoleData`) and is a compound component (grep `Console.Header`/`Console.Rows` or equivalent slot assignments).
    - Context is consumed with `use(` and there is NO `forwardRef` (grep: `use(` present, `forwardRef` absent).
    - ZERO baked product numbers: `grep -nE "[^a-zA-Z_][0-9]{3,}" src/components/product/visuals/Console.tsx` returns no product-data magic numbers (only legit code constants like viewBox/indices are allowed — code-review gate per 10-VALIDATION.md manual-only). Every rendered number traces to `data.*`.
    - The console region exposes `role="img"` + `aria-label={data.ariaSummary}` (grep).
    - Pulse is rendered via `PulseDot` from the barrel when `status.live` (grep `PulseDot`), not a hand-rolled animate-ping.
    - `grep -rn "import.*gsap" src/components/product/visuals/Console.tsx` returns NO matches.
    - `npx tsc --noEmit` exits 0; eslint clean.
  </acceptance_criteria>
  <done>Console compound archetype renders ConsoleData with zero baked numbers, pulse via PulseDot, ariaSummary text alternative, React 19 use() context, no GSAP.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DataStory + Schematic archetypes (render from payload, zero baked numbers)</name>
  <read_first>
    - src/content/visuals/types.ts (DataStoryData chart union + SchematicData)
    - src/components/product/visuals/SolutionsIndustryCards.tsx (the duplicate DataStory.cards subsumes)
    - src/components/product/visuals/parts.tsx (AreaLine/Sparkline/Tag) + primitives ChartFrame/FlowNode/FlowEdge
    - 10-RESEARCH.md §1 DataStoryData + SchematicData (A2 provisional) + Anti-pattern this kills
  </read_first>
  <action>
    1. Create `src/components/product/visuals/DataStory.tsx` ("use client") rendering `{ data }: { data: DataStoryData }`:
       switch on `data.chart.kind` ("area"→AreaLine, "spark"→Sparkline, "bars"→bar series, "cards"→the
       SolutionsIndustryCards layout fed by data.chart.cards). Wrap in ChartFrame with caption (data.headline),
       optional annotation (data.annotation), and `ariaSummary` text alternative. Zero baked numbers.
    2. Create `src/components/product/visuals/Schematic.tsx` ("use client") rendering `{ data }: { data: SchematicData }`:
       lay out data.nodes via FlowNode, data.edges via FlowEdge (flow animation via strokeDashoffset when
       edge.flow + motion allowed; static under reduced motion). role="img" + aria-label={data.ariaSummary}.
       Header comment: "PROVISIONAL (A2): schema + atoms harden in Phase 11 against real 'how it works' data."
    3. Both compose RevealStagger entrances from the barrel; neither imports gsap.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/product/visuals/DataStory.tsx src/components/product/visuals/Schematic.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DataStory.tsx takes `data: DataStoryData` and switches on `data.chart.kind` covering area/spark/bars/cards (grep the kinds); the `cards` branch reproduces the SolutionsIndustryCards layout from payload data (the duplicate-killer).
    - Schematic.tsx takes `data: SchematicData`, composes FlowNode/FlowEdge, carries a PROVISIONAL/A2 header comment, and exposes role="img" + aria-label from ariaSummary.
    - Neither file imports gsap (`grep -rn "import.*gsap"` returns nothing); both expose an ariaSummary-fed text alternative.
    - Zero baked product numbers (same code-review gate as Task 1).
    - `npx tsc --noEmit` exits 0; eslint clean on both.
  </acceptance_criteria>
  <done>DataStory + Schematic archetypes render from typed payloads with zero baked numbers; cards variant subsumes the duplicate; Schematic flagged provisional; no GSAP.</done>
</task>

<task type="auto">
  <name>Task 3: Lazy ssr:false archetype wrappers (keep archetypes off the / eager chunk)</name>
  <read_first>
    - src/components/product/visuals/index.tsx (existing next/dynamic inline-literal pattern — Turbopack requirement)
    - src/content/visuals/types.ts (payload prop types for the wrappers)
    - 10-RESEARCH.md "Lazy archetype wrapper (Turbopack inline-literal requirement)" + Pitfall 4 (skeleton matches resolved box)
  </read_first>
  <action>
    Create `src/components/product/visuals/archetypes.tsx` ("use client") exporting the three EXPLICIT
    VARIANT components as lazy wrappers (mirror index.tsx exactly):
      const LazyConsole = dynamic(() => import("./Console").then(m => m.Console), { ssr: false, loading: VisualSkeleton });
      export function ConsoleVisual({ data }: { data: ConsoleData }) { return <LazyConsole data={data} />; }
      ...DataStoryVisual, SchematicVisual likewise.
    - The dynamic() options MUST be inline object literals (Turbopack static analysis); do not hoist them.
    - `VisualSkeleton` (a local Fallback) must match the resolved box to avoid CLS (Pitfall 4) — reuse the
      `min-h-[20rem] rounded bg-[var(--product-canvas)]` shape from index.tsx's Fallback.
    - Export the three `*Visual` functions as the public archetype API (this is what Phase 11-15 pages import).
    - Do NOT statically import Console/DataStory/Schematic at module top (that would defeat ssr:false).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/product/visuals/archetypes.tsx && grep -q "ssr: false" src/components/product/visuals/archetypes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `archetypes.tsx` exports `ConsoleVisual`, `DataStoryVisual`, `SchematicVisual`, each typed to its payload (`data: ConsoleData` etc.) and each wrapping a `dynamic(..., { ssr: false, loading: ... })` call with an INLINE literal options object (grep `ssr: false` 3 times).
    - The archetype components are imported only inside the dynamic `import()` thunks (no top-level static import of Console/DataStory/Schematic): `grep -n "^import.*\\./Console" archetypes.tsx` returns nothing.
    - The loading skeleton reuses the resolved-box shape (grep `min-h-` or `product-canvas`).
    - `npx tsc --noEmit` exits 0; eslint clean.
  </acceptance_criteria>
  <done>Three explicit lazy ssr:false archetype variant components exported as the public API; never SSR'd, never on the / eager chunk; skeletons match the resolved box.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/` eager chunk ← archetype imports | A statically-imported archetype would pull Framer onto the shared chunk |
| archetype render ← payload-only data | A baked number in the archetype escapes the [CLAIMS REVIEW] audit surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Denial of Service (perf/CLS) | archetype SSR'd or skeleton mismatching resolved box | mitigate | ssr:false inline-literal dynamic wrappers; skeleton matches resolved box (Pitfall 4); route-JS-budget + mobile-GSAP-free specs (10-01) enforce |
| T-10-13 | Information disclosure (claims) | a baked number in archetype JSX bypassing the payload audit | mitigate | Zero-baked-number grep + code-review gate (10-VALIDATION manual-only); every number traces to data.*; payload is the single [CLAIMS REVIEW] surface |
| T-10-14 | Tampering (a11y) | meaningful visual with no text alternative | mitigate | Every archetype exposes role="img" + aria-label fed by payload.ariaSummary (required field); pulse via PulseDot stays accessible under reduced motion |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (archetypes are payload-typed; the placementConsole smoke render type-checks).
- `npx eslint src/components/product/visuals/` clean.
- grep: explicit variant components, `use()` not forwardRef, no gsap import, ssr:false inline literals, ariaSummary-fed role=img, zero baked numbers.
- Runtime (CI / Vercel preview): reveal-fail-open + reduced-motion + mobile-GSAP-free specs (10-01) green on the routes that mount these; route-JS-budget confirms `/` did not grow; LHCI TBT on visual routes within budget.
</verification>

<success_criteria>
Three explicit archetype variant components render entirely from typed payloads with zero baked numbers,
Console is a compound component using React 19 use() context, all three are lazy ssr:false and never enter
the `/` eager chunk, every archetype exposes an ariaSummary text alternative, and none imports GSAP. A
developer can now build any product visual by importing an archetype and passing a payload.
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-05-SUMMARY.md`.
</output>
