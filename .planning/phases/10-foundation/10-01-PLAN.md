---
phase: 10-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/routes.ts
  - tests/responsive/reveal-fail-open.spec.ts
  - tests/responsive/reduced-motion.spec.ts
  - tests/responsive/hero-gsap-free-mobile.spec.ts
  - scripts/check-route-js-budget.sh
  - lighthouserc.json
  - .github/workflows/perf.yml
autonomous: true
requirements: [FND-05, FND-06]
tags: [motion, ci, perf, a11y, testing]

must_haves:
  truths:
    - "CI fails if `/` First-Load-JS exceeds its byte budget (route-JS-budget script wired into perf.yml)"
    - "CI fails if any in-viewport text node sits at opacity < 1 after scroll + tab/accordion open (reveal-fail-open spec)"
    - "CI fails if any visual-heavy route fetches GSAP at mobile width (generalized mobile-GSAP-free spec)"
    - "LHCI asserts total-blocking-time on `/` plus at least one platform and one solutions route"
  artifacts:
    - path: "scripts/check-route-js-budget.sh"
      provides: "Route First-Load-JS budget guard for /"
      contains: "app-build-manifest"
    - path: "tests/responsive/reveal-fail-open.spec.ts"
      provides: "no-stuck-opacity:0 reveal regression net"
    - path: "tests/helpers/routes.ts"
      provides: "VISUAL_ROUTES list covering solutions + company + compare sub-pages"
      contains: "VISUAL_ROUTES"
  key_links:
    - from: ".github/workflows/perf.yml"
      to: "scripts/check-route-js-budget.sh"
      via: "bash step after the hero-asset-budget step"
      pattern: "check-route-js-budget"
    - from: "lighthouserc.json"
      to: "total-blocking-time"
      via: "assertMatrix assertion + collect.url additions"
      pattern: "total-blocking-time"
---

<objective>
Land all six Wave 0 validation gaps from `10-VALIDATION.md` BEFORE the implementation
they guard. This is the keystone-of-the-keystone: every later Phase 10 plan attaches its
`<automated>` verify to a spec/script/config created here. Nothing in this plan touches
`src/` production code, so it runs first and alone in Wave 1 with zero file conflicts.

Purpose: make the fail-open contract, the `/` JS budget, the TBT proxy, and the
mobile-GSAP-free guarantee enforceable in CI so consumers in Phases 11-15 cannot regress them.
Output: 1 new script, 1 new spec, 3 extended specs/configs, 1 workflow wiring, 1 routes list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-RESEARCH.md
@.planning/phases/10-foundation/10-VALIDATION.md

<interfaces>
<!-- Patterns the executor reuses directly. No exploration needed. -->

Existing budget-guard pattern — scripts/check-hero-assets.sh:
  - bash, `set -euo pipefail`, declare -a BUDGETS=("file:bytes"), portable `stat -f %z` || `stat -c %s`, exit on FAIL.
  - Wired in .github/workflows/perf.yml as: `- name: Verify hero asset budgets / run: bash scripts/check-hero-assets.sh` (line 22-23), BEFORE the "Run Lighthouse CI" step.

Existing mobile-GSAP-free spec — tests/responsive/hero-gsap-free-mobile.spec.ts:
  - 412x823 viewport, `page.on("request")` watcher, `/gsap/i` URL pattern, scroll-to-bottom-and-back, assert `gsapRequests` length 0.

Existing reduced-motion spec — tests/responsive/reduced-motion.spec.ts:
  - iterates `ROUTES` from ../helpers/routes, `emulateMedia({reducedMotion:"reduce"})`, scrolls bottom+back, asserts no running animation with duration > 1ms.

Existing routes — tests/helpers/routes.ts:
  - `export const ROUTES = ["/", "/platform", "/platform/placement", "/platform/optimization", "/platform/issues", "/platform/reporting", "/solutions", "/why-dplat", "/company", "/resources", "/demo"] as const;`
  - MISSING: the 6 solutions sub-pages, /compare, /company/{about,leadership,careers,contact}, /platform/integrations.

Existing lighthouserc.json:
  - `collect.url` is `["http://localhost:3200/"]` only. `throttlingMethod: "devtools"` (keep). assertMatrix has ONE entry matching `/$` asserting `largest-contentful-paint` maxNumericValue 2300 median-run.

Next 16 build manifest: `.next/app-build-manifest.json` maps route entry → JS chunk file list. First-Load-JS for `/` = the byte sum of the chunks listed under the `/page` (or `/`) key. Alternative: parse `npm run build` stdout "First Load JS" column. Manifest-parse preferred (no new dep).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend routes.ts with a VISUAL_ROUTES list + land the reveal-fail-open spec</name>
  <read_first>
    - tests/helpers/routes.ts (the ROUTES + BREAKPOINTS exports)
    - tests/responsive/reduced-motion.spec.ts (the scroll-to-surface + poll structure to mirror)
    - tests/responsive/hero-gsap-free-mobile.spec.ts (request-watcher style, for consistency)
    - 10-RESEARCH.md §4c (reveal-fail-open spec spec) + "Validation note on collect.url and ROUTES drift"
  </read_first>
  <action>
    1. In `tests/helpers/routes.ts`, ADD (do not remove ROUTES — existing specs iterate it) a new export:
       ```ts
       // Routes that carry product visuals / archetypes (Phases 11-15 add the most here).
       // The reveal-fail-open + reduced-motion + mobile-GSAP-free specs iterate this so the
       // foundation's nets cover the pages later phases touch. See 10-RESEARCH.md §4 ROUTES drift.
       export const VISUAL_ROUTES = [
         "/",
         "/platform", "/platform/placement", "/platform/optimization",
         "/platform/issues", "/platform/reporting", "/platform/integrations",
         "/solutions", "/solutions/utilities", "/solutions/financial-services",
         "/solutions/telecom", "/solutions/fintech", "/solutions/insurance",
         "/solutions/healthcare",
         "/why-dplat", "/compare",
         "/company", "/company/about", "/company/leadership",
         "/company/careers", "/company/contact",
         "/resources", "/demo",
       ] as const;
       ```
    2. Create `tests/responsive/reveal-fail-open.spec.ts`. For each `route` in `VISUAL_ROUTES`: `page.goto(route)`, `waitForLoadState("networkidle")`, scroll to `document.body.scrollHeight` then back to 0 (200ms waits, mirroring reduced-motion.spec), then open every accordion/tab control (click each `[data-state="closed"]` accordion trigger and each tab button if present, guarded so absence is not a failure). Then assert: no in-viewport, text-bearing element has computed `opacity < 1`. Implementation: in `page.evaluate`, collect elements whose `getBoundingClientRect()` intersects the viewport AND whose `textContent.trim()` is non-empty AND `getComputedStyle(el).opacity` parsed as float `< 1`; return up to 5 offenders {tag, className, opacity}; assert the array is empty with a diagnostic message. Run under default (motion-on) color-scheme dark.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint tests/helpers/routes.ts tests/responsive/reveal-fail-open.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VISUAL_ROUTES" tests/helpers/routes.ts` matches; the list contains the 6 solutions sub-pages, /compare, /platform/integrations, and the 4 /company sub-pages.
    - `tests/responsive/reveal-fail-open.spec.ts` exists, imports `VISUAL_ROUTES` from `../helpers/routes`, and contains a `getComputedStyle` + `opacity` `< 1` assertion.
    - `npx tsc --noEmit` exits 0; `npx eslint` on both files exits 0.
    - ROUTES is still exported unchanged (existing specs unbroken): `grep -n "export const ROUTES" tests/helpers/routes.ts` matches.
  </acceptance_criteria>
  <done>VISUAL_ROUTES exported; reveal-fail-open spec authored and type/lint-clean; ROUTES untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend reduced-motion spec (opacity===1) + generalize mobile-GSAP-free across visual routes</name>
  <read_first>
    - tests/responsive/reduced-motion.spec.ts (current "no running animations" assertion to extend)
    - tests/responsive/hero-gsap-free-mobile.spec.ts (the / -only network watcher to generalize)
    - tests/helpers/routes.ts (now exports VISUAL_ROUTES from Task 1)
    - 10-RESEARCH.md §4c (reduced-motion opacity===1) + §4d (mobile-GSAP-free generalization)
  </read_first>
  <action>
    1. In `tests/responsive/reduced-motion.spec.ts`: keep the existing "no running animations" loop over ROUTES. ADD a second assertion inside the same per-route test (or a sibling test in the same file iterating VISUAL_ROUTES): after the scroll-surface step, under `emulateMedia({reducedMotion:"reduce"})`, assert that text-bearing in-viewport elements have computed `opacity === 1` (reuse the offender-collection helper shape from reveal-fail-open). This proves reveals fail OPEN under reduced motion, not just "no animation running".
    2. Generalize the mobile-GSAP-free guarantee: in `tests/responsive/hero-gsap-free-mobile.spec.ts`, keep the existing `/`-only test, and ADD a loop over `VISUAL_ROUTES` (412x823, same `/gsap/i` request watcher, scroll bottom+back) asserting zero `/gsap/` requests per route. (A sibling spec file is acceptable if cleaner; if so, name it `tests/responsive/visual-routes-gsap-free-mobile.spec.ts` and import VISUAL_ROUTES.) Rationale: Console/DataStory archetypes are Framer/CSS only; only a Schematic `edge.flow` may touch GSAP and that must stay desktop-only + lazy. This spec enforces it as Phases 11-15 add archetypes.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint tests/responsive/reduced-motion.spec.ts tests/responsive/hero-gsap-free-mobile.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "opacity" tests/responsive/reduced-motion.spec.ts` matches an `=== 1` (or `< 1` offender) assertion; the file still imports ROUTES and now also references VISUAL_ROUTES.
    - The mobile-GSAP-free coverage iterates VISUAL_ROUTES (grep `VISUAL_ROUTES` in `hero-gsap-free-mobile.spec.ts` OR the sibling `visual-routes-gsap-free-mobile.spec.ts`); the original `/`-only test is preserved.
    - `npx tsc --noEmit` exits 0; `npx eslint` on the touched specs exits 0.
  </acceptance_criteria>
  <done>Reduced-motion spec asserts opacity===1 on visual routes; mobile-GSAP-free guarantee covers all VISUAL_ROUTES; original tests preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Route First-Load-JS budget script + LHCI TBT assertion + perf.yml wiring</name>
  <read_first>
    - scripts/check-hero-assets.sh (the exit-1 budget-guard pattern to model)
    - lighthouserc.json (collect.url + assertMatrix to extend; throttlingMethod devtools — KEEP)
    - .github/workflows/perf.yml (the "Verify hero asset budgets" step to slot after)
    - 10-RESEARCH.md §4a (route JS budget, manifest-parse, set budget to current+headroom) + §4b (TBT not INP, devtools-measured budget)
  </read_first>
  <action>
    1. Create `scripts/check-route-js-budget.sh` modeled on `check-hero-assets.sh`: `set -euo pipefail`; after a build, read `/`'s First-Load-JS from `.next/app-build-manifest.json` (sum the byte sizes of the JS chunk files listed for the `/page` entry under `.next/`), compare to a byte budget, exit 1 if over with a diagnostic, exit 0 with a PASS+percent line otherwise. If the manifest shape is not parseable, fall back to grepping the `npm run build` stdout "First Load JS" row for `/` (document the chosen method in a header comment). Set the budget as a `ROUTE_JS_BUDGET_BYTES` variable with a header comment instructing: "Set to the current post-5.3 `/` First-Load-JS measured on CI + ~10% headroom; capture the real number on the first green CI build and pin it here (mirror how check-hero-assets.sh cites Phase 5.1 D-07)." Use a conservative placeholder (e.g. document it as PROVISIONAL) so the script is wired but the exact ceiling is pinned on first measured CI build — note this explicitly in the SUMMARY so the executor records the measured number.
    2. Extend `lighthouserc.json`: (a) add to `collect.url` at least one platform route and one solutions route alongside `/` (e.g. `http://localhost:3200/platform/placement`, `http://localhost:3200/solutions/utilities`); (b) add a `total-blocking-time` assertion to the assertMatrix (e.g. a new matrix entry or extend existing) `["error", { "maxNumericValue": 200, "aggregationMethod": "median-run" }]`. KEEP `throttlingMethod: "devtools"`. Add a comment-by-convention (since JSON has no comments, note in the plan SUMMARY) that the TBT budget is set against a measured devtools-throttled run, NOT a guessed simulate number, and that INP is field-only (deferred to RUM) so TBT is the lab proxy.
    3. Wire the script into `.github/workflows/perf.yml`: add a step `- name: Verify route JS budget / run: bash scripts/check-route-js-budget.sh` AFTER "Verify hero asset budgets" and after the Build step (the build must have produced `.next/app-build-manifest.json`), BEFORE "Run Lighthouse CI".
  </action>
  <verify>
    <automated>bash -n scripts/check-route-js-budget.sh && node -e "JSON.parse(require('fs').readFileSync('lighthouserc.json','utf8'))" && grep -q "check-route-js-budget" .github/workflows/perf.yml && grep -q "total-blocking-time" lighthouserc.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-route-js-budget.sh` exists, is `bash -n`-valid, defines a `ROUTE_JS_BUDGET_BYTES` ceiling, references `app-build-manifest.json` (or documents the stdout-parse fallback), and exits 1 when `/` First-Load-JS exceeds the budget.
    - `lighthouserc.json` is valid JSON; `collect.url` includes `/` plus a `/platform/*` and a `/solutions/*` route; assertMatrix contains a `total-blocking-time` assertion; `throttlingMethod` is still `"devtools"`.
    - `.github/workflows/perf.yml` runs `bash scripts/check-route-js-budget.sh` after the Build + hero-asset step and before the LHCI step (`grep -n "check-route-js-budget" .github/workflows/perf.yml`).
    - SUMMARY records that the route-JS budget number and TBT budget must be pinned to the first measured devtools CI build (PROVISIONAL until then).
  </acceptance_criteria>
  <done>Route-JS-budget script created + wired; LHCI asserts TBT on / + a platform + a solutions route under devtools; budgets flagged PROVISIONAL for first-CI pinning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI ← repo source | Scripts/specs gate merges; a mis-wired guard would let regressions through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | check-route-js-budget.sh budget set too loose | mitigate | Budget pinned to measured post-5.3 `/` First-Load-JS + ~10% headroom on first CI build; SUMMARY records the number |
| T-10-02 | Denial of Service (false-green) | LHCI TBT budget guessed against simulate not devtools | mitigate | Budget set against a measured devtools-throttled run; INP correctly deferred to RUM (not asserted in lab) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 across the new/extended specs.
- `npx eslint` clean on every touched file.
- `bash -n scripts/check-route-js-budget.sh` valid; `lighthouserc.json` parses; perf.yml references the new script.
- Runtime (CI / Vercel preview, NOT local — `next build/start` hang in sandbox): the new specs run green via `PLAYWRIGHT_BASE_URL=<preview>`; LHCI runs in CI.
</verification>

<success_criteria>
All six 10-VALIDATION.md Wave 0 gaps exist and are wired: reveal-fail-open spec, route-JS-budget script in perf.yml, lighthouserc TBT + visual routes, generalized mobile-GSAP-free spec, VISUAL_ROUTES in routes.ts, reduced-motion opacity===1 assertion. No production `src/` code touched. ROUTES and existing specs preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-01-SUMMARY.md`. Record the measured `/` First-Load-JS number and TBT budget chosen (or that they remain PROVISIONAL pending the first CI build).
</output>
