---
phase: 10-foundation
plan: 04
type: execute
wave: 3
depends_on: [10-02, 10-03]
files_modified:
  - src/content/visuals/types.ts
  - src/content/visuals/placement.ts
  - src/content/visuals/index.ts
autonomous: true
requirements: [FND-03]
tags: [motion, payloads, types, claims-review]

must_haves:
  truths:
    - "A typed payload model (ConsoleData, DataStoryData, SchematicData) lives in src/content/visuals/, server-safe, importable by Server Components at zero client cost"
    - "Every payload number is [CLAIMS REVIEW]-auditable in one location"
    - "ConsoleData expresses BOTH existing Placement implementations (PlacementMatrix multi-segment + PlacementMockup single-value) without baked constants"
    - "Each payload type carries a REQUIRED ariaSummary text alternative"
  artifacts:
    - path: "src/content/visuals/types.ts"
      provides: "ConsoleData / DataStoryData / SchematicData typed schemas"
      contains: "export interface ConsoleData"
    - path: "src/content/visuals/placement.ts"
      provides: "Reference Console payload validating the schema against PlacementMatrix data"
      contains: "[CLAIMS REVIEW]"
  key_links:
    - from: "src/content/visuals/placement.ts"
      to: "src/content/visuals/types.ts"
      via: "typed payload satisfies ConsoleData"
      pattern: "ConsoleData"
---

<objective>
Create the typed per-context payload home `src/content/visuals/` — the single `[CLAIMS REVIEW]`-
auditable location for every product number a visual renders. Define the three schemas
(`ConsoleData`, `DataStoryData`, `SchematicData`) as three explicit interfaces (no discriminated
union, no `type` field per research Open Question 2), and PROVE `ConsoleData` by authoring a reference
payload (`placement.ts`) that maps the real PlacementMatrix data onto the schema with zero baked
constants. This is the highest-risk artifact in the phase: a schema that cannot express both real
Placement consoles would force a fork in Phase 13.

Purpose: a visual = archetype + payload. The payload contract is defined here so 10-05's archetypes
render from data with zero hardcoded numbers, and so Andrew/Paul audit every claim in one directory.
Output: types.ts (3 schemas) + placement.ts (reference Console payload) + index.ts barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-foundation/10-RESEARCH.md
@.planning/phases/10-foundation/10-SKILLS-BRIEFING.md
@CLAUDE.md

<interfaces>
<!-- The schema shapes are specified VERBATIM in 10-RESEARCH.md §1. Copy them; do not redesign. -->

ConsoleData (10-RESEARCH.md §1 — slot-shaped, compound-component-ready). Author types.ts with these
exact interfaces: BarSpec { segments: number[]; label?: string; tone?: "indigo"|"success"|"warning"|"neutral" },
ConsoleHeader { eyebrow?; title; subtitle?; status?: { label; live? }; kpi?: { caption; value; valueSuffix?; decimals?; sub? } },
ConsoleCallout { icon?: "route"|"alert"|"check"; title; sub?; action? },
ConsoleRow { primary; secondary?; bar?: BarSpec; trailing?: { value; suffix?; decimals?; animate?: "count"|"shift"|"none" } },
ConsoleData { header: ConsoleHeader; callout?: ConsoleCallout; columns?: { primary?; bar?; trailing? }; rows: ConsoleRow[]; pills?: { label; tone? }[]; ariaSummary: string }.

DataStoryData (10-RESEARCH.md §1): { eyebrow?; headline; chart: area|spark|bars|cards (discriminated by `kind`); annotation?: { value; caption }; ariaSummary }.
  The `cards` variant subsumes the SolutionsIndustryCards duplicate (name/accent/tag/value/decimals?/suffix?/bar/sub).

SchematicData (10-RESEARCH.md §1 — MEDIUM confidence, A2, mark PROVISIONAL): SchematicNode { id; label; sub?; kind?: "source"|"engine"|"vendor"|"sink" }, SchematicEdge { from; to; label?; flow? }, SchematicData { eyebrow?; title; nodes; edges; ariaSummary }.

Real PlacementMatrix data to map onto ConsoleData (src/content/visuals/placement.ts — the validation):
  header: { eyebrow: "PLACEMENT MANAGEMENT", title: "Routing rules", subtitle: "National network · decision engine active · 14 rules live", status: { label: "LIVE" } }
  callout: { icon: "route", title: "1,847 accounts ready to route", sub: "Loaded from billing · evaluated against tier rules", action: "Routing now" }
  columns: { primary: "Treatment tier", bar: "Vendor allocation", trailing: "Accounts" }
  rows: 4 (Pre-collect 3 vendors·30-day [40,35,25] 12847; Primary 3 vendors·60-day [45,35,20] 8420; Secondary 2 vendors·90-day [60,40] 4108; Tertiary 2 vendors·120-day [55,45] 2290) — trailing.animate "count"
  pills: ["Reconciliation 04:00 today", "Recall windows configured", { label: "27 vendors active", tone: "indigo" }]
  ariaSummary: governed copy describing the routing console in words.

The PlacementMockup case (single-value pct rows, KPI block, "Engine running" pulse) must ALSO be
expressible: rows use bar.segments:[35] (single), header.kpi present, header.status.live:true (pulse
rendered by the archetype via <PulseDot> — A1). Add a comment in placement.ts noting both cases are
covered (the validation conclusion), even though only the Matrix payload is authored here.

Governance (CLAUDE.md §5/§6/§7/§15): every number → `[CLAIMS REVIEW]` comment; any vendor/TSI framing
→ `[COI REVIEW]`. ariaSummary + any caption is governed copy (no em dashes, no "not X it's Y", banned
phrases, sentence case, digits). Numbers stay anonymized/generic/qualified.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three payload schemas in src/content/visuals/types.ts</name>
  <read_first>
    - 10-RESEARCH.md §1 (the three schemas verbatim + the validation table + A1/A2 gaps)
    - 10-SKILLS-BRIEFING.md A (three explicit interfaces, NOT a discriminated union; state-decouple-implementation)
    - src/content/visuals/ does not exist yet — this task creates the directory
  </read_first>
  <action>
    Create `src/content/visuals/types.ts` (server-safe, NO "use client") with the three interfaces copied
    verbatim from 10-RESEARCH.md §1: ConsoleData (+ BarSpec, ConsoleHeader, ConsoleCallout, ConsoleRow),
    DataStoryData (+ its chart discriminated union by `kind`), SchematicData (+ SchematicNode, SchematicEdge).
    - Three SEPARATE exported interfaces; NO union `VisualData` type; NO `type:` discriminator field
      (research Open Question 2 — the lazy registry picks the component, the page picks the payload).
    - Every interface that renders product numbers includes a REQUIRED `ariaSummary: string`.
    - Add a file header comment: "Typed visual payload model (FND-03). Server-safe; importable by Server
      Components at zero client cost. Single [CLAIMS REVIEW]-auditable home for product numbers."
    - Mark SchematicData with a "PROVISIONAL (A2): no existing schematic impl; hardened in Phase 11" comment.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/content/visuals/types.ts && grep -q "export interface ConsoleData" src/content/visuals/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/visuals/types.ts` exports `ConsoleData`, `DataStoryData`, `SchematicData` plus their helper interfaces (grep each).
    - No discriminated-union top type and no `type:` discriminator field on the three schemas (grep returns no `VisualData =` union).
    - Each of ConsoleData/DataStoryData/SchematicData declares a required `ariaSummary: string` (grep 3 matches).
    - No `"use client"` directive (server-safe): `grep -L "use client" src/content/visuals/types.ts`.
    - SchematicData carries a PROVISIONAL/A2 comment.
    - `npx tsc --noEmit` exits 0; eslint clean.
  </acceptance_criteria>
  <done>types.ts defines three explicit payload schemas, server-safe, each with required ariaSummary; Schematic flagged provisional.</done>
</task>

<task type="auto">
  <name>Task 2: Validate ConsoleData against PlacementMatrix — author reference payload + index barrel</name>
  <read_first>
    - src/content/visuals/types.ts (from Task 1)
    - src/components/product/visuals/PlacementMatrix.tsx (the real data being mapped — the validation source)
    - 10-RESEARCH.md §1 "Validation: mapping BOTH Placement implementations onto ConsoleData" (the field-by-field table + the conclusion + A1 pulse gap)
    - CLAUDE.md §5/§15 (governed copy for ariaSummary; [CLAIMS REVIEW] on numbers)
  </read_first>
  <action>
    1. Create `src/content/visuals/placement.ts` (server-safe): export a `placementConsole` constant typed
       `satisfies ConsoleData` (use `satisfies` so excess/missing fields are caught) holding the real
       PlacementMatrix data mapped per the §1 validation table (values listed in <interfaces> above). Use
       trailing.animate "count" for the account counts, bar.segments for the multi-segment allocations,
       and a governed `ariaSummary`.
    2. Add `[CLAIMS REVIEW]` comments above the numeric blocks (mirroring the existing
       `// [CLAIMS REVIEW] placeholder values — Andrew sign-off required` convention in PlacementMatrix.tsx).
       If the ariaSummary or any caption touches the vendor/TSI relationship, add `[COI REVIEW]`.
    3. Add a comment documenting the validation conclusion: that the same ConsoleData also expresses the
       PlacementMockup case (single-value rows via segments:[N], header.kpi, header.status.live:true with the
       pulse rendered by the archetype via PulseDot per A1) — so the schema covers both real consoles without
       baked constants. This comment IS the schema-validation deliverable.
    4. Create `src/content/visuals/index.ts` re-exporting the types and the placement payload, so pages and
       the lazy registry import from one barrel.
    5. Run the voice self-check on ariaSummary/captions (no em dashes, no "not X it's Y", banned phrases).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/content/visuals/ && grep -q "CLAIMS REVIEW" src/content/visuals/placement.ts && grep -q "satisfies ConsoleData" src/content/visuals/placement.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/visuals/placement.ts` exports a payload `satisfies ConsoleData` (grep) carrying the real PlacementMatrix rows/segments/counts/pills/callout (the validation that the schema fits real data with no baked constants).
    - `grep -n "CLAIMS REVIEW" src/content/visuals/placement.ts` matches (numbers are auditable in this dir).
    - A comment documents that ConsoleData also expresses the PlacementMockup case (single-value rows, KPI block, live pulse via PulseDot/A1) — the both-implementations validation conclusion.
    - ariaSummary/captions pass the voice self-check: no em dashes, no "not X, it's Y" in the new strings.
    - `src/content/visuals/index.ts` re-exports types + placement payload.
    - `npx tsc --noEmit` exits 0; `npx eslint src/content/visuals/` exits 0.
  </acceptance_criteria>
  <done>ConsoleData proven against PlacementMatrix via a satisfies-typed reference payload with [CLAIMS REVIEW] markers and a documented both-implementations validation; barrel exports the payload home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload numbers ← claims governance | Every product number rendered must be auditable + sourced (CLAUDE.md §15) |
| payload schema ← real-data fidelity | A schema that cannot express both Placement consoles forces a Phase 13 fork |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Information disclosure (claims) | unsourced/invented metric leaking into a payload | mitigate | All numbers centralized in src/content/visuals/ with [CLAIMS REVIEW]; numbers anonymized/generic/qualified; Andrew/Paul audit one dir (CLAUDE.md §15) |
| T-10-10 | Repudiation (COI) | ariaSummary/caption obscuring the TSI/vendor relationship | mitigate | Any vendor/TSI framing → [COI REVIEW]; voice self-check; "agency-network-agnostic" language only (CLAUDE.md §6) |
| T-10-11 | Tampering (schema fork risk) | ConsoleData cannot express PlacementMockup → forced fork later | mitigate | Reference payload validates against PlacementMatrix; documented coverage of the Mockup case (single-value + KPI + live pulse via A1) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (the `satisfies ConsoleData` proves the payload fits the schema exactly).
- `npx eslint src/content/visuals/` clean.
- grep confirms 3 schemas, required ariaSummary, [CLAIMS REVIEW] markers, satisfies-typed payload, both-implementations validation comment, server-safe (no "use client").
- Manual gate (10-VALIDATION.md): code-review confirms ariaSummary/captions are sourced + voice-clean.
</verification>

<success_criteria>
The typed payload model exists in src/content/visuals/ as three explicit server-safe schemas each with
a required ariaSummary; ConsoleData is proven to express both real Placement consoles with zero baked
constants; every number is [CLAIMS REVIEW]-auditable in one directory.
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-04-SUMMARY.md`. Record the schema-validation
result (does the authored placement payload satisfy ConsoleData; any field the Mockup case could not express).
</output>
