---
phase: 10-foundation
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/product/primitives/WorklistRow.tsx
  - src/components/product/primitives/ChartFrame.tsx
  - src/components/product/primitives/FlowNode.tsx
  - src/components/product/primitives/FlowEdge.tsx
  - src/components/product/primitives/index.ts
autonomous: true
requirements: [FND-04]
tags: [motion, primitives, charts, a11y, svg]

must_haves:
  truths:
    - "New atoms (WorklistRow, ChartFrame, FlowNode, FlowEdge) extend the existing parts.tsx set under one stroke weight, type scale, and color logic"
    - "Every new atom animates transform/opacity/scaleX/clipPath/pathLength only (never width/height/top)"
    - "Every status field is label-paired (a color-only state is impossible to construct)"
    - "Meaningful atoms expose role=img + aria-label; decorative atoms are aria-hidden"
  artifacts:
    - path: "src/components/product/primitives/WorklistRow.tsx"
      provides: "Console row atom (generalizes the inline grid in PlacementMatrix/IssuesWorklist)"
    - path: "src/components/product/primitives/ChartFrame.tsx"
      provides: "Data-story chart container with caption/annotation slots + ariaSummary wiring"
    - path: "src/components/product/primitives/FlowNode.tsx"
      provides: "Schematic node atom (node group)"
    - path: "src/components/product/primitives/FlowEdge.tsx"
      provides: "Schematic edge atom (<path> edge, optional flow animation)"
  key_links:
    - from: "src/components/product/primitives/WorklistRow.tsx"
      to: "src/components/product/visuals/parts.tsx"
      via: "reuses SegmentedBar/ValueBar atoms + tokens"
      pattern: "from \"@/components/product/visuals/parts\""
---

<objective>
Extend the shared primitive set under `src/components/product/primitives/` with the four new atoms
the three archetypes need (WorklistRow, ChartFrame, FlowNode, FlowEdge), reusing the existing
`parts.tsx` bars/charts and the established stroke/type/color logic. These are token-driven,
compositor-only-animated, label-paired-status atoms so the archetypes (10-05) compose them with
zero per-page styling decisions.

Purpose: the archetype layer must assemble atoms, not re-invent them. Pitfall 5 (no width/height
animation) and Pitfall 8 (no color-only status) are enforced at the atom level so consumers cannot
violate them.
Output: 4 new atoms + an updated primitives index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-foundation/10-RESEARCH.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Existing atoms to REUSE (do not re-implement). Extracted from the codebase. -->

src/components/product/visuals/parts.tsx exports (reuse these inside the new atoms):
  - SegmentedBar({ segments: number[], className })  — multi-segment, clipPath-animated (the RIGHT way)
  - ValueBar({ value: number, tone?: "indigo"|"success"|"warning", className }) — scaleX-animated
  - Sparkline({ bars: number[] }), AreaLine({ points: number[] }) — chart atoms
  - Tag({ label, tone }), TypeChip({ label, tone }) — label-paired status chips; ToneKey type
  - TAG_TONE map → var(--status-success|warning|breach|special) + var(--product-text-3)

src/components/product/primitives/ existing (one stroke/type/color logic to match):
  - EyebrowLabel, LiveStatus, MetricCell, StatPill, ProductCanvas, ProductCard, EventBadge, index.ts

Pitfall constraints (10-RESEARCH.md §6 + Pitfalls):
  - Animate transform/opacity/scaleX/clipPath/pathLength ONLY. NEVER width/height/top.
    (Note: PlacementMockup animates raw width — the WRONG way; do not copy it. parts.tsx ValueBar
     uses scaleX — copy THAT.)
  - Every status is label-paired (Pitfall 8): a tone color always travels with a text label/icon.
  - role="img" + aria-label for meaningful atoms; aria-hidden for decorative.
  - Tokens only: var(--chart-1..5), var(--status-*), var(--primary), var(--product-text*). No hex.

SchematicData is MEDIUM confidence (no existing impl; A2). FlowNode/FlowEdge are therefore
PROVISIONAL in Phase 10, to be hardened in Phase 11 against PlatformSystemMap.tsx + real
"how it works" data. Mark them provisional in a header comment.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorklistRow + ChartFrame atoms</name>
  <read_first>
    - src/components/product/visuals/parts.tsx (SegmentedBar/ValueBar/AreaLine/Sparkline/Tag to reuse; fail-open pattern)
    - src/components/product/visuals/PlacementMatrix.tsx (the inline `grid-cols-[1.1fr_2fr_0.6fr]` row WorklistRow generalizes)
    - src/components/product/primitives/index.ts + ProductCard.tsx + StatPill.tsx (style/stroke/type conventions to match)
    - 10-RESEARCH.md §6 (extended primitives list + the token/animation/label rules)
  </read_first>
  <action>
    1. `src/components/product/primitives/WorklistRow.tsx` ("use client") — generalize the PlacementMatrix
       row: a row atom taking `{ primary: string; secondary?: string; bar?: ...; trailing?: ... }` shaped
       to the ConsoleRow fields (10-RESEARCH.md §1). Render primary/secondary text, an optional bar
       (delegate to `SegmentedBar` when multi-segment, `ValueBar` when single — pick by segment count,
       matching the schema's BarSpec), and an optional trailing value (delegate to LiveValue from the
       barrel when `animate:"count"`). Grid layout matching PlacementMatrix's column proportions. Status
       tones are label-paired (reuse Tag/TypeChip). No width/height animation.
    2. `src/components/product/primitives/ChartFrame.tsx` ("use client") — a data-story chart container
       with a caption slot, an optional annotation slot ({value, caption}), and REQUIRED `ariaSummary`
       wiring: the frame sets `role="img"` + `aria-label={ariaSummary}` on the chart region so the chart
       (AreaLine/Sparkline/bars, passed as children) has a text alternative (Pitfall 8). Token-driven
       container styling consistent with ProductCard.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/product/primitives/WorklistRow.tsx src/components/product/primitives/ChartFrame.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; WorklistRow imports SegmentedBar/ValueBar from `@/components/product/visuals/parts` (grep) and the trailing-value animation delegates to the motion barrel (no re-implemented count-up).
    - `grep -nE "animate=.*(width|height|top|left)" src/components/product/primitives/WorklistRow.tsx src/components/product/primitives/ChartFrame.tsx` returns NO matches (Pitfall 5).
    - ChartFrame renders `role="img"` + an `aria-label` fed by an `ariaSummary` prop (grep `ariaSummary` + `role="img"`).
    - `grep -nE "#[0-9a-fA-F]{6}" src/components/product/primitives/WorklistRow.tsx src/components/product/primitives/ChartFrame.tsx` returns no hardcoded hex (tokens only).
    - `npx tsc --noEmit` exits 0; eslint clean on both.
  </acceptance_criteria>
  <done>WorklistRow + ChartFrame atoms created, reuse parts.tsx, compositor-only, label-paired, ariaSummary-wired.</done>
</task>

<task type="auto">
  <name>Task 2: FlowNode + FlowEdge atoms (PROVISIONAL — hardened in Phase 11) + primitives index</name>
  <read_first>
    - src/components/product/visuals/parts.tsx (AreaLine's pathLength animation is the model for FlowEdge flow)
    - src/components/product/primitives/index.ts (export surface to extend)
    - 10-RESEARCH.md §1 SchematicData (SchematicNode/SchematicEdge shapes) + §6 + Assumption A2 (provisional)
  </read_first>
  <action>
    1. `src/components/product/primitives/FlowNode.tsx` ("use client") — a schematic node atom taking
       `{ label: string; sub?: string; kind?: "source"|"engine"|"vendor"|"sink" }`. Render a token-styled
       node group (rounded box, label + optional sub, kind-driven tone via var(--*)). role/aria for
       meaningful nodes. Header comment: "PROVISIONAL (A2): no existing schematic impl; harden in Phase 11
       against PlatformSystemMap.tsx + real 'how it works' data before locking the shape."
    2. `src/components/product/primitives/FlowEdge.tsx` ("use client") — an SVG `<path>` edge atom taking
       `{ from/to geometry or path d; label?: string; flow?: boolean }`. When `flow` is true and motion is
       allowed, animate `strokeDashoffset`/`pathLength` (the AreaLine pattern, compositor-safe) to show data
       traveling; under reduced motion render the static path. CSS strokeDashoffset is the default engine;
       if GSAP is ever needed for flow it must stay desktop-only + lazy (engine-per-job) and the
       mobile-GSAP-free spec (10-01) guards it. Same PROVISIONAL header comment.
    3. Update `src/components/product/primitives/index.ts` to export WorklistRow, ChartFrame, FlowNode,
       FlowEdge alongside the existing atoms.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/product/primitives/</automated>
  </verify>
  <acceptance_criteria>
    - FlowNode.tsx + FlowEdge.tsx exist; both carry a "PROVISIONAL" header comment referencing Phase 11 hardening (grep `PROVISIONAL`).
    - FlowEdge animates `strokeDashoffset` or `pathLength` (grep), not a layout property, and branches on reduced motion for a static fallback.
    - `grep -rn "import.*gsap" src/components/product/primitives/` returns NO matches (CSS strokeDashoffset is the default; any future GSAP flow is lazy/desktop-only, not added here).
    - `src/components/product/primitives/index.ts` exports all four new atoms (grep each name).
    - `npx tsc --noEmit` exits 0; `npx eslint src/components/product/primitives/` exits 0.
  </acceptance_criteria>
  <done>FlowNode + FlowEdge provisional atoms created (compositor-only, reduced-motion-safe, no GSAP); primitives index updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| atom render ← a11y contract | A color-only or text-alternative-less atom would fail WCAG; atoms must make that impossible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information disclosure (a11y) | status atom conveying state by color only | mitigate | Every status is label-paired by API (reuse Tag/TypeChip); ChartFrame requires ariaSummary; axe-core CI (Phases 11-12 consumers) verifies |
| T-10-06 | Denial of Service (perf/CLS) | atom animating width/height causing layout thrash | mitigate | Compositor-only animation enforced; grep guard in acceptance criteria; reuse parts.tsx scaleX/clipPath pattern not PlacementMockup's width |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx eslint src/components/product/primitives/` clean.
- grep guards: no width/height animation, no hardcoded hex, no GSAP import, ariaSummary + role=img present, PROVISIONAL markers on Flow atoms.
- Runtime (CI): axe-core a11y spec stays green once Phase 11/12 consumers render these atoms on real routes.
</verification>

<success_criteria>
Four new atoms extend parts.tsx under one stroke/type/color logic, animate compositor properties only,
pair every status with a label, expose text alternatives, and are exported from the primitives index.
FlowNode/FlowEdge are explicitly provisional pending Phase 11 hardening.
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-03-SUMMARY.md`.
</output>
