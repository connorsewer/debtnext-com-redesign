---
phase: 11-platform-deep-dive-visuals
plan: 05
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - lighthouserc.json
  - .planning/phases/11-platform-deep-dive-visuals/11-PERF-A11Y-EVIDENCE.md
autonomous: false
requirements: [PLATVIS-01, PLATVIS-02, PLATVIS-03]
user_setup: []

must_haves:
  truths:
    - "LHCI collects all 4 platform routes and keeps LCP under 2.5s, CLS under 0.1, INP/TBT under their gates after the visuals land"
    - "The full Playwright suite stays green, including platform-visuals, reveal-fail-open, reduced-motion, and platform-mobile"
    - "axe-core reports no critical violations on the 4 platform routes"
    - "A reviewer confirms on a Vercel preview that all 4 platform pages show distinct archetype visuals and a keyboard/touch-operable flagship, with no two visuals reading alike"
  artifacts:
    - path: ".planning/phases/11-platform-deep-dive-visuals/11-PERF-A11Y-EVIDENCE.md"
      provides: "Recorded LHCI + Playwright + axe results per platform route and the human-verify sign-off"
      contains: "LCP"
    - path: "lighthouserc.json"
      provides: "LHCI config collecting all 4 platform routes (extends the Phase 10 single-route collection)"
      contains: "platform"
  key_links:
    - from: "lighthouserc.json"
      to: "the 4 platform routes"
      via: "collect.url entries"
      pattern: "/platform/"
---

<objective>
Close Phase 11 success criterion 4 with a verification gate: prove the perf and
a11y budgets hold on all 4 platform pages after the visuals land, confirm the
full Playwright suite stays green, and get a human sign-off on the Vercel preview
that the "no two visuals look alike" bar (D-02) and the flagship parity contract
(D-05) are met in the real rendered product.

Purpose: the platform pages are the schema-hardening proving ground before the
Phase 13 homepage handoff depends on them; this gate is where the LHCI / axe /
Playwright contracts inherited from Phase 10 are confirmed against the new payload
load, and where Connor visually approves the credibility fix.

Output: the extended LHCI config, the recorded evidence doc, and the human-verify
sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-platform-deep-dive-visuals/11-CONTEXT.md
@.planning/phases/10-foundation/10-VERIFICATION.md

<interfaces>
<!-- The existing CI / test surface this plan exercises. Already extracted — do not re-explore. -->

lighthouserc.json (Phase 10): collects `/`, `/platform/placement`, `/solutions/utilities` under
`throttlingMethod: devtools`, with a `total-blocking-time` <= 200ms assertion. This plan adds the
other 3 platform routes to collect.url.

Playwright specs that must stay green (tests/responsive/):
- platform-visuals.spec.ts (authored across 11-01..11-04 — all 4 platform routes)
- reveal-fail-open.spec.ts (no in-viewport node at opacity < 1 over VISUAL_ROUTES)
- reduced-motion.spec.ts (opacity === 1 under prefers-reduced-motion over VISUAL_ROUTES)
- hero-gsap-free-mobile.spec.ts (zero /gsap/ requests over VISUAL_ROUTES at 412x823)
- platform-mobile.spec.ts (homepage handoff contract — unrelated but must not regress)

Project memory: next dev/start hang in this sandbox; `next build`, `tsc --noEmit`, and
Playwright-against-a-preview-URL DO run. LHCI requires CI or a built server — run it in CI / against
the preview, not locally.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend LHCI to all 4 platform routes</name>
  <read_first>
    - lighthouserc.json (current collect.url + the total-blocking-time assertion + throttlingMethod)
    - .planning/phases/10-foundation/10-VERIFICATION.md (the LHCI TBT contract and the devtools-throttling decision)
  </read_first>
  <action>
    In `lighthouserc.json`, add `/platform/optimization`, `/platform/issues`, and `/platform/reporting`
    to `collect.url` (placement is already collected). Keep the existing assertions: LCP under 2.5s
    (2500 ms; the platform pages are content routes, not the `/` hero, so the standard CLAUDE.md §12
    floor applies — do NOT inherit the homepage's tighter 2300 ms bar), CLS under 0.1, and the
    `total-blocking-time` <= 200ms assertion under `throttlingMethod: devtools`. Do not loosen any
    ceiling preemptively; if a route trips TBT in CI, that is a real signal handled in Task 2, not a
    reason to pre-widen here.
  </action>
  <acceptance_criteria>
    - `lighthouserc.json` collect.url contains all 4 platform routes: `grep -o "/platform/placement\|/platform/optimization\|/platform/issues\|/platform/reporting" lighthouserc.json` returns all 4
    - The `total-blocking-time` assertion and `throttlingMethod: devtools` are still present
    - `node -e "JSON.parse(require('fs').readFileSync('lighthouserc.json','utf8'))"` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>LHCI collects all 4 platform routes with the inherited perf + TBT assertions intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the perf + a11y + Playwright gates on a preview and record evidence</name>
  <action>Deploy a Vercel preview, run LHCI + the full Playwright suite + axe-core against the 4 platform routes, visually review the pages for distinct visuals and flagship parity, record everything in 11-PERF-A11Y-EVIDENCE.md, and pause for Connor's approval. The detailed steps are in how-to-verify below.</action>
  <what-built>
    Across 11-01..11-04: 19 accordion archetype visuals + 4 Explorable flagships across the 4 platform
    pages, all fed typed real-shaped payloads, with the platform-visuals Playwright spec, the extended
    LHCI config (Task 1), and the inherited reveal-fail-open / reduced-motion / mobile-GSAP-free nets.
  </what-built>
  <how-to-verify>
    Deploy a Vercel preview from the phase branch, then:
    1. CI / preview: confirm LHCI passes on all 4 platform routes — LCP < 2.5s, CLS < 0.1,
       total-blocking-time <= 200ms (devtools throttling). Record the per-route numbers.
    2. Run the full Playwright suite against the preview via `PLAYWRIGHT_BASE_URL`:
       `npx playwright test` — confirm platform-visuals, reveal-fail-open, reduced-motion,
       hero-gsap-free-mobile, and platform-mobile all pass. Record pass counts; note any
       pre-existing main failures (e.g. container-query-layouts.spec.ts:17) as non-regressions.
    3. Run axe-core on the 4 platform routes (the project's axe CI step or `npx playwright test tests/a11y`)
       — confirm no critical violations.
    4. Visit each of /platform/placement, /platform/optimization, /platform/issues, /platform/reporting
       on the preview. Confirm: every accordion item shows a real archetype visual (no "Visual" text
       placeholder); the BenefitSplit flagship is operable by mouse, keyboard (tab + Enter/Space/Arrow),
       and touch; values are visible before any hover; and no two visuals across the 4 pages read alike
       (D-02). Toggle `prefers-reduced-motion` and confirm the same data is visible with no motion.
    5. Record all results in `.planning/phases/11-platform-deep-dive-visuals/11-PERF-A11Y-EVIDENCE.md`
       (per-route LCP/CLS/TBT, Playwright pass counts, axe result, the visual + parity + distinctness
       sign-off).
  </how-to-verify>
  <resume-signal>Type "approved" once LHCI, Playwright, axe all pass and the visual/parity/distinctness review passes, or describe the failures (route + symptom) to route into a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / preview -> evidence record | This plan only runs existing gates and records results; it adds no runtime code path and no new untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Tampering | lighthouserc.json assertions | accept | Config-only change adding routes to collection; assertions are not loosened. Reviewed in Task 1 acceptance criteria. |
| T-11-14 | Spoofing/Repudiation/Info-disclosure/DoS/EoP | n/a (verification gate) | accept | No new untrusted input, data sink, auth surface, or server action; this plan runs read-only gates against a preview. |

No high-severity threats.
</threat_model>

<verification>
- `lighthouserc.json` is valid JSON and collects all 4 platform routes.
- LHCI green on all 4 platform routes (LCP < 2.5s, CLS < 0.1, TBT <= 200ms) — recorded in evidence doc.
- Full Playwright suite green against the preview — recorded.
- axe-core: no critical violations on the 4 platform routes — recorded.
- Human-verify sign-off captured (Task 2 checkpoint).
</verification>

<success_criteria>
- Phase 11 success criterion 4 satisfied: LHCI holds the perf budget and all existing Playwright specs
  stay green after the visuals land.
- The 4 platform pages are confirmed on a preview to show distinct archetype visuals (D-02) and a
  keyboard/touch/reduced-motion-parity flagship (D-05).
- Evidence recorded for the eventual PR description, including [CLAIMS REVIEW]/[COI REVIEW] audit
  surfacing per D-10.
</success_criteria>

<output>
After completion, create `.planning/phases/11-platform-deep-dive-visuals/11-05-SUMMARY.md` summarizing
the gate results and linking the evidence doc. Note any deferred/preview-only items per the sandbox
constraint.
</output>
