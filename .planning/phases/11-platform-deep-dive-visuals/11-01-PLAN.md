---
phase: 11-platform-deep-dive-visuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md
  - src/content/visuals/types.ts
  - src/content/visuals/placement.ts
  - src/content/visuals/index.ts
  - src/content/visuals/placement-accordion.ts
  - src/app/platform/placement/page.tsx
  - src/components/product/visuals/PlacementFlagship.tsx
  - src/components/product/visuals/lazy.tsx
  - tests/responsive/platform-visuals.spec.ts
autonomous: false
requirements: [PLATVIS-01, PLATVIS-02, PLATVIS-03]
user_setup: []

must_haves:
  truths:
    - "A reviewer can read the full ~19-item archetype-mapping table (item id -> archetype -> one-line payload intent) before any payload is authored"
    - "Connor approves the mapping table at a blocking checkpoint before payload authoring begins"
    - "Every FeatureAccordion item on /platform/placement renders a real archetype visual, no text-on-dark placeholder remains"
    - "The /platform/placement BenefitSplit visual is an Explorable-composed archetype instance fed a real payload, values visible by default, keyboard + touch + reduced-motion reach the same data"
    - "Reduced-motion users see the SAME flagship data values that normal-load users see (D-05 data-parity), not just non-zero opacity"
    - "The lazy loading skeleton for each placement visual reserves the resolved archetype box so /platform/placement holds CLS < 0.1 when chunks resolve"
    - "Any schema extension to types.ts keeps the existing placement.ts validation green"
  artifacts:
    - path: ".planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md"
      provides: "The D-03 approval-gated archetype-mapping table for all 19 accordion items across 4 pages"
      contains: "decision-engine"
    - path: "src/content/visuals/placement-accordion.ts"
      provides: "5 typed payloads for the placement accordion items (decision-engine, vendor-pools, recall, business-rules, reconciliation)"
    - path: "src/components/product/visuals/PlacementFlagship.tsx"
      provides: "The placement BenefitSplit explorable flagship, an Explorable-composed Console archetype instance"
    - path: "src/app/platform/placement/page.tsx"
      provides: "Placement page wiring all 5 accordion visuals + the explorable flagship"
  key_links:
    - from: "src/app/platform/placement/page.tsx"
      to: "src/content/visuals/placement-accordion.ts"
      via: "visuals={{ <itemId>: <ArchetypeVisual data={payload} /> }} prop on FeatureAccordion"
      pattern: "visuals=\\{\\{"
    - from: "src/components/product/visuals/PlacementFlagship.tsx"
      to: "src/components/motion/Explorable.tsx"
      via: "Explorable.Toggle / Explorable.Panel composition"
      pattern: "Explorable\\."
---

<objective>
Stand up the Phase 11 pattern on the lowest-risk page (placement) and gate the
full archetype mapping for Connor's approval before any large authoring begins.

Two deliverables, one approval gate between them:
1. The full ~19-item archetype-mapping table (D-03) covering all 4 platform
   pages, refined from the real accordion item ids and body copy, written to
   `11-ARCHETYPE-MAP.md` and approved by Connor at a blocking checkpoint. This
   is the cheapest place to course-correct 19 distinct payloads before they are
   authored.
2. Placement implemented end to end as the proven reference: 5 accordion item
   payloads wired through `FeatureAccordion.visuals` (PLATVIS-01), and the
   placement BenefitSplit visual refactored into an `Explorable`-composed
   Console archetype instance (PLATVIS-02 + PLATVIS-03 together, per D-06).

Purpose: prove the archetype + payload + Explorable model on a page where the
Console reference already exists (PlacementMatrix was validated against
ConsoleData in Phase 10 / 10-04), so optimization / issues / reporting can
parallelize against a locked pattern in Wave 2. This plan also LOCKS two
patterns the Wave 2 pages replicate blind: the lazy-skeleton sizing that holds
CLS, and the reduced-motion data-parity assertion shape. Both are proven here on
placement before the parallel fan-out.

Output: the approved mapping table, the placement payload modules, the placement
flagship component, the wired placement page, and the shared platform-visuals
Playwright spec the Wave 2 pages extend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-platform-deep-dive-visuals/11-CONTEXT.md
@.planning/phases/10-foundation/10-VERIFICATION.md

<interfaces>
<!-- Contracts the executor needs. Already extracted from the codebase — do not re-explore. -->

From src/content/visuals/types.ts (the payload schemas — single source, extend here under D-08):
```typescript
export interface ConsoleData {
  header: ConsoleHeader;        // eyebrow?, title, subtitle?, status?{label,live?}, kpi?{caption,value,valueSuffix?,decimals?,sub?}
  callout?: ConsoleCallout;     // icon? "route"|"alert"|"check", title, sub?, action?
  columns?: { primary?: string; bar?: string; trailing?: string };
  rows: ConsoleRow[];           // primary, secondary?, bar?{segments:number[],label?,tone?}, trailing?{value,suffix?,decimals?,animate? "count"|"shift"|"none"}
  pills?: { label: string; tone?: "indigo" | "neutral" }[];
  ariaSummary: string;          // REQUIRED text alternative — governed copy
}
export interface DataStoryData {
  eyebrow?: string;
  headline: string;
  chart:
    | { kind: "area"; points: number[] }
    | { kind: "spark"; bars: number[] }
    | { kind: "bars"; series: { label: string; value: number; tone?: string }[] }
    | { kind: "cards"; cards: { name; accent; tag; value; decimals?; suffix?; bar; sub }[] };
  annotation?: { value: string; caption: string };
  ariaSummary: string;          // REQUIRED
}
export interface SchematicData {
  eyebrow?: string;
  title: string;
  nodes: SchematicNode[];       // id, label, sub?, kind? "source"|"engine"|"vendor"|"sink"
  edges: SchematicEdge[];       // from, to, label?, flow?
  ariaSummary: string;          // REQUIRED
}
```

From src/components/product/visuals/archetypes.tsx (the lazy public API — pass a payload):
```typescript
export function ConsoleVisual({ data }: { data: ConsoleData }): JSX.Element;     // lazy, ssr:false
export function DataStoryVisual({ data }: { data: DataStoryData }): JSX.Element; // lazy, ssr:false
export function SchematicVisual({ data }: { data: SchematicData }): JSX.Element; // lazy, ssr:false
```

From src/components/product/visuals/Console.tsx (compound slots for explorable composition):
```typescript
export const Console: typeof ConsoleRoot & { Header; Callout; Rows; Pills };
// <Console data={payload}>{children?}</Console> — omit children for flat render;
// pass <Console.Header/>/<Console.Rows/> etc. to compose explorable layouts.
```

From src/components/motion/Explorable.tsx (the flagship shell — compose, do NOT use boolean props):
```typescript
export const Explorable: typeof ExplorableRoot & { Toggle; Panel };
// <Explorable label="...">; <Explorable.Toggle id="x">…</Explorable.Toggle> is a real <button>
//   with aria-expanded/aria-controls; <Explorable.Panel id="x">…</Explorable.Panel> renders
//   content immediately (values visible by default), data-active toggles, motion is additive.
```

From src/components/sections/FeatureAccordion.tsx (the PLATVIS-01 wiring point):
```typescript
// visuals?: Record<string, React.ReactNode>  — keyed by item id; takes precedence over
// the homepage AccordionVisual registry AND the text placeholder. Pass one entry per item id.
```
</interfaces>

@src/content/visuals/placement.ts
@src/components/product/visuals/PlacementMatrix.tsx
@src/components/product/visuals/DecisionEnginePreview.tsx
@src/content/placement.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the full archetype-mapping table (D-03 deliverable)</name>
  <read_first>
    - .planning/phases/11-platform-deep-dive-visuals/11-CONTEXT.md (D-01, D-02, D-03 indicative mapping; D-09 realism)
    - src/content/placement.ts, src/content/optimization.ts, src/content/issues.ts, src/content/reporting.ts (the real accordion item ids + body copy that drive the mapping)
    - src/content/visuals/types.ts (which archetype can express which content shape)
  </read_first>
  <action>
    Create `.planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md`. Produce a single
    markdown table with one row per accordion item across all 4 pages (19 rows total). Columns:
    `Page | Item id | Archetype | One-line payload intent`.

    Use these EXACT item ids (verified from the content modules):
    - placement (5): decision-engine, vendor-pools, recall, business-rules, reconciliation
    - optimization (4): bands, share, bonus, history
    - issues (5): auto-handling, workflows, vendor-portal, sla, audit
    - reporting (5): inventory, vendor, cost, sla, activity

    Assign each item to Console / Data-story / Schematic per D-01 (routing/workflow -> Schematic,
    metric/console-screen -> Console, trend/proof -> Data-story). Start from the CONTEXT.md D-03
    indicative mapping but REFINE it against the actual body copy. Honor D-02: every one of the 19
    payload intents must read DISTINCTLY (its own numbers/shape); no two intents may describe the
    same visual within or across pages. The one-line intent must name the concrete data shape, e.g.
    "Console: rule hierarchy with platform/tier/vendor override rows and a live rule count" not
    "decision logic visual".

    Add a short "Schema-fit notes" section below the table: for each item, note whether an existing
    archetype field expresses it, or flag a candidate D-08 schema extension (additive optional field
    only). Add a "[CLAIMS REVIEW] / [COI REVIEW] surface" note listing which payloads will carry
    numeric claims and which will carry vendor/TSI framing.

    Voice: the table text and intents obey CLAUDE.md §5 (no em dashes, no "not X, it's Y", no banned
    phrases, sentence case, digits for numbers).
  </action>
  <acceptance_criteria>
    - `.planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md` exists
    - The table contains exactly 19 item rows; `grep -c "^| " 11-ARCHETYPE-MAP.md` accounts for 19 item rows plus header
    - Every one of the 19 verified item ids appears once: `grep -c "decision-engine\|vendor-pools\|recall\|business-rules\|reconciliation\|bands\|share\|bonus\|history\|auto-handling\|workflows\|vendor-portal\|sla\|audit\|inventory\|vendor\|cost\|activity" 11-ARCHETYPE-MAP.md` is non-zero for each
    - Each row names one of Console / Data-story / Schematic in the archetype column
    - No em dash (`grep "—" 11-ARCHETYPE-MAP.md` returns nothing)
    - A "Schema-fit notes" section and a "[CLAIMS REVIEW]" surface note are present
  </acceptance_criteria>
  <done>The 19-row mapping table exists with distinct per-item intents, schema-fit notes, and a governance-surface note, ready for the approval checkpoint.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Connor approves the archetype-mapping table (D-03 gate)</name>
  <action>Present 11-ARCHETYPE-MAP.md to Connor and pause for the D-03 approval decision. Do not author any payload module until the mapping is approved; apply any requested row changes (item id -> archetype / intent) and re-present.</action>
  <decision>Approve the 19-item archetype -> payload-intent mapping in 11-ARCHETYPE-MAP.md before any payload authoring begins.</decision>
  <context>
    D-03 makes this a hard gate: 19 distinct payloads are a large authoring and [CLAIMS REVIEW]
    surface, and the mapping is the cheapest place to course-correct. Approving here locks the
    pattern for the parallel Wave 2 pages (optimization / issues / reporting). Changing an
    archetype assignment after payloads are authored is expensive; changing it now is one table edit.
  </context>
  <options>
    <option id="approve">
      <name>Approve as written</name>
      <pros>Wave 2 unblocks immediately; pattern locked</pros>
      <cons>None if the table reads right</cons>
    </option>
    <option id="revise">
      <name>Revise specific rows</name>
      <pros>Fix any archetype-to-content mismatch or any two visuals that read alike (D-02) before authoring</pros>
      <cons>One more table-edit cycle</cons>
    </option>
  </options>
  <resume-signal>Reply "approved" to lock the mapping, or list the rows to change (item id -> new archetype / new intent).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author the 5 placement accordion payloads + extend schema if needed</name>
  <read_first>
    - src/content/visuals/placement.ts (the authoring reference — `satisfies ConsoleData`, [CLAIMS REVIEW]/[COI REVIEW] comment convention)
    - src/content/visuals/types.ts (the schemas; extend here under D-08 only if a payload cannot be expressed)
    - src/content/placement.ts (the 5 item ids + body copy: decision-engine, vendor-pools, recall, business-rules, reconciliation)
    - .planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md (the approved intents for these 5 items)
  </read_first>
  <action>
    Create `src/content/visuals/placement-accordion.ts`. Export one typed const per placement
    accordion item, each typed with `satisfies ConsoleData | DataStoryData | SchematicData` per the
    approved mapping. Per the CONTEXT.md indicative mapping (refine to the approved table):
    decision-engine -> Console (already exists as `placementConsole` in placement.ts — reuse/import
    it, do not duplicate), vendor-pools -> Schematic, recall -> Schematic, business-rules -> Console,
    reconciliation -> Data-story.

    Each payload MUST be visually DISTINCT (D-02): different node/row shapes, different numbers.
    Numbers are real-shaped and anonymized (D-09): plausible dPlat magnitudes, zero client
    identifiers, generic and qualified. Every numeric payload block carries a `// [CLAIMS REVIEW]`
    comment; any caption touching vendor/TSI framing carries `// [COI REVIEW]` (D-10 audit tags,
    non-blocking this phase). Every payload sets the REQUIRED `ariaSummary` describing the data in
    words (Pitfall 8). Voice rules apply to ariaSummary and every caption (no em dashes, no banned
    phrases, sentence case, digits).

    Export all 5 (the existing `placementConsole` for decision-engine plus the 4 new) from
    `src/content/visuals/index.ts`.

    D-08: if a Schematic or Data-story payload genuinely cannot express a needed detail, add an
    ADDITIVE OPTIONAL field to the relevant interface in `types.ts` (never a breaking change), and
    keep `placement.ts`'s existing `satisfies ConsoleData` green. Log any field you add in a one-line
    comment in types.ts.
  </action>
  <acceptance_criteria>
    - `src/content/visuals/placement-accordion.ts` exists and exports a const for each of vendor-pools, recall, business-rules, reconciliation typed with `satisfies` against a Phase 10 schema
    - `grep -c "CLAIMS REVIEW" src/content/visuals/placement-accordion.ts` returns at least one per numeric payload
    - Every exported const sets `ariaSummary`
    - `grep "—" src/content/visuals/placement-accordion.ts` returns nothing (no em dashes)
    - `src/content/visuals/index.ts` re-exports the new placement-accordion payloads
    - `npx tsc --noEmit` exits 0 (placement.ts `satisfies ConsoleData` still compiles; any types.ts extension is additive)
  </acceptance_criteria>
  <done>5 distinct, governed, typed placement accordion payloads exist and are exported; tsc clean; any schema change is additive and documented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Build the placement explorable flagship + wire the placement page</name>
  <read_first>
    - src/components/motion/Explorable.tsx (Explorable.Toggle / Explorable.Panel contract; values-visible-by-default rule)
    - src/components/product/visuals/Console.tsx (compound slots Console.Header/Callout/Rows/Pills for explorable composition)
    - src/components/product/visuals/DecisionEnginePreview.tsx (the bespoke flagship being replaced — preserve its information: tabs, vendor allocations, reallocation story, the two metric cards)
    - src/components/product/visuals/archetypes.tsx (ConsoleVisual/DataStoryVisual/SchematicVisual lazy API)
    - src/app/platform/placement/page.tsx (current wiring: LazyPlacementMatrix on decision-engine, LazyDecisionEnginePreview in BenefitSplit)
    - src/components/product/visuals/lazy.tsx (lazy wrapper registry — note the existing `loading` skeleton prop convention on each dynamic() wrapper)
  </read_first>
  <action>
    Create `src/components/product/visuals/PlacementFlagship.tsx` ("use client"). It refactors
    DecisionEnginePreview (D-07) into an `Explorable`-composed Console-archetype instance fed a typed
    ConsoleData payload (author the payload inline in placement-accordion.ts or a sibling const, real-
    shaped per D-09, with `ariaSummary` and `[CLAIMS REVIEW]`). Compose
    `<Explorable label="Placement decision engine">` with `Explorable.Toggle` buttons (one per
    treatment tier or per vendor pool) and `Explorable.Panel`s that reveal the per-tier vendor
    allocation. HARD CONSTRAINTS (D-05, Phase 10 parity contract):
    - All headline values render in the DOM by default, never hover-gated.
    - Toggles are the real `<button>`s from Explorable.Toggle (keyboard Enter/Space + focus-visible
      ring come from the shell); a pointer user and a keyboard user reach the same panels.
    - Under `prefers-reduced-motion` the same data is visible (inherited from the archetype/barrel);
      do not hide any value behind motion.
    Use `Console.Header` / `Console.Rows` slots inside the panels rather than passing interactive
    boolean props.

    Add a `LazyPlacementFlagship` lazy `ssr:false` wrapper in `src/components/product/visuals/lazy.tsx`
    following the existing inline-literal `dynamic(...)` pattern.

    CLS / Pitfall 4 (the pattern Wave 2 replicates): every dynamic() wrapper you add or repoint here
    (the 5 accordion archetype wrappers if any are added, and `LazyPlacementFlagship`) MUST pass a
    `loading` skeleton whose box reserves the SAME height the resolved archetype renders, so no layout
    shift occurs when the lazy chunk resolves. Give the skeleton an EXPLICIT `min-height` (or a fixed
    aspect box) equal to the archetype's rendered height for its container, not a bare spinner that
    collapses to zero height. Use one shared skeleton sizing constant per archetype box so the Wave 2
    pages inherit the same reserved dimensions. This is the discretion item from CONTEXT.md "lazy-
    skeleton dimensions per archetype to match the resolved box (Pitfall 4)" — resolve it concretely
    here and document the chosen min-height value in a one-line comment in lazy.tsx so Wave 2 copies it.

    In `src/app/platform/placement/page.tsx`:
    - Replace the single `visuals={{ "decision-engine": <LazyPlacementMatrix /> }}` with a full
      `visuals` map covering ALL 5 item ids, each rendering the matching archetype instance fed its
      placement-accordion payload (e.g. `<ConsoleVisual data={...} />`, `<SchematicVisual data={...} />`,
      `<DataStoryVisual data={...} />`). Zero placeholders remain.
    - Replace `visual={<LazyDecisionEnginePreview />}` in the BenefitSplit with
      `visual={<LazyPlacementFlagship />}`.
    Keep the page a Server Component; visuals stay lazy/client via the wrappers (no eager motion JS).
    Do not delete DecisionEnginePreview.tsx / PlacementMatrix.tsx yet (Phase 13 audits dead bespoke
    visuals); just stop importing the ones the page no longer uses.
  </action>
  <acceptance_criteria>
    - `src/components/product/visuals/PlacementFlagship.tsx` exists, imports `Explorable` and composes `Explorable.Toggle` + `Explorable.Panel` (`grep "Explorable\." PlacementFlagship.tsx` matches Toggle and Panel)
    - PlacementFlagship does NOT pass `interactive`/`showKpis` boolean props (composes slots instead)
    - `src/app/platform/placement/page.tsx` passes a `visuals` map with all 5 placement item ids: `grep -o "decision-engine\|vendor-pools\|recall\|business-rules\|reconciliation" src/app/platform/placement/page.tsx` returns all 5
    - The placement BenefitSplit `visual=` references `LazyPlacementFlagship`, not `LazyDecisionEnginePreview`
    - `lazy.tsx` exports `LazyPlacementFlagship` via inline-literal `dynamic(..., { ssr: false, loading })`
    - CLS-SKELETON: every `dynamic(...)` wrapper added/repointed in this task passes a `loading` skeleton with an explicit `min-height` (or fixed aspect box) equal to the resolved archetype box, not a zero-height/spinner-only fallback. Verifiable: `grep -A3 "loading:" src/components/product/visuals/lazy.tsx` shows a `minHeight`/`min-h-` value on the new wrapper(s), and a one-line comment records the chosen px value for Wave 2 to copy
    - `npx tsc --noEmit` exits 0
    - `npx eslint src/components/product/visuals/PlacementFlagship.tsx src/app/platform/placement/page.tsx` exits 0
  </acceptance_criteria>
  <done>Placement renders real archetype visuals on all 5 accordion items and an Explorable-composed Console flagship on the BenefitSplit band; every lazy wrapper reserves its archetype box via an explicit skeleton min-height so the chunk resolves with no layout shift; page stays a lean Server Component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Platform-visuals Playwright spec (placement coverage, Wave 2 extends it)</name>
  <read_first>
    - tests/responsive/reveal-fail-open.spec.ts (the no-stuck-opacity:0 pattern + VISUAL_ROUTES import)
    - tests/responsive/reduced-motion.spec.ts (the inherited site-wide opacity===1 reduced-motion check this spec complements with DATA-parity)
    - tests/responsive/platform-mobile.spec.ts (existing platform spec style, locators, networkidle wait)
    - tests/helpers/routes.ts (VISUAL_ROUTES already includes the 4 platform routes)
  </read_first>
  <behavior>
    - For /platform/placement: opening each of the 5 accordion items renders a visual whose container is NOT the text placeholder (assert the placeholder's "Visual" eyebrow span is absent for these items, or assert an archetype `role="img"` node with a non-empty aria-label is present in the active panel region).
    - The BenefitSplit flagship on /platform/placement exposes its headline values in the DOM without any hover (assert the values are present via text query on initial load).
    - The flagship's Explorable toggles are reachable by keyboard: tab to a toggle, it is a `<button>` with `aria-expanded`, activating with Enter/Space toggles `aria-expanded`.
    - REDUCED-MOTION DATA-PARITY (D-05): under `page.emulateMedia({ reducedMotion: 'reduce' })`, the flagship's data values that are visible on normal load are STILL present in the DOM. Capture one or more concrete flagship text values on normal load, then reload (or re-query) with reduced motion emulated and assert those same values are found. This guards against a flagship that conditionally hides panel content when motion is off (the inherited reduced-motion.spec.ts only checks opacity===1, not content presence).
    - No in-viewport text-bearing element on /platform/placement sits at computed opacity < 1 after the accordion/flagship interaction (reuse the fail-open assertion shape).
  </behavior>
  <action>
    Create `tests/responsive/platform-visuals.spec.ts`. Author the assertions above against
    /platform/placement only in this plan, but structure the spec so a route list can be widened: put
    the per-page assertions in a helper that takes a route, and run it for `["/platform/placement"]`
    now with a comment that Wave 2 adds optimization / issues / reporting. The reduced-motion
    data-parity assertion lives inside the same per-route helper (parameterized on the route's expected
    flagship value(s)) so the Wave 2 pages inherit it automatically. Follow the existing spec
    conventions (Playwright `test`, `page.goto`, `waitForLoadState("networkidle")`, and
    `page.emulateMedia` for the reduced-motion pass). The spec must be runnable against a Vercel
    preview via `PLAYWRIGHT_BASE_URL` (do not assume a local server — next dev/start hang in this
    sandbox per project memory).
  </action>
  <acceptance_criteria>
    - `tests/responsive/platform-visuals.spec.ts` exists and references `/platform/placement`
    - Spec asserts an archetype `role="img"` node with non-empty aria-label inside an active accordion panel
    - Spec asserts flagship headline values are present in the DOM without hover (text query on load)
    - Spec asserts a flagship toggle is a `<button>` with `aria-expanded` toggled by keyboard activation
    - REDUCED-MOTION DATA-PARITY: the spec calls `page.emulateMedia({ reducedMotion: 'reduce' })` and asserts the flagship's on-load text value(s) are still found in the DOM under reduced motion (`grep "emulateMedia" tests/responsive/platform-visuals.spec.ts` matches and the assertion checks a flagship value, not only opacity)
    - `npx tsc --noEmit` exits 0 (spec typechecks)
    - Spec file imports from `../helpers/routes` or defines a route-parameterized helper (Wave 2 extension point present)
  </acceptance_criteria>
  <done>A platform-visuals spec proves placement's accordion visuals, flagship default-visible values, keyboard parity, AND reduced-motion data-parity; the spec is parameterized for Wave 2 pages to extend.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Lock the CLS skeleton pattern on /platform/placement (per-route CLS gate)</name>
  <read_first>
    - lighthouserc / the existing LHCI config that Phase 10 uses for per-route ceilings (the project already runs LHCI against a built/preview build, not next dev)
    - .planning/projects memory: next dev/start hang in this sandbox; `next build`, `tsc --noEmit`, and Playwright/LHCI against a preview build DO run
    - src/components/product/visuals/lazy.tsx (the skeleton min-height set in Task 4)
  </read_first>
  <action>
    Prove the Task 4 skeleton-sizing pattern holds CLS on placement BEFORE Wave 2 fans out and copies
    it blind. Run a per-route CLS check against /platform/placement in isolation, using the project's
    existing non-hanging tooling (LHCI against a built/preview build, OR a Playwright CLS assertion
    against `PLAYWRIGHT_BASE_URL`). Do NOT use `next dev` / `next start` (they hang in this sandbox).

    Concretely: build the app (`next build`) or point at the Vercel preview, then assert
    /platform/placement reports CLS < 0.1. If LHCI already has a per-route ceiling map (Phase 10
    pattern), add /platform/placement with a CLS ceiling of 0.1; otherwise add a focused Playwright
    layout-shift assertion for the route. If the check fails, the skeleton min-height from Task 4 is
    wrong — correct it on placement here, so the locked dimensions Wave 2 inherits are already proven.

    Document the proven skeleton min-height value (and the CLS result) in the 11-01 SUMMARY so 11-02 /
    11-03 / 11-04 copy a known-good number rather than guessing.
  </action>
  <acceptance_criteria>
    - A per-route CLS check for /platform/placement exists in the project's CLS tooling (an LHCI per-route CLS ceiling of 0.1 for /platform/placement, or a Playwright layout-shift assertion targeting the route)
    - The check runs via non-hanging tooling (`next build` + LHCI, or Playwright vs `PLAYWRIGHT_BASE_URL`), NOT `next dev`/`next start`
    - The check asserts CLS < 0.1 for /platform/placement
    - The proven skeleton min-height value is recorded for Wave 2 to copy (in lazy.tsx comment from Task 4 and echoed in the SUMMARY)
  </acceptance_criteria>
  <done>/platform/placement has an isolated CLS < 0.1 gate proving the lazy-skeleton sizing pattern before Wave 2 replicates it; the known-good skeleton dimensions are documented for the Wave 2 pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> rendered page | The only new content is build-time-authored, typed, anonymized payload modules. No user input, no network reads/writes, no auth, no dynamic data source crosses into these visuals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information disclosure | placement payload numbers | accept | Figures are real-shaped but anonymized and generic per D-09; Andrew pre-cleared (D-10). [CLAIMS REVIEW] tags preserved for audit. No real client identifiers may appear (CLAUDE.md §15). |
| T-11-02 | Tampering | typed payload modules | accept | Payloads are static TypeScript validated by `satisfies` at compile time; no runtime mutation path, no user-supplied value reaches them. |
| T-11-03 | Spoofing/Repudiation/DoS/EoP | n/a (static marketing visuals) | accept | No new untrusted input, no new data sink, no auth surface, no server action. Realistic threat surface is none beyond the two above. |

No high-severity threats. Nothing to block on.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 across all changed files.
- `npx eslint` clean on PlacementFlagship.tsx, placement-accordion.ts, placement/page.tsx.
- 11-ARCHETYPE-MAP.md approved by Connor (checkpoint Task 2).
- Per-route CLS gate for /platform/placement reports CLS < 0.1 via non-hanging tooling (Task 6),
  proving the skeleton-sizing pattern before Wave 2.
- DEFERRED-TO-PREVIEW (next dev/start hang locally): run `tests/responsive/platform-visuals.spec.ts`,
  `reveal-fail-open.spec.ts`, and `reduced-motion.spec.ts` against a Vercel preview via
  PLAYWRIGHT_BASE_URL; confirm placement passes, including the reduced-motion data-parity assertion.
</verification>

<success_criteria>
- The 19-item archetype-mapping table exists and is approved.
- /platform/placement shows real archetype visuals on all 5 accordion items, zero placeholders.
- The placement BenefitSplit visual is an Explorable-composed Console archetype with values visible
  by default, keyboard parity, and reduced-motion DATA-parity (same values present when motion is off).
- Every lazy visual reserves its archetype box via an explicit skeleton min-height, and the
  /platform/placement CLS gate confirms CLS < 0.1 — the skeleton pattern is locked for Wave 2.
- All new payload numbers carry [CLAIMS REVIEW]; vendor/TSI captions carry [COI REVIEW].
- tsc + eslint clean; the parameterized platform-visuals spec covers placement.
</success_criteria>

<output>
After completion, create `.planning/phases/11-platform-deep-dive-visuals/11-01-SUMMARY.md` including:
the approved mapping table location, the placement payload module surface, the flagship component,
the spec extension point for Wave 2, the proven lazy-skeleton min-height value(s) per archetype box,
and the recorded /platform/placement CLS result so Wave 2 copies a known-good number.
</output>
