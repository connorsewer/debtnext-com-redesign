---
phase: 11-platform-deep-dive-visuals
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/content/visuals/optimization.ts
  - src/content/visuals/index.ts
  - src/app/platform/optimization/page.tsx
  - src/components/product/visuals/OptimizationFlagship.tsx
  - src/components/product/visuals/lazy.tsx
  - tests/responsive/platform-visuals.spec.ts
autonomous: true
requirements: [PLATVIS-01, PLATVIS-02, PLATVIS-03]
user_setup: []

must_haves:
  truths:
    - "Every FeatureAccordion item on /platform/optimization (bands, share, bonus, history) renders a real archetype visual; no text-on-dark placeholder remains"
    - "The /platform/optimization BenefitSplit visual is an Explorable-composed archetype instance fed a real payload, values visible by default, keyboard + touch + reduced-motion reach the same data"
    - "Every optimization payload is visually distinct from every other payload in the phase (D-02)"
    - "Every numeric payload carries a [CLAIMS REVIEW] comment; vendor/TSI captions carry [COI REVIEW]"
  artifacts:
    - path: "src/content/visuals/optimization.ts"
      provides: "4 typed payloads for the optimization accordion items (bands, share, bonus, history) plus the flagship payload"
      contains: "bands"
    - path: "src/components/product/visuals/OptimizationFlagship.tsx"
      provides: "The optimization BenefitSplit explorable flagship, an Explorable-composed archetype instance"
    - path: "src/app/platform/optimization/page.tsx"
      provides: "Optimization page wiring all 4 accordion visuals + the explorable flagship"
  key_links:
    - from: "src/app/platform/optimization/page.tsx"
      to: "src/content/visuals/optimization.ts"
      via: "visuals={{ <itemId>: <ArchetypeVisual data={payload} /> }} prop on FeatureAccordion"
      pattern: "visuals=\\{\\{"
    - from: "src/components/product/visuals/OptimizationFlagship.tsx"
      to: "src/components/motion/Explorable.tsx"
      via: "Explorable.Toggle / Explorable.Panel composition"
      pattern: "Explorable\\."
---

<objective>
Implement /platform/optimization end to end against the locked Wave 1 pattern:
author the 4 accordion payloads, wire them through `FeatureAccordion.visuals`
(PLATVIS-01), and refactor the OptimizationEngine BenefitSplit visual into an
`Explorable`-composed archetype instance (PLATVIS-02 + PLATVIS-03 together,
per D-06).

Purpose: replace every text-on-dark placeholder on the optimization page with
real, distinct archetype visuals fed real-shaped payloads, and give the page its
one keyboard-accessible, reduced-motion-safe explorable flagship.

Output: the optimization payload module, the optimization flagship component, the
wired optimization page, and optimization assertions added to the shared
platform-visuals Playwright spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-platform-deep-dive-visuals/11-CONTEXT.md
@.planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md

<interfaces>
<!-- Same Phase 10 contracts as 11-01. Compose archetypes from payloads; compose the flagship from Explorable slots. -->

Lazy archetype API (src/components/product/visuals/archetypes.tsx):
```typescript
export function ConsoleVisual({ data }: { data: ConsoleData }): JSX.Element;     // lazy, ssr:false
export function DataStoryVisual({ data }: { data: DataStoryData }): JSX.Element; // lazy, ssr:false
export function SchematicVisual({ data }: { data: SchematicData }): JSX.Element; // lazy, ssr:false
```

Explorable shell (src/components/motion/Explorable.tsx): compose `<Explorable label>`,
`Explorable.Toggle id` (real <button>, aria-expanded/aria-controls), `Explorable.Panel id`
(content visible by default, motion additive). Do NOT use boolean props.

FeatureAccordion wiring: `visuals?: Record<string, React.ReactNode>` keyed by item id — takes
precedence over the placeholder. Pass one entry per item id.

ConsoleData / DataStoryData / SchematicData schemas: see src/content/visuals/types.ts. Every payload
sets the REQUIRED `ariaSummary`. The reference authoring pattern is src/content/visuals/placement.ts
(`satisfies`, [CLAIMS REVIEW]/[COI REVIEW] comments).
</interfaces>

@src/content/visuals/placement.ts
@src/content/visuals/placement-accordion.ts
@src/components/product/visuals/PlacementFlagship.tsx
@src/components/product/visuals/OptimizationEngine.tsx
@src/content/optimization.ts
@src/app/platform/optimization/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the 4 optimization accordion payloads + flagship payload</name>
  <read_first>
    - .planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md (approved archetype + intent for bands, share, bonus, history)
    - src/content/visuals/placement-accordion.ts (the Wave 1 authoring reference — `satisfies`, governance comments, ariaSummary)
    - src/content/optimization.ts (the 4 item ids + body copy: bands, share, bonus, history)
    - src/components/product/visuals/OptimizationEngine.tsx (the bespoke flagship's real data: vendor liquidation bands, reallocation shift, bonus card — preserve this information in the flagship payload)
    - src/content/visuals/types.ts (schemas; extend additively under D-08 only if needed)
  </read_first>
  <action>
    Create `src/content/visuals/optimization.ts`. Export one typed const per accordion item
    (`satisfies` the archetype from the approved map) plus one flagship payload const. Per the
    CONTEXT.md indicative mapping (refine to the approved table): bands -> Data-story (performance band
    thresholds), share -> Data-story (share adjustment with caps/floors), bonus -> Console (bonus
    configuration with target thresholds), history -> Data-story (optimization audit trail / trend).

    Real-shaped anonymized numbers (D-09): plausible liquidation percentages, share-shift magnitudes,
    bonus thresholds; zero client identifiers; generic and qualified. Each payload must be DISTINCT
    (D-02) from every placement payload and from each other (different numbers, different chart/row
    shapes). The flagship payload should carry the OptimizationEngine information (vendor closed-pool
    liquidation, band, this-cycle -> next-cycle share shift, bonus-triggered callout).

    Governance (D-10, audit-only this phase): `// [CLAIMS REVIEW]` on every numeric block; `// [COI REVIEW]`
    on any vendor/TSI-framing caption. Every payload sets `ariaSummary`. Voice rules on ariaSummary and
    all captions (no em dashes, no banned phrases, sentence case, digits).

    Export the new payloads from `src/content/visuals/index.ts`. If a schema field is missing, add an
    ADDITIVE OPTIONAL field to types.ts (never breaking) and keep placement.ts validation green.
  </action>
  <acceptance_criteria>
    - `src/content/visuals/optimization.ts` exports a const for bands, share, bonus, history each typed via `satisfies` against a Phase 10 schema, plus a flagship payload const
    - `grep -c "CLAIMS REVIEW" src/content/visuals/optimization.ts` returns at least one per numeric payload
    - Every exported const sets `ariaSummary`
    - `grep "—" src/content/visuals/optimization.ts` returns nothing
    - `src/content/visuals/index.ts` re-exports the optimization payloads
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>4 distinct, governed, typed optimization accordion payloads plus a flagship payload exist and are exported; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the optimization explorable flagship + wire the optimization page</name>
  <read_first>
    - src/components/product/visuals/PlacementFlagship.tsx (the Wave 1 reference flagship — copy its Explorable composition pattern + parity guarantees)
    - src/components/motion/Explorable.tsx (Toggle/Panel contract, values-visible-by-default)
    - src/components/product/visuals/OptimizationEngine.tsx (the bespoke flagship being replaced — preserve its vendor bands + reallocation + bonus story)
    - src/app/platform/optimization/page.tsx (current wiring: accordion has no visuals map; BenefitSplit uses LazyOptimizationEngine)
    - src/components/product/visuals/lazy.tsx (lazy wrapper registry pattern)
  </read_first>
  <action>
    Create `src/components/product/visuals/OptimizationFlagship.tsx` ("use client"), refactoring
    OptimizationEngine (D-07) into an `Explorable`-composed archetype instance fed the flagship payload
    from optimization.ts. The natural interaction is toggle-state: `Explorable.Toggle` per vendor (or
    per evaluation cycle) revealing that vendor's band detail and the this-cycle -> next-cycle share
    shift in an `Explorable.Panel`. HARD CONSTRAINTS (D-05 / Phase 10 parity): all headline values
    render by default (never hover-gated); toggles are Explorable.Toggle `<button>`s (keyboard +
    focus-visible from the shell); reduced-motion shows the same data. Compose archetype slots, not
    boolean props.

    Add `LazyOptimizationFlagship` to `src/components/product/visuals/lazy.tsx` (inline-literal
    `dynamic(..., { ssr:false, loading })`).

    In `src/app/platform/optimization/page.tsx`:
    - Add `visuals={{ bands: <DataStoryVisual data={...} />, share: <DataStoryVisual data={...} />,
      bonus: <ConsoleVisual data={...} />, history: <DataStoryVisual data={...} /> }}` (archetypes per
      the approved map) to the `<FeatureAccordion>`. All 4 ids covered; zero placeholders.
    - Replace `visual={<LazyOptimizationEngine />}` with `visual={<LazyOptimizationFlagship />}`.
    Keep the page a Server Component; do not delete OptimizationEngine.tsx (Phase 13 audits dead
    bespoke visuals), just stop importing it on this page.
  </action>
  <acceptance_criteria>
    - `src/components/product/visuals/OptimizationFlagship.tsx` exists and composes `Explorable.Toggle` + `Explorable.Panel` (`grep "Explorable\." OptimizationFlagship.tsx` matches both)
    - No `interactive`/`showKpis` boolean props passed to archetypes
    - `src/app/platform/optimization/page.tsx` passes a `visuals` map covering all 4 ids: `grep -o "bands\|share\|bonus\|history" src/app/platform/optimization/page.tsx` returns all 4
    - The optimization BenefitSplit `visual=` references `LazyOptimizationFlagship`, not `LazyOptimizationEngine`
    - `lazy.tsx` exports `LazyOptimizationFlagship`
    - `npx tsc --noEmit` exits 0; `npx eslint src/components/product/visuals/OptimizationFlagship.tsx src/app/platform/optimization/page.tsx` exits 0
  </acceptance_criteria>
  <done>Optimization renders real archetype visuals on all 4 accordion items and an Explorable-composed flagship on the BenefitSplit band; page stays a lean Server Component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend platform-visuals spec to cover /platform/optimization</name>
  <read_first>
    - tests/responsive/platform-visuals.spec.ts (the Wave 1 route-parameterized helper to extend)
    - tests/helpers/routes.ts (VISUAL_ROUTES already includes /platform/optimization)
  </read_first>
  <behavior>
    - Opening each of the 4 optimization accordion items renders an archetype `role="img"` node with a non-empty aria-label in the active panel (no text placeholder).
    - The optimization BenefitSplit flagship exposes headline values in the DOM without hover.
    - A flagship toggle is a keyboard-reachable `<button>` with `aria-expanded` toggled by Enter/Space.
    - No in-viewport text-bearing element on /platform/optimization sits at opacity < 1 after interaction.
  </behavior>
  <action>
    Add `/platform/optimization` to the route list driving the parameterized helper in
    `tests/responsive/platform-visuals.spec.ts` (created in 11-01). If 11-01's helper already iterates
    a route array, append the route; otherwise add an optimization `test` block mirroring the placement
    one. Keep it runnable against a Vercel preview via `PLAYWRIGHT_BASE_URL`.
  </action>
  <acceptance_criteria>
    - `tests/responsive/platform-visuals.spec.ts` references `/platform/optimization`
    - The optimization assertions cover: archetype role="img" in an active accordion panel, flagship values present without hover, a keyboard-togglable flagship button
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The platform-visuals spec now asserts optimization's accordion visuals, flagship default-visible values, and keyboard parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> rendered page | Only new content is build-time-authored, typed, anonymized payloads. No user input, no network reads/writes, no auth crosses into these visuals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Information disclosure | optimization payload numbers | accept | Real-shaped but anonymized per D-09; Andrew pre-cleared (D-10); [CLAIMS REVIEW] tags preserved. No real client identifiers (CLAUDE.md §15). |
| T-11-05 | Tampering | typed payload modules | accept | Static TS validated by `satisfies` at compile time; no runtime mutation, no user-supplied value reaches them. |
| T-11-06 | Spoofing/Repudiation/DoS/EoP | n/a (static marketing visuals) | accept | No new untrusted input, data sink, auth surface, or server action. |

No high-severity threats.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx eslint` clean on the changed files.
- DEFERRED-TO-PREVIEW: run `tests/responsive/platform-visuals.spec.ts`, `reveal-fail-open.spec.ts`,
  `reduced-motion.spec.ts` against a Vercel preview via PLAYWRIGHT_BASE_URL; confirm optimization passes.
</verification>

<success_criteria>
- /platform/optimization shows real archetype visuals on all 4 accordion items, zero placeholders.
- The optimization BenefitSplit visual is an Explorable-composed archetype with values visible by
  default and keyboard + reduced-motion parity.
- All new payload numbers carry [CLAIMS REVIEW]; vendor/TSI captions carry [COI REVIEW].
- Every optimization payload reads distinctly from every other phase payload (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/11-platform-deep-dive-visuals/11-02-SUMMARY.md`.
</output>
