---
phase: 11-platform-deep-dive-visuals
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/content/visuals/issues.ts
  - src/content/visuals/index.ts
  - src/app/platform/issues/page.tsx
  - src/components/product/visuals/IssuesFlagship.tsx
  - src/components/product/visuals/lazy.tsx
  - tests/responsive/platform-visuals.spec.ts
autonomous: true
requirements: [PLATVIS-01, PLATVIS-02, PLATVIS-03]
user_setup: []

must_haves:
  truths:
    - "Every FeatureAccordion item on /platform/issues (auto-handling, workflows, vendor-portal, sla, audit) renders a real archetype visual; no text-on-dark placeholder remains"
    - "The /platform/issues BenefitSplit visual is an Explorable-composed archetype instance fed a real payload, values visible by default, keyboard + touch + reduced-motion reach the same data"
    - "Every issues payload is visually distinct from every other payload in the phase (D-02)"
    - "Every numeric payload carries a [CLAIMS REVIEW] comment; vendor/TSI/regulatory captions carry [COI REVIEW]"
  artifacts:
    - path: "src/content/visuals/issues.ts"
      provides: "5 typed payloads for the issues accordion items (auto-handling, workflows, vendor-portal, sla, audit) plus the flagship payload"
      contains: "auto-handling"
    - path: "src/components/product/visuals/IssuesFlagship.tsx"
      provides: "The issues BenefitSplit explorable flagship, an Explorable-composed archetype instance"
    - path: "src/app/platform/issues/page.tsx"
      provides: "Issues page wiring all 5 accordion visuals + the explorable flagship"
  key_links:
    - from: "src/app/platform/issues/page.tsx"
      to: "src/content/visuals/issues.ts"
      via: "visuals={{ <itemId>: <ArchetypeVisual data={payload} /> }} prop on FeatureAccordion"
      pattern: "visuals=\\{\\{"
    - from: "src/components/product/visuals/IssuesFlagship.tsx"
      to: "src/components/motion/Explorable.tsx"
      via: "Explorable.Toggle / Explorable.Panel composition"
      pattern: "Explorable\\."
---

<objective>
Implement /platform/issues end to end against the locked Wave 1 pattern: author
the 5 accordion payloads, wire them through `FeatureAccordion.visuals`
(PLATVIS-01), and refactor the IssuesWorklist BenefitSplit visual into an
`Explorable`-composed archetype instance (PLATVIS-02 + PLATVIS-03 together,
per D-06).

This page carries regulatory framing (deceased / bankruptcy / SCRA / CFPB) in
its copy, so every payload caption that touches regulator/vendor framing gets a
[COI REVIEW] audit tag (D-10, non-blocking this phase). Do not introduce client
names.

Output: the issues payload module, the issues flagship component, the wired
issues page, and issues assertions added to the shared platform-visuals spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-platform-deep-dive-visuals/11-CONTEXT.md
@.planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md

<interfaces>
<!-- Same Phase 10 contracts as 11-01 / 11-02. -->

Lazy archetype API: `ConsoleVisual({data})`, `DataStoryVisual({data})`, `SchematicVisual({data})`
(all lazy ssr:false; src/components/product/visuals/archetypes.tsx).

Explorable shell: compose `<Explorable label>`, `Explorable.Toggle id` (real <button>,
aria-expanded), `Explorable.Panel id` (content visible by default). No boolean props.

FeatureAccordion: `visuals?: Record<string, React.ReactNode>` keyed by item id; takes precedence
over the placeholder.

ConsoleData / DataStoryData / SchematicData: see src/content/visuals/types.ts; every payload sets the
REQUIRED `ariaSummary`. Reference authoring pattern: src/content/visuals/placement-accordion.ts.
</interfaces>

@src/content/visuals/placement.ts
@src/content/visuals/placement-accordion.ts
@src/components/product/visuals/PlacementFlagship.tsx
@src/components/product/visuals/IssuesWorklist.tsx
@src/content/issues.ts
@src/app/platform/issues/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the 5 issues accordion payloads + flagship payload</name>
  <read_first>
    - .planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md (approved archetype + intent for auto-handling, workflows, vendor-portal, sla, audit)
    - src/content/visuals/placement-accordion.ts (the authoring reference)
    - src/content/issues.ts (the 5 item ids + body copy; note the existing [COI REVIEW] regulatory-framing comments)
    - src/components/product/visuals/IssuesWorklist.tsx (the bespoke flagship's real data: SLA worklist rows, timers, status tones, the 3 metric cells — preserve in the flagship payload)
    - src/content/visuals/types.ts (schemas; extend additively under D-08 only if needed)
  </read_first>
  <action>
    Create `src/content/visuals/issues.ts`. Export one typed const per accordion item plus a flagship
    payload const. Per the CONTEXT.md indicative mapping (refine to the approved table): auto-handling
    -> Console (auto-handling config screen), workflows -> Schematic (workflow editor / issue-type
    routing), vendor-portal -> Console (issue thread with vendor + operator messages), sla -> Console
    (SLA worklist with timer badges), audit -> Data-story (audit-log timeline / trend).

    Real-shaped anonymized numbers (D-09): plausible issue counts, SLA windows, adherence percentages;
    zero client identifiers; generic and qualified. Each payload DISTINCT (D-02) from every other phase
    payload. The flagship payload should carry the IssuesWorklist information (active worklist rows with
    issue type, timer, status; the SLA adherence / avg resolution / auto-resolved metric cells).

    Governance: `// [CLAIMS REVIEW]` on every numeric block. `// [COI REVIEW]` on captions touching
    regulators / vendor framing (this page has live regulatory language; Andrew's D-10 clearance covers
    the framing, keep the tag for audit). Do NOT name a specific client or use a client logo (CLAUDE.md
    §15 — outside D-10's clearance). Every payload sets `ariaSummary`. Voice rules on ariaSummary and
    captions (no em dashes, no banned phrases, sentence case, digits).

    Export from `src/content/visuals/index.ts`. Additive-only schema extensions under D-08; keep
    placement.ts validation green.
  </action>
  <acceptance_criteria>
    - `src/content/visuals/issues.ts` exports a const for auto-handling, workflows, vendor-portal, sla, audit each typed via `satisfies`, plus a flagship payload const
    - `grep -c "CLAIMS REVIEW" src/content/visuals/issues.ts` returns at least one per numeric payload
    - `grep -c "COI REVIEW" src/content/visuals/issues.ts` returns at least one (regulatory/vendor framing)
    - Every exported const sets `ariaSummary`; `grep "—" src/content/visuals/issues.ts` returns nothing
    - `src/content/visuals/index.ts` re-exports the issues payloads
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>5 distinct, governed, typed issues accordion payloads plus a flagship payload exist and are exported; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the issues explorable flagship + wire the issues page</name>
  <read_first>
    - src/components/product/visuals/PlacementFlagship.tsx (the Wave 1 reference flagship pattern + parity guarantees)
    - src/components/motion/Explorable.tsx (Toggle/Panel contract)
    - src/components/product/visuals/IssuesWorklist.tsx (the bespoke flagship being replaced — preserve worklist rows, timers, status, metric cells)
    - src/app/platform/issues/page.tsx (current wiring: accordion has no visuals map; BenefitSplit uses LazyIssuesWorklist)
    - src/components/product/visuals/lazy.tsx (lazy wrapper registry pattern)
  </read_first>
  <action>
    Create `src/components/product/visuals/IssuesFlagship.tsx` ("use client"), refactoring
    IssuesWorklist (D-07) into an `Explorable`-composed archetype instance fed the flagship payload.
    Natural interaction: `Explorable.Toggle` per issue row (or per issue-type filter) revealing that
    issue's thread / SLA detail in an `Explorable.Panel`. HARD CONSTRAINTS (D-05 / Phase 10 parity):
    all worklist values + the metric cells render by default (never hover-gated); status is paired with
    a text label, not color alone (Pitfall 8); toggles are Explorable.Toggle `<button>`s (keyboard +
    focus-visible from the shell); reduced-motion shows the same data and no pulsing-only signal. Compose
    archetype slots, not boolean props.

    Add `LazyIssuesFlagship` to `src/components/product/visuals/lazy.tsx`.

    In `src/app/platform/issues/page.tsx`:
    - Add `visuals={{ "auto-handling": <ConsoleVisual data={...} />, workflows: <SchematicVisual data={...} />,
      "vendor-portal": <ConsoleVisual data={...} />, sla: <ConsoleVisual data={...} />, audit: <DataStoryVisual data={...} /> }}`
      (archetypes per the approved map) to the `<FeatureAccordion>`. All 5 ids; zero placeholders.
    - Replace `visual={<LazyIssuesWorklist />}` with `visual={<LazyIssuesFlagship />}`.
    Keep the page a Server Component; do not delete IssuesWorklist.tsx (Phase 13 audit), just stop
    importing it here.
  </action>
  <acceptance_criteria>
    - `src/components/product/visuals/IssuesFlagship.tsx` exists and composes `Explorable.Toggle` + `Explorable.Panel` (`grep "Explorable\." IssuesFlagship.tsx` matches both)
    - No boolean interactive props passed to archetypes
    - `src/app/platform/issues/page.tsx` passes a `visuals` map covering all 5 ids: `grep -o "auto-handling\|workflows\|vendor-portal\|sla\|audit" src/app/platform/issues/page.tsx` returns all 5
    - The issues BenefitSplit `visual=` references `LazyIssuesFlagship`, not `LazyIssuesWorklist`
    - `lazy.tsx` exports `LazyIssuesFlagship`
    - `npx tsc --noEmit` exits 0; `npx eslint src/components/product/visuals/IssuesFlagship.tsx src/app/platform/issues/page.tsx` exits 0
  </acceptance_criteria>
  <done>Issues renders real archetype visuals on all 5 accordion items and an Explorable-composed flagship on the BenefitSplit band; status is label-paired; page stays a lean Server Component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend platform-visuals spec to cover /platform/issues</name>
  <read_first>
    - tests/responsive/platform-visuals.spec.ts (the route-parameterized helper to extend)
    - tests/helpers/routes.ts (VISUAL_ROUTES already includes /platform/issues)
  </read_first>
  <behavior>
    - Opening each of the 5 issues accordion items renders an archetype `role="img"` node with non-empty aria-label in the active panel (no text placeholder).
    - The issues BenefitSplit flagship exposes headline values + status labels in the DOM without hover.
    - A flagship toggle is a keyboard-reachable `<button>` with `aria-expanded` toggled by Enter/Space.
    - No in-viewport text-bearing element on /platform/issues sits at opacity < 1 after interaction.
  </behavior>
  <action>
    Add `/platform/issues` to the parameterized helper route list in
    `tests/responsive/platform-visuals.spec.ts`. Keep it runnable against a Vercel preview via
    `PLAYWRIGHT_BASE_URL`.
  </action>
  <acceptance_criteria>
    - `tests/responsive/platform-visuals.spec.ts` references `/platform/issues`
    - Issues assertions cover archetype role="img" in an active accordion panel, flagship values present without hover, a keyboard-togglable flagship button
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The platform-visuals spec now asserts issues' accordion visuals, flagship default-visible values, and keyboard parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> rendered page | Only new content is build-time-authored, typed, anonymized payloads. No user input, no network reads/writes, no auth crosses into these visuals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information disclosure | issues payload numbers + regulatory framing | accept | Real-shaped but anonymized per D-09; Andrew pre-cleared figures AND vendor/TSI framing (D-10); [CLAIMS REVIEW]/[COI REVIEW] tags preserved for audit. No real client identifiers (CLAUDE.md §15). |
| T-11-08 | Tampering | typed payload modules | accept | Static TS validated by `satisfies`; no runtime mutation, no user-supplied value. |
| T-11-09 | Spoofing/Repudiation/DoS/EoP | n/a (static marketing visuals) | accept | No new untrusted input, data sink, auth surface, or server action. |

No high-severity threats.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx eslint` clean on the changed files.
- DEFERRED-TO-PREVIEW: run `tests/responsive/platform-visuals.spec.ts`, `reveal-fail-open.spec.ts`,
  `reduced-motion.spec.ts` against a Vercel preview via PLAYWRIGHT_BASE_URL; confirm issues passes.
</verification>

<success_criteria>
- /platform/issues shows real archetype visuals on all 5 accordion items, zero placeholders.
- The issues BenefitSplit visual is an Explorable-composed archetype with values visible by default,
  status label-paired, and keyboard + reduced-motion parity.
- All new payload numbers carry [CLAIMS REVIEW]; regulatory/vendor captions carry [COI REVIEW]; no
  client names.
- Every issues payload reads distinctly from every other phase payload (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/11-platform-deep-dive-visuals/11-03-SUMMARY.md`.
</output>
