---
phase: 11-platform-deep-dive-visuals
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/content/visuals/reporting.ts
  - src/content/visuals/index.ts
  - src/app/platform/reporting/page.tsx
  - src/components/product/visuals/ReportingFlagship.tsx
  - src/components/product/visuals/lazy.tsx
  - tests/responsive/platform-visuals.spec.ts
autonomous: true
requirements: [PLATVIS-01, PLATVIS-02, PLATVIS-03]
user_setup: []

must_haves:
  truths:
    - "Every FeatureAccordion item on /platform/reporting (inventory, vendor, cost, sla, activity) renders a real archetype visual; no text-on-dark placeholder remains"
    - "The /platform/reporting BenefitSplit visual is an Explorable-composed archetype instance fed a real payload, values visible by default, keyboard + touch + reduced-motion reach the same data"
    - "Reduced-motion users see the SAME reporting flagship data values that normal-load users see (D-05 data-parity)"
    - "The lazy loading skeleton for each reporting visual reserves the resolved archetype box (the 11-01-locked min-height) so no layout shift occurs when chunks resolve"
    - "Every reporting payload is visually distinct from every other payload in the phase (D-02)"
    - "Every numeric payload carries a [CLAIMS REVIEW] comment; vendor/TSI captions carry [COI REVIEW]"
  artifacts:
    - path: "src/content/visuals/reporting.ts"
      provides: "5 typed payloads for the reporting accordion items (inventory, vendor, cost, sla, activity) plus the flagship payload"
      contains: "inventory"
    - path: "src/components/product/visuals/ReportingFlagship.tsx"
      provides: "The reporting BenefitSplit explorable flagship, an Explorable-composed archetype instance"
    - path: "src/app/platform/reporting/page.tsx"
      provides: "Reporting page wiring all 5 accordion visuals + the explorable flagship"
  key_links:
    - from: "src/app/platform/reporting/page.tsx"
      to: "src/content/visuals/reporting.ts"
      via: "visuals={{ <itemId>: <ArchetypeVisual data={payload} /> }} prop on FeatureAccordion"
      pattern: "visuals=\\{\\{"
    - from: "src/components/product/visuals/ReportingFlagship.tsx"
      to: "src/components/motion/Explorable.tsx"
      via: "Explorable.Toggle / Explorable.Panel composition"
      pattern: "Explorable\\."
---

<objective>
Implement /platform/reporting end to end against the locked Wave 1 pattern:
author the 5 accordion payloads, wire them through `FeatureAccordion.visuals`
(PLATVIS-01), and refactor the ReportingDashboard BenefitSplit visual into an
`Explorable`-composed archetype instance (PLATVIS-02 + PLATVIS-03 together,
per D-06).

Note: reporting has two items sharing the id `sla` semantics with issues, and an
`inventory`/`vendor`/`cost`/`activity` set heavy on Data-story trends. Honor D-02:
the reporting `sla` and `vendor` payloads must read distinctly from the issues
`sla` and the optimization vendor visuals (different numbers, different shapes).
Inherit the 11-01-locked lazy-skeleton sizing and reduced-motion data-parity
assertion shape; do not re-derive them.

Output: the reporting payload module, the reporting flagship component, the wired
reporting page, and reporting assertions added to the shared platform-visuals spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-platform-deep-dive-visuals/11-CONTEXT.md
@.planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md

<interfaces>
<!-- Same Phase 10 contracts as 11-01 / 11-02 / 11-03. -->

Lazy archetype API: `ConsoleVisual({data})`, `DataStoryVisual({data})`, `SchematicVisual({data})`
(lazy ssr:false; src/components/product/visuals/archetypes.tsx).

Explorable shell: compose `<Explorable label>`, `Explorable.Toggle id` (real <button>,
aria-expanded), `Explorable.Panel id` (content visible by default). No boolean props.

FeatureAccordion: `visuals?: Record<string, React.ReactNode>` keyed by item id; takes precedence
over the placeholder.

ConsoleData / DataStoryData / SchematicData: see src/content/visuals/types.ts; every payload sets the
REQUIRED `ariaSummary`. Reference authoring pattern: src/content/visuals/placement-accordion.ts.
DataStoryData chart kinds available: area / spark / bars / cards.

Lazy-skeleton sizing: 11-01 locked an explicit per-archetype skeleton `min-height` in lazy.tsx (read
the comment recording the proven px value). Reuse the SAME reserved dimensions for the reporting
wrappers — the placement CLS gate already proved them.
</interfaces>

@src/content/visuals/placement.ts
@src/content/visuals/placement-accordion.ts
@src/components/product/visuals/PlacementFlagship.tsx
@src/components/product/visuals/ReportingDashboard.tsx
@src/content/reporting.ts
@src/app/platform/reporting/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the 5 reporting accordion payloads + flagship payload</name>
  <read_first>
    - .planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md (approved archetype + intent for inventory, vendor, cost, sla, activity)
    - src/content/visuals/placement-accordion.ts (authoring reference)
    - src/content/reporting.ts (the 5 item ids + body copy)
    - src/components/product/visuals/ReportingDashboard.tsx (the bespoke flagship's real data: liquidation-by-tier bars, net-back area chart, top-vendor list, SLA adherence stat + sparkline — preserve in the flagship payload)
    - src/content/visuals/types.ts (schemas; extend additively under D-08 only if needed; note DataStoryData supports area/spark/bars/cards chart kinds)
  </read_first>
  <action>
    Create `src/content/visuals/reporting.ts`. Export one typed const per accordion item plus a flagship
    payload const. Per the CONTEXT.md indicative mapping (refine to the approved table): inventory ->
    Console (inventory + liquidation dashboard), vendor -> Console (vendor performance comparison), cost
    -> Data-story (cost trend with commission modeling, area chart), sla -> Data-story (SLA adherence by
    vendor, bars), activity -> Console (activity volume breakdown).

    Real-shaped anonymized numbers (D-09): plausible inventory counts, liquidation/net-back percentages,
    commission costs, SLA adherence; zero client identifiers; generic and qualified. Each payload DISTINCT
    (D-02): the reporting `sla` payload must NOT reuse the issues `sla` numbers or shape, and the reporting
    `vendor` payload must differ from the optimization vendor visuals. The flagship payload carries the
    ReportingDashboard information (liquidation-by-tier, net-back trend, top vendors, SLA adherence).

    Governance: `// [CLAIMS REVIEW]` on every numeric block; `// [COI REVIEW]` on any vendor/TSI-framing
    caption. Every payload sets `ariaSummary`. Voice rules on ariaSummary and captions (no em dashes, no
    banned phrases, sentence case, digits). No client names (CLAUDE.md §15).

    Export from `src/content/visuals/index.ts`. Additive-only schema extensions under D-08; keep
    placement.ts validation green.
  </action>
  <acceptance_criteria>
    - `src/content/visuals/reporting.ts` exports a const for inventory, vendor, cost, sla, activity each typed via `satisfies`, plus a flagship payload const
    - `grep -c "CLAIMS REVIEW" src/content/visuals/reporting.ts` returns at least one per numeric payload
    - Every exported const sets `ariaSummary`; `grep "—" src/content/visuals/reporting.ts` returns nothing
    - The reporting `sla` payload numbers differ from the issues `sla` payload (manual distinctness check noted in SUMMARY)
    - `src/content/visuals/index.ts` re-exports the reporting payloads
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>5 distinct, governed, typed reporting accordion payloads plus a flagship payload exist and are exported; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the reporting explorable flagship + wire the reporting page</name>
  <read_first>
    - src/components/product/visuals/PlacementFlagship.tsx (the Wave 1 reference flagship pattern + parity guarantees)
    - src/components/motion/Explorable.tsx (Toggle/Panel contract)
    - src/components/product/visuals/ReportingDashboard.tsx (the bespoke flagship being replaced — preserve its four dashboard cards)
    - src/app/platform/reporting/page.tsx (current wiring: accordion has no visuals map; BenefitSplit uses LazyReportingDashboard)
    - src/components/product/visuals/lazy.tsx (lazy wrapper registry pattern AND the 11-01 skeleton `min-height` comment recording the proven per-archetype reserved height)
  </read_first>
  <action>
    Create `src/components/product/visuals/ReportingFlagship.tsx` ("use client"), refactoring
    ReportingDashboard (D-07) into an `Explorable`-composed archetype instance fed the flagship payload.
    Natural interaction: `Explorable.Toggle` per dashboard metric / time-range (the existing 7D/30D/90D/YTD
    range chips are a good toggle set), revealing the corresponding chart in an `Explorable.Panel`. HARD
    CONSTRAINTS (D-05 / Phase 10 parity): all dashboard headline values render by default (never
    hover-gated); toggles are Explorable.Toggle `<button>`s (keyboard + focus-visible from the shell);
    reduced-motion shows the same data. Compose archetype slots, not boolean props.

    Add `LazyReportingFlagship` to `src/components/product/visuals/lazy.tsx`. CLS / Pitfall 4: the
    `loading` skeleton MUST reuse the explicit per-archetype `min-height` (or fixed aspect box) that
    11-01 locked and proved on placement, so the resolved chunk produces no layout shift. Do NOT ship a
    zero-height/spinner-only fallback. Read the 11-01 skeleton min-height value from the lazy.tsx
    comment and apply the SAME reserved height to every reporting wrapper.

    In `src/app/platform/reporting/page.tsx`:
    - Add `visuals={{ inventory: <ConsoleVisual data={...} />, vendor: <ConsoleVisual data={...} />,
      cost: <DataStoryVisual data={...} />, sla: <DataStoryVisual data={...} />, activity: <ConsoleVisual data={...} /> }}`
      (archetypes per the approved map) to the `<FeatureAccordion>`. All 5 ids; zero placeholders.
    - Replace `visual={<LazyReportingDashboard />}` with `visual={<LazyReportingFlagship />}`.
    Keep the page a Server Component; do not delete ReportingDashboard.tsx (Phase 13 audit), just stop
    importing it here.
  </action>
  <acceptance_criteria>
    - `src/components/product/visuals/ReportingFlagship.tsx` exists and composes `Explorable.Toggle` + `Explorable.Panel` (`grep "Explorable\." ReportingFlagship.tsx` matches both)
    - No boolean interactive props passed to archetypes
    - `src/app/platform/reporting/page.tsx` passes a `visuals` map covering all 5 ids: `grep -o "inventory\|vendor\|cost\|sla\|activity" src/app/platform/reporting/page.tsx` returns all 5
    - The reporting BenefitSplit `visual=` references `LazyReportingFlagship`, not `LazyReportingDashboard`
    - `lazy.tsx` exports `LazyReportingFlagship`
    - CLS-SKELETON: each reporting `dynamic(...)` wrapper's `loading` skeleton sets the SAME explicit `min-height` (or fixed aspect box) that 11-01 locked for that archetype box, not a zero-height/spinner-only fallback (`grep -A3 "loading:" src/components/product/visuals/lazy.tsx` shows a `minHeight`/`min-h-` value on the reporting wrapper matching the 11-01 comment) — so the chunk resolves with no layout shift
    - `npx tsc --noEmit` exits 0; `npx eslint src/components/product/visuals/ReportingFlagship.tsx src/app/platform/reporting/page.tsx` exits 0
  </acceptance_criteria>
  <done>Reporting renders real archetype visuals on all 5 accordion items and an Explorable-composed flagship on the BenefitSplit band; every lazy wrapper reuses the 11-01-locked skeleton min-height so chunks resolve with no layout shift; page stays a lean Server Component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend platform-visuals spec to cover /platform/reporting</name>
  <read_first>
    - tests/responsive/platform-visuals.spec.ts (the route-parameterized helper to extend, including its reduced-motion data-parity assertion)
    - tests/helpers/routes.ts (VISUAL_ROUTES already includes /platform/reporting)
  </read_first>
  <behavior>
    - Opening each of the 5 reporting accordion items renders an archetype `role="img"` node with non-empty aria-label in the active panel (no text placeholder).
    - The reporting BenefitSplit flagship exposes headline values in the DOM without hover.
    - A flagship toggle is a keyboard-reachable `<button>` with `aria-expanded` toggled by Enter/Space.
    - REDUCED-MOTION DATA-PARITY (D-05): under `page.emulateMedia({ reducedMotion: 'reduce' })`, the reporting flagship's data values that are present on normal load are STILL present in the DOM. Feed the reporting flagship's expected value(s) into the 11-01 per-route helper so the same data-parity assertion runs for this page.
    - No in-viewport text-bearing element on /platform/reporting sits at opacity < 1 after interaction.
  </behavior>
  <action>
    Add `/platform/reporting` to the parameterized helper route list in
    `tests/responsive/platform-visuals.spec.ts`, supplying the reporting flagship's expected value(s)
    so the reduced-motion data-parity assertion runs for it. With all 4 platform routes now covered,
    confirm the helper iterates the full set
    `["/platform/placement","/platform/optimization","/platform/issues","/platform/reporting"]`.
    Keep it runnable against a Vercel preview via `PLAYWRIGHT_BASE_URL`.
  </action>
  <acceptance_criteria>
    - `tests/responsive/platform-visuals.spec.ts` references all 4 platform routes (`grep -o "/platform/placement\|/platform/optimization\|/platform/issues\|/platform/reporting" returns all 4)
    - Reporting assertions cover archetype role="img" in an active accordion panel, flagship values present without hover, a keyboard-togglable flagship button
    - REDUCED-MOTION DATA-PARITY: the reporting route runs the `page.emulateMedia({ reducedMotion: 'reduce' })` assertion that its flagship's on-load value(s) remain present in the DOM (all 4 routes now exercise the data-parity assertion)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The platform-visuals spec now asserts reporting's accordion visuals, flagship default-visible values, keyboard parity, and reduced-motion data-parity, and covers all 4 platform pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> rendered page | Only new content is build-time-authored, typed, anonymized payloads. No user input, no network reads/writes, no auth crosses into these visuals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Information disclosure | reporting payload numbers | accept | Real-shaped but anonymized per D-09; Andrew pre-cleared (D-10); [CLAIMS REVIEW] tags preserved. No real client identifiers (CLAUDE.md §15). |
| T-11-11 | Tampering | typed payload modules | accept | Static TS validated by `satisfies`; no runtime mutation, no user-supplied value. |
| T-11-12 | Spoofing/Repudiation/DoS/EoP | n/a (static marketing visuals) | accept | No new untrusted input, data sink, auth surface, or server action. |

No high-severity threats.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx eslint` clean on the changed files.
- DEFERRED-TO-PREVIEW: run `tests/responsive/platform-visuals.spec.ts`, `reveal-fail-open.spec.ts`,
  `reduced-motion.spec.ts` against a Vercel preview via PLAYWRIGHT_BASE_URL; confirm reporting passes,
  including the reduced-motion data-parity assertion.
</verification>

<success_criteria>
- /platform/reporting shows real archetype visuals on all 5 accordion items, zero placeholders.
- The reporting BenefitSplit visual is an Explorable-composed archetype with values visible by default,
  keyboard parity, and reduced-motion DATA-parity (same values present when motion is off).
- Every reporting lazy wrapper reuses the 11-01-locked skeleton min-height (no layout shift on chunk
  resolve).
- All new payload numbers carry [CLAIMS REVIEW]; vendor/TSI captions carry [COI REVIEW].
- The reporting sla/vendor payloads read distinctly from the issues sla and optimization vendor visuals (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/11-platform-deep-dive-visuals/11-04-SUMMARY.md`.
</output>
