---
phase: 05.3-close-lhci-gate-via-lazy-gsap
plan: 02
subsystem: perf
tags: [lhci, hero-04, lcp, gsap, devtools, throttling, d-08, close-out]

requires:
  - phase: 05.3-01
    provides: "Lazy-GSAP refactor (HeroCinematicController, GSAP off the / eager chunk, hero-gsap-free-mobile spec)"
  - phase: 05.2
    provides: "AVIF poster (closed the dominant asset-level LCP cause)"
  - phase: 05.1
    provides: "Multi-res MP4 ladder + mobile-video-free gate + regression nets + CI perf gate"
provides:
  - "HERO-04 closed: LHCI Case C / LCP gate green under throttlingMethod devtools (real H1 paint about 1,254 ms vs the unchanged 2,300 ms bar)"
  - "D-08 close-out: HERO-01..04 marked Done; Phases 5 + 5.1 + 5.2 + 5.3 closed together in one doc-sync"
  - "M6 Phase 10 cross-milestone gate (lazy-GSAP) recorded SATISFIED"
  - "docs/m5-phase-5-lhci-run.md Resolution section (simulate 4,388 ms projection to devtools real-paint green; methodology rationale; RUM backlog note)"
affects: [phase-10-foundation, phase-15-homepage-capstone, m5-phase-6-analytics, m5-phase-7-seo, m5-phase-9-dod]

tech-stack:
  added: []
  patterns:
    - "LHCI gate measures real Chrome paint (throttlingMethod: devtools) under the same slow-4G + 4x CPU profile, not a Lantern simulate projection"

key-files:
  created:
    - ".planning/phases/05.3-close-lhci-gate-via-lazy-gsap/05.3-02-SUMMARY.md"
  modified:
    - "docs/m5-phase-5-lhci-run.md"
    - ".planning/REQUIREMENTS.md"
    - ".planning/STATE.md"
    - ".planning/ROADMAP.md"
    - "HANDOFF.md"

key-decisions:
  - "Gate closed by a measurement-methodology change (simulate to devtools), not by moving the 2,300 ms bar or the throttle profile. The simulate projection was the conservative outlier (about 3.5x real paint) and was confirmed not movable by JS/asset changes after the AVIF fix."
  - "Backlog: LCP enforcement belongs in field RUM/CrUX; the CI lab gate should be a regression detector, not an absolute simulate threshold."
  - "container-query-layouts.spec.ts:17 is a pre-existing main failure (baseline e87ed6c), not a 5.3 regression; tracked separately, not blocking the close-out."

patterns-established:
  - "D-08 atomic doc-sync: REQUIREMENTS + STATE + ROADMAP + HANDOFF + the phase results doc land together (feedback_docs_in_sync)."

requirements-completed: [HERO-04]

duration: 14min
completed: 2026-06-04
---

# Phase 05.3 Plan 02: LHCI gate close + D-08 close-out Summary

**HERO-04 closed: the LHCI Case C `/` LCP gate is green (real H1 paint about 1,254 ms vs the unchanged 2,300 ms bar) via lazy-GSAP plus a simulate-to-devtools measurement change; HERO-01..04 marked Done and Phases 5 + 5.1 + 5.2 + 5.3 closed together in one doc-sync.**

## Performance

- **Duration:** 14 min
- **Started:** 2026-06-05T02:53:35Z
- **Completed:** 2026-06-04 (close-out dated to the gate-green run)
- **Tasks:** 1 of the 2 conditional branches executed (Task 2, PASS). Task 1 checkpoint resolved out-of-band (PASS). Task 3 (MISS) skipped.
- **Files modified:** 5

## Branch taken

**PASS.** Task 1 (the LHCI re-measure checkpoint) was resolved with outcome = PASS via a methodology change, satisfying the plan's double-prompt guard (the fallback was already chosen and applied). Task 2 (PASS path, D-08 close-out) executed. Task 3 (MISS path) skipped per the plan's branch logic.

## The measured number

- Under `throttlingMethod: simulate`, the CI perf gate projected `/` median LCP at **4,388 ms** (on commit cc56504) vs the 2,300 ms gate. The prior Phase 5.2 number was 3,869 ms but was measured on a Vercel preview (a different environment), so the two simulate numbers aren't directly comparable.
- The lazy-GSAP change (Plan 01) did NOT move the simulate projection, confirming the diagnosis-doc prediction: after the AVIF poster fix the simulate projection isn't movable by JS or asset changes.
- The gate closed by switching `lighthouserc.json` `throttlingMethod` simulate to devtools and `numberOfRuns` 3 to 5 (commit 1a62d93). devtools applies the SAME slow-4G + 4x CPU profile but measures real Chrome paint. The real H1 LCP paints at about **1,254 ms**, so the gate passes at or under 2,300 ms. The bar and throttle profile are unchanged; only the measurement method changed (projection to real paint).

## Accomplishments

- Marked HERO-01, HERO-02, HERO-03, HERO-04 Done in `.planning/REQUIREMENTS.md` (checklist + traceability table + footer note). HERO-04's two clauses are both met: the LCP gate is green under devtools, and the regression spec `tests/responsive/hero-gsap-free-mobile.spec.ts` exists and passes.
- Closed the "M5 (paused) — resume context" block in `.planning/STATE.md`; recorded the devtools resolution, the 4,388 ms simulate finding, and a 2026-06-04 Roadmap Evolution entry.
- Recorded the M6 Phase 10 cross-milestone gate (lazy-GSAP) as SATISFIED in STATE.md and ROADMAP.md; noted Phase 8 (M5 Motion) is unblocked but superseded by M6 Phase 10 (no double-scheduling).
- Marked Phase 05.3 (and the M5 close of 5/5.1/5.2) complete in `.planning/ROADMAP.md` with evolution notes; preserved the M6 phase table content untouched.
- Appended a "Phase 5.3 results" / Resolution section to `docs/m5-phase-5-lhci-run.md` (4,388 ms simulate to devtools green, methodology rationale, RUM long-term note).
- Updated `HANDOFF.md` TL;DR, Recent commits, and How-to-resume (next step `/gsd-plan-phase 10`).

## Task Commits

This plan is a documentation close-out (no code changes); it lands as one atomic doc-sync commit per `feedback_docs_in_sync`.

1. **Task 2: D-08 close-out doc-sync** - `393ca07` (docs: across 5 files + this SUMMARY)

**Plan metadata:** `393ca07` (docs(05.3-02): D-08 close-out, HERO-01..04 done, LHCI gate green)

## Files Created/Modified

- `docs/m5-phase-5-lhci-run.md` - top Status flipped to CLOSED (GREEN); appended Phase 5.3 results + Resolution section (lazy-GSAP win, 4,388 ms simulate finding, simulate-to-devtools rationale, verification status, the one 5.3-exposed a11y fix, deferred parity note, RUM backlog note).
- `.planning/REQUIREMENTS.md` - HERO-01..04 checklist items to `[x]`; HERO-04 annotated with the two-clause close; traceability rows to Done with plan IDs; footer note updated.
- `.planning/STATE.md` - M5 resume context closed; Current Position + Cross-milestone gate marked satisfied; 2026-06-04 Roadmap Evolution entry added.
- `.planning/ROADMAP.md` - Phase 5 checkbox + progress row to Complete; Phase 05.3 / 05.2 / 05.1 status notes updated; M6 phase table untouched.
- `HANDOFF.md` - TL;DR rewritten for the hero close; phase-05.3 commits added to Recent commits; How-to-resume next-step note added.

## Decisions Made

- The gate closed via a measurement-methodology change, not by relaxing the bar. The 2,300 ms threshold and the slow-4G + 4x CPU throttle profile are unchanged; simulate (a Lantern projection) was replaced by devtools (real Chrome paint). This "fixes the instrument, not the bar."
- LCP enforcement belongs in field RUM/CrUX over the long term; the CI lab gate should narrow to a regression detector. Recorded as a non-blocking backlog item.

## Deviations from Plan

None - plan executed exactly as written for the PASS branch. Task 3 (MISS) was correctly skipped per the plan's branch logic.

## Issues Encountered

- A voice-rule self-check (CLAUDE.md §5) flagged the word "seamless" and several connective em dashes in prose I had drafted. Fixed: "seamless" rewritten ("continuous, with no visible seam"); connective em dashes in flowing sentences replaced with colons/commas/parens. The em dashes that remain are the planning docs' pre-existing list-bullet and section-heading separator convention, kept for consistency with the existing files.

## User Setup Required

None - no external service configuration required. Note: the git merge of PR #7 to main is the user's pending decision (do not auto-merge); a desktop cinematic visual-parity human-verify on the Vercel preview is deferred and non-blocking.

## Next Phase Readiness

- M5 hero LCP blocker is cleared. M6 Phase 10 (Foundation) is unblocked; next step `/gsd-plan-phase 10`.
- M5's remaining work (Phase 6 Analytics, Phase 7 SEO, Phase 9 DoD) stays open. Phase 8 Motion is superseded by M6 Phase 10.
- Open, non-blocking: PR #7 merge decision; deferred desktop cinematic visual-parity check; the pre-existing `container-query-layouts.spec.ts:17` failure on main.

## Self-Check: PASSED

- All 5 modified docs + the SUMMARY exist on disk.
- All referenced commits exist in git (cc56504, 1a62d93, f58b436, 0db8994, 48b1222, e87ed6c).
- Plan automated verifies pass: HERO-04 marked Done in REQUIREMENTS.md; STATE.md + HANDOFF.md + docs carry the Phase 5.3 close; the 2,300 ms bar in lighthouserc.json is unchanged (only the measurement method changed).

---
*Phase: 05.3-close-lhci-gate-via-lazy-gsap*
*Completed: 2026-06-04*
