---
phase: 05.3-close-lhci-gate-via-lazy-gsap
plan: 02
type: execute
wave: 2
depends_on: ["05.3-01"]
files_modified:
  - docs/m5-phase-5-lhci-run.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - HANDOFF.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [HERO-04]
user_setup: []

must_haves:
  truths:
    - "A fresh LHCI Case C run against a Vercel preview of the lazy-GSAP branch has a recorded median LCP number."
    - "If median LCP <= 2,300 ms: HERO-01..04 are marked Done and Phases 5 + 5.1 + 5.2 + 5.3 are closed together in one doc-sync (D-08)."
    - "If median LCP > 2,300 ms: the run STOPS and surfaces the measured number + the fallback menu to the user; the gate is NOT silently relaxed and HERO-04 stays open."
    - "The measured number is recorded in docs/m5-phase-5-lhci-run.md regardless of pass/fail."
  artifacts:
    - path: "docs/m5-phase-5-lhci-run.md"
      provides: "Phase 5.3 LHCI Case C results section: branch, preview URL, 3-run table, median LCP, delta vs 2,300 gate, public LHR link, and the before/after JS-chunk note."
      contains: "Phase 5.3"
    - path: ".planning/STATE.md"
      provides: "M5 resume context closed (on pass) or updated with the residual number + fallback decision request (on miss)."
    - path: "HANDOFF.md"
      provides: "TL;DR + recent commits reflect the gate outcome."
  key_links:
    - from: "Vercel preview of the lazy-GSAP branch"
      to: "LHCI Case C assert (lighthouserc.json, 2300 ms median-run)"
      via: "lhci autorun against the preview URL (or CI perf gate on the PR)"
      pattern: "largest-contentful-paint"
    - from: "gate outcome (pass)"
      to: "D-08 close-out doc-sync"
      via: "HERO-01..04 -> Done; Phases 5/5.1/5.2/5.3 closed in one commit series"
      pattern: "HERO-04"
---

<objective>
Re-run LHCI Case C against a Vercel preview of the Plan 01 lazy-GSAP branch, record the
new median `/` LCP, and branch on the result:

- PASS (median LCP <= 2,300 ms): fire the D-08 close-out. Mark HERO-01..04 Done and
  close Phases 5 + 5.1 + 5.2 + 5.3 together via a single doc-sync (STATE.md / ROADMAP.md
  / HANDOFF.md / REQUIREMENTS.md / docs/m5-phase-5-lhci-run.md), per feedback_docs_in_sync.
- MISS (median LCP > 2,300 ms): STOP. Record the number, surface it plus the fallback
  menu (relax gate to the 2,500 ms CLAUDE.md §12 floor; conditional General Sans preload;
  switch LHCI throttlingMethod simulate->devtools) to the user. Do NOT auto-relax the
  gate and do NOT mark HERO-04 Done.

Purpose: HERO-04 is the single open requirement for all of M5 Phase 5. The gate is the
arbiter; the close-out and Phase 8 / M6 Phase 10 unblocking all hinge on it going green.

Output: A recorded LHCI result and EITHER the D-08 close-out OR an escalation to the user.

CRITICAL: local `next dev/build/start` does NOT bind a port in this sandbox (it hangs —
see memory project_next_server_hangs_in_sandbox). LHCI verification MUST go through a
Vercel preview deploy + LHCI logs (or the GitHub Actions perf gate on the PR), never a
local server.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-close-lhci-gate-via-lazy-gsap/05.3-CONTEXT.md
@docs/m5-phase-5-lhci-run.md
@.planning/STATE.md
@lighthouserc.json
@.github/workflows/perf.yml
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-run LHCI Case C on a Vercel preview and record the number</name>
  <files>docs/m5-phase-5-lhci-run.md</files>
  <read_first>
    - lighthouserc.json (Case C profile + the 2,300 ms median-run assertion)
    - .github/workflows/perf.yml (where check-hero-assets.sh + lhci autorun run in CI)
    - docs/m5-phase-5-lhci-run.md (the prior 3,869 ms result + table format to match)
    - .planning/phases/05.3-close-lhci-gate-via-lazy-gsap/05.3-CONTEXT.md (Verification + D-08)
  </read_first>
  <action>
    Push the Plan 01 branch, open a PR to main, and let the perf gate run (or deploy a
    Vercel preview and run lhci against it). Read the LHCI output, then write the
    "## Phase 5.3 results" section into docs/m5-phase-5-lhci-run.md (3-run table, median
    LCP, delta vs 2,300, branch/PR URL, public LHR link). This is the gate measurement;
    no code changes here. After recording, report the numbers to the user and pause for
    the gate decision (Task 2 on pass, Task 3 on miss).
  </action>
  <what-built>
    Plan 01 moved GSAP + ScrollTrigger + useGSAP off the `/` eager chunk into a
    dynamic-imported, desktop-only HeroCinematicController. This removes ~80-120 KB of
    JS the LHCI simulator was modeling against the mobile CPU/JS graph. This task
    measures whether that closes the 2,300 ms gate.
  </what-built>
  <how-to-verify>
    Because local next build/start hangs in this sandbox, drive LHCI through Vercel:

    1. Push the Plan 01 branch and open a PR to main. The `.github/workflows/perf.yml`
       perf gate runs `check-hero-assets.sh` then `npx lhci autorun` (3 runs, Case C
       profile, 2,300 ms median-run assertion). Use that CI run as the authority.
       (Alternatively, deploy a Vercel preview and run `npx lhci autorun` with the
       collect.url repointed at the preview URL — but the CI perf gate is the canonical
       gate and is preferred.)
    2. Read the LHCI output: the 3 per-run LCP values, the median, and pass/fail of the
       `largest-contentful-paint maxNumericValue 2300 median-run` assertion. Capture the
       public temporary-storage LHR URL.
    3. Confirm the GSAP-removal landed in the measured build: the LHR's
       "Reduce unused JavaScript" / network panel should no longer list a GSAP chunk on
       the initial `/` load (cross-check with the Plan 01 mobile-GSAP-free spec).
    4. Report to the user: the 3 run numbers, the median, delta vs 2,300, and whether
       the assertion passed. Then await the gate decision before Task 2/Task 3 run.

    Record the result in docs/m5-phase-5-lhci-run.md in a new "## Phase 5.3 results"
    section matching the existing table format (Run / TTFB / FCP / LCP / Δ vs 2,300),
    plus branch, preview/PR URL, LHCI version, and the public LHR link. Do this
    regardless of pass or fail.
  </how-to-verify>
  <verify>
    <automated>cd "$PWD" && grep -q "Phase 5.3" docs/m5-phase-5-lhci-run.md && grep -qE "median|LCP" docs/m5-phase-5-lhci-run.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/m5-phase-5-lhci-run.md contains a "Phase 5.3" results section with: branch name, PR/preview URL, a 3-run LCP table, an explicit median LCP value in ms, and the delta vs the 2,300 ms gate.
    - The section states PASS or MISS against the 2,300 ms median-run assertion explicitly.
    - A public LHR report URL for the median run is recorded.
  </acceptance_criteria>
  <done>The Phase 5.3 LHCI Case C result (3-run table + median LCP + pass/miss verdict + LHR link) is recorded in docs/m5-phase-5-lhci-run.md and reported to the user.</done>
  <resume-signal>
    Reply with the gate decision: "pass" (median LCP <= 2,300 ms — proceed to D-08
    close-out, Task 2) OR "miss + {chosen fallback}" (median LCP > 2,300 ms — do NOT
    close out; Task 3 records the escalation and stops). If "miss" with no fallback
    chosen yet, the run stops and waits for your call (relax gate to 2,500 / conditional
    font preload / throttlingMethod swap / something else).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: [PASS PATH ONLY] D-08 close-out — mark HERO-01..04 Done, close Phases 5/5.1/5.2/5.3</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md, HANDOFF.md</files>
  <read_first>
    - .planning/STATE.md (M5 resume context, Roadmap Evolution — grep "^## " first per memory feedback_verify_doc_structure)
    - .planning/ROADMAP.md (current head holds M6; add an evolution note, do not overwrite M6)
    - .planning/REQUIREMENTS.md (HERO-01..04 status fields)
    - HANDOFF.md (grep "^## " first; TL;DR + Recent commits + How to resume sections)
    - docs/m5-phase-5-lhci-run.md (the recorded Phase 5.3 PASS result from Task 1)
  </read_first>
  <action>
    RUN THIS TASK ONLY IF Task 1's gate decision was "pass" (median LCP <= 2,300 ms).
    If the decision was "miss", SKIP this task entirely and run Task 3 instead.

    Single doc-sync (commit alongside the Plan 01 code per feedback_docs_in_sync):
    1. .planning/REQUIREMENTS.md: set HERO-01, HERO-02, HERO-03, HERO-04 status to Done
       (Done/✓ in whatever convention that file already uses — match existing rows).
    2. .planning/STATE.md:
       - Close the "M5 (paused) — resume context" section: state Phase 5 + 5.1 + 5.2 +
         5.3 are CLOSED, HERO-04 is Done, gate cleared at {median} ms vs 2,300 ms.
       - Update the paused_milestone frontmatter / Current Position note: M5 hero LCP
         gate is no longer a blocker for M6 Phase 10 (the cross-milestone gate in
         STATE.md "Cross-milestone gate" is satisfied — note it).
       - Add a Roadmap Evolution entry dated 2026-06-04: "Phase 5.3 closed the LHCI
         Case C gate via lazy-loaded GSAP. Median `/` LCP {n} ms vs 2,300 ms.
         HERO-01..04 Done. Phases 5 + 5.1 + 5.2 + 5.3 shipped together (D-08). Phase 8
         (M5 Motion) / M6 Phase 10 (Foundation) cross-milestone gate now satisfied."
    3. .planning/ROADMAP.md: ROADMAP currently holds M6 — do NOT overwrite it. Add a
       short note (in the appropriate M5-reference / evolution area, or a one-line
       cross-reference near the Phase 10 dependency) that the M5 Phase 5.3 lazy-GSAP
       gate is closed and the Phase 10 cross-milestone dependency on it is satisfied.
    4. HANDOFF.md: update TL;DR for the next agent (M5 hero LCP gate closed), add the
       close-out to "Recent commits", and update "How to resume" (next step is
       /gsd-plan-phase 10, M5 hero blocker cleared).

    Follow CLAUDE.md cadence reminders in STATE.md: GPG signing off
    (git -c commit.gpgsign=false), Co-Authored-By footer, banned-punctuation/voice
    self-check on any prose added (no em dashes, sentence-case headings).
    Do not touch ROADMAP's M6 phase table content beyond the cross-reference note.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -E "HERO-04.*(Done|✓)" .planning/REQUIREMENTS.md && grep -q "Phase 5.3" .planning/STATE.md && grep -q "5.3" HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md shows HERO-01, HERO-02, HERO-03, HERO-04 all marked Done (match the file's existing status convention).
    - .planning/STATE.md "M5 (paused) — resume context" is updated to a CLOSED state and a 2026-06-04 Roadmap Evolution entry records the Phase 5.3 gate close with the median LCP number.
    - .planning/STATE.md notes the M6 Phase 10 cross-milestone gate (lazy-GSAP) is satisfied.
    - .planning/ROADMAP.md M6 phase table is NOT overwritten (still lists Phases 10-15) and carries a note that the Phase 5.3 dependency is closed.
    - HANDOFF.md TL;DR and How-to-resume reflect the gate close (next step /gsd-plan-phase 10).
    - No em dash characters in any added prose (banned punctuation, CLAUDE.md §5).
  </acceptance_criteria>
  <done>HERO-01..04 Done; M5 Phase 5/5.1/5.2/5.3 closed in one doc-sync; M6 Phase 10 cross-milestone gate satisfied; ROADMAP M6 content intact.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: [MISS PATH ONLY] Record residual number and escalate the fallback decision</name>
  <files>docs/m5-phase-5-lhci-run.md, .planning/STATE.md</files>
  <read_first>
    - docs/m5-phase-5-lhci-run.md (the recorded Phase 5.3 MISS result from Task 1)
    - .planning/STATE.md (M5 resume context to update with the residual)
    - .planning/phases/05.3-close-lhci-gate-via-lazy-gsap/05.3-CONTEXT.md (deferred fallback levers)
    - CLAUDE.md §12 (the 2,500 ms spec floor)
  </read_first>
  <action>
    RUN THIS TASK ONLY IF Task 1's gate decision was "miss" (median LCP > 2,300 ms).
    If the decision was "pass", SKIP this task — Task 2 owns the close-out.

    Do NOT mark HERO-04 Done. Do NOT relax the gate. Update STATE.md's M5 resume context
    with the residual median LCP and that lazy-GSAP alone did not close the gate.

    HERO-04 has two clauses: (1) the LHCI Case C LCP gate is green, and (2) the
    regression spec exists and passes. On the MISS path, note explicitly in STATE.md /
    docs that clause (2) IS met (the GSAP-free-on-mobile spec from Plan 01 exists and is
    green) but clause (1) is NOT met, so HERO-04 stays Open pending a fallback lever.

    Double-prompt guard: if Task 1's resume-signal already named a fallback
    (e.g. "miss + relax-gate"), do NOT re-ask. Record that chosen lever here, note it
    becomes a follow-up plan/phase, and stop. Only present the options menu below if
    Task 1 returned "miss" with no lever chosen yet.
  </action>
  <decision>Lazy-GSAP did not close the 2,300 ms LHCI Case C gate. Which fallback lever? (Skip this prompt if Task 1 already named one — just record it and stop.)</decision>
  <context>
    Plan 01 removed GSAP from the `/` eager chunk; the LHCI simulator's median `/` LCP
    is now {n} ms, still over the 2,300 ms gate by {n-2300} ms. Observed (unthrottled)
    LCP remains ~1,254 ms, so the page is fast in reality; the residual is the
    simulator's projection. The remaining levers are deferred-only per CONTEXT.md and
    require your call (do not auto-apply).
  </context>
  <options>
    <option id="relax-gate">
      <name>Relax the gate to 2,500 ms (CLAUDE.md §12 spec floor)</name>
      <pros>Matches the documented launch spec; one-line lighthouserc.json change; likely closes if {n} is between 2,300 and 2,500.</pros>
      <cons>Loses the 200 ms Phase 8 motion headroom; motion regression protection moves to post-launch RUM. If {n} > 2,500 this still misses.</cons>
    </option>
    <option id="conditional-font-preload">
      <name>Drop General Sans preload from the mobile critical path</name>
      <pros>Frees ~47 KB the mobile simulator pays for a desktop-only Wordmark; no gate change.</pros>
      <cons>Needs Wordmark verified to not block hydration; more code than a gate change; uncertain simulator delta.</cons>
    </option>
    <option id="throttling-method">
      <name>Switch LHCI throttlingMethod simulate -> devtools</name>
      <pros>Real Chrome throttling, closer to RUM; one-line lighthouserc.json change; the conservative simulator is what is inflating the number.</pros>
      <cons>Changes what the gate measures (defensible but is a measurement-policy change, not a page change); confirm with you it is acceptable.</cons>
    </option>
  </options>
  <verify>
    <automated>cd "$PWD" && grep -q "Phase 5.3" docs/m5-phase-5-lhci-run.md && grep -qiE "miss|still.*over|did not close" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - HERO-04 is NOT marked Done anywhere (`grep -E "HERO-04.*(Done|✓)" .planning/REQUIREMENTS.md` returns nothing).
    - lighthouserc.json 2,300 ms assertion is UNCHANGED (`grep -q "2300" lighthouserc.json` still succeeds; not silently relaxed).
    - .planning/STATE.md M5 resume context records the residual median LCP, that lazy-GSAP alone missed clause (1) of HERO-04 (the LCP gate), and that clause (2) (the regression spec) IS met but does not by itself close HERO-04.
    - docs/m5-phase-5-lhci-run.md records the MISS with the number.
  </acceptance_criteria>
  <done>Residual median LCP recorded; gate left intact at 2,300 ms; HERO-04 still Open (clause 1 LCP-gate unmet, clause 2 regression-spec met); the chosen fallback lever is recorded (re-using Task 1's answer if one was given, otherwise selected from the menu) and handed to a follow-up plan/phase.</done>
  <resume-signal>If Task 1 already named a lever, this task records it and stops (no re-prompt). Otherwise select: relax-gate, conditional-font-preload, throttling-method, or describe another approach. The chosen lever becomes a new follow-up plan/phase; this phase stays open with HERO-04 unclosed.</resume-signal>
</task>

</tasks>

<verification>
- A Phase 5.3 LHCI Case C result (3-run table + median) is recorded in docs/m5-phase-5-lhci-run.md.
- The pass/miss branch was honored: on PASS, HERO-01..04 are Done and M5 closed; on MISS, HERO-04 stays open, the 2,300 ms gate is unchanged, and the fallback decision is surfaced to the user.
- The cross-milestone gate note in STATE.md is reconciled (satisfied on pass; still-open on miss).
</verification>

<success_criteria>
The LHCI gate has a recorded median number from a Vercel-preview / CI run, and exactly
one branch fired: a clean D-08 close-out of M5 Phases 5/5.1/5.2/5.3 with HERO-01..04
Done (pass), or a recorded miss with the fallback menu escalated to the user and the
gate left intact (miss).
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-close-lhci-gate-via-lazy-gsap/05.3-02-SUMMARY.md`.
Record the measured median LCP, the branch taken (pass/miss), and — on pass — the list
of docs synced; on miss — the residual number and the fallback options presented.
</output>
