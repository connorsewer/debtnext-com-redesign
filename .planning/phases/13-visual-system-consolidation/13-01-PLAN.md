---
phase: 13-visual-system-consolidation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/product/visuals/Console.tsx
  - src/content/visuals/handoff-placement.ts
  - src/content/visuals/index.ts
  - tests/responsive/handoff-pin-anchored.spec.ts
  - tests/responsive/handoff-bezel-seam.spec.ts
  - tests/responsive/handoff-dashboard-static.spec.ts
autonomous: true
requirements: [SYSVIS-01]
must_haves:
  truths:
    - "Console can render its slots WITHOUT a ProductCanvas card when bare={true}"
    - "Every existing <Console data /> call site renders byte-identically (bare defaults false)"
    - "The 3 cinematic regression specs exist and pass against current main (placement still bespoke)"
    - "The placement-tab Console payload exists as a typed module and satisfies ConsoleData"
  artifacts:
    - path: "src/components/product/visuals/Console.tsx"
      provides: "bare render path on ConsoleRoot (no ProductCanvas, ariaSummary on bare root)"
      contains: "bare"
    - path: "src/content/visuals/handoff-placement.ts"
      provides: "placement-tab ConsoleData payload mapped from PlacementMockup"
      contains: "satisfies ConsoleData"
    - path: "tests/responsive/handoff-pin-anchored.spec.ts"
      provides: "desktop sticky-pin-anchored regression spec"
    - path: "tests/responsive/handoff-bezel-seam.spec.ts"
      provides: "hero/handoff bezel center-x parity spec at the seam"
    - path: "tests/responsive/handoff-dashboard-static.spec.ts"
      provides: "dashboard bounding-box stability across a tab crossfade"
  key_links:
    - from: "ConsoleRoot bare path"
      to: "data.ariaSummary on the bare root element"
      via: "role=img aria-label"
      pattern: "aria-label=\\{data.ariaSummary\\}"
---

<objective>
Lay the Wave 0 foundation so the placement-tab proof (Plan 02) can repoint with a machine-checkable gate. Three deliverables, all backward-compatible:
1. Add a backward-compatible `bare?: boolean` render path to `Console` so a Console instance can render its slots WITHOUT its own `ProductCanvas` card (the parent `FramedDashboard` supplies the single frame). This is the single fix for the double-frame landmine (RESEARCH §Console Contract).
2. Author the 3 MISSING cinematic regression specs that D-07's gate depends on (they do not exist today; the desktop cinematic was only human-verified).
3. Author the placement-tab `ConsoleData` payload mapped field-for-field from `PlacementMockup`, with DESIGN.md chart tokens only (no off-token hex).

Purpose: De-risk the riskiest visual surface in the build before touching any tab. Without the bare path, `<FramedDashboard><Console/></FramedDashboard>` double-frames. Without the 3 specs, "validate the full regression-spec set against placement" (D-06) is not machine-checkable.
Output: edited Console.tsx, 1 new payload module, 3 new spec files. NO tab is repointed in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-visual-system-consolidation/13-CONTEXT.md
@.planning/phases/13-visual-system-consolidation/13-RESEARCH.md
@.planning/phases/13-visual-system-consolidation/13-VALIDATION.md

<interfaces>
<!-- Console contract (VERIFIED src/components/product/visuals/Console.tsx). The slots
     read ConsoleContext via React.use(), so they work under any provider. ConsoleRoot
     is what mounts ProductCanvas (the double-frame source). -->

ConsoleProps (Console.tsx:212-217):
```ts
export interface ConsoleProps {
  data: ConsoleData;
  children?: React.ReactNode; // omit = default flat render: Header → Callout → Rows → Pills
  className?: string;
}
export const Console = Object.assign(ConsoleRoot, { Header, Callout, Rows, Pills });
```

ConsoleRoot current body (Console.tsx:229-248) — wraps children in ProductCanvas:
```tsx
function ConsoleRoot({ data, children, className }: ConsoleProps) {
  return (
    <ConsoleContext value={data}>
      <ProductCanvas role="img" aria-label={data.ariaSummary}
        className={cn("flex flex-col gap-4 text-[var(--product-text)]", className)}>
        {children ?? (<><Header /><Callout /><Rows /><Pills /></>)}
      </ProductCanvas>
    </ConsoleContext>
  );
}
```

ConsoleData shape (src/content/visuals/types.ts:75-82): header, callout?, columns?, rows[], pills?, ariaSummary (REQUIRED).
BarSpec single-segment: `bar: { segments: [35], tone?: "indigo"|"success"|"warning"|"neutral" }`.
header.kpi: `{ caption, value, valueSuffix?, decimals?, sub? }`.
header.status: `{ label, live? }` (live=true renders PulseDot).

Reference payload pattern (src/content/visuals/placement.ts) — uses `satisfies ConsoleData`, [CLAIMS REVIEW]/[COI REVIEW] header comments, tone tokens (never gradient hex).

PlacementMockup data to map (VERIFIED src/components/sections/mockups/PlacementMockup.tsx:6-13, 33-53):
- title const: `placementMockupTitle = "Placement run · 12:04 PM"` (stays in index.tsx; NOT in this payload)
- KPI block: caption "Inbound batch", value 120418, sub "accounts · $284.6M"
- status: label "Engine running", live true
- pools (4 rows): Pre-collect 35% / 2 vendors; Primary 28% / 3 vendors; Secondary 18% / 2 vendors; Tertiary 12% / 4 vendors
- off-token gradient endpoints (#22c55e/#d97706/#0891b2) MUST NOT appear in the payload — bar color comes from tone tokens.

Firewall selectors (VERIFIED, used by the new specs):
- `[data-handoff-section]` — HomepageHandoffSection.tsx:170 (400vh outer; sticky inner inside)
- `[data-handoff-mockup-frame]` — HomepageHandoffSection.tsx:205 (the centered handoff frame wrapper)
- `[data-hero-framed-dashboard]` — HomepageHero.tsx:178 (the hero's framed dashboard)
- desktop pin: the sticky inner is `<div class="sticky top-0 h-screen ...">` directly inside `[data-handoff-section]` (HandoffSection.tsx:188)
- tab buttons live in `[role="tablist"][aria-label="dPlat capability surfaces"]` (HandoffSection.tsx:223); 4 tabs labelled Placement / Vendor performance / Issues / Reporting
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a backward-compatible bare render path to Console</name>
  <read_first>
    - src/components/product/visuals/Console.tsx (the file being modified; read ConsoleRoot 229-248 + ConsoleProps 212-217)
    - src/components/product/primitives/ProductCanvas.tsx (what bare must NOT render)
    - src/content/visuals/placement.ts (a real payload to test the bare render against)
  </read_first>
  <behavior>
    - Default (bare omitted or false): ConsoleRoot renders EXACTLY as today — children wrapped in <ProductCanvas role="img" aria-label={data.ariaSummary}>. Byte-behavior-unchanged for all existing call sites.
    - bare={true}: ConsoleRoot renders the same ConsoleContext provider + the same default slot order (Header → Callout → Rows → Pills) or passed children, but in a plain <div role="img" aria-label={data.ariaSummary}> with className cn("flex flex-col gap-4 text-[var(--product-text)]", className) — NO ProductCanvas wrapper, no rounded card, no padding box, no radial bloom.
    - The aria-label (data.ariaSummary) is present on the bare root so the a11y text-alternative contract (Pitfall 8) holds in both modes.
  </behavior>
  <action>
    In src/components/product/visuals/Console.tsx:
    1. Add `bare?: boolean;` to the `ConsoleProps` interface (after `className?`), with a doc comment: "When true, render the slots in a plain div instead of a ProductCanvas card (parent supplies the frame, e.g. FramedDashboard). Default false keeps every existing call site byte-identical."
    2. In `ConsoleRoot`, destructure `bare = false` from props. Compute the slot body once: `const body = children ?? (<><Header /><Callout /><Rows /><Pills /></>);`.
    3. When `bare` is true, return `<ConsoleContext value={data}><div role="img" aria-label={data.ariaSummary} className={cn("flex flex-col gap-4 text-[var(--product-text)]", className)}>{body}</div></ConsoleContext>`.
    4. When `bare` is false, keep the existing `<ProductCanvas role="img" aria-label={data.ariaSummary} className={cn("flex flex-col gap-4 text-[var(--product-text)]", className)}>{body}</ProductCanvas>` path unchanged.
    5. Do NOT change the `Console = Object.assign(ConsoleRoot, {...})` export, the slots, useConsole, or ConsoleContext. ConsoleContext stays unexported.
    Rationale (per RESEARCH Open Question 3): a localized backward-compatible `bare?` prop is the recommended fix over exporting ConsoleContext or building a separate wrapper — one edit, all existing `<Console data/>` calls unaffected (default bare=false).
  </action>
  <acceptance_criteria>
    - `grep -n "bare" src/components/product/visuals/Console.tsx` shows `bare?: boolean` in ConsoleProps AND a `bare = false` destructure AND a conditional branch.
    - `grep -c "aria-label={data.ariaSummary}" src/components/product/visuals/Console.tsx` returns 2 (both the ProductCanvas path and the bare div path carry it).
    - The bare branch renders a plain `<div role="img"` (grep `role="img"` shows it on a div, not only on ProductCanvas).
    - `npx tsc --noEmit` exits 0 for Console.tsx (no new type errors).
    - `npx eslint src/components/product/visuals/Console.tsx` exits 0.
    - `npx playwright test tests/responsive/platform-visuals.spec.ts tests/responsive/solutions-visuals.spec.ts tests/responsive/reveal-fail-open.spec.ts` stay green (existing Console call sites unchanged) — run against `next build` output or preview, never `next dev` (sandbox hangs).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/product/visuals/Console.tsx</automated>
  </verify>
  <done>Console has a `bare` render path; default path is byte-behavior-unchanged; ariaSummary is on the bare root; tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author the placement-tab Console payload (DESIGN.md tokens only)</name>
  <read_first>
    - src/content/visuals/placement.ts (the reference payload pattern to mirror: satisfies ConsoleData, header comments, tone tokens)
    - src/content/visuals/types.ts (ConsoleData / ConsoleHeader / BarSpec / ConsoleRow exact field names)
    - src/components/sections/mockups/PlacementMockup.tsx (the data source: pools, KPI, status — lines 6-13, 33-53)
    - src/content/visuals/index.ts (the barrel to add the export to)
  </read_first>
  <action>
    Create src/content/visuals/handoff-placement.ts exporting `handoffPlacementConsole` typed with `satisfies ConsoleData`, mapped field-for-field from PlacementMockup:
    - Header comments at top: `// [CLAIMS REVIEW]` (the KPI/pool numbers are governed copy, mirror placement.ts wording) and `// [COI REVIEW]` (vendor-network framing). Real-shaped anonymized figures only.
    - `header`: `{ title: "Routing pools", kpi: { caption: "Inbound batch", value: 120418, sub: "accounts · $284.6M" }, status: { label: "Engine running", live: true } }`. (No eyebrow needed; the FramedDashboard chrome title "Placement run · 12:04 PM" is supplied separately by mockupTitleForTab.)
    - `columns`: omit (the mockup has no column header labels).
    - `rows`: 4 single-segment pool rows, each `{ primary, secondary: "N vendors", bar: { segments: [pct] }, trailing: { value: pct, suffix: "%" } }`:
      - Pre-collect: secondary "2 vendors", segments [35], trailing { value: 35, suffix: "%" }
      - Primary: secondary "3 vendors", segments [28], trailing { value: 28, suffix: "%" }
      - Secondary: secondary "2 vendors", segments [18], trailing { value: 18, suffix: "%" }
      - Tertiary: secondary "4 vendors", segments [12], trailing { value: 12, suffix: "%" }
    - Use `tone` ONLY where a non-default bar color is wanted (e.g. omit tone to inherit ValueBar default indigo, or set `tone: "indigo"`). DO NOT use any hex literal anywhere — the off-token endpoints #22c55e/#d97706/#0891b2 must not appear (D-10).
    - `ariaSummary`: a governed-copy sentence describing the placement run (mirror placement.ts voice: sentence case, digits, no em dashes, no banned phrases). Example shape: "Placement run console. 120,418 accounts are loaded in the inbound batch worth $284.6M, with the routing engine running. Four pools split the batch: pre-collect 35% across 2 vendors, primary 28% across 3 vendors, secondary 18% across 2 vendors, tertiary 12% across 4 vendors."
    Then add `export { handoffPlacementConsole } from "./handoff-placement";` to src/content/visuals/index.ts (alphabetically near the other handoff/placement exports).
  </action>
  <acceptance_criteria>
    - File src/content/visuals/handoff-placement.ts exists and ends with `} satisfies ConsoleData;`.
    - `grep -c "segments" src/content/visuals/handoff-placement.ts` returns 4 (one per pool row).
    - `grep -n "#22c55e\|#d97706\|#0891b2\|#" src/content/visuals/handoff-placement.ts` returns ZERO hex literals.
    - `grep -n "CLAIMS REVIEW" src/content/visuals/handoff-placement.ts` and `grep -n "COI REVIEW" src/content/visuals/handoff-placement.ts` each return at least one match.
    - `grep -n "handoffPlacementConsole" src/content/visuals/index.ts` shows the re-export.
    - `npx tsc --noEmit` exits 0 (the `satisfies ConsoleData` proves the mapping fits the schema).
    - ariaSummary contains no em dash (`grep "—" src/content/visuals/handoff-placement.ts` returns nothing).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && ! grep -q "#22c55e\|#d97706\|#0891b2" src/content/visuals/handoff-placement.ts</automated>
  </verify>
  <done>handoffPlacementConsole exists, satisfies ConsoleData, uses tokens only (zero hex), carries CLAIMS/COI markers, exported from the barrel.</done>
</task>

<task type="auto">
  <name>Task 3: Author the 3 missing cinematic regression specs</name>
  <read_first>
    - tests/responsive/platform-mobile.spec.ts (the existing handoff spec pattern: viewport, goto "/", waitForLoadState networkidle, bounding-box evaluate)
    - src/components/sections/HomepageHandoffSection.tsx lines 167-235 (the desktop pin structure, [data-handoff-section], the sticky inner, [data-handoff-mockup-frame], the tablist)
    - src/components/sections/HomepageHero.tsx lines 178-194 ([data-hero-framed-dashboard])
    - tests/helpers/routes.ts (if a helper is needed; otherwise inline "/")
  </read_first>
  <action>
    Author 3 Playwright specs under tests/responsive/. They must PASS against current main (placement still bespoke) so they are true regression guards, not migration assertions. Desktop viewport 1440x900 unless noted. Each: `await page.goto("/"); await page.waitForLoadState("networkidle");`.

    1. tests/responsive/handoff-pin-anchored.spec.ts — "desktop handoff pin stays anchored at viewport top through the scroll range":
       - At 1440x900, assert `[data-handoff-section]` boundingBox height > 2400 (the 400vh outer; matches platform-mobile.spec.ts:37).
       - Locate the sticky inner: `page.locator('[data-handoff-section] > div.sticky').first()` (the `sticky top-0 h-screen` div). Scroll the window to roughly the middle of the handoff section (e.g. evaluate the section's offsetTop, scroll to offsetTop + viewportHeight*2), wait a tick, and assert the sticky inner's boundingBox `y` is within a small tolerance of 0 (it is pinned to the viewport top: assert `Math.abs(y) <= 2`). Then scroll a bit further and assert `y` is STILL within tolerance of 0 (it does not scroll away). This is the "pin anchored" half of SYSVIS-01 SC2.
       - If the sticky element is not directly selectable by `div.sticky`, select `[data-handoff-mockup-frame]`'s nearest pinned ancestor by reading the structure in HandoffSection.tsx:188; document the selector chosen in a comment.

    2. tests/responsive/handoff-bezel-seam.spec.ts — "hero and handoff framed dashboards share the same horizontal center at the seam":
       - At 1440x900, get `[data-hero-framed-dashboard]` boundingBox and `[data-handoff-mockup-frame]` boundingBox (scroll each into a measurable position as needed — the hero frame is near top of page; the handoff frame is in the pinned section). Compute centerX = box.x + box.width/2 for each. Assert `Math.abs(heroCenterX - handoffCenterX) <= 2` (both are `left-1/2 -translate-x-1/2` centered; the bezel stays viewport-centered across the hero→Platform seam).

    3. tests/responsive/handoff-dashboard-static.spec.ts — "the dashboard frame does not move across a tab crossfade":
       - At 1440x900, scroll into the pinned handoff range so `[data-handoff-mockup-frame]` is visible. Record its boundingBox {x, y}. Activate a different tab: click a tab button in `[role="tablist"][aria-label="dPlat capability surfaces"]` whose text is not the active one (e.g. the "Issues" tab), wait ~400ms for the crossfade. Re-read `[data-handoff-mockup-frame]` boundingBox. Assert `Math.abs(after.x - before.x) <= 1` AND `Math.abs(after.y - before.y) <= 1` (only the content swaps; the frame is static).
       - NOTE: on desktop, tab switching is driven by GSAP scroll progress, not click. If clicking a tab does not change the active tab on desktop, instead scroll the window by VH_PER_TAB worth of pixels to advance the active tab (read VH_PER_TAB usage in HandoffSection.tsx), then assert the frame box is unchanged. Document which mechanism the spec uses in a comment.

    All three must be deterministic and not flaky: use generous waits (waitForLoadState networkidle + explicit timeouts) and tolerances as specified. Reduced-motion is NOT set in these (they test the desktop cinematic path).
  </action>
  <acceptance_criteria>
    - Three files exist: tests/responsive/handoff-pin-anchored.spec.ts, tests/responsive/handoff-bezel-seam.spec.ts, tests/responsive/handoff-dashboard-static.spec.ts.
    - Each references the verified selectors: `grep -l "data-handoff-section" tests/responsive/handoff-pin-anchored.spec.ts`, `grep -l "data-hero-framed-dashboard" tests/responsive/handoff-bezel-seam.spec.ts` and `grep -l "data-handoff-mockup-frame" tests/responsive/handoff-bezel-seam.spec.ts`, `grep -l "data-handoff-mockup-frame" tests/responsive/handoff-dashboard-static.spec.ts` all match.
    - `npx playwright test tests/responsive/handoff-pin-anchored.spec.ts tests/responsive/handoff-bezel-seam.spec.ts tests/responsive/handoff-dashboard-static.spec.ts` ALL PASS against the current build (run against `next build` output or the Vercel preview — sandbox `next dev` hangs).
    - `npx tsc --noEmit` and `npx eslint tests/responsive/handoff-*.spec.ts` exit 0.
    - If any spec proves irreducibly flaky after two tuning attempts, mark it `test.fixme` with a comment and record it in the SUMMARY as a documented human-verify fallback per VALIDATION.md (this weakens the gate; prefer making it deterministic).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/responsive/handoff-pin-anchored.spec.ts tests/responsive/handoff-bezel-seam.spec.ts tests/responsive/handoff-dashboard-static.spec.ts</automated>
  </verify>
  <done>3 cinematic specs exist, key off verified selectors, and pass against current main so they guard the migration in Plans 02-03.</done>
</task>

</tasks>

<threat_model>
No new security surface. Phase 13 is a presentational component re-point behind an existing facade — no auth, session, input-handling, or crypto. The relevant non-functional gate is standing accessibility (WCAG 2.2 AA + axe-core) plus the `ariaSummary` text-alternative contract, which the bare render path preserves by carrying `aria-label={data.ariaSummary}` on the bare root.
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx eslint` clean on all modified files.
- The 3 new cinematic specs pass against current main.
- Existing Console-consumer specs (platform-visuals, solutions-visuals, reveal-fail-open) stay green — the bare default does not alter them.
- handoff-placement.ts: zero hex literals; CLAIMS + COI markers present; `satisfies ConsoleData` compiles.
</verification>

<success_criteria>
The double-frame fix (bare Console), the placement payload, and the 3 cinematic regression specs all land WITHOUT repointing any tab. The D-06/D-07 gate now has teeth for Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/13-visual-system-consolidation/13-01-SUMMARY.md`.
</output>
