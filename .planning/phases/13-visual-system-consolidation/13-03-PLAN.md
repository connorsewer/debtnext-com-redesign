---
phase: 13-visual-system-consolidation
plan: 03
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - src/components/sections/mockups/index.tsx
  - src/content/visuals/handoff-performance.ts
  - src/content/visuals/handoff-issues.ts
  - src/content/visuals/handoff-reporting.ts
  - src/content/visuals/index.ts
autonomous: false
requirements: [SYSVIS-01]
must_haves:
  truths:
    - "All 4 handoff tabs (placement, performance, issues, reporting) render archetype instances through MockupForTab"
    - "Each migrated tab passes the full 8-spec regression set + firewall byte-check before the next tab starts"
    - "performance and reporting parity is human-verified; reporting's archetype choice is documented (Console approximation or a granted DataStory exception)"
  artifacts:
    - path: "src/content/visuals/handoff-performance.ts"
      provides: "performance-tab payload (vendor scorecard mapped to Console rows + pills)"
      contains: "satisfies ConsoleData"
    - path: "src/content/visuals/handoff-issues.ts"
      provides: "issues-tab payload (queue stats + issue rows, label-paired status)"
      contains: "satisfies ConsoleData"
    - path: "src/content/visuals/handoff-reporting.ts"
      provides: "reporting-tab payload (Console summary KPIs, or DataStory if exception granted)"
      contains: "satisfies"
  key_links:
    - from: "MockupForTab cases performance/issues/reporting"
      to: "bare archetype instances + typed payloads"
      via: "<Console bare data={...} /> (or <DataStory data={...} /> for reporting if exception granted)"
      pattern: "handoff(Performance|Issues|Reporting)"
---

<objective>
Fan out the remaining 3 tabs (performance → issues → reporting, in that order) behind the unchanged `MockupForTab` signature, each as its own gated step. Author one typed payload per tab. Per RESEARCH, performance (grade badge + per-row sparkline) and reporting (dual-line 8-week trend) are the least-clean Console mappings — approximate within the Console schema where possible; for reporting, attempt Console-with-KPIs first and, if it loses the trend story, raise a scoped DataStory exception with the user at the reporting checkpoint (D-04 locks "Console-archetype instance"; the planner grants reporting a documented DataStory exception only with user approval).

Purpose: Complete SYSVIS-01's "all 4 tabs render archetype instances" while preserving the regression firewall. Each tab re-runs the full gate so a bad mapping is caught before the next.
Output: 3 edited switch arms + 3 payload modules; each gated by the 8-spec set + firewall byte-check + a per-tab human-verify for the two least-clean tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-visual-system-consolidation/13-CONTEXT.md
@.planning/phases/13-visual-system-consolidation/13-RESEARCH.md
@.planning/phases/13-visual-system-consolidation/13-VALIDATION.md
@.planning/phases/13-visual-system-consolidation/13-02-SUMMARY.md

<interfaces>
<!-- Bespoke data to map (VERIFIED). Bar color = tone tokens only (no hex). -->

performance — VendorPerformanceMockup.tsx:6-13:
- title const `vendorPerformanceMockupTitle = "Vendor scorecard · YTD"` (stays in index.tsx)
- header KPI: "Liquidation, all pools" 14.7%, delta "+1.3% vs prior 30d"
- 4 vendor rows: ABC Recovery grade A liq 18.7 delta +2.1; Global Collect A- 16.3 +0.8; Summit Recovery B 13.5 -0.4; GDW Recovery C 9.6 -1.7
- grade badge + per-row sparkline are NOT native Console features → approximate (RESEARCH Open Question 2: tone-coded rows + pills; no schema extension). Map grade to a pill or to row tone; liq% to trailing; delta to secondary text.

issues — IssuesMockup.tsx:6-12:
- title const `issuesMockupTitle = "Issues queue · all vendors"` (stays in index.tsx)
- 3 stat tiles: Open 127 / Due today 12 / Overdue 3 → header.kpi (one) + pills, or a stat row
- 3 issue cards: SCRA Account 7715-009 (Best Resolution, Overdue 1h, Escalated); Dispute Account 1042-887 (Global Collect, Due 22h, Investigating); Decedent Account 9821-204 (ABC Recovery, Due 3d, Auto-handled)
- SLA tone must be LABEL-PAIRED (Pitfall 8): the SLA text ("Overdue 1h") carries the status, color is secondary.

reporting — ReportingMockup.tsx:6-9:
- title const `reportingMockupTitle = "Liquidation trend · 8 weeks"` (stays in index.tsx)
- 3 summary tiles: Inventory $2.48B / Liquidation $38.7M / Net-back 68.2%
- dual 8-week polyline (inventory + liquidation) → NOT a Console feature. DataStory `chart.kind:"area"` territory. Footer "Scheduled · weekly · Power BI · Snowflake".

DataStory archetype (if exception granted): `src/components/product/visuals/DataStory.tsx`, fed `DataStoryData` (types.ts:88-110): `{ headline, chart: {kind:"area", points:number[]} | ..., annotation?, ariaSummary }`. Read DataStory.tsx to confirm whether it renders its own ProductCanvas; if it does and reporting picks DataStory, add the same backward-compatible `bare?` prop pattern Plan 01 added to Console (default false; bare strips ProductCanvas, keeps role="img" + aria-label on a plain div).

Bare instances: `<Console bare data={...} />` (from Plan 01). The parent FramedDashboard supplies the frame.

ConsoleData has no "danger" bar tone — only indigo/success/warning/neutral. Use "warning" plus a label-paired text ("Grade C", "Overdue") to carry severity (Pitfall 8). NEVER color-only.

Firewall files (MUST stay byte-identical): HomepageHero.tsx, HomepageHandoffSection.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the performance tab (author payload + repoint + gate)</name>
  <read_first>
    - src/components/sections/mockups/VendorPerformanceMockup.tsx (data source)
    - src/content/visuals/handoff-placement.ts (the payload pattern proven in Plans 01-02)
    - src/content/visuals/types.ts (ConsoleData fields; confirm pills/trailing/secondary)
    - src/components/sections/mockups/index.tsx (the switch to repoint)
  </read_first>
  <action>
    1. Create src/content/visuals/handoff-performance.ts exporting `handoffPerformanceConsole satisfies ConsoleData`, mapped from VendorPerformanceMockup. Header comments: [CLAIMS REVIEW] + [COI REVIEW] (vendor names stay real-shaped/anonymized as in the bespoke; pre-cleared per 2026-06). Map:
       - header: `{ title: "Liquidation, all pools", kpi: { caption: "Liquidation, all pools", value: 14.7, valueSuffix: "%", decimals: 1, sub: "+1.3% vs prior 30d" } }`.
       - rows: 4 vendor rows. Put grade into a label-paired field (e.g. `secondary: "Grade A · +2.1 vs prior 30d"`), liq% into `trailing: { value: 18.7, suffix: "%", decimals: 1 }`, and a bar `tone` ("success" for A/A-, "warning" for B and for C/negative; the row label carries the real severity per Pitfall 8). NO sparkline (approximate; no schema extension per RESEARCH OQ2).
       - Optionally a `pills` footer. ariaSummary: governed-copy sentence naming each vendor, grade, and liquidation.
       - ZERO hex literals (tone tokens only).
       Add `export { handoffPerformanceConsole } from "./handoff-performance";` to index.ts.
    2. In src/components/sections/mockups/index.tsx, change ONLY `case "performance":` to `return <Console bare data={handoffPerformanceConsole} />;` and add the import from "@/content/visuals". Leave issues/reporting bespoke. mockupTitleForTab unchanged.
    3. Run the gate (the 8-spec set + full suite) for the performance tab. Fix any failure without touching the firewall.
  </action>
  <acceptance_criteria>
    - src/content/visuals/handoff-performance.ts exists, ends `} satisfies ConsoleData;`, has CLAIMS + COI markers, ZERO color hex (`! grep -qE "#[0-9a-fA-F]{6}"`).
    - `grep -n "Console bare data={handoffPerformanceConsole}" src/components/sections/mockups/index.tsx` matches.
    - issues + reporting arms still call IssuesMockup / ReportingMockup.
    - `git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx src/components/sections/mockups/FramedDashboard.tsx` exits 0.
    - `npx playwright test tests/responsive/platform-mobile.spec.ts tests/responsive/reduced-motion.spec.ts tests/responsive/handoff-pin-anchored.spec.ts tests/responsive/handoff-bezel-seam.spec.ts tests/responsive/handoff-dashboard-static.spec.ts` PASS.
    - `npx tsc --noEmit` + `npx eslint` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/sections/mockups/index.tsx src/content/visuals/handoff-performance.ts && git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx</automated>
  </verify>
  <done>Performance tab renders a bare Console; payload tokens-only; firewall byte-unchanged; gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the issues tab (author payload + repoint + gate)</name>
  <read_first>
    - src/components/sections/mockups/IssuesMockup.tsx (data source; note the label-paired SLA pattern)
    - src/content/visuals/handoff-placement.ts and handoff-performance.ts (payload patterns)
    - src/components/product/visuals/IssuesFlagship.tsx (Phase 11 reference for label-paired issue rows, if useful)
    - src/components/sections/mockups/index.tsx
  </read_first>
  <action>
    1. Create src/content/visuals/handoff-issues.ts exporting `handoffIssuesConsole satisfies ConsoleData`. Map:
       - header.kpi for one tile (e.g. "Open" 127) + pills for the other two (`{ label: "Due today 12" }`, `{ label: "Overdue 3" }`).
       - rows: 3 issue rows. primary "SCRA · Active-duty verification"; secondary "Account 7715-009 · Best Resolution". Carry SLA + status as LABEL-PAIRED text (Pitfall 8): the SLA ("Overdue 1h") and status ("Escalated") must be present as TEXT, not color-only — fold them into `secondary` or render them in `pills`/`trailing` text. Do NOT rely on color alone for severity.
       - ariaSummary: names each issue, its SLA, and its status.
       - [CLAIMS REVIEW] + [COI REVIEW] markers; ZERO hex.
       Add `export { handoffIssuesConsole } from "./handoff-issues";` to index.ts.
    2. In index.tsx, change ONLY `case "issues":` to `return <Console bare data={handoffIssuesConsole} />;` + import. Leave reporting bespoke. mockupTitleForTab unchanged.
    3. Run the gate (the 8-spec set + full suite) for the issues tab.
  </action>
  <acceptance_criteria>
    - src/content/visuals/handoff-issues.ts exists, ends `} satisfies ConsoleData;`, CLAIMS + COI markers, ZERO color hex.
    - SLA + status strings appear as TEXT: `grep -nE "Overdue|Escalated|Investigating|Auto-handled" src/content/visuals/handoff-issues.ts` matches.
    - `grep -n "Console bare data={handoffIssuesConsole}" src/components/sections/mockups/index.tsx` matches.
    - reporting arm still calls ReportingMockup.
    - `git diff --exit-code` on firewall + FramedDashboard exits 0.
    - 8-spec set passes; `npx tsc --noEmit` + `npx eslint` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/sections/mockups/index.tsx src/content/visuals/handoff-issues.ts && git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx</automated>
  </verify>
  <done>Issues tab renders a bare Console with label-paired status; firewall byte-unchanged; gate green.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Decide the reporting tab archetype (Console approximation vs DataStory exception)</name>
  <decision>The reporting tab's load-bearing visual is a dual-line 8-week trend (inventory + liquidation), which is time-series — DataStory `chart.kind:"area"` territory, not a Console worklist. D-04 locks "Console-archetype instance" for all 4 tabs. Choose how to render reporting.</decision>
  <context>RESEARCH Open Question 1: a Console of summary KPIs (Inventory $2.48B / Liquidation $38.7M / Net-back 68.2%) drops the trend story, which is the reporting tab's signature visual. Either approximate within Console (loses the trend) or grant reporting a scoped DataStory exception to D-04. DataStory may render its own ProductCanvas — confirm whether it needs the same bare treatment as Console (Plan 01) before choosing it.</context>
  <options>
    <option id="console-kpis">
      <name>Console of summary KPIs (stay within D-04)</name>
      <pros>No exception needed; reuses the proven bare Console; smallest change.</pros>
      <cons>Loses the 8-week dual-line trend, the reporting tab's signature visual; weakest parity of the four tabs.</cons>
    </option>
    <option id="datastory-exception">
      <name>DataStory area-chart instance (scoped exception to D-04)</name>
      <pros>Preserves the trend story at true parity; DataStory area chart is the right archetype for time-series.</pros>
      <cons>Requires a documented exception to D-04; may require adding a bare path to DataStory (mirror Plan 01) to avoid a double-frame inside FramedDashboard.</cons>
    </option>
  </options>
  <resume-signal>Select: console-kpis or datastory-exception. If datastory-exception, document the D-04 exception in the SUMMARY and HANDOFF.md.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Migrate the reporting tab per the Task 3 decision + run the full per-tab gate</name>
  <read_first>
    - src/components/sections/mockups/ReportingMockup.tsx (data source)
    - src/components/product/visuals/DataStory.tsx (if datastory-exception: confirm it renders ProductCanvas and whether it needs a bare path)
    - src/content/visuals/types.ts (DataStoryData if needed)
    - .planning/phases/13-visual-system-consolidation/13-VALIDATION.md (the gate)
    - tests/responsive/handoff-pin-anchored.spec.ts, handoff-bezel-seam.spec.ts, handoff-dashboard-static.spec.ts, platform-mobile.spec.ts, reduced-motion.spec.ts, hero-gsap-free-mobile.spec.ts
  </read_first>
  <action>
    1. If console-kpis: create src/content/visuals/handoff-reporting.ts `satisfies ConsoleData` — header.kpi "Net-back" 68.2% + rows/pills for Inventory $2.48B and Liquidation $38.7M; a row can state the 8-week direction in text (e.g. primary "Liquidation", secondary "up across 8 weeks"). ariaSummary states the trend in words. Repoint `case "reporting":` to `<Console bare data={handoffReportingConsole} />`.
       If datastory-exception: create src/content/visuals/handoff-reporting.ts `satisfies DataStoryData` with `chart.kind:"area"` (read DataStory.tsx to confirm the supported chart shape; if it supports only a single `points[]` area, render the liquidation trend and carry inventory context in the annotation + ariaSummary). If DataStory renders its own ProductCanvas, add a backward-compatible `bare?` prop to DataStory.tsx exactly as Plan 01 did for Console (default false; bare strips ProductCanvas, keeps role="img" aria-label={data.ariaSummary} on a plain div). Repoint `case "reporting":` to `<DataStory bare data={handoffReportingDataStory} />`. Document the D-04 exception in the SUMMARY and HANDOFF.md.
    2. Either way: header comments [CLAIMS REVIEW] + [COI REVIEW]; ZERO hex literals. Add the export to index.ts. mockupTitleForTab unchanged. Do NOT touch the firewall files.
    3. Run the FULL 8-spec regression gate AND the full Playwright suite (all 4 tabs are now migrated). Fix any failure without touching the firewall.
  </action>
  <acceptance_criteria>
    - src/content/visuals/handoff-reporting.ts exists, ends with `} satisfies ConsoleData;` OR `} satisfies DataStoryData;` per the decision, CLAIMS + COI markers, ZERO color hex.
    - The reporting switch arm renders an archetype: `grep -nE "case \"reporting\"" -A1 src/components/sections/mockups/index.tsx` shows `<Console bare data={handoffReportingConsole} />` or `<DataStory bare data=`, NOT `ReportingMockup`.
    - All 4 MockupForTab arms return an archetype instance: the switch body contains no `<PlacementMockup`, `<VendorPerformanceMockup`, `<IssuesMockup`, `<ReportingMockup` JSX (the top re-export lines may still exist; Plan 04 deletes the files).
    - If datastory-exception: `grep -n "bare" src/components/product/visuals/DataStory.tsx` shows the new prop AND the D-04 exception is noted in HANDOFF.md.
    - `npx playwright test tests/responsive/platform-mobile.spec.ts tests/responsive/reduced-motion.spec.ts tests/responsive/hero-gsap-free-mobile.spec.ts tests/responsive/handoff-pin-anchored.spec.ts tests/responsive/handoff-bezel-seam.spec.ts tests/responsive/handoff-dashboard-static.spec.ts` ALL PASS; `npx playwright test` full suite green.
    - `git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx src/components/sections/mockups/FramedDashboard.tsx` exits 0.
    - `npx tsc --noEmit` + `npx eslint` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/sections/mockups/index.tsx src/content/visuals/handoff-reporting.ts && npx playwright test tests/responsive/platform-mobile.spec.ts tests/responsive/reduced-motion.spec.ts tests/responsive/hero-gsap-free-mobile.spec.ts tests/responsive/handoff-pin-anchored.spec.ts tests/responsive/handoff-bezel-seam.spec.ts tests/responsive/handoff-dashboard-static.spec.ts && git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx</automated>
  </verify>
  <done>All 4 tabs render archetype instances; reporting archetype chosen + documented; full 8-spec gate + suite green; firewall byte-unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human-verify performance + reporting desktop cinematic parity</name>
  <what-built>The performance, issues, and reporting tabs now render archetype instances (bare Console, plus DataStory for reporting if that exception was granted) inside the unchanged FramedDashboard, completing all 4 tabs. The full 8-spec automated gate is green and the firewall files are byte-unchanged.</what-built>
  <how-to-verify>
    1. Open the Vercel preview at desktop width (≥1440px). Scroll through the hero → Platform seam and cycle all 4 tabs.
    2. Performance tab: confirm the vendor scorecard reads at parity — each vendor's grade, liquidation %, and delta are present and legible; the approximation (no sparkline) still tells the scorecard story; severity is conveyed by text labels, not color alone.
    3. Reporting tab: confirm the chosen archetype tells the reporting truth. If console-kpis was chosen, confirm the loss of the trend line is acceptable. If datastory-exception, confirm the trend chart renders inside the bezel with NO double-frame.
    4. All tabs: bezel stays viewport-centered across the seam; dashboard does not move during crossfades; no double-frame anywhere.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to cleanup (Plan 04), or describe parity issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
No new security surface. Presentational re-point behind the existing `MockupForTab` facade; no auth/session/input/crypto. Standing a11y (WCAG 2.2 AA + axe) plus the label-paired-status contract (Pitfall 8) and `ariaSummary` text alternatives are the relevant non-functional gates.
</threat_model>

<verification>
- All 4 MockupForTab arms return archetype instances; mockupTitleForTab unchanged.
- Firewall + FramedDashboard byte-unchanged across every tab migration.
- Each tab gated by the 8-spec set; full suite green after the last tab.
- Reporting archetype decision recorded; performance + reporting parity human-verified.
- All handoff payloads: zero color hex, CLAIMS + COI markers.
</verification>

<success_criteria>
SYSVIS-01's "homepage handoff renders archetype instances for all 4 tabs behind unchanged signatures" is met, with the firewall held and the regression gate green at every step.
</success_criteria>

<output>
After completion, create `.planning/phases/13-visual-system-consolidation/13-03-SUMMARY.md`.
</output>
