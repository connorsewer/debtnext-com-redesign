---
phase: 13-visual-system-consolidation
plan: 02
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - src/components/sections/mockups/index.tsx
autonomous: false
requirements: [SYSVIS-01]
must_haves:
  truths:
    - "The placement tab renders a bare Console instance fed handoffPlacementConsole inside FramedDashboard (no double-frame)"
    - "performance, issues, reporting tabs still render their bespoke mockups (unchanged this plan)"
    - "The full regression-spec set is green with placement migrated; the firewall files are byte-unchanged"
    - "The desktop cinematic reads identically to today on a preview (human-verify)"
  artifacts:
    - path: "src/components/sections/mockups/index.tsx"
      provides: "MockupForTab case 'placement' repointed to bare Console; signatures unchanged"
      contains: "handoffPlacementConsole"
  key_links:
    - from: "MockupForTab case 'placement'"
      to: "Console bare instance + handoffPlacementConsole payload"
      via: "<Console bare data={handoffPlacementConsole} />"
      pattern: "Console.*bare.*handoffPlacementConsole"
    - from: "HomepageHandoffSection / HomepageHero"
      to: "the mockups facade"
      via: "unchanged MockupForTab / mockupTitleForTab / FramedDashboard exports"
      pattern: "git diff --exit-code firewall files"
---

<objective>
The D-06 proof checkpoint. Re-point ONLY the `placement` case in `MockupForTab` to render a bare Console instance fed `handoffPlacementConsole` (from Plan 01), leaving `performance`, `issues`, `reporting` on their bespoke mockups. Keep `mockupTitleForTab` and the export signatures byte-identical. Then gate on the full regression-spec set + firewall byte-check + human-verify before fanning out.

Purpose: Prove Console renders the placement tab at visual parity inside FramedDashboard on the riskiest visual surface in the build, before migrating the other three tabs blind (D-06). This is the single most important gate in the phase.
Output: one edited file (sections/mockups/index.tsx, placement arm only) + a passing regression gate + a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-visual-system-consolidation/13-CONTEXT.md
@.planning/phases/13-visual-system-consolidation/13-RESEARCH.md
@.planning/phases/13-visual-system-consolidation/13-01-SUMMARY.md

<interfaces>
<!-- The facade to repoint (VERIFIED src/components/sections/mockups/index.tsx). The
     firewall calls ONLY MockupForTab / mockupTitleForTab / FramedDashboard, never the
     bespoke components, so the placement arm can change behind the stable surface. -->

MockupForTab + mockupTitleForTab (index.tsx:44-68): a switch on `id: PlatformTab["id"]` with arms placement | performance | issues | reporting.

Console bare instance (from Plan 01): `<Console bare data={handoffPlacementConsole} />` renders the slots in a plain div with role="img" aria-label={data.ariaSummary}, NO ProductCanvas. The parent FramedDashboard supplies the single frame.

Imports to add to index.tsx:
- `import { Console } from "@/components/product/visuals/Console";`
- `import { handoffPlacementConsole } from "@/content/visuals";`

FramedDashboard chrome title: `mockupTitleForTab("placement")` must still return `"Placement run · 12:04 PM"` (the existing `placementMockupTitle` const). Keep returning it — the title is window-chrome, rendered by FramedDashboard, NOT by Console. Both coexist (RESEARCH §Reconciling mockupTitleForTab).

Firewall files (MUST stay byte-identical):
- src/components/sections/HomepageHero.tsx
- src/components/sections/HomepageHandoffSection.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint MockupForTab case 'placement' to a bare Console instance</name>
  <read_first>
    - src/components/sections/mockups/index.tsx (the file being modified; the switch at 44-68 and the imports at 26-42)
    - src/content/visuals/handoff-placement.ts (the payload from Plan 01)
    - src/components/product/visuals/Console.tsx (confirm the bare prop from Plan 01)
    - src/components/sections/mockups/PlacementMockup.tsx (the bespoke render being replaced for placement — for parity reference)
  </read_first>
  <action>
    In src/components/sections/mockups/index.tsx:
    1. Add the two imports: `import { Console } from "@/components/product/visuals/Console";` and `import { handoffPlacementConsole } from "@/content/visuals";`.
    2. Change ONLY the `case "placement":` arm of `MockupForTab` to `return <Console bare data={handoffPlacementConsole} />;`. Leave `performance`, `issues`, `reporting` arms calling the bespoke `VendorPerformanceMockup` / `IssuesMockup` / `ReportingMockup` unchanged.
    3. Do NOT change `mockupTitleForTab` — its `case "placement": return placementMockupTitle;` stays (chrome title is unchanged). Keep the `placementMockupTitle` import.
    4. Do NOT delete the `PlacementMockup` import/re-export yet — it is still re-exported from the facade and deleted only in Plan 04 (Wave 3) after all 4 tabs migrate. (If the unused-import lint trips because `PlacementMockup` is now imported but unused in the switch, keep the named re-export `export { PlacementMockup, placementMockupTitle } from "./PlacementMockup";` which keeps `placementMockupTitle` live, and remove only the now-unused `PlacementMockup` from the SECOND import block at the bottom if eslint flags it — but keep the top re-export line intact so Plan 04 owns the file deletion. Document whichever you do in the SUMMARY.)
    5. Do NOT touch FramedDashboard.tsx (D-02) or the firewall files (D-01).
  </action>
  <acceptance_criteria>
    - `grep -n "Console bare data={handoffPlacementConsole}" src/components/sections/mockups/index.tsx` matches in the placement arm.
    - `grep -n "VendorPerformanceMockup\|IssuesMockup\|ReportingMockup" src/components/sections/mockups/index.tsx` still shows the other 3 arms unchanged.
    - `grep -n "placementMockupTitle" src/components/sections/mockups/index.tsx` shows mockupTitleForTab still returns it for placement.
    - `git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx` exits 0 (firewall byte-unchanged).
    - `git diff --exit-code -- src/components/sections/mockups/FramedDashboard.tsx` exits 0 (bezel unchanged, D-02).
    - `npx tsc --noEmit` exits 0; `npx eslint src/components/sections/mockups/index.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/sections/mockups/index.tsx && git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx src/components/sections/mockups/FramedDashboard.tsx</automated>
  </verify>
  <done>Placement tab renders a bare Console; the other 3 tabs untouched; firewall + bezel byte-unchanged; tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full regression-spec gate for the placement migration (D-06/D-07)</name>
  <read_first>
    - .planning/phases/13-visual-system-consolidation/13-VALIDATION.md (the per-requirement test map + sampling rate)
    - tests/responsive/platform-mobile.spec.ts, tests/responsive/reduced-motion.spec.ts, tests/responsive/hero-gsap-free-mobile.spec.ts (the existing 5 of the gate)
    - tests/responsive/handoff-pin-anchored.spec.ts, handoff-bezel-seam.spec.ts, handoff-dashboard-static.spec.ts (the 3 from Plan 01)
  </read_first>
  <action>
    Run the FULL D-07 regression-spec set against a `next build` output (or the Vercel preview — sandbox `next dev`/`next start` hang). The set is the 5 named specs + the 3 new cinematic specs:
    - tests/responsive/platform-mobile.spec.ts (mobile 4-stack + desktop pin at 1440)
    - tests/responsive/reduced-motion.spec.ts (handoff content fails open, static stack visible)
    - tests/responsive/hero-gsap-free-mobile.spec.ts (mobile never fetches GSAP)
    - tests/responsive/handoff-pin-anchored.spec.ts
    - tests/responsive/handoff-bezel-seam.spec.ts
    - tests/responsive/handoff-dashboard-static.spec.ts
    Plus, since the placement Console now enters the handoff, run the full suite once to confirm no other spec regressed: `npx playwright test`.
    If reduced-motion or platform-mobile FAILS: the bare Console likely renders at the wrong size or fails-open incorrectly inside the mobile stacked FramedDashboard. Inspect the mobile branch (HandoffSection.tsx:140-150 renders MockupForTab inside FramedDashboard per tab) and fix the payload/bare render until green. Do NOT touch the firewall files to fix it.
    Run `tsc --noEmit` + `eslint` as the per-commit sample.
  </action>
  <acceptance_criteria>
    - `npx playwright test tests/responsive/platform-mobile.spec.ts tests/responsive/reduced-motion.spec.ts tests/responsive/hero-gsap-free-mobile.spec.ts tests/responsive/handoff-pin-anchored.spec.ts tests/responsive/handoff-bezel-seam.spec.ts tests/responsive/handoff-dashboard-static.spec.ts` — ALL PASS.
    - `npx playwright test` — full suite green (no regression from the placement Console entering the handoff path).
    - `git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx` exits 0.
    - `npx tsc --noEmit` and `npx eslint` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/responsive/platform-mobile.spec.ts tests/responsive/reduced-motion.spec.ts tests/responsive/hero-gsap-free-mobile.spec.ts tests/responsive/handoff-pin-anchored.spec.ts tests/responsive/handoff-bezel-seam.spec.ts tests/responsive/handoff-dashboard-static.spec.ts</automated>
  </verify>
  <done>The full 8-spec regression set is green with placement migrated; full suite green; firewall byte-unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the placement-tab desktop cinematic parity (D-06)</name>
  <what-built>The placement tab now renders a bare Console instance (fed handoffPlacementConsole) inside the unchanged FramedDashboard bezel, replacing the bespoke PlacementMockup. The other 3 tabs are still bespoke. The full automated regression set (8 specs) is green and the firewall files are byte-unchanged.</what-built>
  <how-to-verify>
    1. Open the Vercel preview URL for this branch at desktop width (≥1440px).
    2. Scroll slowly through the hero → Platform handoff seam. Confirm: the framed dashboard stays viewport-centered across the seam (no horizontal jump), the bezel does not move, and the dashboard does not drift during the crossfade.
    3. Watch the placement tab specifically: the routing pools (Pre-collect 35% / Primary 28% / Secondary 18% / Tertiary 12%), the "Inbound batch / 120,418 / accounts · $284.6M" KPI, and the "Engine running" pulse should read at parity with today (a bar per pool, the percent on the right, a live pulse). There must be NO double-frame (no inner rounded card with its own padding inside the window chrome).
    4. Confirm the bar colors use the design system (no jarring off-token green/amber/cyan gradient endpoints).
    5. Compare against the production site (or a screenshot of today's placement tab) for parity.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to the fan-out (Plan 03), or describe the parity issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
No new security surface. Presentational re-point behind the existing `MockupForTab` facade; no auth/session/input/crypto. Standing a11y (WCAG 2.2 AA + axe) plus the `ariaSummary` text-alternative on the bare Console root are the relevant non-functional gates.
</threat_model>

<verification>
- Placement arm repointed; other 3 arms unchanged; mockupTitleForTab unchanged.
- Firewall files + FramedDashboard byte-unchanged (`git diff --exit-code`).
- The 8-spec D-07 regression set green; full Playwright suite green.
- Human-verify: desktop cinematic parity approved on the preview.
</verification>

<success_criteria>
The placement tab renders identically through a bare Console; the gate (8 specs + firewall byte-check + human-verify) is green; the migration mechanism is proven before fan-out.
</success_criteria>

<output>
After completion, create `.planning/phases/13-visual-system-consolidation/13-02-SUMMARY.md`.
</output>
