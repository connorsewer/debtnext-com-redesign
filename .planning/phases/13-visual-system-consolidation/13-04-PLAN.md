---
phase: 13-visual-system-consolidation
plan: 04
type: execute
wave: 3
depends_on: ["13-03"]
files_modified:
  - src/components/sections/mockups/index.tsx
  - src/components/sections/mockups/PlacementMockup.tsx
  - src/components/sections/mockups/VendorPerformanceMockup.tsx
  - src/components/sections/mockups/IssuesMockup.tsx
  - src/components/sections/mockups/ReportingMockup.tsx
  - HANDOFF.md
requirements: [SYSVIS-01, SYSVIS-02]
autonomous: true
must_haves:
  truths:
    - "The 4 bespoke per-tab mockup files are deleted; sections/mockups/ slims to index.tsx + FramedDashboard.tsx"
    - "No off-token hex (#22c55e/#d97706/#0891b2) survives anywhere in src/"
    - "The only .png reference in src/ is the hero finale dashboard-dark.png (Phase 15's; intentionally retained)"
    - "The firewall files are byte-unchanged for the whole phase; the full suite + LHCI / gate are green"
  artifacts:
    - path: "src/components/sections/mockups/index.tsx"
      provides: "facade slimmed: re-exports FramedDashboard + the 4 archetype-backed *Title consts; no bespoke mockup imports"
      contains: "FramedDashboard"
    - path: "HANDOFF.md"
      provides: "phase-13 consolidation record + the retained hero PNG note (Phase 15) + any reporting DataStory exception"
  key_links:
    - from: "sections/mockups/ directory"
      to: "index.tsx + FramedDashboard.tsx only"
      via: "deletion of the 4 bespoke mockup files"
      pattern: "ls src/components/sections/mockups/"
---

<objective>
Retire the bespoke handoff mockups now that all 4 tabs render archetype instances (Plan 03), and close out the folded P13-02 token sweep + dead-PNG confirmation. Delete the 4 bespoke per-tab mockup files, slim the facade to its stable exports, confirm zero off-token hex survives, confirm the only remaining `.png` reference is the intentionally-retained hero finale (Phase 15's deliverable, not Phase 13's), and run the phase verification gate.

Purpose: SYSVIS-01's "retire sections/mockups/ after parity" (D-05) + SYSVIS-02's dead-asset confirmation + P13-02 token elimination. This is deletion-by-substitution, only after the replacement landed.
Output: 4 deleted files, a slimmed facade, an updated HANDOFF.md, green gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-visual-system-consolidation/13-CONTEXT.md
@.planning/phases/13-visual-system-consolidation/13-RESEARCH.md
@.planning/phases/13-visual-system-consolidation/13-VALIDATION.md
@.planning/phases/13-visual-system-consolidation/13-03-SUMMARY.md

<interfaces>
<!-- After Plan 03, MockupForTab renders only archetype instances. The 4 bespoke
     files are still imported/re-exported by index.tsx (Plan 03 left them for this
     plan). The *Title consts (placementMockupTitle etc.) currently live IN the
     bespoke files and are re-exported + consumed by mockupTitleForTab. Deleting the
     files means relocating those title strings. -->

Title strings to preserve (mockupTitleForTab must keep returning them — D-03 byte-identical signatures, RESEARCH §Reconciling):
- placement: "Placement run · 12:04 PM"
- performance: "Vendor scorecard · YTD"
- issues: "Issues queue · all vendors"
- reporting: "Liquidation trend · 8 weeks"

Firewall (MUST stay byte-identical the whole phase): HomepageHero.tsx, HomepageHandoffSection.tsx.
FramedDashboard.tsx: stays put (D-02), unchanged.

Dead-asset facts (VERIFIED this session):
- The ONLY .png ref in src/ is HomepageHero.tsx:187 `src="/product/dashboard-dark.png"` (hero finale, aria-hidden, decorative). It is Phase 15's (HOMEVIS-01), NOT Phase 13's — editing the hero also violates D-01. Phase 13 action = confirm + document, NOT delete.
- The 6 dead BenefitSplit dashboard-dark.png fallbacks were already removed in Phase 10 (10-06-PLAN, FND-05). Confirm none remain.
- The off-token hexes #22c55e/#d97706/#0891b2 currently live ONLY in PlacementMockup.tsx:10-12 — deleting that file eliminates them (no replacement carries them; the payloads use tone tokens).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the 4 title consts into the facade and delete the bespoke mockup files</name>
  <read_first>
    - src/components/sections/mockups/index.tsx (the facade: re-exports + the second import block + mockupTitleForTab at 57-68)
    - src/components/sections/mockups/PlacementMockup.tsx, VendorPerformanceMockup.tsx, IssuesMockup.tsx, ReportingMockup.tsx (the files being deleted; the title consts at line 6 of each)
    - src/components/sections/HomepageHandoffSection.tsx (confirm it imports only MockupForTab / mockupTitleForTab / FramedDashboard from the facade — do not edit it)
  </read_first>
  <action>
    1. In src/components/sections/mockups/index.tsx, define the 4 title strings inline at the top (they no longer come from the deleted files):
       `const placementMockupTitle = "Placement run · 12:04 PM";`
       `const vendorPerformanceMockupTitle = "Vendor scorecard · YTD";`
       `const issuesMockupTitle = "Issues queue · all vendors";`
       `const reportingMockupTitle = "Liquidation trend · 8 weeks";`
       (mockupTitleForTab already returns these names — keep its body unchanged.)
    2. Remove ALL imports and re-exports of the bespoke mockups from index.tsx: the top `export { ... } from "./PlacementMockup"` etc. lines AND the bottom `import { ... } from "./PlacementMockup"` etc. lines. Keep `export { FramedDashboard } from "./FramedDashboard";`. Keep the Console / archetype imports + the `import { handoff* } from "@/content/visuals"` payload imports added in Plans 02-03. Keep the `import type { PlatformTab } from "@/content/homepage-hero";`.
    3. Delete the 4 files: `git rm src/components/sections/mockups/PlacementMockup.tsx src/components/sections/mockups/VendorPerformanceMockup.tsx src/components/sections/mockups/IssuesMockup.tsx src/components/sections/mockups/ReportingMockup.tsx`.
    4. Run a clean rebuild to clear stale chunks (RESEARCH §Runtime State): `rm -rf .next` before the verification build.
    5. Do NOT touch FramedDashboard.tsx, HomepageHero.tsx, or HomepageHandoffSection.tsx.
  </action>
  <acceptance_criteria>
    - The 4 files are gone: `ls src/components/sections/mockups/` returns exactly `FramedDashboard.tsx` and `index.tsx` (two entries).
    - `grep -n "PlacementMockup\|VendorPerformanceMockup\|IssuesMockup\|ReportingMockup" src/components/sections/mockups/index.tsx` returns ZERO (no lingering imports/re-exports).
    - `grep -n "mockupTitleForTab" src/components/sections/mockups/index.tsx` still returns the 4 title strings; `grep -c "= \"Placement run · 12:04 PM\"\|= \"Vendor scorecard · YTD\"\|= \"Issues queue · all vendors\"\|= \"Liquidation trend · 8 weeks\"" src/components/sections/mockups/index.tsx` returns 4.
    - `grep -rn "from \"@/components/sections/mockups/PlacementMockup\"\|from \"./PlacementMockup\"" src/` returns ZERO (nothing else imported them — VERIFIED only index.tsx did).
    - `git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx src/components/sections/mockups/FramedDashboard.tsx` exits 0.
    - `npx tsc --noEmit` + `npx eslint src/components/sections/mockups/index.tsx` clean (no unresolved imports).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/sections/mockups/index.tsx && [ "$(ls src/components/sections/mockups/ | sort | tr '\n' ' ')" = "FramedDashboard.tsx index.tsx " ] && git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx</automated>
  </verify>
  <done>The 4 bespoke mockups are deleted; the facade slims to index.tsx + FramedDashboard.tsx with title consts inlined; firewall byte-unchanged; tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: P13-02 token sweep + SYSVIS-02 dead-PNG confirmation + HANDOFF record</name>
  <read_first>
    - HANDOFF.md (read `grep -n "^## " HANDOFF.md` first to find the right section to edit, per the verify-doc-structure rule)
    - .planning/AUDIT-2026-06-12.md §Phase 13 (P13-01/P13-02 wording)
  </read_first>
  <action>
    1. Off-token hex sweep (P13-02): run `grep -rn "#22c55e\|#d97706\|#0891b2" src/`. It MUST return zero now (the only home, PlacementMockup.tsx, was deleted in Task 1; the payloads use tone tokens). If any survive, find and remap to DESIGN.md chart tokens / Console `tone` (RESEARCH replacement map: #22c55e→tone "success"/--chart-3, #d97706→tone "warning"/--chart-4, #0891b2→--chart-5 token or tone "indigo").
    2. Dead-PNG confirmation (SYSVIS-02): run `grep -rn "\.png" src/`. It MUST return exactly one line — HomepageHero.tsx:187 `dashboard-dark.png` (the hero finale). Confirm no other `.png` refs and confirm there are zero remaining BenefitSplit dashboard-dark.png fallbacks (they were removed in Phase 10). Do NOT delete the hero PNG (Phase 15's, and editing the hero violates D-01).
    3. Update HANDOFF.md (in the SAME commit per the docs-in-sync rule): record that Phase 13 consolidated the homepage handoff onto archetype instances behind the unchanged MockupForTab facade; the 4 bespoke mockups were deleted; sections/mockups/ now holds only index.tsx + FramedDashboard.tsx; off-token gradient hex eliminated (P13-02); the hero finale dashboard-dark.png is intentionally retained pending Phase 15 (HOMEVIS-01); and (if granted in Plan 03) the reporting-tab DataStory exception to D-04. If Front B (Plan 05) is deferred, note the P13-01 deferral here too (Plan 05 owns that note if it runs).
  </action>
  <acceptance_criteria>
    - `grep -rn "#22c55e\|#d97706\|#0891b2" src/` returns ZERO lines.
    - `grep -rcn "\.png" src/ | grep -v ":0"` resolves to exactly one match, in src/components/sections/HomepageHero.tsx (the hero finale). Equivalent: `grep -rn "\.png" src/ | grep -c "HomepageHero.tsx"` returns 1 and the total `.png` line count in src/ is 1.
    - HANDOFF.md contains a Phase 13 consolidation entry mentioning "sections/mockups", "dashboard-dark.png", and "Phase 15".
    - `git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq "#22c55e\|#d97706\|#0891b2" src/ && [ "$(grep -rn "\.png" src/ | wc -l | tr -d ' ')" = "1" ] && grep -q "sections/mockups" HANDOFF.md</automated>
  </verify>
  <done>Zero off-token hex; the only src .png ref is the retained hero finale; HANDOFF records the consolidation, the retained hero PNG, and any reporting exception.</done>
</task>

<task type="auto">
  <name>Task 3: Phase verification gate (full suite + firewall byte-check + LHCI / gate)</name>
  <read_first>
    - .planning/phases/13-visual-system-consolidation/13-VALIDATION.md (the phase gate definition)
    - .github/workflows/perf.yml (the LHCI Case C / gate)
  </read_first>
  <action>
    Run the phase-gate sample from VALIDATION.md against a clean `next build` (after `rm -rf .next`) / the Vercel preview / CI — sandbox `next dev`/`next start` hang:
    1. Full Playwright suite: `npx playwright test` — all specs green (the 8-spec handoff set + axe + touch-targets + everything else).
    2. Firewall byte-check: `git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx` exits 0 across the whole phase.
    3. `tsc --noEmit` + `eslint` clean repo-wide for the touched files.
    4. LHCI Case C on / holds (CLS < 0.1, INP < 200ms, LCP under the established gate) — via CI / preview. If the Console instances entering the handoff path regress the / JS budget or CLS, address per RESEARCH Pitfall 3 (eager vs lazy box-matched skeleton) without touching the firewall.
  </action>
  <acceptance_criteria>
    - `npx playwright test` full suite green.
    - `git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx src/components/sections/mockups/FramedDashboard.tsx` exits 0.
    - `npx tsc --noEmit` + `npx eslint` clean.
    - LHCI / gate green on CI / preview (CLS < 0.1, INP < 200ms, LCP under the gate); record the run in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npx playwright test && git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx</automated>
  </verify>
  <done>Full suite green; firewall byte-unchanged for the whole phase; LHCI / gate green; SYSVIS-01 + SYSVIS-02 satisfied.</done>
</task>

</tasks>

<threat_model>
No new security surface. This plan deletes presentational components and confirms dead assets; no auth/session/input/crypto. Standing a11y (WCAG 2.2 AA + axe) remains the relevant non-functional gate; the full Playwright suite (including axe-routes) verifies it.
</threat_model>

<verification>
- 4 bespoke mockup files deleted; sections/mockups/ = index.tsx + FramedDashboard.tsx only.
- mockupTitleForTab returns the 4 title strings from inlined consts; signatures byte-identical.
- Zero off-token hex; only the hero finale .png remains in src/.
- Firewall byte-unchanged; full suite + LHCI / gate green.
- HANDOFF.md updated in the same commit as the deletions.
</verification>

<success_criteria>
SYSVIS-01 (retire sections/mockups/ after parity) and SYSVIS-02 (dead-asset confirmation) are met. The directory is slimmed, the off-token hex is gone, the hero PNG is documented as Phase 15's, and the regression firewall held the entire phase.
</success_criteria>

<output>
After completion, create `.planning/phases/13-visual-system-consolidation/13-04-SUMMARY.md`.
</output>
