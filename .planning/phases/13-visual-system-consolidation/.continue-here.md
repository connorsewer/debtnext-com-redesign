# Phase 13 — CONTINUE HERE (resume after /clear)

**Date:** 2026-06-13
**Branch:** `phase-13-visual-system-consolidation` · **PR:** [#12](https://github.com/connorsewer/debtnext-com-redesign/pull/12) (open, DO NOT MERGE — pending fixes + human-verify)
**Last commit:** `c354978`

## State: implementation COMPLETE, CI RED on a real a11y regression

All 5 plans (13-01..13-05) are implemented and pushed. Front A (4 handoff tabs → bare Console, mockups retired) + Front B (FeatureAccordion VISUALS → Phase 11 Flagships) both landed. Firewall byte-clean (HomepageHero.tsx + HomepageHandoffSection.tsx identical to base `ab0ad77`). `tsc`/`eslint`/`next build` green locally.

CI (`a11y + responsive`, run 27465864367 on c354978): **lighthouse/perf PASSED earlier on the full migration; Vercel preview PASSES.** The `test` job is RED.

## What's RED (fix these next — order by priority)

### 1. [REAL REGRESSION] axe `/` @ mobile-375 — `tests/a11y/axe-routes.spec.ts:12`
- **578 color-contrast violations**: `fgColor #87878e` on `bgColor #20212d` = **4.47** vs required **4.5:1**, at fontSize 7.9pt (10.5px). This is Console muted-text on the product-canvas, rendered ×4 stacked in the mobile handoff. `/` passed axe before Phase 13 → the Console-in-handoff migration introduced it.
- **nested-interactive**: interactive control nested. SUSPECT: the Wave 0 bare Console path put `role="img"` on the bare root (Console.tsx) — if the bare content contains interactive children, or the handoff tab buttons wrap interactive Console content, axe flags it. INVESTIGATE the bare render tree + the mobile stacked layout in HomepageHandoffSection.
- **Fix direction:** bump the muted-text token used by Console rows so it clears 4.5:1 on `#20212d` (DESIGN.md chart/text tokens — find the token, not a raw hex; coordinate with DESIGN.md so it stays system-wide consistent). Resolve nested-interactive by checking what interactive element sits inside another interactive element (or inside role="img") in the bare Console handoff render. This is the load-bearing fix.
- NOTE: confirm whether platform/solutions pages (which also use Console) are axe-tested at mobile-375 — if they are and pass, the contrast delta may be homepage-context-specific (different bg). If they're NOT tested at 375, the same latent issue may exist there too.

### 2. [TEST] `tests/responsive/handoff-dashboard-static.spec.ts:29` still failing
- Was rewritten (commit c354978) to a structural assertion (frame has centering transform classes + computed `transform` is a resolved matrix, not `none`; childElementCount>0). STILL red — get the exact assertion line + received value from `gh run view 27465864367 --log-failed`. Likely the computed `transform` resolves to `none` in the test context, OR the class-presence check is off. The pin-anchored sibling (same commit) and bezel-seam appear to pass — diff the approach.

### Already RESOLVED (do not re-touch)
- `solutions-visuals.spec.ts` reveal flakiness (utilities/fintech/telecom "stuck opacity") — fixed in `685593f` (scroll bottom→top + finished-gate + settle). Green since round 3.
- `handoff-bezel-seam.spec.ts` — passing throughout.
- Firewall: byte-clean, do NOT edit HomepageHero/HomepageHandoffSection/FramedDashboard.

## Sandbox constraint
`next dev`/`next start` HANG; `playwright test` auto-starts a server (hangs). Verify with `tsc --noEmit`, `eslint`, `next build`, `playwright test --list`. Spec/axe truth comes from CI: push the branch, `gh run watch <id>`. CI workflows trigger on PR (already open as #12) and push-to-the-PR-branch.

## DEFERRED to Connor's single end-of-phase Vercel preview review (per his directive this run)
- 4-tab handoff cinematic parity (pin/bezel/crossfade) at desktop ≥1440.
- Reporting tab: implemented as **Console-with-KPIs** (D-04-compliant); judge if the dual-line trend story reads weak → DataStory exception is the fallback (needs Connor's approval; NOT taken).
- FeatureAccordion (5 items) renders correct Flagship visuals after the Front B repoint; reduced-motion opacity:1.

## After CI green
- Run `/gsd-code-review 13` then `/gsd-code-review-fix` if findings (the /finish-build cycle step not yet run — deferred because the phase was paused mid-CI).
- Run `/gsd-verify-work 13` (gsd-verifier) for goal-backward phase verification.
- Consolidate the deferred human-verify into a 13-HUMAN-UAT.md for Connor.
- STATE.md frontmatter milestone label keeps drifting to v1.0/milestone on gsd-tools writes — restore to `M6` / `Premium visual + motion system`.
- Phase boundary: STOP for Connor's preview review before any merge (his approval required per /finish-build).

## Housekeeping
- Untracked `plans/` dir (7 files, design-polish backlog from a DIFFERENT process, dated 2026-06-13 06:xx) — UNRELATED to Phase 13, left untouched, keep OUT of this PR.
- Debug session record: `.planning/debug/ci-5-failures-pr12.md` (committed).
