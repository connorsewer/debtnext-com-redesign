---
phase: 13-visual-system-consolidation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/product/visuals/index.tsx
  - src/components/product/visuals/PlacementMatrix.tsx
  - src/components/product/visuals/OptimizationEngine.tsx
  - src/components/product/visuals/IssuesWorklist.tsx
  - src/components/product/visuals/ReportingDashboard.tsx
  - HANDOFF.md
requirements: [SYSVIS-01]
autonomous: false
must_haves:
  truths:
    - "The VISUALS registry entries for placement/optimization/issues/reporting render Phase 11 Flagship (or Console) instances, NOT the bespoke PlacementMatrix/OptimizationEngine/IssuesWorklist/ReportingDashboard"
    - "The VISUALS.reporting key migration is verified explicitly (P13-01)"
    - "ComplianceStandards (the 5th id) is KEPT — it is not in the P13-01 deletion four"
    - "The homepage FeatureAccordion still shows a visual for every id; reveal-fail-open and reduced-motion specs stay green"
    - "The 4 bespoke FeatureAccordion components are deleted ONLY AFTER the repoint renders correctly"
  artifacts:
    - path: "src/components/product/visuals/index.tsx"
      provides: "VISUALS registry repointed to Flagship/Console equivalents for the 4 ids; compliance unchanged"
      contains: "compliance"
  key_links:
    - from: "VISUALS.reporting"
      to: "ReportingFlagship (or a Console-backed instance), NOT ReportingDashboard"
      via: "dynamic import in the registry"
      pattern: "VISUALS.*reporting"
---

<objective>
Front B / P13-01: repoint the homepage FeatureAccordion's `VISUALS` registry so `placement`, `optimization`, `issues`, `reporting` render their Phase 11 Flagship (or Console) equivalents instead of the bespoke `PlacementMatrix` / `OptimizationEngine` / `IssuesWorklist` / `ReportingDashboard`. The backing payloads + Flagship components already exist (authored in Phase 11), so this is authoring-light. KEEP `ComplianceStandards` (the 5th id, not in the deletion four). Verify the `VISUALS.reporting` key migration explicitly (the named P13-01 requirement). Delete the 4 bespoke `Lazy*` wrappers + components AS A SET, only after the repoint renders correctly.

This plan is independent of Front A (it touches only `product/visuals/`, zero file overlap with the handoff plans), so it can run in parallel with Plan 02.

Purpose: P13-01 dead-code-after-repoint. These bespoke visuals are LIVE on the homepage FeatureAccordion (D-08) — they are deletable only after a working replacement is wired. RESEARCH §Front B verdict: PROCEED, low risk.
Output: repointed registry, 4 deleted bespoke files, HANDOFF record, green homepage accordion specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-visual-system-consolidation/13-CONTEXT.md
@.planning/phases/13-visual-system-consolidation/13-RESEARCH.md
@.planning/AUDIT-2026-06-12.md

<interfaces>
<!-- VERIFIED this session. -->

VISUALS registry (src/components/product/visuals/index.tsx:34-55): a `Record<AccordionVisualId, React.ComponentType>` where each value is `dynamic(() => import("./X").then(m => m.X), { ssr: false, loading: Fallback })`. The 5 ids: placement, optimization, issues, reporting, compliance.
- next/dynamic options MUST be an inline object literal (Turbopack static-analysis requirement) — repeat `{ ssr: false, loading: Fallback }` per entry.
- AccordionVisual({ id }) just renders `VISUALS[id]`.

Current (bespoke) targets to replace:
- placement → ./PlacementMatrix (m.PlacementMatrix)
- optimization → ./OptimizationEngine (m.OptimizationEngine)
- issues → ./IssuesWorklist (m.IssuesWorklist)
- reporting → ./ReportingDashboard (m.ReportingDashboard)  ← P13-01 names this key explicitly
- compliance → ./ComplianceStandards (m.ComplianceStandards)  ← KEEP, do not touch

Phase 11 Flagship backers that exist (VERIFIED in src/components/product/visuals/):
- PlacementFlagship.tsx (exports PlacementFlagship; Console-composed, fed placementFlagshipConsole/placementFlagshipTiers)
- OptimizationFlagship.tsx (OptimizationFlagship)
- IssuesFlagship.tsx (IssuesFlagship)
- ReportingFlagship.tsx (ReportingFlagship)
Their payloads are exported from src/content/visuals/index.ts (placementFlagshipConsole, optimizationFlagshipConsole, issuesFlagshipConsole, reportingFlagshipConsole, etc.).

No double-frame risk on Front B (RESEARCH §Front B point 2): the FeatureAccordion visual container (FeatureAccordion.tsx:140) is its own `bg-[var(--product-canvas)]` box, and the bespoke visuals already render their own ProductCanvas inside it. The Flagships render the same canvas-in-a-box look, so repointing keeps the visual contract. (Do NOT use the bare Console here — Front B WANTS the canvas, unlike Front A.)

Specs covering the homepage accordion (NOT inside [data-handoff-section], so unlike Front A they ARE covered):
- tests/responsive/reveal-fail-open.spec.ts (no stuck opacity:0 on / after accordion open)
- tests/responsive/reduced-motion.spec.ts (/ covered)
- tests/responsive/platform-visuals.spec.ts (if it asserts the / accordion visuals)

The 4 bespoke files to delete after repoint: PlacementMatrix.tsx, OptimizationEngine.tsx, IssuesWorklist.tsx, ReportingDashboard.tsx. (DecisionEnginePreview.tsx was already refactored into PlacementFlagship in Phase 11 — check whether it is still imported anywhere before assuming it is safe; it is NOT in the P13-01 four, leave it unless verified dead.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the VISUALS registry to Flagship equivalents (compliance untouched)</name>
  <read_first>
    - src/components/product/visuals/index.tsx (the registry being repointed)
    - src/components/product/visuals/PlacementFlagship.tsx (confirm export name + that it needs no props)
    - src/components/product/visuals/OptimizationFlagship.tsx, IssuesFlagship.tsx, ReportingFlagship.tsx (confirm export names + prop-less)
    - src/components/sections/FeatureAccordion.tsx lines 140-176 (the consumer + container that already supplies a product-canvas box)
  </read_first>
  <action>
    In src/components/product/visuals/index.tsx, change the 4 registry entries to import the Flagship components instead of the bespoke ones, keeping the inline `{ ssr: false, loading: Fallback }` literal per entry:
    - `placement: dynamic(() => import("./PlacementFlagship").then((m) => m.PlacementFlagship), { ssr: false, loading: Fallback })`
    - `optimization: dynamic(() => import("./OptimizationFlagship").then((m) => m.OptimizationFlagship), { ssr: false, loading: Fallback })`
    - `issues: dynamic(() => import("./IssuesFlagship").then((m) => m.IssuesFlagship), { ssr: false, loading: Fallback })`
    - `reporting: dynamic(() => import("./ReportingFlagship").then((m) => m.ReportingFlagship), { ssr: false, loading: Fallback })`
    - LEAVE `compliance` pointed at `./ComplianceStandards` exactly as today.
    If any Flagship requires props (it should not — Phase 11 Flagships are prop-less, fed by imported payloads), confirm by reading the component; if a Flagship is NOT prop-less, wrap it in a tiny local prop-less component in index.tsx that supplies the payload, OR fall back to a Console-backed instance fed the Flagship's payload. Document which path was used.
    Do NOT delete any bespoke file in this task — only repoint. Do NOT touch the firewall files.
  </action>
  <acceptance_criteria>
    - `grep -n "PlacementFlagship\|OptimizationFlagship\|IssuesFlagship\|ReportingFlagship" src/components/product/visuals/index.tsx` shows all 4 Flagships imported in the registry.
    - `grep -n "PlacementMatrix\|OptimizationEngine\|IssuesWorklist\|ReportingDashboard" src/components/product/visuals/index.tsx` returns ZERO (no bespoke imports remain in the registry).
    - `grep -n "reporting:" src/components/product/visuals/index.tsx` shows the reporting key now imports `./ReportingFlagship` (explicit P13-01 verification).
    - `grep -n "compliance:" src/components/product/visuals/index.tsx` still imports `./ComplianceStandards` (KEPT).
    - `npx tsc --noEmit` + `npx eslint src/components/product/visuals/index.tsx` clean.
    - `npx playwright test tests/responsive/reveal-fail-open.spec.ts tests/responsive/reduced-motion.spec.ts` PASS (homepage accordion still shows a visual per id; no stuck opacity:0).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/product/visuals/index.tsx && npx playwright test tests/responsive/reveal-fail-open.spec.ts tests/responsive/reduced-motion.spec.ts</automated>
  </verify>
  <done>The 4 registry ids render Flagship instances; reporting key migrated + verified; compliance kept; homepage accordion specs green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the repointed FeatureAccordion before deleting the bespoke set</name>
  <what-built>The homepage FeatureAccordion's VISUALS registry now renders the Phase 11 Flagship instances for placement/optimization/issues/reporting (compliance unchanged). The bespoke PlacementMatrix/OptimizationEngine/IssuesWorklist/ReportingDashboard are still on disk but no longer referenced by the registry.</what-built>
  <how-to-verify>
    1. Open the Vercel preview at desktop width. On the homepage, open each FeatureAccordion item: Placement Management, Optimization Engine, Issues Management, Reporting and Dashboards, and the compliance item.
    2. Confirm every item shows a real, correct archetype visual (the Flagship for the first four; the unchanged compliance visual for the fifth). No blank box, no wrong visual, no double-frame.
    3. Tab/keyboard through the accordion; confirm the visuals are reduced-motion safe (open one with reduced motion on — content visible at opacity 1).
  </how-to-verify>
  <resume-signal>Type "approved" to delete the 4 bespoke components, or describe what renders wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Delete the 4 bespoke FeatureAccordion components as a set + HANDOFF record</name>
  <read_first>
    - src/components/product/visuals/index.tsx (confirm the registry no longer imports the four)
    - HANDOFF.md (`grep -n "^## " HANDOFF.md` first to find the right section)
    - Run `grep -rn "PlacementMatrix\|OptimizationEngine\|IssuesWorklist\|ReportingDashboard" src/` to confirm nothing else imports them before deleting
  </read_first>
  <action>
    1. Confirm via grep that nothing in src/ imports PlacementMatrix, OptimizationEngine, IssuesWorklist, or ReportingDashboard anymore (only the now-repointed registry referenced them; the bespoke handoff mockups in Front A are different files). If anything still imports them, STOP and resolve before deleting.
    2. Delete the 4 files as a set: `git rm src/components/product/visuals/PlacementMatrix.tsx src/components/product/visuals/OptimizationEngine.tsx src/components/product/visuals/IssuesWorklist.tsx src/components/product/visuals/ReportingDashboard.tsx`.
    3. `rm -rf .next` for a clean rebuild (stale chunk graph).
    4. Update HANDOFF.md (same commit): record the P13-01 closure — the FeatureAccordion VISUALS registry was repointed to Phase 11 Flagships for placement/optimization/issues/reporting (reporting key migration verified), ComplianceStandards kept, and the 4 bespoke visuals deleted. If Front A's Plan 04 has not yet recorded the phase consolidation, this entry can stand alone; otherwise append to the Phase 13 section.
    5. Do NOT touch the firewall files.
  </action>
  <acceptance_criteria>
    - The 4 files are gone: `ls src/components/product/visuals/ | grep -E "^(PlacementMatrix|OptimizationEngine|IssuesWorklist|ReportingDashboard)\.tsx$"` returns ZERO.
    - ComplianceStandards.tsx + the 4 Flagship files still exist.
    - `grep -rn "PlacementMatrix\|OptimizationEngine\|IssuesWorklist\|ReportingDashboard" src/` returns ZERO.
    - HANDOFF.md contains a P13-01 closure note mentioning "VISUALS", "reporting", and "ComplianceStandards".
    - `npx tsc --noEmit` + `npx eslint` clean; `npx playwright test tests/responsive/reveal-fail-open.spec.ts tests/responsive/reduced-motion.spec.ts` PASS; full suite `npx playwright test` green.
    - `git diff --exit-code -- src/components/sections/HomepageHero.tsx src/components/sections/HomepageHandoffSection.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && ! grep -rq "PlacementMatrix\|OptimizationEngine\|IssuesWorklist\|ReportingDashboard" src/ && npx playwright test tests/responsive/reveal-fail-open.spec.ts tests/responsive/reduced-motion.spec.ts</automated>
  </verify>
  <done>The 4 bespoke FeatureAccordion components are deleted after the verified repoint; ComplianceStandards kept; P13-01 closed in HANDOFF; specs green.</done>
</task>

</tasks>

<threat_model>
No new security surface. Presentational registry re-point + dead-code deletion on the homepage FeatureAccordion; no auth/session/input/crypto. Standing a11y (WCAG 2.2 AA + axe) plus the reveal-fail-open contract (the accordion is on / and IS covered by reveal-fail-open.spec.ts) are the relevant non-functional gates.
</threat_model>

<verification>
- VISUALS registry repointed to Flagships for the 4 ids; reporting key migration verified explicitly; compliance kept.
- Human-verify the repoint renders correctly BEFORE deleting the bespoke set (D-08: never delete LIVE without a working replacement).
- 4 bespoke files deleted; nothing else imports them.
- Homepage accordion specs (reveal-fail-open, reduced-motion) + full suite green; firewall byte-unchanged.
- HANDOFF.md records the P13-01 closure in the same commit.
</verification>

<success_criteria>
P13-01 is closed: the FeatureAccordion renders archetype/Flagship instances behind the same ids, the reporting key migration is verified, ComplianceStandards is preserved, and the 4 bespoke visuals are deleted only after the working replacement landed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-visual-system-consolidation/13-05-SUMMARY.md`.
</output>
