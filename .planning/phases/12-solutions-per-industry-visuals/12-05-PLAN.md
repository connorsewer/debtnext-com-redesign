---
phase: 12-solutions-per-industry-visuals
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - src/content/visuals/solutions-hub.ts
  - src/app/solutions/page.tsx
  - src/components/product/visuals/SolutionsIndustryCards.tsx
  - src/components/product/visuals/lazy.tsx
  - tests/responsive/reduced-motion.spec.ts
  - HANDOFF.md
autonomous: false
requirements: [SOLVIS-01, SOLVIS-02, SOLVIS-03, SOLVIS-04, SOLVIS-05]
user_setup: []

must_haves:
  truths:
    - "The /solutions hub renders a single cross-industry DataStory (6-card branch) distinct from all 6 industry pages, in the BenefitSplit visual slot"
    - "The SolutionsIndustryCards component and its LazySolutionsIndustryCards wrapper no longer exist; grep for SolutionsIndustryCards across src/ returns zero live references"
    - "Comparing any two of the 6 industry sub-pages, a user sees distinct industry-specific data on each, never the same widget twice (the duplicate is gone)"
    - "solutions-visuals.spec.ts passes for all 6 industry routes + the hub against the preview; the existing axe + touch-target + reduced-motion specs stay green"
  artifacts:
    - path: "src/content/visuals/solutions-hub.ts"
      provides: "solutionsHubStory (6-card cross-industry DataStory payload)"
      contains: "satisfies DataStoryData"
  key_links:
    - from: "src/app/solutions/page.tsx"
      to: "src/content/visuals/solutions-hub.ts"
      via: "BenefitSplit visual prop fed DataStoryVisual"
      pattern: "solutionsHubStory"
---

<objective>
Land the last consumer (the /solutions hub) and delete the duplicate widget in the SAME plan, so no consumer is ever orphaned (AUDIT P12-01). Then run the phase verification gate: full solutions-visuals.spec.ts across all 7 surfaces, the zero-references grep (SOLVIS-01), LHCI on /solutions/utilities, and a Connor visual review.

Purpose: SOLVIS-01 (kill the duplicate) plus the cross-page distinctness gate that is the reason this phase exists. Closes all 5 SOLVIS requirements.
Output: hub payload + repointed hub page, deleted component + lazy export, updated comment reference, HANDOFF sync, verification evidence.

Set REPO to the repo root for verify commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-solutions-per-industry-visuals/12-CONTEXT.md
@.planning/phases/12-solutions-per-industry-visuals/12-UI-SPEC.md
@.planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md
@.planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md
@.planning/phases/12-solutions-per-industry-visuals/12-02-SUMMARY.md
@.planning/phases/12-solutions-per-industry-visuals/12-03-SUMMARY.md
@.planning/phases/12-solutions-per-industry-visuals/12-04-SUMMARY.md

# The widget being ported then deleted, and the hub consumer:
@src/components/product/visuals/SolutionsIndustryCards.tsx
@src/components/product/visuals/DataStory.tsx
@src/app/solutions/page.tsx
@src/content/visuals/types.ts

<interfaces>
DataStoryVisual (src/components/product/visuals/archetypes.tsx, role="img") takes data: DataStoryData.

DataStoryData cards branch (types.ts, built to subsume SolutionsIndustryCards — DataStory.tsx:9):
  chart kind "cards", cards array of name/accent/tag/value/decimals?/suffix?/bar/sub.
The widget's old INDUSTRIES shape (name/accent/tag/liq/bar/second) maps 1:1 onto a card. The widget had 4 industries; the hub payload extends to 6 (adds insurance + healthcare).

Hub consumer (src/app/solutions/page.tsx): BenefitSplit currently passes visual set to LazySolutionsIndustryCards (line ~50). Repoint to a DataStoryVisual fed solutionsHubStory.

Accents pass as strings inside the cards branch (e.g. accent set to "var(--chart-5)"); the cards branch already mixes var(--product-canvas) inside color-mix (DataStory.tsx:63), so token strings work.

Deletion targets (verified migration map):
  - src/components/product/visuals/SolutionsIndustryCards.tsx (delete file — contains the 3 invented hexes P12-02)
  - src/components/product/visuals/lazy.tsx (delete the LazySolutionsIndustryCards export block, lines ~47-50)
  - tests/responsive/reduced-motion.spec.ts:90 (comment-only mention — update the comment, no behavior change)
Comment-only mentions in Console.tsx / DataStory.tsx / types.ts may stay as history (optional).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the hub DataStory payload + repoint the hub page, then delete the widget</name>
  <files>src/content/visuals/solutions-hub.ts, src/app/solutions/page.tsx, src/components/product/visuals/SolutionsIndustryCards.tsx, src/components/product/visuals/lazy.tsx, tests/responsive/reduced-motion.spec.ts, HANDOFF.md</files>
  <read_first>
    - src/components/product/visuals/SolutionsIndustryCards.tsx (the INDUSTRIES array shape to port: name/accent/tag/liq/bar/second per industry — port the 4 + add insurance, healthcare = 6 cards)
    - src/components/product/visuals/DataStory.tsx (the cards-branch render + the color-mix accent handling at line ~63)
    - src/app/solutions/page.tsx (BenefitSplit visual prop at line ~50; import at line 9)
    - src/content/visuals/solutions-utilities.ts (header + satisfies template)
    - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md (the hub row + the 6 per-industry accents to reuse on the cards)
  </read_first>
  <action>
Create `src/content/visuals/solutions-hub.ts` with a `[CLAIMS REVIEW]` + `[COI REVIEW]` header and:
- solutionsHubStory satisfies DataStoryData — a cross-industry overview. headline like "One platform across every portfolio you run." chart kind "cards" with 6 cards (utilities, financial services, telecom, fintech, insurance, healthcare). Each card: name, accent set to that industry's token string from the map (utilities var(--chart-5), financial-services var(--chart-1), telecom var(--chart-3), fintech var(--chart-5), insurance var(--chart-3), healthcare var(--chart-4)), a tag (label-paired per D-12), value + decimals + suffix, bar, sub. ariaSummary as full governed prose telling the cross-industry story. §5 voice; no em dashes; `·` separators; figures real-shaped/anonymized/generic with plausible relative magnitudes (D-09). This payload must read DISTINCT from all 6 industry pages (it is an overview, not any single industry's hero).

Edit `src/app/solutions/page.tsx`:
- Replace the LazySolutionsIndustryCards import with DataStoryVisual from archetypes and solutionsHubStory from the new module.
- Set the BenefitSplit visual prop to a DataStoryVisual fed solutionsHubStory (D-03).

Delete the widget (only after the hub is repointed — all 7 consumers now repointed across Plans 01-04 + this task):
- Delete the file `src/components/product/visuals/SolutionsIndustryCards.tsx`.
- Remove the LazySolutionsIndustryCards export block from `src/components/product/visuals/lazy.tsx` (lines ~47-50).
- Update the comment at `tests/responsive/reduced-motion.spec.ts:90` so it no longer names the deleted widget (pick a still-existing ssr:false visual as the example, e.g. a solutions Console). No behavior change.

Update HANDOFF.md in this same commit (docs-in-sync rule): note the duplicate widget removal, the per-industry payload modules added, and the hub DataStory.
  </action>
  <verify>
    <automated>cd "$REPO" && test ! -f src/components/product/visuals/SolutionsIndustryCards.tsx && grep -q "solutionsHubStory" src/app/solutions/page.tsx && grep -q "satisfies DataStoryData" src/content/visuals/solutions-hub.ts && echo HUB_OK</automated>
  </verify>
  <done>The hub renders a 6-card cross-industry DataStory; SolutionsIndustryCards.tsx and its lazy export are deleted; the reduced-motion comment is updated; HANDOFF.md is synced in the same commit.</done>
  <acceptance_criteria>
    - src/content/visuals/solutions-hub.ts exports solutionsHubStory `satisfies DataStoryData` with a 6-card cards branch; header has [CLAIMS REVIEW] + [COI REVIEW]
    - solutions/page.tsx BenefitSplit visual is a DataStoryVisual fed solutionsHubStory; no LazySolutionsIndustryCards import remains
    - SolutionsIndustryCards.tsx file is deleted; the LazySolutionsIndustryCards export is gone from lazy.tsx
    - reduced-motion.spec.ts:90 comment no longer names SolutionsIndustryCards
    - HANDOFF.md updated in this commit
    - No em dashes; `·` separators; card accents only chart-1/3/4/5
    - `npx tsc --noEmit` shows no new errors; `npx eslint` exit 0 on changed files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase verification gate — zero-references grep, full spec, build</name>
  <files>tests/responsive/solutions-visuals.spec.ts, .planning/phases/12-solutions-per-industry-visuals/12-05-SUMMARY.md</files>
  <read_first>
    - tests/responsive/solutions-visuals.spec.ts (the spec authored in Plan 01 — now all 7 surfaces should pass)
    - .planning/phases/12-solutions-per-industry-visuals/12-VALIDATION.md (the per-requirement test map + sampling rate)
    - lighthouserc.json (confirm /solutions/utilities is the only solutions route — D-13, do not add others)
  </read_first>
  <action>
Run the phase gate and capture evidence:
1. SOLVIS-01 zero-references: `grep -rn "SolutionsIndustryCards" src/` must return zero LIVE references (any remaining hits must be inert history comments in Console.tsx/DataStory.tsx/types.ts only — if a live import/usage remains, fix it). Record the count.
2. `npx tsc --noEmit` clean (ignore the pre-existing reduced-motion.spec.ts out-of-scope error noted in prior phases).
3. `npx eslint` exit 0 on all changed files across the phase.
4. `next build` succeeds locally (dev server hangs in sandbox — do not run next dev/start).
5. Push the branch and run, against the Vercel preview (PLAYWRIGHT_BASE_URL): `npx playwright test tests/responsive/solutions-visuals.spec.ts` — all 6 industry routes + the hub must pass archetype-presence, cross-route distinctness, and reduced-motion parity. Also run the existing reduced-motion + axe-routes + touch-targets specs over VISUAL_ROUTES; all stay green.
6. Confirm LHCI on /solutions/utilities in CI (perf.yml) stays within gates: LCP < 2.5s, CLS < 0.1, TBT <= 300ms (BL-07). A run at ~290ms TBT is runner noise (hydration-bound, project memory) — do not chase micro-cuts.

Record all outputs in 12-05-SUMMARY.md as the verification evidence.
  </action>
  <verify>
    <automated>cd "$REPO" && LIVE=$(grep -rn "SolutionsIndustryCards" src/ | grep -vE "// |/\*|\* " | wc -l | tr -d ' ') && echo "live_refs=$LIVE" && test "$LIVE" -eq 0 && echo SOLVIS01_OK</automated>
  </verify>
  <done>SOLVIS-01 zero-references grep passes; tsc/eslint/build clean; solutions-visuals.spec.ts green across all 7 surfaces; existing a11y/perf specs green; LHCI within gates; all evidence recorded in 12-05-SUMMARY.md.</done>
  <acceptance_criteria>
    - grep for live (non-comment) "SolutionsIndustryCards" references in src/ returns 0 (SOLVIS-01)
    - `npx tsc --noEmit` clean except the known pre-existing reduced-motion.spec.ts error
    - `next build` succeeds
    - solutions-visuals.spec.ts green for all 6 industry routes + hub against the preview (archetype presence + cross-route distinctness + reduced-motion parity)
    - existing axe-routes + touch-targets + reduced-motion specs stay green over VISUAL_ROUTES
    - LHCI /solutions/utilities within LCP/CLS/TBT gates
    - 12-05-SUMMARY.md records every command output as evidence
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-page distinctness visual review</name>
  <files>.planning/phases/12-solutions-per-industry-visuals/12-05-SUMMARY.md</files>
  <action>
PAUSE for Connor's final sign-off after the automated gate is green. Present the 6 industry pages + the hub on the Vercel preview side by side and wait for the resume signal. Do not flip SOLVIS-01..05 to verified or close the phase until "approved" is given.
  </action>
  <what-built>
    All 6 industry pages now render per-industry Console heroes + routing Schematics + distinct accordion archetypes; the /solutions hub renders a 6-card cross-industry DataStory; the duplicate SolutionsIndustryCards widget is deleted. solutions-visuals.spec.ts and the existing a11y/perf specs are green.
  </what-built>
  <how-to-verify>
    1. On the Vercel preview, open all 6 industry pages side by side: /solutions/utilities, /financial-services, /telecom, /fintech, /insurance, /healthcare. Confirm NO two pages show the same visual — each hero, schematic, and proof reads as that industry's world (arrears/deposits, charge-off, prepaid/postpaid, BNPL, subrogation, self-pay).
    2. Pay special attention to the utilities/insurance/healthcare cluster (they share "reconcile daily" wording): confirm the reconciliation visuals differ (spark vs bars vs area) and the schematics differ (billing-CIS vs policy-admin vs EHR).
    3. Open /solutions and confirm the hub overview reads distinct from every industry page.
    4. Confirm no layout jump on load anywhere (CLS), and the single "Request a demo" CTA per band is untouched.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe distinctness/visual issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> static payload | The only "input" is developer-authored typed payload; no runtime/user input on this static marketing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17 | Information disclosure | hub cross-industry payload numbers + copy | accept | Real-shaped, anonymized, generic per D-09; Andrew pre-cleared 2026-06-12. [CLAIMS REVIEW]/[COI REVIEW] tags retained. No real client identifiers (§15); COI agency-network-agnostic (§6). |
| T-12-18 | Tampering | typed payload module | accept | Static TypeScript validated by `satisfies`; no runtime mutation path. |
| T-12-19 | Supply chain | component deletion + new code, no new deps | accept | Zero new libraries (§2). Deletion removes the invented-hex widget; `next build` from clean in CI confirms no stale chunk references. |
| T-12-20 | Spoofing/Repudiation/DoS/EoP | n/a (static visuals) | accept | No new untrusted input, no data sink, no auth surface. |
</threat_model>

<verification>
- `grep -rn "SolutionsIndustryCards" src/` returns zero LIVE references (SOLVIS-01)
- `npx tsc --noEmit` clean; `npx eslint` exit 0; `next build` succeeds
- solutions-visuals.spec.ts green across all 6 industry routes + hub (preview); existing axe + touch + reduced-motion specs green
- LHCI /solutions/utilities within LCP/CLS/TBT gates
- Connor approves cross-page distinctness at the checkpoint
</verification>

<success_criteria>
- The duplicate widget is gone; no two solutions pages show an identical visual (SOLVIS-01)
- The hub renders a distinct 6-card cross-industry DataStory
- All 5 SOLVIS requirements verified green; existing specs stay green; LHCI within gates
- Connor signs off on distinctness
</success_criteria>

<output>
After completion, create `.planning/phases/12-solutions-per-industry-visuals/12-05-SUMMARY.md` with the full verification evidence (grep count, tsc/eslint/build outputs, spec + LHCI results) and the phase close-out note flipping SOLVIS-01..05 to verified.
</output>
