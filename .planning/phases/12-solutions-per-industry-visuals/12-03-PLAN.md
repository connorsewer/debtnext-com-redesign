---
phase: 12-solutions-per-industry-visuals
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/content/visuals/solutions-fintech.ts
  - src/content/visuals/solutions-insurance.ts
  - src/app/solutions/fintech/page.tsx
  - src/app/solutions/insurance/page.tsx
  - src/content/solutions-fintech.ts
  - src/content/solutions-insurance.ts
autonomous: true
requirements: [SOLVIS-02, SOLVIS-03, SOLVIS-04, SOLVIS-05]
user_setup: []

must_haves:
  truths:
    - "A user on /solutions/fintech sees a Console hero with BNPL installments + personal loans and a steep early-delinquency story, a routing Schematic (lending API source), config Console, and a network-consolidation Data-story, all distinct from utilities (which shares the chart-5 accent)"
    - "A user on /solutions/insurance sees a Console hero with earned-premium / subrogation / deductible rows and business-vs-consumer, a routing Schematic (policy admin source), config Console, and a reconciliation Data-story, all distinct from telecom (which shares the chart-3 accent)"
    - "Opening each of the 3 accordion items on both pages shows a role=img archetype with a non-empty industry-specific aria-label"
    - "Neither page references LazySolutionsIndustryCards"
  artifacts:
    - path: "src/content/visuals/solutions-fintech.ts"
      provides: "fintechConsole + 3 accordion payloads"
      contains: "satisfies ConsoleData"
    - path: "src/content/visuals/solutions-insurance.ts"
      provides: "insuranceConsole + 3 accordion payloads"
      contains: "satisfies ConsoleData"
  key_links:
    - from: "src/app/solutions/fintech/page.tsx"
      to: "src/content/visuals/solutions-fintech.ts"
      via: "ConsoleVisual hero + visuals prop on FeatureAccordion"
      pattern: "fintechConsole"
    - from: "src/app/solutions/insurance/page.tsx"
      to: "src/content/visuals/solutions-insurance.ts"
      via: "ConsoleVisual hero + visuals prop on FeatureAccordion"
      pattern: "insuranceConsole"
---

<objective>
Apply the proven utilities pattern to fintech and insurance. Both have a `reporting` accordion item, so the DataStory proof rides `reporting` per the map. These two carry the accent-collision distinctness burden: fintech shares chart-5 with utilities, insurance shares chart-3 with telecom — so composition and data must visibly differ (map-enforced).

Purpose: SOLVIS-02/03/04/05 for two more industries, in parallel with Plans 02 and 04.
Output: 2 payload modules, 2 repointed pages, 2 updated content modules.

Set REPO to the repo root for verify commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-solutions-per-industry-visuals/12-CONTEXT.md
@.planning/phases/12-solutions-per-industry-visuals/12-UI-SPEC.md
@.planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md
@.planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md

# Proven Plan 01 reference (copy its shape exactly):
@src/content/visuals/solutions-utilities.ts
@src/app/solutions/utilities/page.tsx
@src/content/visuals/types.ts

<interfaces>
Archetypes (src/components/product/visuals/archetypes.tsx, role="img"):
  ConsoleVisual takes data: ConsoleData; DataStoryVisual takes data: DataStoryData; SchematicVisual takes data: SchematicData.

Verified accordion item ids:
  fintech:   placement, optimization, reporting  (DataStory carrier = reporting, per map)
  insurance: placement, optimization, reporting  (DataStory carrier = reporting, per map)

FeatureAccordion visuals prop is keyed by item id and takes precedence over the homepage registry. Do NOT rename ids.
Import payloads directly from each module. Do NOT import Console.tsx/DataStory.tsx/Schematic.tsx directly (Pitfall 5).

Accent tokens (12-UI-SPEC binding): fintech chart-5 (shares with utilities — composition MUST differ); insurance chart-3 (shares with telecom — composition MUST differ).
Match the hero box min-h recorded in 12-01-SUMMARY (CLS, Pitfall 1).

Wiring pattern: ProductVisualBand child becomes ConsoleVisual fed the industry hero payload; FeatureAccordion gains a visuals prop, an object literal keyed by the 3 item ids, each value an archetype element fed its payload (copy from utilities/page.tsx as wired in Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fintech payloads + page repoint</name>
  <read_first>
    - src/content/visuals/solutions-utilities.ts (payload shape + header + satisfies pattern to copy)
    - src/app/solutions/fintech/page.tsx (current: LazySolutionsIndustryCards; item ids placement/optimization/reporting at lines 67/74/81)
    - src/content/solutions-fintech.ts (visualLabel fields)
    - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md (fintech rows + fintech-vs-utilities distinctness note: steep early-delinquency vs seasonal arrears)
    - .planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md (hero min-h)
  </read_first>
  <action>
Create `src/content/visuals/solutions-fintech.ts` with a `[CLAIMS REVIEW]` + `[COI REVIEW]` header and `satisfies` payloads:
- fintechConsole satisfies ConsoleData — hero: BNPL installments + personal loan rows; fast cycle-time pills; a callout reflecting steep early-delinquency. MUST contain "BNPL". Composition must visibly differ from utilities (it shares the chart-5 accent): different row nouns, different bar pattern, delinquency-curve framing not seasonal-arrears framing.
- fintechRouting satisfies SchematicData — placement item: source node "Lending stack (API)", an engine, vendor pools, a sink; agency-network-agnostic edges; API-first framing.
- fintechConfig satisfies ConsoleData — the optimization item: in-app configuration rows ("no release cycle").
- fintechConsolidation satisfies DataStoryData — the reporting item (DataStory carrier, SOLVIS-04): one-connection-across-the-network consolidation story (area or spark) with an annotation.

Edit `src/app/solutions/fintech/page.tsx`:
- Replace the lazy import with archetypes + the 4 fintech payloads.
- ProductVisualBand child becomes ConsoleVisual fed fintechConsole.
- Add a FeatureAccordion visuals prop: placement to SchematicVisual fed fintechRouting, optimization to ConsoleVisual fed fintechConfig, reporting to DataStoryVisual fed fintechConsolidation.
- Reserve hero box min-h per 12-01-SUMMARY.

Edit `src/content/solutions-fintech.ts`: update 3 visualLabel strings. §5 voice; no em dashes; `·` separators.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "BNPL" src/content/visuals/solutions-fintech.ts && grep -q "satisfies SchematicData" src/content/visuals/solutions-fintech.ts && grep -q "fintechConsole" src/app/solutions/fintech/page.tsx && echo FINTECH_OK</automated>
  </verify>
  <acceptance_criteria>
    - Module exports fintechConsole/Routing/Config/Consolidation, each `satisfies <Schema>`; header has [CLAIMS REVIEW] + [COI REVIEW]
    - Payload contains "BNPL"; routing source node is the lending API stack
    - Composition differs visibly from utilities (different row nouns and chart pattern) — the chart-5 accent is shared, so this is the distinctness guard
    - page.tsx ProductVisualBand renders ConsoleVisual fed fintechConsole; FeatureAccordion visuals keyed placement/optimization/reporting; grep for "LazySolutionsIndustryCards" returns nothing
    - No em dashes; `·` separators; hero min-h matches 12-01-SUMMARY
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Insurance payloads + page repoint</name>
  <read_first>
    - src/content/visuals/solutions-utilities.ts (payload shape to copy)
    - src/app/solutions/insurance/page.tsx (current: LazySolutionsIndustryCards; item ids placement/optimization/reporting at lines 68/76/84)
    - src/content/solutions-insurance.ts (visualLabel fields)
    - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md (insurance rows + the utilities/insurance/healthcare "reconcile daily" cluster differentiator: insurance reconciles premium/subrogation, bars chart, policy-admin source)
    - .planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md (hero min-h)
  </read_first>
  <action>
Create `src/content/visuals/solutions-insurance.ts` with a `[CLAIMS REVIEW]` + `[COI REVIEW]` header and `satisfies` payloads:
- insuranceConsole satisfies ConsoleData — hero: earned-premium, subrogation recovery, deductible rows; business vs consumer obligor pills; a jurisdiction-variance callout. MUST contain "subrogation". Composition must differ visibly from telecom (shared chart-3 accent): policy-admin / jurisdiction framing, not high-volume short-cycle.
- insuranceRouting satisfies SchematicData — placement item: source node "Policy admin system", an engine, vendor pools, a sink; recovery-type-and-obligor routing edges.
- insuranceConfig satisfies ConsoleData — the optimization item: separate business vs consumer book config rows.
- insuranceReconciliation satisfies DataStoryData — the reporting item (DataStory carrier, SOLVIS-04): premium/subrogation daily reconciliation as a BARS chart (distinct from utilities' spark and healthcare's area — the cluster differentiator) with an annotation.

Edit `src/app/solutions/insurance/page.tsx`:
- Replace the lazy import with archetypes + the 4 insurance payloads.
- ProductVisualBand child becomes ConsoleVisual fed insuranceConsole.
- Add a FeatureAccordion visuals prop: placement to SchematicVisual fed insuranceRouting, optimization to ConsoleVisual fed insuranceConfig, reporting to DataStoryVisual fed insuranceReconciliation.
- Reserve hero box min-h per 12-01-SUMMARY.

Edit `src/content/solutions-insurance.ts`: update 3 visualLabel strings. §5 voice; no em dashes; `·` separators.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "subrogation" src/content/visuals/solutions-insurance.ts && grep -q "insuranceConsole" src/app/solutions/insurance/page.tsx && echo INSURANCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Module exports insuranceConsole/Routing/Config/Reconciliation, each `satisfies <Schema>`; header has [CLAIMS REVIEW] + [COI REVIEW]
    - Payload contains "subrogation"; routing source node is policy admin
    - The reporting DataStory uses a bars chart (distinct from utilities spark and healthcare area — cluster differentiator)
    - Composition differs visibly from telecom (shared chart-3 accent)
    - page.tsx ProductVisualBand renders ConsoleVisual fed insuranceConsole; FeatureAccordion visuals keyed placement/optimization/reporting; grep for "LazySolutionsIndustryCards" returns nothing
    - No em dashes; `·` separators; hero min-h matches 12-01-SUMMARY
    - `npx tsc --noEmit` shows no new errors for the fintech or insurance files; `npx eslint` exit 0 on changed files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> static payload | The only "input" is developer-authored typed payload; no runtime/user input on this static marketing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Information disclosure | fintech + insurance payload numbers + copy | accept | Real-shaped, anonymized, generic per D-09; Andrew pre-cleared 2026-06-12. [CLAIMS REVIEW]/[COI REVIEW] tags retained. No real client identifiers (§15); COI agency-network-agnostic (§6). |
| T-12-10 | Tampering | typed payload modules | accept | Static TypeScript validated by `satisfies`; no runtime mutation path. |
| T-12-11 | Supply chain | new code, no new deps | accept | Zero new libraries (§2); existing archetypes + schemas only. |
| T-12-12 | Spoofing/Repudiation/DoS/EoP | n/a (static visuals) | accept | No new untrusted input, no data sink, no auth surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for both payload modules + both pages
- `npx eslint` exit 0 on changed files
- `next build` succeeds locally (do not run next dev/start)
- After this wave merges, solutions-visuals.spec.ts against the preview: /solutions/fintech and /solutions/insurance pass archetype-presence + distinctness + reduced-motion parity
</verification>

<success_criteria>
- fintech and insurance each render a per-industry Console hero + routing Schematic + 3 distinct accordion archetypes
- "BNPL" present on fintech; "subrogation" present on insurance
- Accent-collision distinctness holds: fintech differs visibly from utilities (chart-5), insurance differs visibly from telecom (chart-3)
- LazySolutionsIndustryCards gone from both pages; CLS holds
</success_criteria>

<output>
After completion, create `.planning/phases/12-solutions-per-industry-visuals/12-03-SUMMARY.md` recording the fintech and insurance payload export names and the accent-collision distinctness resolution for the verification plan.
</output>
