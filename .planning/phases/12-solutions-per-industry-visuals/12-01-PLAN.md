---
phase: 12-solutions-per-industry-visuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md
  - tests/responsive/solutions-visuals.spec.ts
  - src/content/visuals/solutions-utilities.ts
  - src/app/solutions/utilities/page.tsx
  - src/content/solutions-utilities.ts
autonomous: false
requirements: [SOLVIS-02, SOLVIS-03, SOLVIS-04, SOLVIS-05]
user_setup: []

must_haves:
  truths:
    - "A reviewer can read 12-ARCHETYPE-MAP.md and see, for all 18 accordion items + 6 Console heroes + 1 hub, which archetype + payload each gets, with a distinctness table proving no two industries share a composition"
    - "tests/responsive/solutions-visuals.spec.ts exists with a per-route config for 6 industry routes + the hub, asserting industry-UNIQUE payload strings per route (not mere archetype presence)"
    - "A user on /solutions/utilities sees a Console hero with utilities account-type nouns (arrears, deposits), a routing Schematic on the placement accordion item, and a reconciliation Data-story, all distinct from any homepage visual"
    - "Opening each of the 3 utilities accordion items shows a role=img archetype with a non-empty industry-specific aria-label"
  artifacts:
    - path: ".planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md"
      provides: "D-08 approval artifact: item->archetype+payload map for all 25 instances + distinctness table"
      contains: "Distinctness check"
    - path: "tests/responsive/solutions-visuals.spec.ts"
      provides: "Cross-route distinctness + archetype-presence + reduced-motion parity spec"
      contains: "industryUniqueStrings"
    - path: "src/content/visuals/solutions-utilities.ts"
      provides: "utilitiesConsole, utilitiesRouting, utilitiesConfig, utilitiesReconciliation payloads"
      contains: "satisfies ConsoleData"
  key_links:
    - from: "src/app/solutions/utilities/page.tsx"
      to: "src/content/visuals/solutions-utilities.ts"
      via: "ConsoleVisual hero in ProductVisualBand + visuals prop on FeatureAccordion"
      pattern: "utilitiesConsole"
---

<objective>
Stand up the two Wave-0 gating artifacts (the D-08 archetype map and the distinctness-asserting Playwright spec), then prove the full per-industry pattern end-to-end on `/solutions/utilities` — exactly mirroring how Phase 11 proved placement first (11-01). This plan is the template every Wave-2 industry plan copies.

Purpose: De-risk the highest-volume phase by locking the distinctness contract (map + spec) and proving one industry before parallelizing the other five. SOLVIS-02/03/04/05 for utilities; the map covers all 6 industries + hub.
Output: `12-ARCHETYPE-MAP.md`, `solutions-visuals.spec.ts`, `src/content/visuals/solutions-utilities.ts`, repointed `utilities/page.tsx`, updated `solutions-utilities.ts` visualLabels.

Set REPO to the repo root for the verify commands below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-solutions-per-industry-visuals/12-CONTEXT.md
@.planning/phases/12-solutions-per-industry-visuals/12-RESEARCH.md
@.planning/phases/12-solutions-per-industry-visuals/12-UI-SPEC.md

# Proven Phase 11 pattern to replicate (read before authoring):
@.planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md
@tests/responsive/platform-visuals.spec.ts
@src/content/visuals/placement.ts
@src/content/visuals/types.ts

<interfaces>
Archetype components (src/components/product/visuals/archetypes.tsx — lazy, ssr:false, role="img"):
  ConsoleVisual takes data: ConsoleData; DataStoryVisual takes data: DataStoryData; SchematicVisual takes data: SchematicData.

Payload schemas (src/content/visuals/types.ts) — author with `satisfies`, NEVER a type annotation (excess-property checking is the compile-time proof):
- ConsoleData: header.kpi (caption/value/sub), rows (primary/secondary?/bar?/trailing?/prefix?), pills?, callout?, ariaSummary
- SchematicData: nodes (kind source/engine/vendor/sink, label), edges (label, flow?), ariaSummary
- DataStoryData: headline, chart (kind area/spark/bars/cards), annotation?, ariaSummary; cards branch: chart.cards (name/accent/tag/value/decimals?/suffix?/bar/sub)

FeatureAccordion visuals prop (src/components/sections/FeatureAccordion.tsx:159-172): page-supplied visuals keyed by item id takes PRECEDENCE over the homepage registry fallback.

Utilities accordion item ids (verified, src/content/solutions-utilities.ts): placement, optimization, reporting.

CRITICAL ID-collision fact: these ids collide with the homepage ACCORDION_VISUAL_IDS registry. Today the solutions accordions silently render the homepage bespoke visuals (PlacementMatrix etc.), identical across all 6 industries; those bespoke visuals lack role="img". Wiring the visuals prop fixes it. Do NOT rename ids (breaks #feat-{id}-button anchors + accordion_toggle analytics).

Import payloads DIRECTLY from the module (not the barrel) to keep parallel plans file-disjoint:
  import the utilities payloads from "@/content/visuals/solutions-utilities".

Wiring shape: ProductVisualBand child becomes ConsoleVisual fed utilitiesConsole; FeatureAccordion gains a visuals prop, an object literal keyed by the 3 item ids, each value an archetype element fed its payload.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 12-ARCHETYPE-MAP.md (D-08 deliverable, all 6 industries + hub)</name>
  <files>.planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md</files>
  <read_first>
    - .planning/phases/11-platform-deep-dive-visuals/11-ARCHETYPE-MAP.md (the exact format + distinctness-table shape to mirror)
    - .planning/phases/12-solutions-per-industry-visuals/12-RESEARCH.md (§"Accordion inventory" full 18-item table; §"Per-industry payload raw material"; §"Industry accent mapping")
    - .planning/phases/12-solutions-per-industry-visuals/12-UI-SPEC.md (§"Industry accent mapping" binding table; §"Distinctness Contract")
  </read_first>
  <action>
Create `.planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md` mirroring 11-ARCHETYPE-MAP.md's structure. Cover all 25 instances.

A "## Mapping table" with columns: Page | Item id (or "hero"/"hub") | Archetype | One-line payload intent. Rows required:
- 6 Console heroes (one per industry, ProductVisualBand slot, SOLVIS-02). Each intent names that industry's account-type rows:
  - utilities: final-bill, active-service arrears, deposit offsets; residential vs commercial
  - financial-services: charge-off card, consumer loan, line of credit
  - telecom: device receivables vs service balances; prepaid vs postpaid
  - fintech: BNPL installments, personal loans
  - insurance: earned-premium, subrogation, deductibles; business vs consumer
  - healthcare: self-pay, balance-after-insurance; EBO vs bad-debt
- 18 accordion items (3 per industry), keyed by these VERIFIED ids:
  - utilities: placement (Schematic, routing), optimization (Console, config), reporting (DataStory, reconciliation)
  - financial-services: placement (Schematic), issues (Console, exceptions: bankruptcy/deceased/disputes), optimization (Console, settlement floors/payment plans)
  - telecom: placement (Schematic), optimization (Console, short-cycle recall), issues (Console, prepaid/postpaid)
  - fintech: placement (Schematic), optimization (Console, in-app config), reporting (DataStory, network consolidation)
  - insurance: placement (Schematic), optimization (Console, business/consumer books), reporting (DataStory, reconciliation)
  - healthcare: placement (Schematic), optimization (Console, EBO/bad-debt), reporting (DataStory, reconciliation)
- 1 hub: DataStory cards branch, 6-card cross-industry overview.

Apply D-05: every `placement` item is routing-themed -> Schematic (satisfies SOLVIS-03 in-accordion on all 6 pages). For SOLVIS-04 designate the per-page DataStory carrier: utilities/fintech/insurance/healthcare use the `reporting` item; financial-services and telecom (no reporting item) use the `issues` item as the DataStory carrier — so each page gets Schematic + DataStory + Console across its 3 items. Record which item carries the DataStory per page.

Bind accents from the UI-SPEC table (ONLY chart-1/3/4/5; chart-2 is excluded as near-canvas): utilities chart-5, financial-services chart-1, telecom chart-3, fintech chart-5, insurance chart-3, healthcare chart-4.

Add "## Distinctness check (D-08/SOLVIS-01)". For each pressure cluster, write the explicit differentiator:
  - utilities/insurance/healthcare share near-identical "reconcile daily" reporting + parallel "configure X and Y separately" optimization items. Differentiate: utilities reconciles arrears/deposit balances (daily spark); insurance reconciles premium/subrogation recoveries (different row nouns, bars chart); healthcare reconciles self-pay/balance-after-insurance (area chart). Name the distinct Schematic source node per page: utilities billing/CIS, insurance policy admin, healthcare EHR/clearinghouse.
  - fintech vs utilities share accent chart-5: fintech Console rows = BNPL/personal-loan steep early-delinquency; utilities = arrears/deposits seasonal.
  - telecom vs insurance share accent chart-3: telecom = OSS/BSS high-volume short-cycle; insurance = policy-admin jurisdiction-variance.

Add "## Per-route distinctness strings": 2-3 industry-unique substrings per route the spec will assert: utilities "arrears","deposit"; financial-services "charge-off"; telecom "prepaid"; fintech "BNPL"; insurance "subrogation"; healthcare "self-pay". These become the spec's `industryUniqueStrings`.

Header block: authored date 2026-06-12, "Status: Awaiting Connor approval (Task 4 checkpoint)", decision basis (D-08, D-09, D-11).
  </action>
  <verify>
    <automated>cd "$REPO" && test -f .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md && grep -q "Distinctness check" .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md && grep -q "Per-route distinctness strings" .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md && echo MAP_OK</automated>
  </verify>
  <done>12-ARCHETYPE-MAP.md exists with a mapping table covering all 6 industries + hub (25 instance rows), a Distinctness check section, and a Per-route distinctness strings section; accents use only chart-1/3/4/5.</done>
  <acceptance_criteria>
    - File exists at the exact path
    - Mapping table has rows for all 6 industries + hub (>= 25 instance rows: 6 heroes + 18 accordion + 1 hub)
    - Contains a "Distinctness check" section naming the utilities/insurance/healthcare differentiators explicitly
    - Contains a "Per-route distinctness strings" section listing unique substrings per route
    - Accent column uses only chart-1/chart-3/chart-4/chart-5 (no chart-2 in any accent cell)
    - Every `placement` item maps to Schematic; each page names exactly one DataStory carrier item
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create solutions-visuals.spec.ts with cross-route distinctness config (RED at Wave 0)</name>
  <files>tests/responsive/solutions-visuals.spec.ts</files>
  <read_first>
    - tests/responsive/platform-visuals.spec.ts (full structure to port: per-route config, accordion-archetype loop, assertReducedMotionDataParity, assertNoStuckOpacity, the `iterations === Infinity` idle-guard)
    - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md ("Per-route distinctness strings" from Task 1 — source the industryUniqueStrings verbatim)
    - tests/helpers/routes.ts (VISUAL_ROUTES — already includes solutions routes)
  </read_first>
  <behavior>
    - For each of the 6 industry routes: every mapped accordion item id renders a role="img" archetype with a non-empty aria-label (catches the registry-fallback state, which lacks role="img")
    - Each industry route's DOM contains its industryUniqueStrings (e.g. /solutions/utilities contains "arrears" AND "deposit")
    - Cross-route distinctness: an industry's full uniqueStrings set must NOT all appear on a sibling industry route (a copy-pasted payload fails)
    - Each industryUniqueString visible on load is STILL present under prefers-reduced-motion (data parity, D-05)
    - No in-viewport text-bearing element sits at opacity < 1 after accordion interaction (assertNoStuckOpacity with iterations === Infinity idle-guard)
    - Hub route: BenefitSplit DataStory role="img" present + 6 industry card names in DOM
    - NO Explorable-toggle assertion (flagships not required on solutions pages, D-04)
  </behavior>
  <action>
Create `tests/responsive/solutions-visuals.spec.ts` by porting `platform-visuals.spec.ts`. Define an interface SolutionsVisualConfig with route, accordionItemIds (string array), industryUniqueStrings (string array).

Per-route configs (accordionItemIds from the verified inventory):
- "/solutions/utilities": placement/optimization/reporting, unique: arrears, deposit
- "/solutions/financial-services": placement/issues/optimization, unique: charge-off
- "/solutions/telecom": placement/optimization/issues, unique: prepaid
- "/solutions/fintech": placement/optimization/reporting, unique: BNPL
- "/solutions/insurance": placement/optimization/reporting, unique: subrogation
- "/solutions/healthcare": placement/optimization/reporting, unique: self-pay
- "/solutions" (hub): accordionItemIds empty; assert BenefitSplit DataStory role="img" + 6 card names present.

Use the EXACT industryUniqueStrings authored in 12-ARCHETYPE-MAP.md (match Task 1 if it chose different nouns). Port helpers verbatim: assertAccordionArchetypes (click the feat button per id, assert active panel has role="img" non-empty aria-label), assertReducedMotionDataParity (emulateMedia reduce, uniqueStrings still in DOM), assertNoStuckOpacity (KEEP the iterations === Infinity idle-guard). Add a cross-route distinctness test asserting at least one route's uniqueStrings set is absent on a sibling. Runs against PLAYWRIGHT_BASE_URL preview.

This spec is RED until pages are wired (correct Wave-0 state: solutions accordions render registry visuals lacking role="img", and unique strings aren't in any payload yet).
  </action>
  <verify>
    <automated>cd "$REPO" && test -f tests/responsive/solutions-visuals.spec.ts && grep -q "industryUniqueStrings" tests/responsive/solutions-visuals.spec.ts && grep -q "iterations === Infinity" tests/responsive/solutions-visuals.spec.ts && grep -q "self-pay" tests/responsive/solutions-visuals.spec.ts && echo SPEC_OK</automated>
  </verify>
  <done>solutions-visuals.spec.ts exists, type-checks, and asserts per-route industryUniqueStrings + cross-route distinctness + reduced-motion parity for 6 industry routes + hub, with no Explorable-toggle assertion.</done>
  <acceptance_criteria>
    - tests/responsive/solutions-visuals.spec.ts exists and grep-OK on industryUniqueStrings, iterations === Infinity, and self-pay
    - `npx tsc --noEmit` reports no NEW errors attributable to solutions-visuals.spec.ts (the pre-existing reduced-motion.spec.ts error is out of scope)
    - Per-route config covers all 6 industry routes + the hub with the exact accordionItemIds listed above
    - No Explorable-toggle assertion present (D-04)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author utilities payloads + repoint utilities/page.tsx (proven pattern end-to-end)</name>
  <files>src/content/visuals/solutions-utilities.ts, src/app/solutions/utilities/page.tsx, src/content/solutions-utilities.ts</files>
  <read_first>
    - src/content/visuals/placement.ts (payload authoring template: [CLAIMS REVIEW]/[COI REVIEW] header, `satisfies`, `·` separators, ariaSummary prose)
    - src/app/solutions/utilities/page.tsx (current: LazySolutionsIndustryCards in ProductVisualBand, FeatureAccordion has NO visuals prop)
    - src/content/solutions-utilities.ts (item ids placement/optimization/reporting at lines 68/76/84; visualLabel fields to update)
    - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md (utilities rows: archetype + payload intent per item)
    - .planning/phases/12-solutions-per-industry-visuals/12-UI-SPEC.md (§"Copywriting Contract" payload copy rules; utilities accent chart-5)
  </read_first>
  <action>
Create `src/content/visuals/solutions-utilities.ts` exporting four payloads with `satisfies` (never type annotations) and a `[CLAIMS REVIEW]` + `[COI REVIEW]` header comment block (audit-trail, non-blocking per D-09):
- utilitiesConsole satisfies ConsoleData — the hero. header.kpi caption like "Utilities accounts in treatment"; rows for final-bill, active-service arrears, deposit offsets, write-off disputes with prefix "$" balances and bars; pills like residential/commercial; a callout. MUST contain the strings "arrears" and "deposit" (the spec distinctness hook). Smaller balances / higher counts than financial-services (relative-magnitude guardrail).
- utilitiesRouting satisfies SchematicData — "how dPlat routes utility accounts": source node "Billing / CIS system", engine node "Routing decision engine", vendor pool nodes, sink; labeled edges; ariaSummary prose. Agency-network-agnostic framing ("routes across the originator's existing vendors"), never independence claims (§6).
- utilitiesConfig satisfies ConsoleData — the optimization item: separate residential vs commercial workflow config rows.
- utilitiesReconciliation satisfies DataStoryData — the reporting item: a daily-reconciliation spark/area with an arrears/deposit match-rate annotation. The DataStory carrier for utilities (SOLVIS-04).

Every string follows §5 voice: no em dashes (use `·`), no banned phrases, sentence case, digits. accent uses var(--chart-5) where an accent slot applies.

Edit `src/app/solutions/utilities/page.tsx`:
- Replace the LazySolutionsIndustryCards import with ConsoleVisual, DataStoryVisual, SchematicVisual from archetypes plus the 4 utilities payloads from the new module.
- ProductVisualBand child becomes ConsoleVisual fed utilitiesConsole (D-02, SOLVIS-02).
- On the FeatureAccordion (section utilities_how_it_runs), add a visuals prop mapping: placement to SchematicVisual fed utilitiesRouting, optimization to ConsoleVisual fed utilitiesConfig, reporting to DataStoryVisual fed utilitiesReconciliation (SOLVIS-03/04/05).

Edit `src/content/solutions-utilities.ts`: update the three visualLabel strings (lines ~72/80/88) to accurately describe the NEW visuals (they feed the motion wrapper aria-label), §5-compliant.

CLS guard (Pitfall 1): the Console hero in ProductVisualBand resolves taller than the 20rem skeleton. Since the page imports ConsoleVisual (20rem skeleton) directly, wrap the band child in a min-h matching the resolved hero box, or follow the FLAGSHIP_SKELETON_MIN_H precedent in lazy.tsx. Author the utilities hero to a fixed row/pill budget and RECORD the resolved min-h in the SUMMARY so Wave-2 plans match all 6 heroes to the same box.

Do NOT import Console.tsx/DataStory.tsx/Schematic.tsx directly (Pitfall 5 — eager-import TBT blowout). Only archetypes.tsx wrappers + payload module.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "satisfies ConsoleData" src/content/visuals/solutions-utilities.ts && grep -q "arrears" src/content/visuals/solutions-utilities.ts && grep -q "deposit" src/content/visuals/solutions-utilities.ts && grep -q "utilitiesConsole" src/app/solutions/utilities/page.tsx && echo UTIL_OK</automated>
  </verify>
  <done>utilities payloads authored and wired; ProductVisualBand renders the utilities Console hero; the 3 accordion items render Schematic/Console/DataStory; LazySolutionsIndustryCards gone from the page; CLS holds and the hero min-h is recorded in the SUMMARY.</done>
  <acceptance_criteria>
    - src/content/visuals/solutions-utilities.ts exports utilitiesConsole/Routing/Config/Reconciliation, each `satisfies <Schema>` (no type annotations)
    - Payload contains "arrears" and "deposit"; module has a [CLAIMS REVIEW] and [COI REVIEW] header
    - utilities/page.tsx: ProductVisualBand renders ConsoleVisual fed utilitiesConsole; FeatureAccordion has a visuals prop keyed placement/optimization/reporting; grep for "LazySolutionsIndustryCards" in the page returns nothing
    - `npx tsc --noEmit` produces no new errors for the utilities files
    - No em dashes in any payload string; `·` used as separator
    - SUMMARY records the resolved hero min-h for Wave-2 to match
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: D-08 archetype-map approval + utilities visual review</name>
  <files>.planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md</files>
  <action>
PAUSE for Connor's approval before Wave 2 begins. This is the D-08 gate: all three Wave-2 plans hard-depend on the approved map. Present the 12-ARCHETYPE-MAP.md distinctness table and the wired /solutions/utilities preview, then wait for the resume signal. Do not start any Wave-2 industry work until "approved" is given.
  </action>
  <what-built>
    12-ARCHETYPE-MAP.md (all 6 industries + hub, archetype + payload intent + distinctness table), the solutions-visuals.spec.ts distinctness spec, and the fully-wired /solutions/utilities page (Console hero + routing Schematic + config Console + reconciliation DataStory, duplicate widget removed).
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md`. Confirm the distinctness table is convincing: no two industries read alike, especially the utilities/insurance/healthcare "reconcile daily" cluster. This is the D-08 gate.
    2. On the Vercel preview, visit /solutions/utilities. Confirm: the hero shows a utilities Console (arrears, deposits, residential/commercial), not the old generic industry-cards widget; opening each of the 3 accordion items (placement, optimization, reporting) shows a distinct real visual; no layout jump on load (CLS).
    3. Confirm the utilities page reads like a utilities buyer's world, not a template.
  </how-to-verify>
  <resume-signal>Type "approved" to unlock Wave 2 (the other 5 industries), or describe map/payload changes needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> static payload | The only "input" is the developer-authored typed payload; no runtime/user input crosses any boundary on this static marketing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information disclosure | utilities payload numbers + ariaSummary copy | accept | Figures are real-shaped, anonymized, generic per D-09; Andrew pre-cleared 2026-06-12. [CLAIMS REVIEW]/[COI REVIEW] tags retained for audit. No real client identifiers may appear (CLAUDE.md §15); COI framing stays agency-network-agnostic (§6). |
| T-12-02 | Tampering | typed payload modules | accept | Payloads are static TypeScript validated by `satisfies` at compile time; no runtime mutation path, no user-supplied value reaches them. |
| T-12-03 | Supply chain | new code, no new deps | accept | Zero new libraries (CLAUDE.md §2); only existing archetypes/motion-barrel/payload schemas are used. `next build` from clean in CI. |
| T-12-04 | Spoofing/Repudiation/DoS/EoP | n/a (static marketing visuals) | accept | No new untrusted input, no data sink, no auth surface, no server action. Realistic threat surface is none beyond the above. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the utilities payload + page (pre-existing reduced-motion.spec.ts error is out of scope)
- `npx eslint` exit 0 on changed files
- `next build` succeeds locally (dev server hangs in sandbox — do not run next dev/start)
- `solutions-visuals.spec.ts` against the Vercel preview: /solutions/utilities passes archetype-presence + distinctness + reduced-motion parity (the other 5 routes remain RED until Wave 2 — expected)
- D-08 map approved at the Task 4 checkpoint before Wave 2 begins
</verification>

<success_criteria>
- 12-ARCHETYPE-MAP.md covers all 25 instances with an explicit distinctness table (D-08)
- solutions-visuals.spec.ts exists, type-checks, and asserts per-route industry-unique strings (not mere archetype presence)
- /solutions/utilities renders Console hero + routing Schematic + config Console + reconciliation DataStory; LazySolutionsIndustryCards gone from that page
- CLS holds on /solutions/utilities (resolved hero box reserved); SUMMARY records the min-h for Wave-2 reuse
- Connor approves the map + utilities page at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md`. MUST record: the approved per-page archetype/DataStory-carrier assignments, the chosen hero min-h box for CLS, and the exact industryUniqueStrings so Wave-2 plans stay consistent.
</output>
