---
phase: 12-solutions-per-industry-visuals
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/content/visuals/solutions-healthcare.ts
  - src/app/solutions/healthcare/page.tsx
  - src/content/solutions-healthcare.ts
autonomous: true
requirements: [SOLVIS-02, SOLVIS-03, SOLVIS-04, SOLVIS-05]
user_setup: []

must_haves:
  truths:
    - "A user on /solutions/healthcare sees a Console hero with self-pay and balance-after-insurance rows and EBO vs bad-debt tracks, a routing Schematic (EHR / clearinghouse source), an EBO/bad-debt config Console, and a reconciliation Data-story (area chart), all distinct from utilities and insurance which share the 'reconcile daily' wording"
    - "Opening each of the 3 healthcare accordion items shows a role=img archetype with a non-empty industry-specific aria-label"
    - "The healthcare page does not reference LazySolutionsIndustryCards"
  artifacts:
    - path: "src/content/visuals/solutions-healthcare.ts"
      provides: "healthcareConsole + 3 accordion payloads"
      contains: "satisfies ConsoleData"
  key_links:
    - from: "src/app/solutions/healthcare/page.tsx"
      to: "src/content/visuals/solutions-healthcare.ts"
      via: "ConsoleVisual hero + visuals prop on FeatureAccordion"
      pattern: "healthcareConsole"
---

<objective>
Apply the proven utilities pattern to healthcare, the last of the six industries. Healthcare is the third member of the utilities/insurance/healthcare "reconcile daily" cluster and carries the heaviest distinctness burden: its reconciliation Data-story must use an AREA chart (utilities = spark, insurance = bars) and its Schematic source must be EHR / clearinghouse, with self-pay / balance-after-insurance row nouns no other industry uses.

Purpose: SOLVIS-02/03/04/05 for healthcare, in parallel with Plans 02 and 03.
Output: 1 payload module, 1 repointed page, 1 updated content module.

Set REPO to the repo root for verify commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-solutions-per-industry-visuals/12-CONTEXT.md
@.planning/phases/12-solutions-per-industry-visuals/12-UI-SPEC.md
@.planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md
@.planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md

# Proven Plan 01 reference (copy its shape exactly):
@src/content/visuals/solutions-utilities.ts
@src/app/solutions/utilities/page.tsx
@src/content/visuals/types.ts

<interfaces>
Archetypes (src/components/product/visuals/archetypes.tsx, role="img"):
  ConsoleVisual takes data: ConsoleData; DataStoryVisual takes data: DataStoryData; SchematicVisual takes data: SchematicData.

Verified healthcare accordion item ids: placement, optimization, reporting (DataStory carrier = reporting, per map).

FeatureAccordion visuals prop is keyed by item id and takes precedence over the homepage registry. Do NOT rename ids.
Import payloads directly from the module. Do NOT import Console.tsx/DataStory.tsx/Schematic.tsx directly (Pitfall 5).

Accent token (12-UI-SPEC binding): healthcare chart-4 (unshared).
Match the hero box min-h recorded in 12-01-SUMMARY (CLS, Pitfall 1).

Wiring pattern: ProductVisualBand child becomes ConsoleVisual fed healthcareConsole; FeatureAccordion gains a visuals prop, an object literal keyed by the 3 item ids, each value an archetype element fed its payload (copy from utilities/page.tsx as wired in Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Healthcare payloads + page repoint</name>
  <read_first>
    - src/content/visuals/solutions-utilities.ts (payload shape + header + satisfies pattern to copy)
    - src/app/solutions/healthcare/page.tsx (current: LazySolutionsIndustryCards; item ids placement/optimization/reporting at lines 68/76/84)
    - src/content/solutions-healthcare.ts (visualLabel fields)
    - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md (healthcare rows + the "reconcile daily" cluster differentiator: healthcare = area chart, EHR/clearinghouse source, self-pay/balance-after-insurance nouns)
    - .planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md (hero min-h)
  </read_first>
  <action>
Create `src/content/visuals/solutions-healthcare.ts` with a `[CLAIMS REVIEW]` + `[COI REVIEW]` header and `satisfies` payloads:
- healthcareConsole satisfies ConsoleData — hero: self-pay and balance-after-insurance rows; EBO vs bad-debt track pills; a billing-cycle-driven placement-timing callout. MUST contain "self-pay". Patient-data-handling framing (not vendor-independence). Balances/counts plausibly between utilities and financial-services.
- healthcareRouting satisfies SchematicData — placement item: source node "EHR / patient accounting" (or "EHR / clearinghouse"), an engine, vendor pools, a sink; route-by-balance-stage edges. Source node MUST differ from utilities billing/CIS and insurance policy admin.
- healthcareConfig satisfies ConsoleData — the optimization item: separate EBO vs bad-debt work config rows.
- healthcareReconciliation satisfies DataStoryData — the reporting item (DataStory carrier, SOLVIS-04): self-pay / balance-after-insurance daily reconciliation as an AREA chart (distinct from utilities' spark and insurance's bars — the cluster differentiator) with an annotation.

Edit `src/app/solutions/healthcare/page.tsx`:
- Replace the LazySolutionsIndustryCards import with ConsoleVisual, DataStoryVisual, SchematicVisual from archetypes + the 4 healthcare payloads.
- ProductVisualBand child becomes ConsoleVisual fed healthcareConsole.
- Add a FeatureAccordion visuals prop: placement to SchematicVisual fed healthcareRouting, optimization to ConsoleVisual fed healthcareConfig, reporting to DataStoryVisual fed healthcareReconciliation.
- Reserve hero box min-h per 12-01-SUMMARY.

Edit `src/content/solutions-healthcare.ts`: update 3 visualLabel strings. §5 voice; no em dashes; `·` separators.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "self-pay" src/content/visuals/solutions-healthcare.ts && grep -q "satisfies SchematicData" src/content/visuals/solutions-healthcare.ts && grep -q "healthcareConsole" src/app/solutions/healthcare/page.tsx && echo HEALTHCARE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Module exports healthcareConsole/Routing/Config/Reconciliation, each `satisfies <Schema>`; header has [CLAIMS REVIEW] + [COI REVIEW]
    - Payload contains "self-pay"; routing source node is EHR / clearinghouse (distinct from utilities billing/CIS and insurance policy admin)
    - The reporting DataStory uses an area chart (distinct from utilities spark and insurance bars — cluster differentiator)
    - page.tsx ProductVisualBand renders ConsoleVisual fed healthcareConsole; FeatureAccordion visuals keyed placement/optimization/reporting; grep for "LazySolutionsIndustryCards" returns nothing
    - No em dashes; `·` separators; hero min-h matches 12-01-SUMMARY
    - `npx tsc --noEmit` shows no new errors for the healthcare files; `npx eslint` exit 0 on changed files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> static payload | The only "input" is developer-authored typed payload; no runtime/user input on this static marketing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Information disclosure | healthcare payload numbers + copy | accept | Real-shaped, anonymized, generic per D-09; Andrew pre-cleared 2026-06-12. [CLAIMS REVIEW]/[COI REVIEW] tags retained. Patient-data framing is illustrative only; no real PHI, no real client identifiers (§15); COI agency-network-agnostic (§6). |
| T-12-14 | Tampering | typed payload module | accept | Static TypeScript validated by `satisfies`; no runtime mutation path. |
| T-12-15 | Supply chain | new code, no new deps | accept | Zero new libraries (§2); existing archetypes + schemas only. |
| T-12-16 | Spoofing/Repudiation/DoS/EoP | n/a (static visuals) | accept | No new untrusted input, no data sink, no auth surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the payload module + page
- `npx eslint` exit 0 on changed files
- `next build` succeeds locally (do not run next dev/start)
- After this wave merges, solutions-visuals.spec.ts against the preview: /solutions/healthcare passes archetype-presence + distinctness + reduced-motion parity
</verification>

<success_criteria>
- healthcare renders a per-industry Console hero + routing Schematic + 3 distinct accordion archetypes
- "self-pay" present; routing source is EHR / clearinghouse; reporting DataStory is an area chart
- The utilities/insurance/healthcare cluster is fully differentiated (spark / bars / area; billing-CIS / policy-admin / EHR sources)
- LazySolutionsIndustryCards gone from the healthcare page; CLS holds
</success_criteria>

<output>
After completion, create `.planning/phases/12-solutions-per-industry-visuals/12-04-SUMMARY.md` recording the healthcare payload export names and confirming the three-way cluster differentiation (chart kinds + source nodes) for the verification plan.
</output>
