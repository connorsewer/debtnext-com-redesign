---
phase: 12-solutions-per-industry-visuals
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/content/visuals/solutions-financial-services.ts
  - src/content/visuals/solutions-telecom.ts
  - src/app/solutions/financial-services/page.tsx
  - src/app/solutions/telecom/page.tsx
  - src/content/solutions-financial-services.ts
  - src/content/solutions-telecom.ts
autonomous: true
requirements: [SOLVIS-02, SOLVIS-03, SOLVIS-04, SOLVIS-05]
user_setup: []

must_haves:
  truths:
    - "A user on /solutions/financial-services sees a Console hero with charge-off / consumer-loan / line-of-credit rows, a routing Schematic (core banking source), exception + settlement Consoles, all distinct from utilities and every other industry"
    - "A user on /solutions/telecom sees a Console hero with device vs service receivables and prepaid/postpaid, a routing Schematic (OSS/BSS source), short-cycle-recall + prepaid/postpaid visuals, all distinct from financial-services"
    - "Opening each of the 3 accordion items on both pages shows a role=img archetype with a non-empty industry-specific aria-label"
    - "Neither page references LazySolutionsIndustryCards"
  artifacts:
    - path: "src/content/visuals/solutions-financial-services.ts"
      provides: "financialServicesConsole + 3 accordion payloads"
      contains: "satisfies ConsoleData"
    - path: "src/content/visuals/solutions-telecom.ts"
      provides: "telecomConsole + 3 accordion payloads"
      contains: "satisfies ConsoleData"
  key_links:
    - from: "src/app/solutions/financial-services/page.tsx"
      to: "src/content/visuals/solutions-financial-services.ts"
      via: "ConsoleVisual hero + visuals prop on FeatureAccordion"
      pattern: "financialServicesConsole"
    - from: "src/app/solutions/telecom/page.tsx"
      to: "src/content/visuals/solutions-telecom.ts"
      via: "ConsoleVisual hero + visuals prop on FeatureAccordion"
      pattern: "telecomConsole"
---

<objective>
Apply the proven utilities pattern (Plan 01) to financial-services and telecom: per-industry Console hero, routing Schematic, and the per-item accordion archetypes from the approved 12-ARCHETYPE-MAP.md. These two industries have an `issues` accordion item (not `reporting`), so the DataStory proof rides the `issues` item per the map.

Purpose: SOLVIS-02/03/04/05 for two of the six industries, in parallel with Plans 03 and 04.
Output: 2 payload modules, 2 repointed pages, 2 updated content modules.

Set REPO to the repo root for verify commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-solutions-per-industry-visuals/12-CONTEXT.md
@.planning/phases/12-solutions-per-industry-visuals/12-UI-SPEC.md
@.planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md
@.planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md

# The proven Plan 01 reference (copy its shape exactly):
@src/content/visuals/solutions-utilities.ts
@src/app/solutions/utilities/page.tsx
@src/content/visuals/placement.ts
@src/content/visuals/types.ts

<interfaces>
Archetypes (src/components/product/visuals/archetypes.tsx, role="img"):
  ConsoleVisual takes data: ConsoleData; DataStoryVisual takes data: DataStoryData; SchematicVisual takes data: SchematicData.

Verified accordion item ids:
  financial-services: placement, issues, optimization  (DataStory carrier = issues, per map)
  telecom:            placement, optimization, issues   (DataStory carrier = issues, per map)

FeatureAccordion visuals prop is keyed by item id and takes precedence over the homepage registry. Do NOT rename ids.
Import payloads directly from each module (not the barrel). Do NOT import Console.tsx/DataStory.tsx/Schematic.tsx directly (Pitfall 5).

Accent tokens (12-UI-SPEC binding): financial-services chart-1; telecom chart-3.
Match the hero box min-h recorded in 12-01-SUMMARY (CLS, Pitfall 1).

Wiring pattern (copy verbatim from utilities/page.tsx as wired in Plan 01):
  ProductVisualBand child becomes ConsoleVisual fed the industry hero payload.
  FeatureAccordion gains a visuals prop: an object literal keyed by the 3 item ids, each value an archetype element fed its payload.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Financial-services payloads + page repoint</name>
  <read_first>
    - src/content/visuals/solutions-utilities.ts (the exact payload shape + header + satisfies pattern to copy)
    - src/app/solutions/financial-services/page.tsx (current: LazySolutionsIndustryCards in ProductVisualBand; FeatureAccordion has no visuals prop; item ids placement/issues/optimization at lines 66/74/81)
    - src/content/solutions-financial-services.ts (visualLabel fields to update)
    - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md (financial-services rows + distinctness intent)
    - .planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md (hero min-h to match)
  </read_first>
  <action>
Create `src/content/visuals/solutions-financial-services.ts` with a `[CLAIMS REVIEW]` + `[COI REVIEW]` header and `satisfies` payloads (never type annotations):
- financialServicesConsole satisfies ConsoleData — hero: rows for charge-off card, consumer loan, line of credit with prefix "$" balances (LARGER balances than utilities, relative-magnitude guardrail), bars, Reg-F-aware pills, a callout. MUST contain "charge-off".
- financialServicesRouting satisfies SchematicData — placement item: source node "Core banking / system of record", an engine node, vendor pool nodes, a sink; agency-network-agnostic edge labels.
- financialServicesExceptions satisfies DataStoryData — the issues item (DataStory carrier, SOLVIS-04): bankruptcy / deceased / dispute exception volumes as bars or cards with an examiner-ready annotation.
- financialServicesSettlement satisfies ConsoleData — the optimization item: settlement-floor + payment-plan enforcement rows.

Edit `src/app/solutions/financial-services/page.tsx`:
- Replace the LazySolutionsIndustryCards import with ConsoleVisual, DataStoryVisual, SchematicVisual from archetypes plus the 4 payloads from the new module.
- ProductVisualBand child becomes ConsoleVisual fed financialServicesConsole.
- Add a FeatureAccordion visuals prop mapping: placement to SchematicVisual fed financialServicesRouting, issues to DataStoryVisual fed financialServicesExceptions, optimization to ConsoleVisual fed financialServicesSettlement.
- Reserve the hero box min-h to match 12-01-SUMMARY.

Edit `src/content/solutions-financial-services.ts`: update the 3 visualLabel strings to describe the new visuals (§5 voice). No em dashes anywhere; use `·` separators; sentence case; digits.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "charge-off" src/content/visuals/solutions-financial-services.ts && grep -q "satisfies SchematicData" src/content/visuals/solutions-financial-services.ts && grep -q "financialServicesConsole" src/app/solutions/financial-services/page.tsx && echo FS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Module exports financialServicesConsole/Routing/Exceptions/Settlement, each `satisfies <Schema>` (no annotations); header has [CLAIMS REVIEW] + [COI REVIEW]
    - Payload contains "charge-off"; routing source node is core banking (distinct from utilities billing/CIS)
    - page.tsx ProductVisualBand renders ConsoleVisual fed financialServicesConsole; FeatureAccordion has a visuals prop keyed placement/issues/optimization; grep for "LazySolutionsIndustryCards" in the page returns nothing
    - No em dashes in any payload string; `·` separators used
    - Hero min-h matches the value recorded in 12-01-SUMMARY
    - `npx tsc --noEmit` shows no new errors for the financial-services files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Telecom payloads + page repoint</name>
  <read_first>
    - src/content/visuals/solutions-utilities.ts (payload shape to copy)
    - src/app/solutions/telecom/page.tsx (current: LazySolutionsIndustryCards; item ids placement/optimization/issues at lines 66/74/81)
    - src/content/solutions-telecom.ts (visualLabel fields)
    - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md (telecom rows + telecom-vs-insurance distinctness: telecom = OSS/BSS high-volume short-cycle)
    - .planning/phases/12-solutions-per-industry-visuals/12-01-SUMMARY.md (hero min-h)
  </read_first>
  <action>
Create `src/content/visuals/solutions-telecom.ts` with a `[CLAIMS REVIEW]` + `[COI REVIEW]` header and `satisfies` payloads:
- telecomConsole satisfies ConsoleData — hero: device receivables vs service balances rows; prepaid/postpaid pills; HIGH account counts (relative-magnitude: telecom counts > insurance counts); short treatment windows reflected in a callout. MUST contain "prepaid".
- telecomRouting satisfies SchematicData — placement item: source node "OSS / BSS billing", an engine node, vendor pools sized for high daily volume; agency-network-agnostic edges. Pool structure must visibly differ from financial-services (distinctness).
- telecomRecall satisfies ConsoleData — the optimization item: short-cycle recall / reallocation rows with window timers.
- telecomPrepaidPostpaid satisfies DataStoryData — the issues item (DataStory carrier, SOLVIS-04): prepaid vs postpaid treatment-path split as bars/cards with an annotation.

Edit `src/app/solutions/telecom/page.tsx`:
- Replace the lazy import with archetypes + the 4 telecom payloads.
- ProductVisualBand child becomes ConsoleVisual fed telecomConsole.
- Add a FeatureAccordion visuals prop: placement to SchematicVisual fed telecomRouting, optimization to ConsoleVisual fed telecomRecall, issues to DataStoryVisual fed telecomPrepaidPostpaid.
- Reserve hero box min-h per 12-01-SUMMARY.

Edit `src/content/solutions-telecom.ts`: update 3 visualLabel strings. §5 voice; no em dashes; `·` separators.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "prepaid" src/content/visuals/solutions-telecom.ts && grep -q "telecomConsole" src/app/solutions/telecom/page.tsx && echo TELECOM_OK</automated>
  </verify>
  <acceptance_criteria>
    - Module exports telecomConsole/Routing/Recall/PrepaidPostpaid, each `satisfies <Schema>`; header has [CLAIMS REVIEW] + [COI REVIEW]
    - Payload contains "prepaid"; routing source node is OSS/BSS (distinct from financial-services core banking)
    - page.tsx ProductVisualBand renders ConsoleVisual fed telecomConsole; FeatureAccordion visuals keyed placement/optimization/issues; grep for "LazySolutionsIndustryCards" in the page returns nothing
    - No em dashes; `·` separators
    - `npx tsc --noEmit` shows no new errors for the financial-services or telecom files; `npx eslint` exit 0 on changed files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> static payload | The only "input" is developer-authored typed payload; no runtime/user input on this static marketing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information disclosure | financial-services + telecom payload numbers + copy | accept | Real-shaped, anonymized, generic figures per D-09; Andrew pre-cleared 2026-06-12. [CLAIMS REVIEW]/[COI REVIEW] tags retained for audit. No real client identifiers (§15); COI framing agency-network-agnostic (§6). |
| T-12-06 | Tampering | typed payload modules | accept | Static TypeScript validated by `satisfies` at compile time; no runtime mutation path. |
| T-12-07 | Supply chain | new code, no new deps | accept | Zero new libraries (§2); only existing archetypes + schemas used. |
| T-12-08 | Spoofing/Repudiation/DoS/EoP | n/a (static visuals) | accept | No new untrusted input, no data sink, no auth surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for both payload modules + both pages
- `npx eslint` exit 0 on changed files
- `next build` succeeds locally (do not run next dev/start — hangs in sandbox)
- After this wave merges, run solutions-visuals.spec.ts against the preview: /solutions/financial-services and /solutions/telecom pass archetype-presence + distinctness + reduced-motion parity
</verification>

<success_criteria>
- financial-services and telecom each render a per-industry Console hero + routing Schematic + 3 distinct accordion archetypes
- "charge-off" present on financial-services; "prepaid" present on telecom; routing source nodes differ (core banking vs OSS/BSS)
- LazySolutionsIndustryCards gone from both pages
- CLS holds (hero box reserved to the 12-01-SUMMARY min-h)
</success_criteria>

<output>
After completion, create `.planning/phases/12-solutions-per-industry-visuals/12-02-SUMMARY.md` recording the financial-services and telecom payload export names and any distinctness notes for the verification plan.
</output>
