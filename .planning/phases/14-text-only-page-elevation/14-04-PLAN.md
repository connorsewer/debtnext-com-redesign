---
phase: 14
slug: text-only-page-elevation
plan: 04
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/app/demo/page.tsx
  - src/app/company/page.tsx
  - src/app/resources/page.tsx
  - .planning/phases/14-text-only-page-elevation/14-MOTION-CONFIRM.md
autonomous: false
requirements: [PAGEVIS-03, PAGEVIS-04]

must_haves:
  truths:
    - "A user on /demo sees a restrained AmbientField atmosphere on the hero band that never competes with the form submit CTA and collapses to a static bloom under prefers-reduced-motion"
    - "A reviewer can read 14-MOTION-CONFIRM.md and see, per /company-set + /resources route, the verified reveal coverage (which sections already animate) and the recorded no-lift decision, so PAGEVIS-03 is closed by confirmation not omission"
    - "The /company/about + /leadership + /careers + /contact TSI/COI section is confirmed NOT obscured by any motion or visual (D-01e/§6)"
    - "The existing reduced-motion.spec.ts + reveal-fail-open.spec.ts (which already iterate VISUAL_ROUTES including all 9 targets) stay green after the /demo Ambient lands, and any newly-added motion rests at final state under reduced motion"
  artifacts:
    - path: ".planning/phases/14-text-only-page-elevation/14-MOTION-CONFIRM.md"
      provides: "Per-route reveal-coverage verification + recorded no-lift decisions for the /company set + /resources (PAGEVIS-03 evidence)"
      contains: "no-lift"
  key_links:
    - from: "src/app/demo/page.tsx"
      to: "src/components/ambient/AmbientField.tsx"
      via: "AmbientField behind the hero band content, aria-hidden, reduced-motion-safe"
      pattern: "AmbientField"
---

<objective>
Close the restraint side of the phase: the pages the approved 14-ARCHETYPE-MAP.md flagged as motion-confirm or Ambient-only. Add a restrained `AmbientField` to `/demo` (never competing with the form submit CTA, D-01d), and produce `14-MOTION-CONFIRM.md` — the per-route verification that the `/company` set + `/resources` already carry the reveal coverage PAGEVIS-03 requires, recording each no-lift call as a deliberate decision. This is the D-01 restraint discipline made auditable: most pages correctly get nothing new because they already animate and a figure would be decoration.

Purpose: Deliver PAGEVIS-03 (/company set + /resources) by confirmation and the /demo half of PAGEVIS-04 with Ambient-only, without out-shouting the CTA or obscuring the TSI/COI sections. Runs Wave 2 in parallel with 14-03 (file-disjoint: 14-03 touches compare/integrations/why-dplat; this plan touches demo/company-hub/resources).
Output: /demo AmbientField, 14-MOTION-CONFIRM.md, optional /company-hub or /resources Reveal-only wrap if the map called for one.

Set REPO to the repo root for the verify commands below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-text-only-page-elevation/14-CONTEXT.md
@.planning/phases/14-text-only-page-elevation/14-RESEARCH.md
@.planning/phases/14-text-only-page-elevation/14-UI-SPEC.md

# The approved Wave-0 gate (read the APPROVED no-lift calls FIRST):
@.planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md
@.planning/phases/14-text-only-page-elevation/14-01-SUMMARY.md

# Motion primitives + reveal template:
@src/components/ambient/AmbientField.tsx
@src/components/sections/RevealSection.tsx

<interfaces>
AmbientField (src/components/ambient/AmbientField.tsx) — VERIFIED: props { particleCount?: number (default 5), bloom?: boolean (default true), className?, seed? }. Renders aria-hidden="true", seeded (no hydration mismatch), collapses to a STATIC bloom under prefers-reduced-motion. Sits BEHIND content (use a relative parent + a z-10 content wrapper). Pattern:
  <section className="relative">
    <AmbientField particleCount={5} bloom />
    <div className="relative z-10">...content...</div>
  </section>

RevealSection (src/components/sections/RevealSection.tsx) — VERIFIED: props { children, delay?: number }. Fade-and-rise; returns children UNTOUCHED under prefers-reduced-motion (fail-open). Do NOT wrap a section whose inner component already animates via product/motion (CardGrid, BulletList, CompareMatrix, ComparisonTable, ProcessStrip, IntegrationTable, LeadershipTable, IntegrationStrip) — that stacks two reveals (Pitfall 3). Only wrap non-self-animating blocks (ProseSection, raw SectionContainer content).

/demo page (src/app/demo/page.tsx) — VERIFIED anatomy: hero SectionContainer(surface="dark") -> form+aside SectionContainer -> CardGrid(demoReassurance). The hero band is the natural Ambient host (atmosphere without a competing figure). The form submit CTA lives in DemoForm — the Ambient must never sit above it in visual weight (D-01d, D-03).

Reveal coverage state (VERIFIED via grep this session):
- Already use RevealSection: /company/about, /company/leadership, /company/careers, /company/contact.
- Do NOT currently use page-level RevealSection: /company (hub), /resources — but their SECTION components (CardGrid/ProofBand/BulletList/CaseStudyBand) already self-animate via product/motion. So they already have restrained motion; a page-level wrap is optional per the map.

VISUAL_ROUTES (tests/helpers/routes.ts) already lists all 9 targets, so reduced-motion.spec.ts + reveal-fail-open.spec.ts already cover them — they will START exercising the /demo Ambient automatically. No new spec wiring needed for the motion-confirm pages; this plan does NOT create a new spec.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a restrained AmbientField to /demo (never competes with the form CTA)</name>
  <files>src/app/demo/page.tsx</files>
  <read_first>
    - src/app/demo/page.tsx (the hero SectionContainer(surface="dark") — the Ambient host; the form/aside band below it)
    - src/components/ambient/AmbientField.tsx (the props + the relative-parent / z-10-content pattern; reduced-motion static-bloom behavior)
    - .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md (/demo row: Ambient/Reveal only, no archetype)
    - .planning/phases/14-text-only-page-elevation/14-UI-SPEC.md (§"Restraint / Lift Contract" /demo row; §"Motion contract")
  </read_first>
  <action>
Add an `AmbientField` behind the /demo hero band only (the first SectionContainer surface="dark"), per D-01d (Ambient/Reveal only, no archetype, never compete with the submit CTA):
- Import AmbientField from "@/components/ambient/AmbientField".
- Make the hero SectionContainer's inner region a positioning context: wrap the hero content so AmbientField sits behind it. Since SectionContainer renders its own element, place a `relative` wrapper: render `<AmbientField particleCount={5} bloom />` as the first child of a `relative` container inside the hero band, then the existing eyebrow/h1/body inside a `relative z-10` wrapper. Keep the existing `mx-auto max-w-3xl text-center` content exactly.
- Use a restrained particleCount (5, the default) and let bloom default true. Do NOT add Ambient to the form band or the CardGrid band — atmosphere belongs on the hero only, and nothing may sit above the DemoForm submit CTA in visual weight (D-03).
- AmbientField is already aria-hidden and reduced-motion-safe (static bloom); no extra a11y wiring needed. Confirm the form's submit button remains the single filled CTA in its band, untouched.

Do NOT add an archetype to /demo (D-01d). Do NOT touch DemoForm here (P14-01/P14-02 are plan 14-02).
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "AmbientField" src/app/demo/page.tsx && grep -q 'from "@/components/ambient/AmbientField"' src/app/demo/page.tsx && grep -q "relative z-10" src/app/demo/page.tsx && npx tsc --noEmit 2>&1 | grep -i "demo/page" || echo DEMO_AMBIENT_OK</automated>
  </verify>
  <done>The /demo hero band renders a restrained AmbientField behind its content (aria-hidden, reduced-motion static bloom); the form band + CardGrid band are untouched; no archetype added; the submit CTA stays the single filled CTA in its band.</done>
  <acceptance_criteria>
    - src/app/demo/page.tsx imports AmbientField and renders it behind the hero band content only
    - The hero content sits in a `relative z-10` wrapper above the AmbientField (correct layering)
    - No AmbientField in the form band or the CardGrid band
    - No archetype (ConsoleVisual/DataStoryVisual/SchematicVisual) added to /demo
    - `npx tsc --noEmit` clean for the demo page; `npx eslint src/app/demo/page.tsx` exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author 14-MOTION-CONFIRM.md (PAGEVIS-03 reveal-coverage verification + recorded no-lift calls)</name>
  <files>.planning/phases/14-text-only-page-elevation/14-MOTION-CONFIRM.md, src/app/company/page.tsx, src/app/resources/page.tsx</files>
  <read_first>
    - .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md (/company set + /resources rows: the approved Reveal-only/nothing calls)
    - src/app/company/page.tsx (hub: CardGrid/ProofBand/BulletList — confirm they self-animate)
    - src/app/company/about/page.tsx (confirm RevealSection wraps the TSI/COI block; that block must stay unobscured)
    - src/app/resources/page.tsx (CardGrid/CaseStudyBand/AttachedForm — confirm they self-animate)
    - .planning/phases/14-text-only-page-elevation/14-UI-SPEC.md (§"Restraint / Lift Contract" — no-lift is a deliberate recorded decision, not an omission)
  </read_first>
  <action>
Create `.planning/phases/14-text-only-page-elevation/14-MOTION-CONFIRM.md` — the PAGEVIS-03 evidence artifact recording, per route, that the reveal coverage already exists and the no-lift call is deliberate:

"## Reveal-coverage verification" table with columns: Route | Section components (self-animate via product/motion?) | Page-level RevealSection? | Decision | Rationale. Rows for /company (hub), /company/about, /company/leadership, /company/careers, /company/contact, /resources. Fill from the verified state:
- /company (hub): CardGrid + ProofBand + BulletList self-animate; no page-level RevealSection today; decision = Reveal-only (already present via section components) OR add one page-level RevealSection wrap on a non-self-animating block if the approved map called for it.
- /company/about: RevealSection ×1 wraps the TSI/COI ownership block; decision = no new visual; note the TSI block stays unobscured (D-01e/§6).
- /company/leadership: RevealSection ×2; decision = no new visual.
- /company/careers: RevealSection ×1; decision = no new visual.
- /company/contact: RevealSection ×1; decision = no new visual.
- /resources: CardGrid + CaseStudyBand self-animate; decision = Reveal-only (already present) unless the map called for a DataStory (default: no archetype, decoration risk).

"## TSI / COI non-obstruction check": one line confirming the /company + /company/about TSI ownership section is not visually buried or reframed by any motion (§6, D-01e).

"## Pitfall-3 guard": confirm no already-self-animating section (CardGrid/BulletList/etc.) was double-wrapped in RevealSection.

ONLY if the approved 14-ARCHETYPE-MAP.md explicitly called for a page-level Reveal-only wrap on /company hub or /resources: apply it by wrapping a NON-self-animating block (e.g. a ProseSection or a raw SectionContainer content block) in RevealSection — never a CardGrid/BulletList/etc. (Pitfall 3). If the map recorded "nothing new" for both, make NO code edits to company/page.tsx or resources/page.tsx and record that; the files stay in files_modified only in case the map called for a wrap.
  </action>
  <verify>
    <automated>cd "$REPO" && test -f .planning/phases/14-text-only-page-elevation/14-MOTION-CONFIRM.md && grep -qi "no-lift\|Reveal-only" .planning/phases/14-text-only-page-elevation/14-MOTION-CONFIRM.md && grep -qi "TSI" .planning/phases/14-text-only-page-elevation/14-MOTION-CONFIRM.md && grep -qi "Pitfall" .planning/phases/14-text-only-page-elevation/14-MOTION-CONFIRM.md && echo MOTION_CONFIRM_OK</automated>
  </verify>
  <done>14-MOTION-CONFIRM.md records per-route reveal coverage + deliberate no-lift decisions for the /company set + /resources, a TSI/COI non-obstruction check, and a Pitfall-3 guard; any map-called Reveal-only wrap targets only a non-self-animating block; PAGEVIS-03 is closed by confirmation.</done>
  <acceptance_criteria>
    - 14-MOTION-CONFIRM.md exists with a reveal-coverage table for all 6 /company-set + /resources routes
    - Each route has an explicit Decision + Rationale (no-lift/Reveal-only recorded as deliberate, not omitted)
    - Contains a TSI/COI non-obstruction check confirming the ownership section stays unobscured
    - Contains a Pitfall-3 guard confirming no double-wrapped self-animating section
    - If any RevealSection was added to company/page.tsx or resources/page.tsx, it wraps a non-self-animating block only; if not, those files are unchanged and the doc says so
    - `npx tsc --noEmit` clean if any page was edited
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase verification gate (preview specs + a11y + CLS + governance)</name>
  <files>.planning/phases/14-text-only-page-elevation/14-MOTION-CONFIRM.md</files>
  <action>
PAUSE for the phase verification gate. All page work (14-03 archetypes + 14-04 motion-confirm) and the form-a11y items (14-02) must be green before /gsd-verify-work. Present the CI run + preview so Connor can confirm the phase closes at full fidelity. This gate covers the D-06 acceptance contract for all touched routes.
  </action>
  <what-built>
    The full Phase 14 surface: archetype visuals on /platform/integrations (Schematic) + /compare (DataStory) [+ /why-dplat if wired] from 14-03; restrained AmbientField on /demo + 14-MOTION-CONFIRM.md reveal-coverage evidence from this plan; DemoForm aria wiring + AttachedForm ring + DESIGN.md §8.3 note from 14-02.
  </what-built>
  <how-to-verify>
    1. In CI on the PR: confirm the full Playwright suite is green — 14-page-elevation.spec.ts (archetype routes), demoform-aria.spec.ts (P14-01), reduced-motion.spec.ts + reveal-fail-open.spec.ts (all 9 targets iterate VISUAL_ROUTES), axe-routes.spec.ts + touch-targets.spec.ts (23 routes), and all pre-existing specs.
    2. In CI on the PR: confirm LHCI on the representative routes holds LCP < 2.5s, CLS < 0.1, INP < 200ms, content-route TBT ≤ 300ms (a ~290ms run is runner noise per BL-07, not a regression).
    3. On the Vercel preview: confirm each new archetype's resolved height ≤ its skeleton box (no layout jump, CLS); the /demo Ambient is restrained and never out-shouts the form submit CTA; the /compare visual does not duplicate the CompareMatrix table; the TSI/COI sections stay unobscured.
    4. Confirm [CLAIMS REVIEW] + [COI REVIEW] are flagged in the PR body, and the /compare comparative claims are routed to the legal gate (punch-list #4) before merge.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase (proceed to /gsd-verify-work), or describe what regressed / what governance flag is outstanding.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> static page motion + planning artifact | The only "input" is developer-authored motion wiring (AmbientField props) and a markdown evidence doc; no runtime or user input crosses any boundary. AmbientField renders aria-hidden decorative layers with no data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | Denial of service / perf | /demo AmbientField particle layer | mitigate | Restrained particleCount (5, the default); AmbientField is CSS-driven, seeded, aria-hidden, and collapses to a static bloom under reduced motion. LHCI on the representative routes holds INP < 200ms / TBT ≤ 300ms (Pitfall 5). |
| T-14-15 | Information disclosure / COI | /company + /company/about TSI ownership section | mitigate | 14-MOTION-CONFIRM.md includes a non-obstruction check confirming no motion/visual buries or reframes the TSI ownership disclosure (§6, D-01e). No new copy authored on these pages. |
| T-14-16 | Tampering | static page edits + evidence doc | accept | Static TypeScript/markdown; no runtime mutation path, no user-supplied value. Type-checked by `tsc --noEmit`. |
| T-14-17 | Supply chain | no new deps | accept | Zero new libraries (CLAUDE.md §2); only the existing AmbientField / RevealSection primitives are used. `next build` from clean in CI. |
| T-14-18 | Spoofing/Repudiation/EoP | n/a (static marketing motion) | accept | No untrusted input, no data sink, no auth surface, no server action. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the demo page (+ company/resources if edited); clear .next and re-run if spurious (D-07)
- `npx eslint src/app/demo/page.tsx` (+ any edited page) exit 0
- `next build` succeeds locally (do not run next dev/start — hangs in sandbox)
- Against the preview / CI: reduced-motion.spec.ts + reveal-fail-open.spec.ts stay green (they now exercise the /demo Ambient); axe-routes + touch-targets green on all 9 targets
- LHCI representative routes: LCP < 2.5s, CLS < 0.1, INP < 200ms, content-route TBT ≤ 300ms
- 14-MOTION-CONFIRM.md records the PAGEVIS-03 no-lift evidence + TSI non-obstruction check
- Phase gate approved at Task 3 before /gsd-verify-work
</verification>

<success_criteria>
- /demo has a restrained AmbientField that never competes with the form submit CTA and is reduced-motion-safe (PAGEVIS-04 half)
- 14-MOTION-CONFIRM.md closes PAGEVIS-03 by verified confirmation of existing reveal coverage + deliberate no-lift calls, with the TSI/COI section confirmed unobscured
- Existing reduced-motion + reveal-fail-open + axe + touch-target specs stay green across all 9 targets
- The phase verification gate passes (all specs + LHCI + governance flags)
</success_criteria>

<output>
After completion, create `.planning/phases/14-text-only-page-elevation/14-04-SUMMARY.md`. MUST record: the /demo Ambient placement, the per-route no-lift decisions confirmed for the /company set + /resources, whether any page-level RevealSection was added, and the phase-gate outcome.
</output>
