---
phase: 14
slug: text-only-page-elevation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md
  - tests/responsive/14-page-elevation.spec.ts
autonomous: false
requirements: [PAGEVIS-01, PAGEVIS-02, PAGEVIS-03, PAGEVIS-04]
user_setup: []

must_haves:
  truths:
    - "A reviewer can read 14-ARCHETYPE-MAP.md and see, for every one of the 9 target routes, an explicit lift/no-lift call: which pages get an archetype (with archetype + slot + payload intent + distinctness strings) and which pages get Reveal/Ambient/nothing (recorded as a deliberate no-lift decision, not an omission)"
    - "The map names, for each archetype page, what argument the visual advances that the prose/table does not (the D-01 lift bar, Pitfall 2)"
    - "tests/responsive/14-page-elevation.spec.ts exists with a per-route config { route, archetypeExpected, uniqueStrings[], revealOnly? } for the archetype pages, asserting page-unique argument strings on load AND under prefers-reduced-motion (not mere role=img presence)"
    - "Connor approves the 14-ARCHETYPE-MAP.md at the checkpoint before any archetype page work begins"
  artifacts:
    - path: ".planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md"
      provides: "Per-page lift/no-lift decision record + archetype + slot + payload intent + per-route distinctness strings; Connor-approval gate"
      contains: "Per-route distinctness strings"
    - path: "tests/responsive/14-page-elevation.spec.ts"
      provides: "Per-route archetype-presence + argument-unique-string + reduced-motion-parity spec (RED at Wave 0)"
      contains: "uniqueStrings"
  key_links:
    - from: "tests/responsive/14-page-elevation.spec.ts"
      to: ".planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md"
      via: "uniqueStrings sourced verbatim from the map's Per-route distinctness strings section"
      pattern: "uniqueStrings"
---

<objective>
Stand up the two Wave-0 gating artifacts that every archetype page plan depends on: the `14-ARCHETYPE-MAP.md` (the D-01 lift/no-lift decision record for all 9 target routes, mirroring the approved Phase 12 D-08 map gate) and the `14-page-elevation.spec.ts` skeleton (per-route argument-unique-string assertions, RED until pages are wired). Pause at a blocking checkpoint for Connor's map approval before Wave 2 begins.

Purpose: The operative discipline this phase is RESTRAINT (D-01). Most target pages already carry section-level scroll-reveal via the legacy `product/motion` barrel; a visual is added only where it advances the page's argument. The map makes the per-page call explicit and reviewable, and the spec turns "argument-unique" into a machine-checkable gate (Pitfall 7). This plan gates 14-03 (archetype pages) and informs 14-04 (motion-confirm).
Output: `14-ARCHETYPE-MAP.md`, `tests/responsive/14-page-elevation.spec.ts`.

Set REPO to the repo root for the verify commands below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-text-only-page-elevation/14-CONTEXT.md
@.planning/phases/14-text-only-page-elevation/14-RESEARCH.md
@.planning/phases/14-text-only-page-elevation/14-UI-SPEC.md

# Proven Phase 12 pattern to replicate (read before authoring):
@.planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md
@tests/responsive/solutions-visuals.spec.ts
@tests/responsive/platform-visuals.spec.ts

<interfaces>
Archetype components (src/components/product/visuals/archetypes.tsx — lazy, ssr:false, role="img"):
  ConsoleVisual takes data: ConsoleData; DataStoryVisual takes data: DataStoryData; SchematicVisual takes data: SchematicData.
  VisualSkeleton default box is min-h-[20rem]; FLAGSHIP_SKELETON_MIN_H (lazy.tsx) is min-h-[44rem] for taller resolves.

Payload schemas (src/content/visuals/types.ts):
- SchematicData: title, nodes (kind: "source"|"engine"|"vendor"|"sink", label, sub?), edges (from, to, label?, flow?), ariaSummary
- DataStoryData: headline, chart (kind "area"{points:number[]} | "spark"{bars:number[]} | "bars"{series} | "cards"{cards:{name,accent,tag,value,decimals?,suffix?,bar,sub}[]}), annotation?, ariaSummary
- ConsoleData: header.kpi, rows, pills?, callout?, ariaSummary

Slot: ProductVisualBand (src/components/sections/ProductVisualBand.tsx) is the standalone framed-visual band; renders bare children with NO height floor (Pitfall 1 — skeleton must reserve resolved height).

Route helper: tests/helpers/routes.ts VISUAL_ROUTES already includes all 9 targets (/compare, /why-dplat, /company, /company/about, /company/leadership, /company/careers, /company/contact, /resources, /platform/integrations, /demo) — the reduced-motion.spec.ts + reveal-fail-open.spec.ts nets iterate it, so those routes already get reduced-motion + fail-open coverage.

Real-shaped raw material already in the repo (for authoring the map's payload intents):
- /platform/integrations (src/content/integrations.ts): SAP/Oracle ERP + billing/CIS systems of record → dPlat → 538 collection agencies/law firms/recovery vendors; 60+ production integrations, 16 platform types. This is the source→engine→vendor→sink Schematic story.
- /compare (src/content/compare.ts): market-map table (CompareMatrix) already renders; the lift is a NON-duplicative argument (time-to-production / continuous-production proof, or network-agnostic routing). AUDIT BL-01: compare.ts:99 "digital journeys" is a banned word — flag it for the 14-03 executor to fix in-line if /compare copy is touched.

Spec shape to port (platform-visuals.spec.ts / solutions-visuals.spec.ts): per-route config, role="img" archetype presence with non-empty aria-label, uniqueStrings-in-DOM on load AND under reducedMotion, assertNoStuckOpacity with the `iterations === Infinity` idle-guard. NO Explorable-toggle assertion (no flagships required this phase, D-01/D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 14-ARCHETYPE-MAP.md (the D-01 lift/no-lift decision record for all 9 routes)</name>
  <files>.planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md</files>
  <read_first>
    - .planning/phases/12-solutions-per-industry-visuals/12-ARCHETYPE-MAP.md (the exact section structure + distinctness-table shape to mirror)
    - .planning/phases/14-text-only-page-elevation/14-RESEARCH.md (§"Per-page elevation map" — the per-route today's-motion + recommended-lift table; §"Open Questions" 1-3)
    - .planning/phases/14-text-only-page-elevation/14-UI-SPEC.md (§"Restraint / Lift Contract (D-01)" — the starting hypotheses per route; §"Color" accent mapping; §"Copywriting Contract" copy rules)
    - src/content/integrations.ts (the real ERP/CIS/vendor-network raw material for the integrations Schematic intent)
    - src/content/compare.ts (the compare argument surface; note line 99 "digital journeys" banned word)
  </read_first>
  <action>
Create `.planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md` mirroring 12-ARCHETYPE-MAP.md's structure. This is the D-01 lift/no-lift decision record and the Connor-approval gate for all archetype page work.

Header block: authored date 2026-07-01, "Status: Awaiting Connor approval (Task 3 checkpoint)", decision basis (D-01 lift-over-coverage, D-02 reuse-only, D-03 CTA-primacy).

"## Per-route lift/no-lift table" with columns: Route | Lift call (Archetype / Reveal-only / Nothing) | Slot | Payload intent (or no-lift rationale) | What argument the visual advances that the prose/table does not. All 9 routes as rows, using the RESEARCH §"Per-page elevation map" recommendations as the confirmed default:
- `/compare` → **Archetype (DataStory OR Schematic)**, ProductVisualBand slot. Payload intent: EITHER a DataStory on the time-to-production / continuous-production contrast (dPlat vs a typical 6-9 month build) OR a Schematic of network-agnostic routing. Lift argument: makes the comparative point WITHOUT duplicating the CompareMatrix market-map table. Recommend the DataStory (time-to-production) as the strongest non-duplicative lift per RESEARCH Open Question 1. Live [CLAIMS REVIEW] + legal + [COI REVIEW] surface.
- `/platform/integrations` → **Archetype (Schematic, system/network map)**, ProductVisualBand slot. Payload intent: source nodes (SAP/Oracle ERP, billing/CIS) → engine (dPlat routing) → vendor pool (collection agencies/law firms/recovery vendors) → sink; labeled edges. Lift argument: the system-of-record → dPlat → vendor-network story is a diagram the IntegrationTable rows cannot show. System-consistent with PlatformSystemMap. No CTA competition.
- `/why-dplat` → **DataStory (proof) OR Reveal-only** — record the confirmed decision. Default per RESEARCH: a proof DataStory on the ProofBand story IF it advances the argument beyond the already-animating CardGrid/ComparisonTable/ProofBand; else Reveal-only. Lift argument (if archetype): a proof figure the ProofBand stat row does not already carry. State the chosen call explicitly.
- `/demo` → **Ambient/Reveal only** (NO archetype). Rationale: never compete with the form submit CTA (D-01d). Pairs with P14-01/P14-02 (plan 14-02).
- `/company` (hub) → **Reveal-only or Nothing**. Rationale: sections (CardGrid/ProofBand/BulletList) already animate; TSI section must stay unobscured (D-01e/§6). Record explicit no-new-archetype call.
- `/company/about` → **Reveal-only (already present)**. TSI ownership already wrapped in RevealSection; no new visual. TSI/COI section not obscured.
- `/company/leadership` → **Reveal-only (already present)**. Already RevealSection ×2; confirm coverage, no new archetype.
- `/company/careers` → **Reveal-only (already present)**. Already RevealSection ×1; confirm, no new archetype.
- `/company/contact` → **Reveal-only (already present)**. Already RevealSection ×1; confirm, no new archetype.
- `/resources` → **Reveal-only or optional DataStory**. Default Reveal-only (a DataStory on resource cards risks decoration). Record the call.

"## Accent binding": any archetype built this phase draws accents ONLY from chart-1 (#5266EB), chart-3 (#10b981), chart-4 (#f59e0b), chart-5 (#06b6d4). chart-2 (#323649) is near-canvas, reserved for baselines/grids/muted comparison (D-11 rule). For /compare specifically: reserve chart-1 (indigo) for the dPlat side of any comparative contrast and chart-2 (muted) for the "typical"/competitor baseline, so the argument survives grayscale.

"## Distinctness / lift check (D-01, Pitfall 2)": for each archetype page, one line stating the argument the visual advances that the prose does not. For each no-lift page, one line stating why a visual would be decoration (the page already animates and a figure would repeat the text/table).

"## Per-route distinctness strings": 2-3 argument-unique substrings per ARCHETYPE route that the spec will assert. These become the spec's uniqueStrings. Choose page-specific nouns from the payload intents, e.g.:
- /platform/integrations: an integrations-only node label like "collection agencies" or "billing" plus a routing edge label.
- /compare: a compare-only annotation like "time to production" or a continuous-production phrase (NOT a string that also appears in the CompareMatrix table copy — pick a payload-only string).
- /why-dplat (only if archetype chosen): a proof-figure-specific string.
Record these verbatim so Task 2 sources them exactly.

Governance note in the header: /compare comparative claims need a legal gate (punch-list #4) in addition to [CLAIMS REVIEW]; the /company TSI section is a live [COI REVIEW] surface; every payload module gets [CLAIMS REVIEW]/[COI REVIEW] header tags (non-blocking per Andrew 2026-06-12, retained for audit).
  </action>
  <verify>
    <automated>cd "$REPO" && test -f .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md && grep -q "Per-route distinctness strings" .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md && grep -q "lift/no-lift" .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md && grep -qi "integrations" .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md && grep -qi "compare" .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md && echo MAP_OK</automated>
  </verify>
  <done>14-ARCHETYPE-MAP.md exists with a per-route lift/no-lift table covering all 9 routes, an accent-binding section (chart-1/3/4/5 only), a distinctness/lift check, and a Per-route distinctness strings section; /compare and /platform/integrations are the confirmed archetype pages; the /company set + /resources + /demo no-lift/Ambient calls are recorded explicitly.</done>
  <acceptance_criteria>
    - File exists at the exact path
    - Per-route table has a row for each of the 9 target routes (/compare, /why-dplat, /company, /company/about, /company/leadership, /company/careers, /company/contact, /resources, /platform/integrations, /demo)
    - /platform/integrations maps to a Schematic; /compare maps to a DataStory or Schematic; each archetype row names its slot + payload intent + the argument it advances beyond the prose
    - Every no-lift page (/company set, /resources, /demo) has an explicit recorded rationale, not an omission
    - Accent section restricts to chart-1/chart-3/chart-4/chart-5 (chart-2 only as baseline/muted); no other color introduced
    - "Per-route distinctness strings" section lists 2-3 payload-only unique substrings per archetype route
    - Header records: Status "Awaiting Connor approval", /compare legal-gate note, TSI [COI REVIEW] note
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 14-page-elevation.spec.ts with per-route argument-unique-string config (RED at Wave 0)</name>
  <files>tests/responsive/14-page-elevation.spec.ts</files>
  <read_first>
    - tests/responsive/solutions-visuals.spec.ts (the closest template: per-route config interface, archetype-presence loop, assertReducedMotionDataParity, assertNoStuckOpacity, the `iterations === Infinity` idle-guard)
    - tests/responsive/platform-visuals.spec.ts (secondary reference for the ProductVisualBand archetype-presence assertion shape)
    - .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md ("Per-route distinctness strings" from Task 1 — source the uniqueStrings verbatim)
    - tests/helpers/routes.ts (VISUAL_ROUTES — already lists all 9 targets)
  </read_first>
  <behavior>
    - For each ARCHETYPE route in the config (/platform/integrations, /compare, and /why-dplat only if the map chose an archetype): the page renders a role="img" archetype with a non-empty aria-label in its ProductVisualBand slot
    - Each archetype route's DOM contains ALL of its uniqueStrings on load (e.g. /platform/integrations contains its integrations-only node label AND a routing edge label)
    - Each uniqueString visible on load is STILL present under prefers-reduced-motion (data parity, reducedMotion:"reduce")
    - No in-viewport text-bearing element sits at opacity < 1 after load (assertNoStuckOpacity with the iterations === Infinity idle-guard)
    - /demo is configured revealOnly:true — asserts NO archetype requirement, asserts the DemoForm submit CTA is present (never displaced by motion), and is exempt from the uniqueStrings check
    - NO Explorable-toggle assertion (flagships not required this phase, D-01/D-02)
  </behavior>
  <action>
Create `tests/responsive/14-page-elevation.spec.ts` by porting `solutions-visuals.spec.ts`. Define an interface PageElevationConfig with: route (string), archetypeExpected (boolean), uniqueStrings (string[]), revealOnly? (boolean).

Per-route configs — use the EXACT uniqueStrings authored in 14-ARCHETYPE-MAP.md (match Task 1's chosen nouns verbatim; do NOT invent new strings):
- "/platform/integrations": archetypeExpected true, uniqueStrings = the 2-3 integrations-only strings from the map (e.g. a source/vendor node label + a routing edge label)
- "/compare": archetypeExpected true, uniqueStrings = the 2-3 compare-only payload strings from the map
- "/why-dplat": include ONLY if the map chose an archetype; if the map chose Reveal-only, either omit this route from the archetype config or add it with archetypeExpected:false + revealOnly:true (no uniqueStrings). Follow the map's decision exactly.
- "/demo": archetypeExpected false, revealOnly true, uniqueStrings = []. Assert the submit CTA (button with accessible name matching /request a demo/i or the form submit) is present.

Port helpers verbatim from solutions-visuals.spec.ts:
- the archetype-presence assertion (locate role="img" in the ProductVisualBand / page main, assert non-empty aria-label)
- assertReducedMotionDataParity (emulateMedia reduce, uniqueStrings still in DOM)
- assertNoStuckOpacity (KEEP the `iterations === Infinity` idle-guard verbatim — this is the fail-open contract)

Runs against PLAYWRIGHT_BASE_URL preview. This spec is RED at Wave 0 (the archetype pages are not wired until plan 14-03), which is the correct Wave-0 state. Do NOT add /company-set or /resources rows here — their reduced-motion + fail-open coverage comes from the existing reduced-motion.spec.ts / reveal-fail-open.spec.ts iterating VISUAL_ROUTES.
  </action>
  <verify>
    <automated>cd "$REPO" && test -f tests/responsive/14-page-elevation.spec.ts && grep -q "uniqueStrings" tests/responsive/14-page-elevation.spec.ts && grep -q "iterations === Infinity" tests/responsive/14-page-elevation.spec.ts && grep -q "/platform/integrations" tests/responsive/14-page-elevation.spec.ts && grep -q "/compare" tests/responsive/14-page-elevation.spec.ts && grep -q "revealOnly" tests/responsive/14-page-elevation.spec.ts && echo SPEC_OK</automated>
  </verify>
  <done>14-page-elevation.spec.ts exists, type-checks with no new errors, and asserts per-route argument-unique strings + reduced-motion parity for the archetype pages (integrations, compare, and why-dplat per the map) plus a /demo revealOnly row that checks the submit CTA, with no Explorable-toggle assertion.</done>
  <acceptance_criteria>
    - tests/responsive/14-page-elevation.spec.ts exists and grep-OK on uniqueStrings, `iterations === Infinity`, /platform/integrations, /compare, revealOnly
    - `npx tsc --noEmit` reports no NEW errors attributable to 14-page-elevation.spec.ts (a pre-existing reduced-motion.spec.ts error, if any, is out of scope — clear .next and re-run if tsc errors spuriously)
    - Per-route config covers /platform/integrations + /compare as archetypeExpected:true with uniqueStrings matching the map verbatim
    - /demo row is revealOnly:true with a submit-CTA-present assertion and no uniqueStrings
    - No Explorable-toggle assertion present
    - No /company-set or /resources rows added (they are covered by the existing VISUAL_ROUTES nets)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-01 lift-map approval (Connor gate before archetype page work)</name>
  <files>.planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md</files>
  <action>
PAUSE for Connor's approval before Wave 2 begins. This is the D-01/Phase-12-D-08 lift gate: plan 14-03 (archetype pages) hard-depends on the approved map, and 14-04 relies on the recorded no-lift calls. Present the 14-ARCHETYPE-MAP.md per-route table and wait for the resume signal. Do not start any archetype page work until "approved" is given.
  </action>
  <what-built>
    14-ARCHETYPE-MAP.md (per-route lift/no-lift decision record for all 9 targets: which pages get an archetype and what argument it advances, which pages get Reveal-only/nothing and why) and the 14-page-elevation.spec.ts skeleton (RED until pages are wired), with per-route argument-unique-string assertions on load AND under reduced motion.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md`. Confirm the lift bar is met: each archetype page (/compare, /platform/integrations, and /why-dplat if chosen) states an argument the visual advances that the prose/table does not — not "it looks nice" (Pitfall 2). This is the D-01 gate.
    2. Confirm the no-lift calls are deliberate and correct: /company set + /resources already animate via their section components and the TSI/COI section stays unobscured; /demo is Ambient/Reveal only so nothing competes with the form submit CTA.
    3. Confirm the /compare visual does NOT duplicate the market-map CompareMatrix table (it advances a distinct argument, e.g. time-to-production).
    4. Confirm accents stay within chart-1/3/4/5 and the /compare contrast reserves chart-1 for dPlat and a muted baseline for "typical".
  </how-to-verify>
  <resume-signal>Type "approved" to unlock Wave 2 (archetype pages + motion-confirm), or describe map changes needed (which pages get a visual, which archetype, what argument).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> static planning artifact / test file | The only "input" is developer-authored markdown (the map) and a TypeScript spec; no runtime or user input crosses any boundary. This plan ships zero product code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information disclosure | payload-intent copy sketched in 14-ARCHETYPE-MAP.md | accept | The map records intents only (no live figures ship here). Real figures land in 14-03 payloads under [CLAIMS REVIEW]; /compare comparative claims carry the legal gate; no real client identifiers (CLAUDE.md §15). |
| T-14-02 | Tampering | 14-page-elevation.spec.ts | accept | Static TypeScript test file; no runtime mutation path, no user-supplied value. Type-checked by `tsc --noEmit`. |
| T-14-03 | Supply chain | new spec file, no new deps | accept | Zero new libraries (CLAUDE.md §2); spec ports existing helpers from solutions-visuals.spec.ts. `next build` from clean in CI. |
| T-14-04 | Spoofing/Repudiation/DoS/EoP | n/a (planning + test artifacts) | accept | No untrusted input, no data sink, no auth surface, no server action. Realistic threat surface is none beyond the above. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for 14-page-elevation.spec.ts (clear .next and re-run if tsc errors spuriously per D-07; a pre-existing out-of-scope reduced-motion.spec.ts error is not this plan's)
- `npx eslint tests/responsive/14-page-elevation.spec.ts` exit 0
- `14-page-elevation.spec.ts` is RED against the preview (archetype pages not wired until 14-03) — correct Wave-0 state
- 14-ARCHETYPE-MAP.md approved at the Task 3 checkpoint before Wave 2 begins
</verification>

<success_criteria>
- 14-ARCHETYPE-MAP.md covers all 9 routes with explicit lift/no-lift calls, an accent-binding section, a distinctness/lift check, and per-route distinctness strings (D-01 gate)
- 14-page-elevation.spec.ts exists, type-checks, and asserts per-route argument-unique strings + reduced-motion parity for the archetype pages + a /demo revealOnly row
- Connor approves the map at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/14-text-only-page-elevation/14-01-SUMMARY.md`. MUST record: the approved per-page lift/no-lift calls (which pages get an archetype and which archetype), the exact uniqueStrings per archetype route so 14-03 payloads contain them, and the /why-dplat archetype-or-reveal decision.
</output>
