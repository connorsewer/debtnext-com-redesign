# Phase 14 — CONTINUE HERE (resume after /clear)

**Date:** 2026-06-13
**Branch:** `phase-14-text-page-elevation` (off `main` @ `ab0ad77`, pushed; NO PR yet)
**Cycle stage:** discuss ✅ → **plan (next)** → execute → code-review → boundary-stop

## Why a fresh context
Started under `/finish-build` after Phase 13's cycle completed in the same long session. Discuss was done here; plan+execute+review were deferred to a clean context to avoid a mid-execute blowout. Phase 14 has zero dependency on that session — all scope is in the files below.

## What's done
- Branch created off `main` (keeps Phase 13 PR #12 an isolated review; Phase 14 only needs the on-main archetype library). Independent of 13.
- `/gsd-discuss-phase 14 --auto` → `14-CONTEXT.md` committed (`db…`). Decisions D-01..D-07 + per-page hypotheses + P14-01/P14-02 fold-ins + governance locked as recommended defaults.

## Next steps (resume the /finish-build cycle for Phase 14)
1. `/gsd-plan-phase 14` (research → plan → plan-check; config has these on).
2. `/gsd-execute-phase 14`.
3. `/gsd-code-review 14` → `/gsd-code-review-fix` if findings.
4. `/gsd-verify-work 14` (gsd-verifier goal-backward) + build the human-preview `14-UAT.md`.
5. STOP at the phase boundary for Connor's preview (no merge without his go-ahead).

## Scope sources (read these)
- `.planning/phases/14-text-only-page-elevation/14-CONTEXT.md` (decisions)
- `.planning/ROADMAP.md` Phase 14 (lines ~280-291): 4 success criteria, "mostly DataStory + Schematic + Reveal/Ambient", restraint discipline.
- `.planning/AUDIT-2026-06-12.md` §Phase 14 (P14-01 DemoForm aria-invalid/aria-describedby; P14-02 form-input focus ring) + launch punch list (human gates).

## Target routes (9)
`/compare`, `/why-dplat`, `/company` (+ `/about` `/leadership` `/careers` `/contact`), `/resources`, `/platform/integrations`, `/demo`. Today's visual/motion ref counts: compare 0, why-dplat 0, company-hub 0, about 3, leadership 5, careers 3, contact 3, resources 0, integrations 2, demo 0. **Lift over coverage** — visual only where it lifts; else motion-only or nothing.

## Hard constraints (from /finish-build + CLAUDE.md)
- Restraint: motion never out-shouts the one "Request a demo" CTA per band.
- DESIGN.md tokens only; WCAG 2.2 AA + axe; reduced-motion gated; LCP<2.5/CLS<0.1/INP<200ms; all existing Playwright specs green; per-commit docs-in-sync.
- Governance: `[CLAIMS REVIEW]` on metrics, `[COI REVIEW]` on vendor/TSI framing; never remove existing markers; real-shaped anonymized figures only; no invented clients.
- Stop-list (ask Connor): new nav item / new CTA / new token / page not in route map / a NEW archetype component.
- Sandbox: `next dev`/`start` HANG. Use `tsc --noEmit`, `eslint`, `next build`, `playwright --list`; CI is spec/axe/LHCI truth. Commits: `git -c commit.gpgsign=false`, Co-Authored-By footer.
- `tsc` can fail spuriously on a dirty `.next` cache (duplicate `.next/types/*\ 2.ts`) — clear `.next` and re-run; CI builds clean.

## Phase 13 status (parallel, do not disturb)
PR #12 (`phase-13-visual-system-consolidation`) is GREEN and awaiting Connor's preview review (`13-UAT.md`) + merge decision. Don't merge or rebase it from here.
