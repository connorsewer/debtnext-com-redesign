---
phase: 14
slug: text-only-page-elevation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/forms/DemoForm.tsx
  - src/components/ui/AttachedForm.tsx
  - DESIGN.md
  - tests/a11y/demoform-aria.spec.ts
autonomous: true
requirements: [PAGEVIS-04]
user_setup: []

must_haves:
  truths:
    - "A screen-reader user who triggers DemoForm validation hears each errored field announced as invalid and hears its error message, because every errored input/select/textarea now carries aria-invalid=\"true\" and aria-describedby=\"{id}-error\" pointing at the already-rendered <p role=\"alert\">"
    - "The honeypot websiteUrl input is untouched (no aria wiring added), so the bot trap is unchanged"
    - "The AttachedForm email input shows the 3px #9CB4E8 focus ring (matching the DemoForm inputs and the DESIGN.md §8.3 shadows.focus token), replacing its prior border-only focus treatment"
    - "DESIGN.md §8.3 documents the 3px #9CB4E8 input-focus ring as the intentional, sanctioned input pattern (distinct from the general 2px-outline rule), citing the shadows.focus token"
    - "A Playwright a11y spec triggers DemoForm validation and asserts aria-invalid + aria-describedby on each errored field"
  artifacts:
    - path: "src/components/forms/DemoForm.tsx"
      provides: "aria-invalid + aria-describedby on all errored fields via extended inputClasses(error, id)"
      contains: "aria-describedby"
    - path: "tests/a11y/demoform-aria.spec.ts"
      provides: "P14-01 assertion: errored fields expose aria-invalid + aria-describedby"
      contains: "aria-invalid"
  key_links:
    - from: "src/components/forms/DemoForm.tsx"
      to: "the <p id={`${id}-error`} role=\"alert\"> in Field"
      via: "aria-describedby={error ? `${id}-error` : undefined}"
      pattern: "aria-describedby"
---

<objective>
Land the two folded audit form-a11y items — independent of the visual work and gating nothing else. P14-01: wire `aria-invalid` + `aria-describedby` on every errored DemoForm field (the error `<p role="alert">` already exists; nothing reads it programmatically today). P14-02: bring `AttachedForm`'s email input in line with the §8.3 3px `#9CB4E8` focus ring and document that ring in DESIGN.md §8.3 as the intentional input-focus pattern (NOT a bug; it matches the `shadows.focus` token).

Purpose: Screen readers must announce the already-present validation message (P14-01), and the input-focus treatment must be consistent and documented so it stops being re-flagged as off-spec (P14-02). These are mechanical, self-contained, and can run in Wave 1 parallel with the map plan (14-01) — they depend on no archetype decision.
Output: edited DemoForm.tsx, AttachedForm.tsx, DESIGN.md §8.3 note, new tests/a11y/demoform-aria.spec.ts.

Set REPO to the repo root for the verify commands below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-text-only-page-elevation/14-CONTEXT.md
@.planning/phases/14-text-only-page-elevation/14-RESEARCH.md
@.planning/phases/14-text-only-page-elevation/14-UI-SPEC.md

<interfaces>
DemoForm (src/components/forms/DemoForm.tsx) — VERIFIED this session:
- Field({label, id, error, hint, input}) renders <p id={`${id}-error`} role="alert"> when error is set (lines ~355-364). The describedby target id ALREADY EXISTS.
- inputClasses(error) at lines ~369-378 returns ONLY { className } today. It sets a red border on error but wires NO aria-invalid and NO aria-describedby.
- All 9 real fields pass an id AND spread {...inputClasses(errors.<field>)}: firstName, lastName, workEmail, company, jobTitle (5 inputs), industry, portfolioSize (2 selects), whatToSee (1 textarea). Each already has a matching id="firstName" etc.
- The honeypot websiteUrl input (lines ~294-300) uses {...register("websiteUrl")} — it does NOT use inputClasses. Extending inputClasses therefore never touches the honeypot (do NOT add aria wiring there).
- inputClasses focus classes today: "focus:outline-none focus:border-[var(--primary)] focus:ring-3 focus:ring-[var(--focus)]/35" — this is already the §8.3 3px ring; leave it.

AttachedForm (src/components/ui/AttachedForm.tsx:55) — VERIFIED: the email input has "focus:border-[var(--primary)] focus:outline-none" but NO ring (border-only). This is the one genuine inconsistency to fix — add the §8.3 ring to match DemoForm.

DESIGN.md — VERIFIED: shadows.focus token at line 133 is `0 0 0 3px rgba(156, 180, 232, 0.35)`; §8.3 "Forms and inputs" starts at line 1076, "Input states" at line 1110. The §8.3 note lands in this section.

HomepageHero.tsx:236 also uses the 3px ring but is a Phase-13/15 FIREWALL file — do NOT touch it. It is already on the §8.3 pattern, so no change is needed; if any change were ever needed it lands on its own track (D-05b). This plan does not modify HomepageHero.tsx.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: P14-01 — wire aria-invalid + aria-describedby on all errored DemoForm fields</name>
  <files>src/components/forms/DemoForm.tsx, tests/a11y/demoform-aria.spec.ts</files>
  <read_first>
    - src/components/forms/DemoForm.tsx (read the Field component ~343-366, inputClasses ~369-378, and ALL 9 input/select/textarea call sites at lines ~147/162/178/195/209/226/249/272, plus the honeypot ~294)
    - tests/a11y/axe-routes.spec.ts (the a11y spec style + how it targets /demo, for porting the new spec's setup)
    - .planning/phases/14-text-only-page-elevation/14-UI-SPEC.md (§"Accessibility Contract" P14-01 binding + §"Interaction States" Error row)
  </read_first>
  <behavior>
    - Test 1: after submitting the empty DemoForm (triggering validation), the firstName input has aria-invalid="true" and aria-describedby="firstName-error", and an element with id="firstName-error" and role="alert" is present
    - Test 2: the same holds for at least workEmail (aria-describedby="workEmail-error") and one select (e.g. industry, aria-describedby="industry-error")
    - Test 3: a field with NO error carries neither aria-invalid="true" nor aria-describedby (attributes are undefined, not empty strings)
    - Test 4 (guard): the honeypot websiteUrl input has NO aria-invalid and NO aria-describedby (untouched)
  </behavior>
  <action>
Extend `inputClasses` to accept the field id and return the aria attributes alongside className, so all 9 fields are fixed in one edit and the honeypot (which does not call inputClasses) is untouched:

Change the signature to `function inputClasses(error: unknown, id: string)` and return:
```
return {
  className: cn( /* unchanged existing classes */ ),
  "aria-invalid": error ? true : undefined,
  "aria-describedby": error ? `${id}-error` : undefined,
};
```
Keep the existing className string EXACTLY as-is (including the current `focus:ring-3 focus:ring-[var(--focus)]/35` — that is the §8.3 pattern, do not change it here).

Then thread the id into each of the 9 call sites: replace `{...inputClasses(errors.firstName)}` with `{...inputClasses(errors.firstName, "firstName")}`, and identically for lastName, workEmail, company, jobTitle, industry, portfolioSize, whatToSee — each id string exactly matching that input's existing `id=` attribute. Because the spread comes AFTER the explicit `id="..."` on each element, the returned aria attributes do not collide with the id prop.

Do NOT modify the honeypot websiteUrl input (it uses {...register("websiteUrl")}, not inputClasses — leave it exactly as-is).

Create `tests/a11y/demoform-aria.spec.ts` (port the setup from axe-routes.spec.ts): navigate to /demo against PLAYWRIGHT_BASE_URL, submit the form empty to trigger zod validation, then assert:
- input#firstName has aria-invalid="true" and aria-describedby="firstName-error"; #firstName-error exists with role="alert"
- #workEmail and select#industry likewise carry aria-invalid + aria-describedby pointing at their {id}-error
- input#websiteUrl (honeypot) has NO aria-invalid attribute (assert it is null)
Runs against the preview.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q 'aria-describedby' src/components/forms/DemoForm.tsx && grep -q 'function inputClasses(error: unknown, id: string)' src/components/forms/DemoForm.tsx && grep -q 'inputClasses(errors.workEmail, "workEmail")' src/components/forms/DemoForm.tsx && test -f tests/a11y/demoform-aria.spec.ts && grep -q 'aria-invalid' tests/a11y/demoform-aria.spec.ts && npx tsc --noEmit 2>&1 | grep -i 'DemoForm' | grep -v 'reduced-motion' || echo P14_01_OK</automated>
  </verify>
  <done>inputClasses returns aria-invalid + aria-describedby keyed to {id}-error on error; all 9 fields thread their id; the honeypot is untouched; the demoform-aria spec asserts the attributes on errored fields.</done>
  <acceptance_criteria>
    - inputClasses signature is `inputClasses(error: unknown, id: string)` and returns aria-invalid + aria-describedby (undefined when no error)
    - All 9 call sites pass the field id string matching the input's id= attribute (grep confirms at least workEmail + industry + whatToSee)
    - `grep -n 'inputClasses' src/components/forms/DemoForm.tsx` shows no call site still using the one-arg form
    - The honeypot websiteUrl input still uses {...register("websiteUrl")} with no aria-invalid/aria-describedby
    - tests/a11y/demoform-aria.spec.ts exists, greps OK on aria-invalid, and asserts the honeypot has no aria-invalid
    - `npx tsc --noEmit` reports no new DemoForm errors (clear .next and re-run if spurious)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: P14-02 — add the §8.3 ring to AttachedForm + document the input-focus ring in DESIGN.md</name>
  <files>src/components/ui/AttachedForm.tsx, DESIGN.md</files>
  <read_first>
    - src/components/ui/AttachedForm.tsx (the email input at line ~55: currently "focus:border-[var(--primary)] focus:outline-none" with no ring)
    - src/components/forms/DemoForm.tsx (the inputClasses focus string to match: "focus:ring-3 focus:ring-[var(--focus)]/35" + "focus:border-[var(--primary)]")
    - DESIGN.md lines 1076-1120 (§8.3 Forms and inputs / Input states) and line 133 (shadows.focus token) — the exact section to annotate
    - .planning/phases/14-text-only-page-elevation/14-UI-SPEC.md (§"P14-02 resolution (documented as intentional)" — the binding decision this plan implements)
  </read_first>
  <action>
P14-02 is resolved as DOCUMENT-not-normalize (per UI-SPEC §"P14-02 resolution" and RESEARCH: the 3px `#9CB4E8` input ring is on-spec for inputs per DESIGN.md §8.3 + the `shadows.focus` token; normalizing to the general 2px outline would regress the AttachedForm pill aesthetic and contradict the token).

1. Edit `src/components/ui/AttachedForm.tsx` line ~55: add the §8.3 ring to the email input's className so it matches DemoForm. Change `focus:border-[var(--primary)] focus:outline-none` to also include `focus:ring-3 focus:ring-[var(--focus)]/35` (keep the existing pill radius, border, and @sm/form: responsive classes exactly). The result: on focus the input shows the border shift to --primary PLUS the 3px --focus ring. Do not alter any non-focus class.

2. Edit `DESIGN.md` §8.3 "Input states" (around line 1110): add a short note recording that the 3px `#9CB4E8` input-focus ring is the intentional, sanctioned input-focus pattern — distinct from the general §8 2px-outline rule for buttons/links/tabs — and cite the `shadows.focus` token (`0 0 0 3px rgba(156, 180, 232, 0.35)`, DESIGN.md line 133). State that DemoForm inputs and the AttachedForm email input both use this ring. Follow §5 voice (no em dashes, sentence case, no banned phrases). This doc note lands in the SAME commit as the AttachedForm code change (docs-in-sync rule).

3. Do NOT touch `HomepageHero.tsx` — it is a Phase-13/15 firewall file and is already on the §8.3 ring pattern (D-05b). If you believe it needs a change, STOP and raise it; do not edit it silently.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q 'focus:ring-3 focus:ring-\[var(--focus)\]/35' src/components/ui/AttachedForm.tsx && grep -qi 'shadows.focus' DESIGN.md && git -C "$REPO" diff --name-only | grep -q 'HomepageHero' && echo FIREWALL_TOUCHED_STOP || echo P14_02_OK</automated>
  </verify>
  <done>AttachedForm's email input carries the §8.3 3px --focus ring matching DemoForm; DESIGN.md §8.3 documents the input-focus ring as the intentional pattern citing shadows.focus; HomepageHero.tsx is untouched.</done>
  <acceptance_criteria>
    - AttachedForm.tsx email input className contains `focus:ring-3 focus:ring-[var(--focus)]/35` (grep confirms)
    - AttachedForm pill radius/border/@sm-form classes are otherwise unchanged (only the focus ring added)
    - DESIGN.md §8.3 contains a new note referencing the `shadows.focus` token and describing the 3px #9CB4E8 input ring as intentional for inputs
    - `git diff --name-only` does NOT list src/components/sections/HomepageHero.tsx
    - No em dashes in the new DESIGN.md prose; sentence case
    - `npx tsc --noEmit` clean; `npx eslint src/components/ui/AttachedForm.tsx` exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted client -> DemoForm inputs | The DemoForm is the site's one real input surface (validated by react-hook-form + zod). P14-01 changes only the a11y ASSOCIATION of the already-rendered error message; it adds no new input, alters no validation, and leaves the honeypot bot-trap untouched. P14-02 is CSS + a doc note. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | DemoForm honeypot (websiteUrl) | mitigate | inputClasses is extended, but the honeypot does not call inputClasses (it uses {...register}); the change provably cannot reach it. Acceptance criteria assert the honeypot has no aria-invalid, so the bot trap stays intact. |
| T-14-06 | Information disclosure | aria-describedby wiring | accept | aria-describedby points at an already-visible on-page error message (no new data exposed); it only makes the existing message programmatically discoverable to assistive tech. No secret or PII surface. |
| T-14-07 | Input validation regression (V5) | react-hook-form + zod validation | accept | P14-01 adds a11y attributes only; zod schema and resolver are untouched. No validation logic changes. |
| T-14-08 | Supply chain | no new deps; CSS + doc + spec | accept | Zero new libraries (CLAUDE.md §2). `next build` from clean in CI. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for DemoForm.tsx + AttachedForm.tsx (clear .next and re-run if spurious, D-07)
- `npx eslint src/components/forms/DemoForm.tsx src/components/ui/AttachedForm.tsx tests/a11y/demoform-aria.spec.ts` exit 0
- `next build` succeeds locally (do not run next dev/start — hangs in sandbox)
- Against the preview: demoform-aria.spec.ts passes (errored fields expose aria-invalid + aria-describedby; honeypot untouched); axe-routes.spec.ts stays green on /demo
- HomepageHero.tsx confirmed untouched in `git diff --name-only`
</verification>

<success_criteria>
- Every errored DemoForm field exposes aria-invalid="true" + aria-describedby="{id}-error"; the honeypot is untouched (P14-01)
- AttachedForm's email input carries the §8.3 3px ring; DESIGN.md §8.3 documents the input-focus ring as intentional citing shadows.focus (P14-02)
- HomepageHero.tsx is not modified
- demoform-aria.spec.ts asserts the P14-01 attributes; all existing specs stay green
</success_criteria>

<output>
After completion, create `.planning/phases/14-text-only-page-elevation/14-02-SUMMARY.md`. MUST record: the exact inputClasses signature change, the list of fields threaded, and confirmation the honeypot + HomepageHero.tsx were untouched.
</output>
