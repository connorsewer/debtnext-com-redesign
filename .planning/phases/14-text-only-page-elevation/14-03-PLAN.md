---
phase: 14
slug: text-only-page-elevation
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/content/visuals/integrations.ts
  - src/content/visuals/compare.ts
  - src/content/visuals/index.ts
  - src/app/platform/integrations/page.tsx
  - src/app/compare/page.tsx
  - src/content/compare.ts
  - tests/responsive/14-page-elevation.spec.ts
autonomous: true
requirements: [PAGEVIS-01, PAGEVIS-02, PAGEVIS-04]

must_haves:
  truths:
    - "A user on /platform/integrations sees a Schematic system/network map (systems of record -> dPlat routing -> collection agencies/law firms/recovery vendors) that advances the routing argument the IntegrationTable rows cannot show; no CTA competition"
    - "A user on /compare sees a DataStory (or Schematic) archetype that makes the comparative point WITHOUT duplicating the CompareMatrix market-map table"
    - "/why-dplat gets exactly what the approved 14-ARCHETYPE-MAP.md specifies: a proof DataStory if it lifts, otherwise nothing new (Reveal-only, already present via section components)"
    - "tests/responsive/14-page-elevation.spec.ts goes GREEN for the archetype routes: each renders a role=img archetype with its page-unique argument strings, present on load AND under prefers-reduced-motion"
    - "Every new payload satisfies its schema (compile-time proof), carries [CLAIMS REVIEW]/[COI REVIEW] header tags and a mandatory ariaSummary, uses `·` not em dashes, and keeps vendor framing agency-network-agnostic"
  artifacts:
    - path: "src/content/visuals/integrations.ts"
      provides: "integrationsSystemMap payload (SchematicData) for /platform/integrations"
      contains: "satisfies SchematicData"
    - path: "src/content/visuals/compare.ts"
      provides: "compare archetype payload (DataStoryData or SchematicData) for /compare"
      contains: "ariaSummary"
  key_links:
    - from: "src/app/platform/integrations/page.tsx"
      to: "src/content/visuals/integrations.ts"
      via: "SchematicVisual fed integrationsSystemMap in a ProductVisualBand"
      pattern: "integrationsSystemMap"
    - from: "src/app/compare/page.tsx"
      to: "src/content/visuals/compare.ts"
      via: "DataStoryVisual/SchematicVisual fed the compare payload in a ProductVisualBand"
      pattern: "compare(SystemMap|DataStory|TimeToProduction)"
---

<objective>
Elevate the two pages that clearly earn an archetype (per the approved 14-ARCHETYPE-MAP.md): `/platform/integrations` gets a Schematic system/network map, `/compare` gets a DataStory (or Schematic) that advances the comparative argument without duplicating the market-map table. Handle `/why-dplat` exactly as the map decided (proof DataStory or nothing new). Author the payloads, wire them through the lazy `*Visual` wrappers into ProductVisualBand slots, and flip the archetype routes in `14-page-elevation.spec.ts` from RED to GREEN. This is the mechanical archetype-replication pattern proven in Phases 11-12, applied under the D-01 restraint filter.

Purpose: Deliver PAGEVIS-01 (/compare) and the /platform/integrations half of PAGEVIS-04, plus the /why-dplat half of PAGEVIS-02, at full fidelity — real archetypes fed real-shaped payloads, argument-unique and reduced-motion-safe. Restraint holds: no archetype where a figure would be decoration.
Output: integrations + compare payload modules, index re-export, repointed page files, in-line BL-01 fix on /compare if touched, activated spec rows.

Set REPO to the repo root for the verify commands below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-text-only-page-elevation/14-CONTEXT.md
@.planning/phases/14-text-only-page-elevation/14-RESEARCH.md
@.planning/phases/14-text-only-page-elevation/14-UI-SPEC.md

# The approved Wave-0 gate (read the APPROVED map + its distinctness strings FIRST):
@.planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md
@.planning/phases/14-text-only-page-elevation/14-01-SUMMARY.md

# Payload authoring templates (read before authoring):
@src/content/visuals/placement.ts
@src/content/visuals/types.ts
@src/content/visuals/solutions-utilities.ts

<interfaces>
Archetype wrappers (src/components/product/visuals/archetypes.tsx — lazy, ssr:false, role="img"):
  SchematicVisual({data: SchematicData}), DataStoryVisual({data: DataStoryData}), ConsoleVisual({data: ConsoleData}).
  Import ONLY these wrappers from archetypes.tsx — NEVER import Console.tsx/DataStory.tsx/Schematic.tsx directly (Pitfall 5: eager import blows the 300ms content-route TBT bar).

Schemas (src/content/visuals/types.ts) — author with `satisfies`, NEVER a `: Type` annotation (excess-property checking is the compile-time proof):
- SchematicData: { eyebrow?, title, nodes: {id, label, sub?, kind?: "source"|"engine"|"vendor"|"sink"}[], edges: {from, to, label?, flow?}[], ariaSummary }
- DataStoryData: { headline, chart: {kind:"area";points:number[]} | {kind:"spark";bars:number[]} | {kind:"bars";series:{label,value,tone?}[]} | {kind:"cards";cards:{name,accent,tag,value,decimals?,suffix?,bar,sub}[]}, annotation?, ariaSummary }

Slot: ProductVisualBand (src/components/sections/ProductVisualBand.tsx) — standalone framed band, surface="dark" default, maxWidthClass default max-w-3xl, NO height floor (Pitfall 1).

CLS (Pitfall 1): the *Visual skeleton is min-h-[20rem] (archetypes.tsx). If a Schematic/DataStory resolves taller than 20rem, reserve the resolved box (wrap the band child in a min-h matching the resolved height, following the FLAGSHIP_SKELETON_MIN_H = min-h-[44rem] precedent in lazy.tsx). RECORD the resolved min-h in the SUMMARY. Verify resolved height on the Vercel preview.

Real-shaped raw material (src/content/integrations.ts, VERIFIED): SAP (23 integrations) + Oracle ERP + billing/CIS systems of record; dPlat routing; 538 collection agencies/law firms/recovery vendors on the back end; 60+ production integrations across 16 platform types. Frame all of it agency-network-agnostic ("routes across the originator's existing vendors"), never independence claims (§6).

/compare argument (src/content/compare.ts, VERIFIED): CompareMatrix already renders the market-map table. AUDIT BL-01: compare.ts:99 contains "digital journeys" (a banned word). If /compare copy is touched this plan, reword it in-line (e.g. "digital outreach flows").

Barrel: src/content/visuals/index.ts re-exports payload modules (Phase 12 convention). Add the new modules' exports. Pages may import directly from the module OR the barrel; import direct to keep this plan file-disjoint from others.

Page anatomy (VERIFIED): /platform/integrations has PageHero -> LogoMarquee -> ProofBand -> IntegrationTable ×4 -> BulletList -> FinalCTA (drop the Schematic ProductVisualBand after the footprint ProofBand or before the IntegrationTable cluster, where the "how routing works" story fits). /compare has PageHero -> ProseSection -> CompareMatrix -> CardGrid -> ... -> FinalCTA (drop the DataStory ProductVisualBand near the CompareMatrix, advancing a non-duplicative argument).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + wire the /platform/integrations Schematic (system/network map)</name>
  <files>src/content/visuals/integrations.ts, src/content/visuals/index.ts, src/app/platform/integrations/page.tsx</files>
  <read_first>
    - .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md (integrations row: slot + payload intent + the integrations Per-route distinctness strings — use them VERBATIM)
    - src/content/visuals/placement.ts (SchematicData authoring template if placement is a Schematic; else src/content/visuals/solutions-utilities.ts utilitiesRouting for the routing-Schematic template: [CLAIMS REVIEW]/[COI REVIEW] header, `satisfies`, `·` separators, ariaSummary prose, source/engine/vendor/sink nodes + labeled edges)
    - src/content/integrations.ts (the real ERP/CIS/vendor-network figures to shape the nodes from)
    - src/app/platform/integrations/page.tsx (current anatomy; where to drop the ProductVisualBand)
    - .planning/phases/14-text-only-page-elevation/14-UI-SPEC.md (§"Color" accent mapping; §"Copywriting Contract" rules; §"Accessibility Contract" ariaSummary requirement)
  </read_first>
  <action>
Create `src/content/visuals/integrations.ts` exporting `integrationsSystemMap` with `satisfies SchematicData` (never a type annotation) and a `[CLAIMS REVIEW]` + `[COI REVIEW]` header comment block:
- title: sentence-case, e.g. "How dPlat connects to your systems"
- nodes: source nodes for the systems of record (e.g. "SAP / Oracle ERP", "Billing / CIS"), an engine node ("dPlat routing engine"), vendor pool node(s) ("Collection agencies · law firms · recovery vendors"), a sink node. Use kind: source/engine/vendor/sink. Node labels/subs shape the real integrations story (60+ integrations, 16 platform types, 538 back-end vendors) but stay generic/anonymized.
- edges: labeled edges connecting source -> engine -> vendor -> sink; set flow:true where a data path should animate.
- ariaSummary: MANDATORY full governed-prose text alternative describing the routing in words (the grayscale/screen-reader story). Agency-network-agnostic framing.
- MUST contain the integrations Per-route distinctness strings from the approved map (e.g. a source/vendor node label + a routing edge label) so the spec's uniqueStrings match.
- Accents: from the approved map's accent binding (chart-1/3/4/5 only); chart-2 baseline/muted only.
- Voice: no em dashes (use `·`), no banned phrases, sentence case, digits.

Add `export * from "./integrations";` (or the named re-export matching the barrel's convention) to `src/content/visuals/index.ts`. NOTE the shared-basename pattern is proven safe: src/content/integrations.ts (page copy) vs src/content/visuals/integrations.ts (payload) mirrors placement.ts vs visuals/placement.ts.

Edit `src/app/platform/integrations/page.tsx`:
- Import SchematicVisual from "@/components/product/visuals/archetypes" and integrationsSystemMap from "@/content/visuals/integrations".
- Drop a `<ProductVisualBand><SchematicVisual data={integrationsSystemMap} /></ProductVisualBand>` at the slot the map specifies (default: after the footprint ProofBand / before the IntegrationTable cluster — the "how routing works" story). Keep the single "Request a demo" CTA per band untouched; the Schematic carries no CTA (D-03).
- CLS guard: verify the Schematic resolved height on the preview; if taller than 20rem, wrap the band child in a min-h matching the resolved box (FLAGSHIP_SKELETON_MIN_H precedent) and record the min-h in the SUMMARY.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "satisfies SchematicData" src/content/visuals/integrations.ts && grep -q "ariaSummary" src/content/visuals/integrations.ts && grep -q "integrationsSystemMap" src/app/platform/integrations/page.tsx && grep -q "SchematicVisual" src/app/platform/integrations/page.tsx && ! grep -q "—" src/content/visuals/integrations.ts && echo INTEG_OK</automated>
  </verify>
  <done>integrationsSystemMap authored (satisfies SchematicData, header tags, ariaSummary, source/engine/vendor/sink nodes, distinctness strings) and wired via SchematicVisual in a ProductVisualBand on the integrations page; re-exported from the barrel; no em dashes; CLS min-h recorded.</done>
  <acceptance_criteria>
    - src/content/visuals/integrations.ts exports integrationsSystemMap `satisfies SchematicData` (no type annotation)
    - Module has [CLAIMS REVIEW] and [COI REVIEW] header tags and a non-empty ariaSummary
    - Nodes include source, engine, vendor, and sink kinds; edges are labeled
    - Payload contains the exact integrations uniqueStrings from the approved map
    - No em dashes anywhere in the module (grep for — returns nothing); `·` used as separator
    - integrations/page.tsx renders SchematicVisual fed integrationsSystemMap inside a ProductVisualBand; the page's single Request-a-demo CTA is unchanged
    - `npx tsc --noEmit` clean for the new files; SUMMARY records the resolved min-h
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author + wire the /compare archetype (non-duplicative comparative argument) + BL-01 fix; handle /why-dplat per the map</name>
  <files>src/content/visuals/compare.ts, src/content/visuals/index.ts, src/app/compare/page.tsx, src/content/compare.ts</files>
  <read_first>
    - .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md (compare row: archetype choice + slot + payload intent + compare Per-route distinctness strings VERBATIM; and the /why-dplat decision — archetype or Reveal-only)
    - .planning/phases/14-text-only-page-elevation/14-01-SUMMARY.md (the /why-dplat archetype-or-reveal decision recorded at the checkpoint)
    - src/content/visuals/placement.ts (DataStory/Schematic authoring template)
    - src/content/visuals/types.ts (DataStoryData chart branches: area/spark/bars/cards)
    - src/app/compare/page.tsx (anatomy; where to drop the ProductVisualBand near CompareMatrix)
    - src/content/compare.ts (line 99 "digital journeys" — the BL-01 banned word to reword if compare copy is touched)
    - .planning/phases/14-text-only-page-elevation/14-UI-SPEC.md (§"Color" — /compare reserves chart-1 for dPlat, chart-2 muted for "typical"; §"Copywriting Contract" incl. the /compare legal-gate note)
  </read_first>
  <action>
Create `src/content/visuals/compare.ts` exporting the compare payload with `satisfies` (DataStoryData or SchematicData per the approved map — default DataStoryData on the time-to-production / continuous-production contrast, the strongest non-duplicative lift per RESEARCH Open Question 1) and a `[CLAIMS REVIEW]` + `[COI REVIEW]` header block. The header MUST additionally note the /compare comparative claims need a legal gate (punch-list #4) before merge.
- If DataStory (recommended): chart contrasts the dPlat time-to-production against a typical multi-month build (bars or area). Reserve chart-1 (indigo) for the dPlat series and chart-2 (muted) for the "typical"/competitor baseline so the argument survives grayscale.
- headline + annotation + MANDATORY ariaSummary telling the comparative argument in words.
- MUST contain the compare Per-route distinctness strings from the approved map (payload-only strings NOT already in the CompareMatrix table copy).
- The argument must ADVANCE the comparative point without duplicating the market-map table (Pitfall 2). Voice: no em dashes (`·`), no banned phrases, sentence case, digits.

Add the re-export to `src/content/visuals/index.ts`.

Edit `src/app/compare/page.tsx`: import the archetype wrapper + payload; drop a `<ProductVisualBand>` with the archetype near the CompareMatrix (the map specifies the slot). Keep the single Request-a-demo CTA per band untouched.

BL-01: if you touch `src/content/compare.ts` at all (or even if not, since it is in this plan's scope), reword the "digital journeys" banned construction at line ~99 to a compliant phrase (e.g. "digital outreach flows"). §5 voice.

/why-dplat: follow the approved map + 14-01-SUMMARY decision EXACTLY:
- If the map chose a proof DataStory: create `src/content/visuals/why-dplat.ts` (satisfies DataStoryData, header tags, ariaSummary, distinctness strings), re-export it, and wire DataStoryVisual into a ProductVisualBand on `src/app/why-dplat/page.tsx` near the ProofBand. Add src/content/visuals/why-dplat.ts and src/app/why-dplat/page.tsx to the files touched and note it in the SUMMARY.
- If the map chose Reveal-only (no archetype): do NOTHING to /why-dplat page code (its section components already animate); record the confirmed no-lift call in the SUMMARY. Do NOT double-wrap already-animating sections in RevealSection (Pitfall 3).
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "ariaSummary" src/content/visuals/compare.ts && grep -Eq "satisfies (DataStoryData|SchematicData)" src/content/visuals/compare.ts && grep -Eq "compare(DataStory|SystemMap|TimeToProduction|Timeline|Proof)" src/app/compare/page.tsx && ! grep -q "digital journeys" src/content/compare.ts && ! grep -q "—" src/content/visuals/compare.ts && echo COMPARE_OK</automated>
  </verify>
  <done>compare archetype payload authored (satisfies its schema, header tags incl. legal-gate note, ariaSummary, distinctness strings, chart-1 dPlat / chart-2 baseline) and wired near CompareMatrix without duplicating it; "digital journeys" reworded (BL-01); /why-dplat handled exactly per the approved map; single CTA per band unchanged.</done>
  <acceptance_criteria>
    - src/content/visuals/compare.ts exports a payload `satisfies DataStoryData` (or SchematicData per the map), with [CLAIMS REVIEW]/[COI REVIEW] header + a legal-gate note, and a non-empty ariaSummary
    - Payload contains the exact compare uniqueStrings from the approved map, and those strings do NOT already appear in the CompareMatrix table copy (non-duplicative)
    - compare/page.tsx renders the archetype in a ProductVisualBand near CompareMatrix; single Request-a-demo CTA per band unchanged
    - `grep -q "digital journeys" src/content/compare.ts` returns nothing (BL-01 fixed)
    - No em dashes in src/content/visuals/compare.ts
    - /why-dplat matches the approved map: either a wired DataStory (why-dplat.ts + page edit) OR an explicit no-lift call recorded in the SUMMARY with zero page edits and no double-wrapped RevealSection
    - `npx tsc --noEmit` clean for the new files
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Activate 14-page-elevation.spec.ts for the archetype routes (RED -> GREEN)</name>
  <files>tests/responsive/14-page-elevation.spec.ts</files>
  <read_first>
    - tests/responsive/14-page-elevation.spec.ts (the Wave-0 skeleton from 14-01 — confirm the per-route config already lists integrations + compare with the map's uniqueStrings)
    - .planning/phases/14-text-only-page-elevation/14-ARCHETYPE-MAP.md (confirm uniqueStrings match what Tasks 1-2 authored into the payloads)
    - src/content/visuals/integrations.ts, src/content/visuals/compare.ts (confirm the payload strings the spec asserts are actually present)
  </read_first>
  <behavior>
    - /platform/integrations: role="img" Schematic present with non-empty aria-label; its uniqueStrings present on load AND under reducedMotion:"reduce"
    - /compare: role="img" archetype present with non-empty aria-label; its uniqueStrings present on load AND under reducedMotion
    - /why-dplat: assertion matches the map decision (archetype-present if wired, else revealOnly/omitted)
    - /demo: revealOnly — submit CTA present, no archetype required (unchanged from Wave 0)
    - No in-viewport text-bearing element at opacity < 1 after load (assertNoStuckOpacity, iterations === Infinity idle-guard)
  </behavior>
  <action>
The per-route config was authored in 14-01 (Task 2). This task RECONCILES it against the shipped payloads and flips it GREEN:
- Confirm the integrations + compare uniqueStrings in the spec match the strings actually authored into the payloads in Tasks 1-2. If Task 1/2 chose slightly different nouns than the map draft, update BOTH so they agree (the map is the source of truth; if a noun changed, update the map note too and record it in the SUMMARY).
- If /why-dplat was wired as an archetype, ensure its config row has archetypeExpected:true with its uniqueStrings; if Reveal-only, ensure it is omitted from the archetype config or set archetypeExpected:false/revealOnly:true.
- Do NOT weaken any assertion (keep assertReducedMotionDataParity + assertNoStuckOpacity with the iterations === Infinity idle-guard).
Run the spec against the preview; the archetype routes must pass.
  </action>
  <verify>
    <automated>cd "$REPO" && grep -q "iterations === Infinity" tests/responsive/14-page-elevation.spec.ts && grep -q "reducedMotion" tests/responsive/14-page-elevation.spec.ts && npx tsc --noEmit 2>&1 | grep -i "14-page-elevation" || echo SPEC_RECONCILED_OK</automated>
  </verify>
  <done>14-page-elevation.spec.ts uniqueStrings match the shipped integrations + compare payloads; the archetype routes assert role=img + unique strings on load AND under reduced motion; the spec goes GREEN for those routes against the preview.</done>
  <acceptance_criteria>
    - Spec uniqueStrings for /platform/integrations and /compare are present in the corresponding payload modules (cross-checked)
    - assertReducedMotionDataParity + assertNoStuckOpacity (iterations === Infinity) are unchanged (not weakened)
    - /why-dplat row matches the map decision; /demo stays revealOnly with the submit-CTA assertion
    - `npx tsc --noEmit` reports no new errors for the spec
    - Against the preview: /platform/integrations + /compare rows pass (GREEN)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> static payload | The only "input" is developer-authored typed payloads; no runtime or user input crosses any boundary on this static marketing surface. Pages render server-safe payload modules through lazy client wrappers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Information disclosure | integrations + compare payload numbers, ariaSummary, node labels | accept | Figures are real-shaped, anonymized, generic (Andrew pre-cleared 2026-06-12); [CLAIMS REVIEW]/[COI REVIEW] retained for audit. No real client identifiers (CLAUDE.md §15). The /compare comparative claims additionally carry a legal gate (punch-list #4) — do not merge comparative-claim copy without it. |
| T-14-10 | Information disclosure / COI | vendor-network framing in the integrations Schematic + compare | mitigate | All vendor framing stays agency-network-agnostic ("routes across the originator's existing vendors"); never independence claims (§6). ariaSummary reviewed to the same bar. [COI REVIEW] flagged in the PR. |
| T-14-11 | Tampering | typed payload modules | accept | Static TypeScript validated by `satisfies` at compile time; no runtime mutation path, no user-supplied value reaches them. |
| T-14-12 | Supply chain / perf-integrity | lazy archetype imports | mitigate | Only the *Visual lazy wrappers + server-safe payloads are imported (never Console/DataStory/Schematic .tsx directly), holding the 300ms content-route TBT bar (Pitfall 5). Zero new libraries (§2). |
| T-14-13 | Spoofing/Repudiation/DoS/EoP | n/a (static marketing visuals) | accept | No untrusted input, no data sink, no auth surface, no server action. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the new payload modules + page edits + spec (clear .next and re-run if spurious, D-07)
- `npx eslint` exit 0 on all changed files
- `next build` succeeds locally (do not run next dev/start — hangs in sandbox)
- Against the preview: 14-page-elevation.spec.ts passes for /platform/integrations + /compare (+ /why-dplat if wired); reduced-motion.spec.ts + reveal-fail-open.spec.ts stay green; axe-routes + touch-targets stay green on all touched routes
- CLS: each new archetype's resolved height verified ≤ its reserved skeleton box on the preview (SUMMARY records the min-h)
- No em dashes in any new payload; [CLAIMS REVIEW]/[COI REVIEW] flagged in the PR; /compare comparative claims routed to the legal gate
</verification>

<success_criteria>
- /platform/integrations renders a Schematic system/network map advancing the routing argument (PAGEVIS-04 half)
- /compare renders a non-duplicative comparative archetype; "digital journeys" reworded (PAGEVIS-01, BL-01)
- /why-dplat matches the approved map decision (PAGEVIS-02 half)
- 14-page-elevation.spec.ts GREEN for the archetype routes on load AND under reduced motion
- Single Request-a-demo CTA per band preserved on every touched page (D-03); CLS < 0.1 holds
</success_criteria>

<output>
After completion, create `.planning/phases/14-text-only-page-elevation/14-03-SUMMARY.md`. MUST record: the final archetype choice per page, the exact uniqueStrings shipped (matching the spec), the resolved min-h per new archetype slot (CLS), the /why-dplat decision as executed, and confirmation the BL-01 "digital journeys" reword landed.
</output>
